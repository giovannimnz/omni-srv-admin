---
phase: 42-atius-wide-sso-login-on-sso-atius-com-br
plan: 42-03
type: execute
wave: 2
depends_on:
  - "42-01"
  - "42-02"
files_modified:
  - /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh
  - /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh
  - /home/ubuntu/GitHub/omni-srv-admin/scripts/keycloak-sso-client-check.sh
  - /home/ubuntu/GitHub/omni-srv-admin/docs/domain/atius-wide-sso.md
  - /home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md
  - /etc/apache2/sites-available/sso.atius.com.br.conf
  - /etc/apache2/sites-available/trade.atius.com.br.conf
  - /etc/apache2/sites-available/painel.atius.com.br.conf
  - /etc/apache2/sites-available/dashboard.atius.com.br.conf
  - /etc/apache2/sites-available/backtest.atius.com.br.conf
  - /etc/apache2/sites-available/strategy.atius.com.br.conf
  - /etc/apache2/sites-available/admin.atius.com.br.conf
autonomous: false
requirements:
  - SSO-01
  - SSO-02
  - SSO-05
  - SSO-06
user_setup:
  - service: cloudflare
    why: "Cloudflare/DNS/TLS publication for sso.atius.com.br needs authenticated dashboard/API access and before-state capture."
    dashboard_config:
      - task: "Capture current DNS/proxy/TLS state for sso.atius.com.br and the six ATS app hosts before publication."
        location: "Cloudflare Dashboard -> atius.com.br -> DNS and SSL/TLS"
      - task: "Approve DNS/proxy/TLS publication only after local Apache --resolve smoke and rollback checklist are green."
        location: "Cloudflare Dashboard -> atius.com.br"
  - service: keycloak
    why: "Production OIDC client readiness needs authenticated admin access and no-secret client-secret storage."
    env_vars:
      - name: ATIUS_SSO_ISSUER
        source: "https://auth.atius.com.br/realms/atius"
      - name: ATIUS_SSO_CLIENT_ID
        source: "Keycloak client id sso.atius.com.br"
      - name: ATIUS_SSO_CLIENT_SECRET_FILE
        source: "If client type is confidential, root-only path /etc/keycloak/sso.atius.com.br-client.env"
      - name: ATIUS_SSO_REDIRECT_URI
        source: "https://sso.atius.com.br/api/sso/callback"
      - name: ATIUS_SSO_POST_LOGOUT_REDIRECT_URI
        source: "https://sso.atius.com.br/login?logout=complete"
  - service: ats-live-smoke
    why: "Live legacy ATS auth smoke requires operator-provided credentials after Wave 0 removes hardcoded fallbacks."
    env_vars:
      - name: BACKEND_API_URL
        source: "ATS API base URL, usually https://api.atius.com.br or local --resolve target"
      - name: SSO_TEST_EMAIL
        source: "Operator-provided non-secret reference account identifier; do not copy into docs if sensitive"
      - name: SSO_TEST_PASSWORD
        source: "Operator-provided secret value in shell/secret store only; never write to Git, planning, Obsidian, logs, or shell history"
must_haves:
  truths:
    - "D-01/SSO-01: `sso.atius.com.br` has DNS/Apache/Cloudflare/TLS and rollback contract before any live publication."
    - "D-02/SSO-02: Keycloak client `sso.atius.com.br` exists or is applied with exact no-secret fields and verified beyond discovery."
    - "D-06/SSO-05: global logout smoke proves Keycloak end-session plus legacy ATS cookie cleanup."
    - "D-07/SSO-01: forwarded-header behavior is explicit and smoked for trade, painel, dashboard, backtest, strategy, and admin."
    - "D-08/SSO-06: docs, scripts, Keycloak inventory, screenshots, logs, and summaries contain only paths/placeholders, not secret values."
  artifacts:
    - "/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh"
    - "/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh"
    - "/home/ubuntu/GitHub/omni-srv-admin/scripts/keycloak-sso-client-check.sh"
    - "/home/ubuntu/GitHub/omni-srv-admin/docs/domain/atius-wide-sso.md"
    - "/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md"
    - "/etc/apache2/sites-available/sso.atius.com.br.conf"
    - "/etc/apache2/sites-available/{trade,painel,dashboard,backtest,strategy,admin}.atius.com.br.conf backups when header updates are required"
  key_links:
    - "Plan 42-02 SSO facade -> Apache `sso.atius.com.br` vhost -> Cloudflare/DNS/TLS publication"
    - "Keycloak client `sso.atius.com.br` -> redirect URI `https://sso.atius.com.br/api/sso/callback`"
    - "App-host vhosts -> explicit `X-Forwarded-Host` -> ATS middleware host routing"
    - "SSO logout route -> Keycloak end-session -> `https://sso.atius.com.br/login?logout=complete`"
    - "Secret hygiene script -> docs/scripts/evidence before publication"
---

<objective>
Publish the no-secrets edge, Keycloak client, app-host header smoke, global logout smoke, and manual-gated rollout contract for Atius-wide SSO.

Purpose: Turn the ATS SSO implementation into a rollbackable production edge path without ambiguous Keycloak setup, fragile forwarded headers, raw curl-only checks, or secret leakage.
Output: Apache/app-host header contract, Keycloak client inventory/apply/checkpoint script, no-secrets SSO runbook, status/header smoke, global logout evidence, and blocking publication gates.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/ROADMAP.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/STATE.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/REQUIREMENTS.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-CONTEXT.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-RESEARCH.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-PATTERNS.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-01-SUMMARY.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-02-SUMMARY.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/36-keycloak-sso-and-coexistence/36-CONTEXT.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/36-keycloak-sso-and-coexistence/36-01-SUMMARY.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/36-keycloak-sso-and-coexistence/36-VERIFICATION.md
@docs/domain/keycloak-freeipa-coexistence.md
@.planning/codebase/CONCERNS.md
@/etc/apache2/sites-available/auth.atius.com.br.conf
</context>

## Artifacts This Phase Produces

- `/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh`: final local/publication smoke for Apache configtest, `sso.atius.com.br`, Keycloak discovery, app-host redirect/header behavior, cookie clearing, and rollback dry-run.
- `/home/ubuntu/GitHub/omni-srv-admin/scripts/keycloak-sso-client-check.sh`: no-secret inventory/assert/apply-plan helper for Keycloak client `sso.atius.com.br`.
- `/home/ubuntu/GitHub/omni-srv-admin/docs/domain/atius-wide-sso.md`: runbook covering DNS, Cloudflare, TLS, Apache, Keycloak client, ATS env, cookie/session compatibility, app-host headers, global logout, rollback, evidence, and secret rules.
- `/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md`: redacted architecture/infra worklog with decisions, gates, rollback posture, and verification summary; no secrets, raw tokens, cookie values, passwords, screenshots containing secrets, or account-sensitive details.
- `/etc/apache2/sites-available/sso.atius.com.br.conf`: gated SSO facade vhost with explicit `ProxyPreserveHost`, `X-Forwarded-Host`, `X-Forwarded-Proto`, `X-Forwarded-Port`, and `X-Forwarded-For`.
- App-host vhost backups/updates for `trade`, `painel`, `dashboard`, `backtest`, `strategy`, and `admin` only when the smoke proves their forwarded-host contract is missing or wrong.

## Scope Justification

Plan 42-03 intentionally keeps Apache/app-host headers, Keycloak client readiness, publication runbook, live smoke, and redacted Obsidian documentation in one manual-gated plan. These items are one production publication gate: enabling DNS or Apache without the exact Keycloak client and rollback evidence would create a partial cutover risk. The plan remains bounded because it runs after 42-01/42-02, is `autonomous: false`, requires backups and human approval, and does not permit Cloudflare/DNS/Apache reload/Keycloak mutation until the scripted checks and no-secret evidence pass.

<tasks>

<task type="auto">
  <name>Task 1: Add edge and app-host forwarded-header contracts with rollback</name>
  <files>/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh, /etc/apache2/sites-available/sso.atius.com.br.conf, /etc/apache2/sites-available/trade.atius.com.br.conf, /etc/apache2/sites-available/painel.atius.com.br.conf, /etc/apache2/sites-available/dashboard.atius.com.br.conf, /etc/apache2/sites-available/backtest.atius.com.br.conf, /etc/apache2/sites-available/strategy.atius.com.br.conf, /etc/apache2/sites-available/admin.atius.com.br.conf</files>
  <read_first>
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-PATTERNS.md`
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md`
    - `.planning/codebase/CONCERNS.md`
    - `/etc/apache2/sites-available/auth.atius.com.br.conf`
    - `/etc/apache2/sites-available/trade.atius.com.br.conf`
    - `/etc/apache2/sites-available/painel.atius.com.br.conf`
    - `/etc/apache2/sites-available/dashboard.atius.com.br.conf`
    - `/etc/apache2/sites-available/backtest.atius.com.br.conf`
    - `/etc/apache2/sites-available/strategy.atius.com.br.conf`
    - `/etc/apache2/sites-available/admin.atius.com.br.conf`
  </read_first>
  <action>Run Graphify status/query before selecting edge files. Create a timestamped backup of each touched Apache vhost before editing. Add `/etc/apache2/sites-available/sso.atius.com.br.conf` as a gated vhost for the ATS Next facade with explicit `ProxyPreserveHost On`, `RequestHeader set X-Forwarded-Host "sso.atius.com.br"`, `RequestHeader set X-Forwarded-Proto "https"`, `RequestHeader set X-Forwarded-Port "443"`, and `RequestHeader set X-Forwarded-For %{REMOTE_ADDR}s`. Extend `scripts/sso-edge-smoke.sh` so app-host checks cover `trade.atius.com.br`, `painel.atius.com.br`, `dashboard.atius.com.br`, `backtest.atius.com.br`, `strategy.atius.com.br`, and `admin.atius.com.br`. For each app host, assert the local `--resolve` unauthenticated response status is the expected redirect status and the `Location` header targets `https://sso.atius.com.br/login` with a normalized original-host `return_to`. If a vhost lacks or misstates explicit forwarded-host behavior, update that vhost only after backup and record rollback commands; do not reload Apache until `apache2ctl configtest` and all local smokes are green.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify query "Phase 42 edge Apache forwarded headers app hosts sso.atius.com.br"</automated>
    <automated>apache2ctl configtest</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/sso-edge-smoke.sh --local --assert-status --assert-headers --assert-app-hosts trade.atius.com.br,painel.atius.com.br,dashboard.atius.com.br,backtest.atius.com.br,strategy.atius.com.br,admin.atius.com.br</automated>
  </verify>
  <acceptance_criteria>
    - `sso.atius.com.br` vhost uses explicit proxy and forwarded headers; client-provided forwarded host behavior is not accepted as authoritative.
    - D-07 app-host smoke covers all six concrete ATS app hosts and fails when the redirect status or `Location` header is wrong.
    - Each edited Apache vhost has a timestamped backup and rollback command recorded in the phase summary.
    - No Apache reload, DNS publication, Cloudflare mutation, PM2 restart, or Keycloak change occurs before green configtest and local smokes.
  </acceptance_criteria>
  <done>Edge and app-host header behavior is explicit, backed up, locally smoke-tested, and rollbackable before publication.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory, apply-plan, and checkpoint the Keycloak SSO client</name>
  <files>/home/ubuntu/GitHub/omni-srv-admin/scripts/keycloak-sso-client-check.sh, /home/ubuntu/GitHub/omni-srv-admin/docs/domain/atius-wide-sso.md</files>
  <read_first>
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-RESEARCH.md`
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/36-keycloak-sso-and-coexistence/36-01-SUMMARY.md`
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/36-keycloak-sso-and-coexistence/36-VERIFICATION.md`
    - `docs/domain/keycloak-freeipa-coexistence.md`
  </read_first>
  <action>Create `scripts/keycloak-sso-client-check.sh` with no-secret modes for `inventory`, `render-apply-plan`, and `assert`. The target Keycloak realm is `atius`; client id is exactly `sso.atius.com.br`; redirect URI is exactly `https://sso.atius.com.br/api/sso/callback`; default web origin is exactly `https://sso.atius.com.br` with no wildcard; post logout redirect URI is exactly `https://sso.atius.com.br/login?logout=complete`; and the confidential-client secret file path, when confidential is selected, is exactly `/etc/keycloak/sso.atius.com.br-client.env` with root-only `0600` ownership. Add a blocking checkpoint in the runbook that chooses client type: confidential is the default for the server-side facade; public is allowed only if the operator explicitly records that no client secret is required for the selected Keycloak flow. The script must inventory/export/apply/check these fields through `kcadm.sh` when authenticated admin CLI is available, or produce a dashboard checklist with the same exact fields when CLI auth is unavailable. It must never print, store, or diff a client secret value.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/keycloak-sso-client-check.sh inventory --realm atius --client-id sso.atius.com.br</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/keycloak-sso-client-check.sh assert --realm atius --client-id sso.atius.com.br --redirect-uri https://sso.atius.com.br/api/sso/callback --web-origin https://sso.atius.com.br --post-logout-redirect-uri 'https://sso.atius.com.br/login?logout=complete'</automated>
    <automated>STATUS=$(curl --fail-with-body --silent --show-error --output /tmp/phase42-keycloak-discovery.json --write-out "%{http_code}" --resolve auth.atius.com.br:443:127.0.0.1 https://auth.atius.com.br/realms/atius/.well-known/openid-configuration) && test "$STATUS" = 200 && jq -e '.issuer=="https://auth.atius.com.br/realms/atius" and .authorization_endpoint and .token_endpoint and .end_session_endpoint' /tmp/phase42-keycloak-discovery.json >/dev/null</automated>
    <human-check>Operator confirms client type selection, exact Keycloak fields, and root-only secret path if confidential; confirmation must not include the secret value.</human-check>
  </verify>
  <acceptance_criteria>
    - Keycloak client readiness is proven by inventory/assert against exact fields, not by discovery alone.
    - Client type decision is recorded as confidential or public with reason; confidential uses `/etc/keycloak/sso.atius.com.br-client.env` and no secret value enters artifacts.
    - Redirect URI, web origin, and post-logout redirect URI exactly match the target fields in this task.
    - Discovery smoke asserts HTTP 200 and required OIDC endpoints with `jq`; raw curl output alone cannot pass.
  </acceptance_criteria>
  <done>Keycloak client readiness has an exact no-secret inventory/apply/checkpoint path stronger than discovery and ready for publication gating.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize no-secrets runbook, global logout smoke, and manual publication gate</name>
  <files>/home/ubuntu/GitHub/omni-srv-admin/docs/domain/atius-wide-sso.md, /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh, /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh, /home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md</files>
  <read_first>
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md`
    - `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-UI-SPEC.md`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_global_logout.spec.ts`
    - `docs/domain/keycloak-freeipa-coexistence.md`
  </read_first>
  <action>Finalize `docs/domain/atius-wide-sso.md` as the no-secrets operational contract for DNS, Cloudflare proxy/TLS, Apache vhosts, Keycloak client, ATS env keys, cookie attributes, app-host forwarded headers, global logout, rollback, and evidence handling. Add a manual-gated publication sequence that requires before-state capture for Cloudflare/DNS/TLS, green local `--resolve` smoke, green Keycloak client assert, green global logout smoke, green legacy ATS live runtime smoke when operator-provided env vars are present, green secret scan, and recorded rollback commands before any public DNS/proxy or Apache reload. Run global logout smoke after Plans 42-01 and 42-02 are green. Create the redacted Obsidian worklog at `/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md` with architecture decisions, gates, rollback posture, and verification summary. Record evidence with timestamp, target host, status, and pass/block outcome only; do not include account identifiers, token values, cookie values, client secrets, passwords, screenshots containing secrets, or shell history with credentials.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/sso-edge-smoke.sh --dry-run --assert-status --assert-headers</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && npx playwright test tests/frontend/e2e/test_sso_global_logout.spec.ts --project=chromium --workers=1</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && test -n "${SSO_TEST_EMAIL:-}" && test -n "${SSO_TEST_PASSWORD:-}" && RUN_LIVE_API_TESTS=1 npx jest --config jest.backend.runtime.config.js tests/backend/auth/test_sso_auth_endpoints.runtime.test.js --runInBand</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/sso-secret-hygiene-scan.sh</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status</automated>
    <human-check>Before publication, operator approves Cloudflare/DNS/TLS before-state capture, Keycloak client fields, Apache rollback commands, live smoke env availability, first ATS login/logout smoke evidence, and redacted Obsidian worklog without secrets.</human-check>
  </verify>
  <acceptance_criteria>
    - Runbook documents exact DNS/Cloudflare/TLS/Apache/Keycloak/ATS contract, rollback steps, drift checks, cookie attributes, env keys, app-host header expectations, and evidence redaction rules.
    - Global logout smoke proves Keycloak browser SSO cleanup plus `.atius.com.br` and no-domain `auth-token` cleanup without auto-login loop.
    - Legacy ATS live runtime smoke runs only in Wave 2 with operator-provided env vars; Wave 0 remains deterministic without live credentials.
    - Obsidian worklog exists at `/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-28-phase42-atius-wide-sso.md` and contains only redacted decisions, gates, rollback posture, and verification summary.
    - Publication gate blocks live Cloudflare/DNS/proxy/Apache reload/Keycloak mutation until automated checks and human confirmation pass.
    - Secret hygiene scan is green after docs/scripts/evidence updates and reports no raw secrets.
  </acceptance_criteria>
  <done>The edge publication path is documented, smoke-tested, secret-safe, and manual-gated with rollback evidence.</done>
</task>

</tasks>

## Plan-Set Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 42 | Create `sso.atius.com.br` as canonical Atius login using Phase 36 Keycloak and ATS reference migration | 42-01, 42-02, 42-03 | COVERED | Validation, ATS implementation, and gated edge publication. |
| REQ | SSO-01 | Canonical host with DNS/Apache/Cloudflare/TLS and rollback before live publication | 42-01, 42-02, 42-03 | COVERED | 42-03 owns edge contract; 42-01 tests it; 42-02 implements facade. |
| REQ | SSO-02 | Existing Keycloak as controlled OIDC provider without breaking legacy SSO/JWT | 42-01, 42-02, 42-03 | COVERED | Exact client checkpoint in 42-03; OIDC bridge in 42-02. |
| REQ | SSO-03 | ATS reference migration preserving `auth-token`, RBAC, and protected routes | 42-01, 42-02 | COVERED | ATS source implementation and tests. |
| REQ | SSO-04 | Safe redirects with no open redirect | 42-01, 42-02 | COVERED | Matrix tests and server-side utility. |
| REQ | SSO-05 | Global logout clears Keycloak and legacy cookies | 42-01, 42-02, 42-03 | COVERED | E2E, route implementation, and publication smoke. |
| REQ | SSO-06 | Secrets outside Git/planning/Obsidian/logs/history | 42-01, 42-03 | COVERED | Secret scan and no-secret runbook/checkpoints. |
| RESEARCH | Open Questions (RESOLVED) #1 | SSO facade lives in existing ATS Next process | 42-02 | COVERED | Implemented as ATS facade; edge in 42-03. |
| RESEARCH | Open Questions (RESOLVED) #2 | Dedicated no-secrets Keycloak client checkpoint for `sso.atius.com.br` | 42-03 | COVERED | Exact target fields and assert script. |
| RESEARCH | Open Questions (RESOLVED) #3 | ATS user mapping by verified email/username with fail-closed collisions | 42-01, 42-02 | COVERED | Tested and implemented in backend bridge. |
| RESEARCH | Open Questions (RESOLVED) #4 | Cloudflare/DNS/TLS publication is blocking manual/API checkpoint | 42-03 | COVERED | user_setup and human-check gate. |
| CONTEXT | D-01 | Canonical login host | 42-02, 42-03 | COVERED | Facade and edge contract. |
| CONTEXT | D-02 | Existing Keycloak provider | 42-02, 42-03 | COVERED | Auth-code bridge and exact client. |
| CONTEXT | D-03 | ATS-first compatibility | 42-01, 42-02 | COVERED | Legacy cookie/session preservation. |
| CONTEXT | D-04 | Authentication before authorization migration | 42-01, 42-02 | COVERED | RBAC remains in ATS DB. |
| CONTEXT | D-05 | No open redirects | 42-01, 42-02 | COVERED | Allowlist tests and utility. |
| CONTEXT | D-06 | Global and compatible logout | 42-01, 42-02, 42-03 | COVERED | E2E, route, and smoke. |
| CONTEXT | D-07 | Edge/header fragility | 42-01, 42-03 | COVERED | App-host smoke and vhost contracts for all six hosts. |
| CONTEXT | D-08 | Secrets stay out of artifacts | 42-01, 42-02, 42-03 | COVERED | Env-only tests, redaction, scan, no-secret runbook. |
| UI-SPEC | Approved SSO login facade | 42-02 | COVERED | Source UI task. |
| VALIDATION | Wave 0 before live SSO changes | 42-01 | COVERED | Plan 42-01 is Wave 0. |

Deferred items from `42-CONTEXT.md` remain excluded: non-ATS app migrations, Horistic cross-domain SSO, Windows/Mac domain login, and secret creation/rotation outside the gated execution path.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cloudflare/DNS -> Apache | Public host routing and TLS/proxy state reach SRV-1. |
| Apache -> ATS Next facade | Forwarded host/proto/port headers affect middleware routing and SSO callback URL behavior. |
| Keycloak admin -> client config | Authenticated admin changes define redirect, origin, logout, and secret behavior. |
| SSO logout smoke -> browser cookies | Browser state contains Keycloak session and ATS legacy cookie variants. |
| Docs/scripts/evidence -> Git/.planning/Obsidian | Operational evidence and the mandatory Obsidian worklog can accidentally persist secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-03-01 | Spoofing / Tampering | Apache vhosts and ATS app-host forwarded headers | high | mitigate | Explicit `X-Forwarded-*` directives and app-host smoke for trade, painel, dashboard, backtest, strategy, and admin per D-07. |
| T-42-03-02 | Spoofing / Information Disclosure | Keycloak client redirect/origin/logout fields | high | mitigate | Exact no-secret inventory/assert/apply checkpoint for `sso.atius.com.br` client per SSO-02. |
| T-42-03-03 | Denial of Service | Apache/Cloudflare/DNS publication | high | mitigate | Backup, configtest, local `--resolve` smoke, rollback commands, and blocking human publication gate. |
| T-42-03-04 | Elevation of Privilege | Logout/cookie cleanup | high | mitigate | Playwright global logout smoke verifies Keycloak end-session and both ATS cookie variants per D-06. |
| T-42-03-05 | Information Disclosure | Docs, scripts, Keycloak inventory, evidence | high | mitigate | Secret scan, no-secret checklist, root-only path references only, and redacted evidence per D-08. |
| T-42-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is planned. If a Cloudflare CLI or other package install is proposed, stop for package legitimacy gate and operator approval. |
</threat_model>

<verification>
Plan 42-03 is complete when Apache configtest, edge/app-host smoke, Keycloak client assert, Keycloak discovery assert, global logout E2E, secret hygiene scan, and Graphify status are green, and the operator approves the manual publication gate without exposing secrets.
</verification>

<success_criteria>
- `sso.atius.com.br` has a rollbackable DNS/Cloudflare/TLS/Apache contract before live publication.
- Keycloak client `sso.atius.com.br` has exact redirect/origin/logout/client-type/secret-path readiness and verification stronger than discovery.
- All six ATS app hosts have explicit forwarded-header behavior covered by smoke or gated vhost updates with backups.
- Global logout is verified across Keycloak and ATS legacy cookies.
- No live publication proceeds without green automated checks and human approval.
</success_criteria>

<output>
Create `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-03-SUMMARY.md` when done.
</output>
