---
phase: 50-atius-wide-sso-closeout
plan: 01
type: execute
wave: 1
depends_on: [42-01, 42-02, 47]
requirements: [SSO-01, SSO-02, SSO-03, SSO-04, SSO-05, SSO-06]
files_modified:
  - docs/domain/atius-wide-sso.md
  - scripts/sso-edge-smoke.sh
  - scripts/keycloak-sso-client-check.sh
  - Apache, Keycloak and approved app SSO configs
  - .planning/workstreams/runtime-trust-codex-delivery-convergence/phases/50-atius-wide-sso-closeout/
must_haves:
  truths:
    - SSO redirects are allowlisted and RBAC remains DB-authoritative.
    - Global logout clears all relevant sessions and cookies.
    - Legacy auth remains rollback-capable until final acceptance.
  artifacts:
    - Static assertion and live browser matrix.
    - Verified edge/Keycloak/app backup and rollback evidence.
  key_links:
    - Phase 47 trusted TLS gates SSO publication.
    - Phase 42-01/42-02 artifacts feed the final 50-01 gate.
autonomous: false
---

<objective>
Close the remaining Phase 42 publication gate after PKI, preserving ATS RBAC
and reversible legacy coexistence.
</objective>

<tasks>
<task type="checkpoint:human-verify">
  <name>Approve edge and Keycloak publication</name>
  <files>Apache vhosts, Keycloak client, app SSO configs, rollback previews</files>
  <action>Review DNS, TLS, vhost, client, redirect and rollback previews before live publication.</action>
  <verify>Static assertions and secret-hygiene scans pass.</verify>
  <done>Human approval and verified backups exist for every live target.</done>
</task>
<task type="auto">
  <name>Publish and validate the SSO facade</name>
  <files>approved edge, Keycloak and application configs</files>
  <action>Apply approved edge/client configuration and validate login/return_to across the allowlisted applications while preserving DB-backed RBAC.</action>
  <verify>Login, callback and protected-route tests pass.</verify>
  <done>Allowlisted applications authenticate and preserve RBAC correctly.</done>
</task>
<task type="auto">
  <name>Prove logout, coexistence and rollback</name>
  <files>SSO smoke scripts, browser evidence, docs and phase closeout</files>
  <action>Validate global logout/cookie cleanup, legacy coexistence, negative redirects and rollback.</action>
  <verify>All checks in 50-VALIDATION.md pass.</verify>
  <done>Logout, negative cases, coexistence and rollback rehearsal pass.</done>
</task>
</tasks>
