---
phase: 24-production-recovery-guard-ats-horistic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/srv1-ops/configs/production-guard.yaml
  - modules/srv1-ops/scripts/production_guard.py
  - cli/omni/srv1_ops.py
  - cli/omni/tests/test_srv1_production_guard.py
autonomous: true
context_budget_target: "75k-95k tokens"
execution_model_target: "gpt-5.3-codex-spark"
requirements:
  - PRG-01
  - PRG-02
  - PRG-03
  - PRG-04
  - PRG-05
must_haves:
  truths:
    - "Validator e read-only; nao executa repair."
    - "PM2 live, dump e ecosystems precisam bater antes de qualquer save futuro."
    - "Namespaces atius/horistic sao obrigatorios para apps desses sistemas."
    - "waiting restart precisa de cycle summary recente para launchers one-shot."
    - "Status/doctor tambem cobre containers, timers e jobs systemd."
    - "Endpoint checks usam GET/HEAD; POST real e proibido."
  artifacts:
    - path: "modules/srv1-ops/configs/production-guard.yaml"
      provides: "Baseline declarativo de PM2/apps/ports/endpoints/containers/timers/jobs."
    - path: "modules/srv1-ops/scripts/production_guard.py"
      provides: "Status/doctor read-only com JSON e resumo PT-BR."
    - path: "cli/omni/tests/test_srv1_production_guard.py"
      provides: "Tests de PM2/dump/ecosystem/namespace/launcher/container/timer/job."
---

<objective>
Implementar o validador read-only do Production Guard.

Purpose: provar em um unico comando que ATS/Horistic tem boot PM2 coerente,
namespaces separados, dump atual, ecosystems validos, portas/endpoints seguros
e classificacao de containers/timers/jobs antes de qualquer reparo.

Output: config baseline, script `production_guard.py`, CLI
`production-guard status/doctor` e testes.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/24-production-recovery-guard-ats-horistic/24-CONTEXT.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/24-production-recovery-guard-ats-horistic/24-RESEARCH.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/24-production-recovery-guard-ats-horistic/24-VALIDATION.md
@docs/operations/pm2-canonical.md
@modules/srv1-ops/systemd/pm2-ubuntu.service
@modules/srv1-ops/scripts/inviolable-watchdog.sh
@cli/omni/srv1_ops.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define production guard baseline</name>
  <files>modules/srv1-ops/configs/production-guard.yaml, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: baseline contains `pm2-ubuntu.service` expected type `oneshot`, `RemainAfterExit=yes`, `PM2_HOME=/home/ubuntu/.pm2` and no `PIDFile`.
    - Test 2: baseline defines namespace counts `atius: 12`, `horistic: 5`.
    - Test 3: baseline defines critical ports 3015, 8015, 3050, 8050, 8099 and 8199.
    - Test 4: baseline defines ATS and Horistic ecosystem paths.
    - Test 5: baseline defines known containers, required timers/services and systemd job classification rules.
    - Test 6: baseline marks public endpoint checks as GET/HEAD only.
  </behavior>
  <action>Create `modules/srv1-ops/configs/production-guard.yaml` with no secrets. Include PM2 unit contract, expected namespace counts, app name prefixes, critical app list, launcher one-shot rules, local port checks, public GET/HEAD endpoints, known containers, required timers/services and systemd job classification rules. Keep dynamic credentials out of config; use env var names only if needed.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "baseline"</automated>
  </verify>
  <done>Baseline is versioned, parseable and covers PRG-01..PRG-05 inputs without secrets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement read-only status and doctor</name>
  <files>modules/srv1-ops/scripts/production_guard.py, cli/omni/srv1_ops.py, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: mocked PM2 live/dump parity returns PASS.
    - Test 2: wrong namespace returns BLOCK.
    - Test 3: missing dump app returns BLOCK and does not call repair.
    - Test 4: launcher `waiting restart` without recent `[CYCLE_SUMMARY]` returns WARN/BLOCK per config.
    - Test 5: output redacts token/password/secret-like values.
    - Test 6: ecosystem parser asserts `autorestart`, `restart_delay`, `max_restarts`, `cwd`, `script`, minimum env keys and expected port ownership per app.
    - Test 7: container, timer and systemd job fixtures are classified as PASS/WARN/BLOCK with noisy jobs separated from critical boot blockers.
    - Test 8: endpoint checker refuses POST by default.
  </behavior>
  <action>Implement `production_guard.py` with subcommands `status` and `doctor`. It should read baseline config, inspect `systemctl show pm2-ubuntu.service`, PM2 `jlist`, `/home/ubuntu/.pm2/dump.pm2`, ATS/Horistic ecosystems, local ports, selected endpoint GET/HEADs, known containers, required systemd timers/services and `systemctl --user list-jobs`. Ecosystem validation must parse each app for namespace, cwd/script existence, `autorestart`, `restart_delay`, `max_restarts`, minimum env keys, expected app-owned port and redacted env output. Timer/job validation must classify noise such as unrelated backup jobs separately from critical boot blockers. Wire `omni srv1-ops production-guard status --json` and `doctor --json` in `cli/omni/srv1_ops.py`. Return structured statuses `pass`, `warn`, `block`, `unknown`; never mutate live state.</action>
  <verify>
    <automated>python3 -m py_compile modules/srv1-ops/scripts/production_guard.py && PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q</automated>
  </verify>
  <done>Operator can run one read-only command to see PM2 boot, namespaces, dump, ecosystems, endpoint, container, timer and job health.</done>
</task>

</tasks>

<verification>
Run in this order, fastest/lowest complexity first:

1. `python3 -m py_compile modules/srv1-ops/scripts/production_guard.py`
2. `PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "baseline or pm2 or namespace or ecosystem or doctor"`
3. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard status --json`
4. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard doctor --json`
5. `node "$HOME/.Codex/get-shit-done/bin/gsd-tools.cjs" graphify status`
6. `$gsd-verify-work 24`
</verification>

<success_criteria>
PRG-01..PRG-05 are complete when the guard can prove PM2 boot owner, live/dump
parity, namespace isolation, full ecosystem contract, critical endpoints,
containers, timers and systemd job health without mutating production.
</success_criteria>

<output>
Create `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/24-production-recovery-guard-ats-horistic/24-01-SUMMARY.md`
when done.
</output>
