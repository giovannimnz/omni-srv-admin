---
phase: 47.1-internal-dns-authority-and-freeipa-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/fleet-control-plane/configs/internal-dns-authority.yaml
  - modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py
  - modules/fleet-control-plane/tests/test_internal_dns_authority.py
  - modules/fleet-control-plane/tests/fixtures/internal-dns-authority.json
autonomous: true
requirements: [IDA-01, IDA-05, IDA-08]
must_haves:
  truths:
    - "Per D-01, desired state derives exactly the four canonical A/PTR pairs and five internal zones from inventory."
    - "Per D-03/D-07, no endpoint, runtime target, approval, or rollback path can be inferred from defaults or secret-bearing input."
    - "Per D-04, every later live layer is hash-bound to a backup, exact target, one writer, approval, bounded verify, and rollback contract."
    - "The final release schema covers IDA-01..IDA-08 and makes stale or altered evidence non-releasable."
  artifacts:
    - path: modules/fleet-control-plane/configs/internal-dns-authority.yaml
      provides: Inventory-driven authority, zone, prohibition, evidence, and release-gate schema.
    - path: modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py
      provides: Deterministic plan/apply/verify/rollback/release-preflight control surface.
      exports: [AuthorityConfig, HostIdentity, TargetManifest, OperationPlan, EvidenceManifest, ReleaseGate]
    - path: modules/fleet-control-plane/tests/test_internal_dns_authority.py
      provides: Offline contracts for every live-layer gate and IDA requirement.
    - path: modules/fleet-control-plane/tests/fixtures/internal-dns-authority.json
      provides: Sanitized positive, stale, tampered, forbidden-address, unknown-target, and release fixtures.
  key_links:
    - from: modules/fleet-control-plane/configs/internal-dns-authority.yaml
      to: inventory/hosts
      via: Exact host IDs load fqdn, oci_private_ip, and owner identity; no copied address table is authoritative.
      pattern: inventory_source|host_id|oci_private_ip|owner
    - from: modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py
      to: .planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/evidence
      via: Every command reads immutable input hashes and emits atomic, redacted JSON.
      pattern: input_hashes|evidence_hashes|approval_hash|writer_scope
  prohibitions:
    - statement: "No guessed secondary IP, .0.* host address, active 10.1.1.* path, srv-3 primary reuse, or Podman-only client endpoint is allowed."
      status: locked
      verification: "Contract fixtures for every prohibited class fail closed."
    - statement: "No secret value, private key, ticket, token, credential, plaintext SSH, srv-3 self-SSH, or public fallback for internal names is allowed."
      status: locked
      verification: "Redaction, transport, target, and NXDOMAIN fixtures fail closed."
---

<objective>
Implement the repository-owned desired-state and validator foundation for D-01, D-03, D-04, and D-07.

Purpose: give every later live mutation a deterministic, tested, hash-bound interface before any target discovery or apply.
Output: declarative authority config, reconciler CLI, sanitized fixtures, and focused tests.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-CONTEXT.md
@.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-RESEARCH.md
@.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-VALIDATION.md
</context>

## Artifacts this phase produces

| Kind | Identifier/path | Contract |
|---|---|---|
| Config | `internal-dns-authority.yaml` | Five zones, four inventory identities, forbidden networks, canary/TTL, layer/evidence policy, and release schema. |
| CLI | `reconcile-internal-dns-authority.py` | `plan`, `apply`, `verify`, `rollback-plan`, `release`, and `release-preflight`; no implicit target or credential fallback. |
| Types | `AuthorityConfig`, `HostIdentity`, `TargetManifest`, `OperationPlan`, `EvidenceManifest`, `ReleaseGate` | Stable contracts consumed by Plans 02-08 and Phase 48 preflight. |
| Release schema | `ReleaseGate` | Exact phase/status, IDA-01..08 map, selected-endpoint hash, replica/failure PASS, evidence/input hashes, generated/expiry timestamps, and `phase_48_03`. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 47.1-01-01: Define the inventory authority, target-manifest, and release schemas</name>
  <read_first>
    @.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-CONTEXT.md
    @.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-RESEARCH.md
    @inventory/hosts/atius-srv-1.yaml
    @inventory/hosts/atius-srv-2.yaml
    @inventory/hosts/atius-srv-3.yaml
    @inventory/hosts/horistic-srv.yaml
    @modules/fleet-control-plane/configs/control-plane.example.yaml
    @modules/fleet-control-plane/tests/test_codex_agents_policy.py
  </read_first>
  <files>modules/fleet-control-plane/configs/internal-dns-authority.yaml, modules/fleet-control-plane/tests/test_internal_dns_authority.py, modules/fleet-control-plane/tests/fixtures/internal-dns-authority.json</files>
  <behavior>
    - Exact inventory produces the four D-01 A/PTR pairs and `atius.internal` plus the four exact reverse zones.
    - Forbidden addresses, public internal fallback, unknown targets, missing owners/writer scopes/commands, and stale hashes are rejected.
    - Each client target requires exact policy surface/store, owner, writer scope, export/plan/apply/verify/rollback commands, and input hash; Windows NRPT and every edge target are mandatory or status is BLOCKED.
    - Release fixtures require IDA-01..08 PASS, selected-endpoint/input hashes, replica and failure-recovery PASS, all named evidence hashes, generated_at/valid_until, and `phase_48_03=RELEASED`.
  </behavior>
  <action>Write failing tests first, then define the config and sanitized fixtures. Per D-01, load the four canonical mappings from inventory and require `atius.internal`, `1.11.10.in-addr.arpa`, `1.12.10.in-addr.arpa`, `1.13.10.in-addr.arpa`, and `1.21.10.in-addr.arpa`, preserving SOA/NS/LDAP/Kerberos SRV/TXT expectations. Per D-03, represent endpoint selection only as a reference to current OCI evidence and an approved hash. Per D-04/D-07, define exact target, backup, owner, one-writer, transaction, approval, rollback, freshness, and structural-redaction fields for endpoint, FreeIPA/identity, resilience, CoreDNS, AdGuard, every client, and final release. Include the exact release fields consumed by 48-03; store hashes and fingerprints, never secret material or a candidate IP.</action>
  <verify><automated>python3 -m pytest modules/fleet-control-plane/tests/test_internal_dns_authority.py -q -x -k 'schema or inventory or target_manifest or release_gate'</automated></verify>
  <done>Schema tests prove exact identity, complete target ownership, forbidden-state rejection, and non-bypassable release inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 47.1-01-02: Implement the fail-closed reconciler and redacted evidence engine</name>
  <read_first>
    @modules/fleet-control-plane/configs/internal-dns-authority.yaml
    @modules/fleet-control-plane/tests/test_internal_dns_authority.py
    @modules/fleet-control-plane/tests/fixtures/internal-dns-authority.json
    @modules/fleet-control-plane/scripts/reconcile-codex-agents-policy.py
  </read_first>
  <files>modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py, modules/fleet-control-plane/tests/test_internal_dns_authority.py</files>
  <behavior>
    - `plan` never discovers or proposes a target; it accepts only sanitized proven target records and produces immutable SHA-256 transactions.
    - `apply` requires current plan, backup, target, approval, and writer-scope hashes and rejects stale/unknown/mismatched inputs before invoking an adapter.
    - `verify --layer {preflight,endpoint,freeipa,identity,resilience,coredns,adguard,clients,all}` emits bounded PASS/BLOCKED/FAIL JSON.
    - `release` and `release-preflight` recompute referenced hashes/timestamps and cannot regenerate or override a missing, stale, or failed release artifact.
  </behavior>
  <action>Implement the declared types and CLI using Python stdlib and existing project patterns; add no dependency. Use atomic JSON writes, canonical serialization, SHA-256, explicit `--target-manifest`, `--operation-plan`, `--approval-file`, `--backup-manifest`, `--writer-scope`, `--transaction`, `--layer`, `--component`, `--target`, `--evidence-dir`, `--redact`, and bounded timeout inputs. Adapters must be allowlisted and selected only by exact manifest records. Per D-07, reject any oci-admin/MCP write surface, secret-like key/value, unknown client, unresolved dirty-owner state, or layer not present in the approved hash. Preserve encrypted SSH/NFS boundaries as assertions and make CPU-heavy suite commands external to the CLI.</action>
  <verify><automated>python3 -m pytest modules/fleet-control-plane/tests/test_internal_dns_authority.py -q -x &amp;&amp; python3 -m py_compile modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py &amp;&amp; python3 modules/fleet-control-plane/scripts/reconcile-internal-dns-authority.py --help</automated></verify>
  <done>The reconciler exposes every deterministic gate used by Plans 02-08 and the Phase 48 release preflight without touching live state.</done>
</task>

</tasks>

<stop_conditions>
- Stop if inventory lacks any canonical host/owner field or contains a prohibited active address.
- Stop if a schema permits an unknown policy surface, implicit target, missing writer, unhashed approval, stale release, public internal fallback, or secret field.
- Stop if implementation would require a new package, edit concurrent oci_admin_http state, weaken encrypted SSH, remove NFS, or create srv-3 self-SSH.
</stop_conditions>

<threat_model>
## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-47.1-01 | Spoofing | high | Inventory A/PTR and release inputs | mitigate | Exact allowlists, canonical serialization, input/evidence hashes, and negative fixtures. |
| T-47.1-01-T | Tampering | high | Operation/approval/rollback manifests | mitigate | Atomic writes and recomputed SHA-256 before every apply/release. |
| T-47.1-01-E | Elevation of Privilege | high | Adapter dispatch | mitigate | Explicit layer/target/writer allowlists and fail-closed approval checks. |
| T-47.1-01-I | Information Disclosure | high | Evidence | mitigate | Structural redaction and secret-field rejection. |
| T-47.1-01-D | Denial of Service | high | Probe/apply runner | mitigate | Bounded timeouts and no live command in this plan. |
| T-47.1-SC | Tampering | medium | Package supply chain | accept | No package installation or version upgrade. |
</threat_model>

## Source Coverage Audit

| Source | ID/item | Coverage | Status |
|---|---|---|---|
| GOAL | Deterministic single-authority convergence | Tasks 01-01/02 establish the executable contract | COVERED |
| REQ | IDA-01, IDA-05, IDA-08 | Exact authority, rollback/evidence, release schema | COVERED |
| RESEARCH | Desired state, validator, tests, no new package | Tasks 01-01/02 | COVERED |
| CONTEXT | D-01, D-03, D-04, D-07 | Cited in truths/actions/prohibitions | COVERED |
| CONTEXT | Deferred Wayland/Headroom/public-zone/realm work | Excluded | EXCLUDED |

<verification>
Run the focused pytest suite, Python compile, CLI help, and `git diff --check` for the four declared files.
</verification>

<success_criteria>
- Exactly four inventory identities and five zones are accepted; every forbidden state is rejected.
- Every live group has a typed immutable transaction/approval/rollback interface.
- Release validation is hash- and time-aware and covers IDA-01..08 without secret persistence.
</success_criteria>

<output>
Create `.planning/phases/47.1-internal-dns-authority-and-freeipa-convergence/47.1-01-SUMMARY.md` when done.
</output>
