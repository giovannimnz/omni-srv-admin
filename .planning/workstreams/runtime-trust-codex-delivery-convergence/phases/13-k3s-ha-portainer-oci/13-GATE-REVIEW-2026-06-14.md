---
phase: 13
slug: k3s-ha-portainer-oci
date: 2026-06-14
status: no-cost-release-gates-in-progress
branch: docs/m005-gate-review-20260614
mode: read-only-gate-review
---

# Phase 13 Gate Review — 2026-06-14

## Verdict

Bootstrap/live baseline: PASS.

Full cluster release: BLOCK until the new no-cost gates are closed: GDrive DR validation, Tailscale management fallback, Cloudflare Access or accepted Basic Auth, Jenkins Podman hotfix and Ubuntu Pro/ESM Apps validation.

Fleet cooldown revalidation: PASS. On 2026-06-14 18:32 BRT, SRV-1 improved from `critical` to `healthy`; Fleet reported `load_1m=5.06`, `memory_used_percent=79.04`, PgBouncer/PostgreSQL `active`, and SRV-2/SRV-3 `healthy`.

This review was read-only. It did not install K3s, write `/etc/rancher`, change firewall, change DNS, disable swap, restart services, publish Cloudflare changes or mutate live hosts.

## Branch decision

Current worktree: `/home/ubuntu/GitHub/omni-srv-admin-m005-execute`.

Decision: keep M005 gate-review work on local branch `docs/m005-gate-review-20260614` and do not push from this review.

Alternatives considered:

- Reuse `docs/m005-watchdog-basic-auth-fix`: rejected because its upstream branch was removed, even though content matches `origin/main`.
- Switch to `main`: rejected because the orchestration checkpoint explicitly says not to use the main worktree for M005 execution/review.
- Reopen `codex/k3s-portainer-oci-plan`: rejected for this checkpoint because it is behind the live docs already merged through the M005 PR chain.

## Live evidence

Validation time: 2026-06-14 03:00-03:20 BRT.

| Area | Evidence | Result |
|---|---|---|
| Pre-orchestration backup | `/home/ubuntu/.hermes/backups/codex-gsd-orch-20260613-200035/omni-m005-planning.tgz` exists | PASS |
| OS | SRV-1/SRV-2/SRV-3 all Ubuntu 24.04.4 LTS, aarch64, kernel `6.17.0-1016-oracle` | PASS |
| WireGuard mesh | All hosts pinged `10.1.1.1`, `10.1.1.2`, `10.1.1.7` successfully | PASS |
| K3s service | `k3s` active on SRV-1/SRV-2/SRV-3 | PASS |
| K3s nodes | 3/3 `Ready`, all `control-plane,etcd`, version `v1.35.5+k3s1` | PASS |
| K3s network | `node-ip` and `advertise-address` use WireGuard IPs; `flannel-iface: wg0` | PASS |
| Pods | No pods outside `Running`/`Succeeded` | PASS |
| Portainer | `portainer` deployment 1/1, API status over edge auth returned HTTP 200 | PASS |
| Observability | Prometheus, Alertmanager, Grafana, kube-state-metrics, operator and node-exporter running | PASS |
| Watchdog | SRV-1 log reports `OK ready_nodes=3/3 notready_pods=0` every minute | PASS |
| Edge protection | Public no-auth returns HTTP 401 for `docker`, `portainer`, `grafana`; authenticated checks returned 200 | PASS |
| Public K3s ingress | Public TCP checks for `6443`, `2379`, `2380`, `10250` on all known public IPs returned closed | PASS |
| Host firewall guard | `atius-k3s-firewall.service` active on all three nodes | PASS |
| Secrets permissions | Portainer/Grafana/edge secrets mode `0600`; Apache htpasswd `0640 root:www-data` | PASS |
| M004 Fleet health | Fleet DB is reachable, PgBouncer/PostgreSQL are `active`, and SRV-1/SRV-2/SRV-3 report `healthy` after post-cooldown revalidation at 2026-06-14 18:32 BRT | PASS |
| GDrive DR bundle | User accepted GDrive DR as the M005 no-cost replacement for OCI snapshot IDs; requires fresh/uploaded bundle path and checksum validation | BLOCK |
| PVC backup strategy | Crash-consistent bundle executed at `/home/ubuntu/.backups/k3s-local-path/20260614-150944` with etcd snapshot + `local-path` PVC archives + `SHA256SUMS`; `local-path` + GDrive backup/restore is accepted for M005 | PASS |
| Restore runbook | Detailed non-destructive restore drill exists at `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/13-k3s-ha-portainer-oci/13-RESTORE-DRILL-2026-06-14.md` | PASS |
| HA storage | Portainer, Grafana, Prometheus and Alertmanager PVCs are still `local-path`/RWO; user accepted this for M005 when paired with GDrive backup/restore. Longhorn/RWX remains deferred | PASS |
| Tailscale fallback outside WireGuard | Tailscale is online on SRV-1 (`100.76.56.62`), SRV-2 (`100.93.43.113`) and SRV-3 (`100.72.102.57`); bidirectional `tailscale ping` passed. **ACL closed 2026-06-16**: giovannimunizds@gmail.com + 3 SRVs, ports 22/6432/6443/8505/8506 only. MagicDNS OFF, `--accept-dns=false` applied on all 3 SRVs. WireGuard remains K3s transport. See `13-ACL-CLOSURE-2026-06-16.md` and `60-LOGS/2026-06-16/2026-06-16-tailscale-acl-closed-and-magicdns-off.md` (vault). | **PASS** |
| API HA endpoint | SRV-2/SRV-3 configs still use `server: https://10.1.1.1:6443`; no internal VIP/LB endpoint documented | PARTIAL |
| Network map | Repo snapshot exists at `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/13-k3s-ha-portainer-oci/13-NETWORK-MAP-2026-06-14.md` and was promoted to `DbOmniFleet/TbConfigItems.key=m005.cluster_operational_snapshot` | PASS |
| Cloudflare Access | User selected Cloudflare Access if free/available; Basic Auth remains fallback until Access is validated | BLOCK |
| Jenkins | `jenkins.atius.com.br` currently returns Apache 503 because Jenkins is not listening on `127.0.0.1:8085`; service depends on missing `/var/run/docker.sock` and must be fixed for Podman socket/CLI | BLOCK |
| Ubuntu Pro / ESM Apps | SRV-1 and SRV-2 are attached with `esm-apps`/`esm-infra` enabled. SRV-3 reports `attached=false`; no Ubuntu Pro token was found in common shell/config env files by redacted-name scan | BLOCK |

## Host snapshot

| Host | WG IP | K3s | Firewall guard | Edge local services | Resource note |
|---|---|---|---|---|---|
| ATIUS-SRV-1 | `10.1.1.1/32` | active | active | Portainer/Grafana port-forward and watchdog active | `kubectl top`: 18% CPU, 81% memory |
| ATIUS-SRV-2 | `10.1.1.2/24` | active | active | edge port-forward/watchdog inactive by design | `kubectl top`: 8% CPU, 29% memory |
| ATIUS-SRV-3 | `10.1.1.7/32` | active | active | edge port-forward/watchdog inactive by design | `kubectl top`: 8% CPU, 16% memory |

## Storage snapshot

| Claim | Namespace | Size | Access | StorageClass | Risk |
|---|---|---:|---|---|---|
| `portainer` | `portainer` | 10Gi | RWO | `local-path` | pinned to local node data path |
| `omni-monitoring-grafana` | `monitoring` | 10Gi | RWO | `local-path` | dashboard/config persistence not node-independent |
| `prometheus-omni-monitoring-prometheus-db-prometheus-omni-monitoring-prometheus-0` | `monitoring` | 20Gi | RWO | `local-path` | metrics history tied to local disk |
| `alertmanager-omni-monitoring-alertmanager-db-alertmanager-omni-monitoring-alertmanager-0` | `monitoring` | 2Gi | RWO | `local-path` | alert state tied to local disk |

## Network snapshot

Known public IP negative checks:

| Host | Public IP | `6443` | `2379` | `2380` | `10250` |
|---|---|---|---|---|---|
| SRV-1 | `137.131.190.161` | CLOSED | CLOSED | CLOSED | CLOSED |
| SRV-2 | `129.148.47.32` | CLOSED | CLOSED | CLOSED | CLOSED |
| SRV-3 | `136.248.126.12` | CLOSED | CLOSED | CLOSED | CLOSED |

Podman networks observed read-only:

| Host | Network | Subnet | Gateway |
|---|---|---|---|
| SRV-1 | `podman` | `10.89.0.0/24` | `10.89.0.1` |
| SRV-1 | `srv1-podman` | `10.10.1.0/24` | `10.10.1.1` |
| SRV-2 | `podman` | `10.10.200.0/24` | `10.10.200.1` |
| SRV-2 | `srv2-podman` | `10.10.2.0/24` | `10.10.2.1` |
| SRV-3 | `srv3-podman` | `10.10.3.0/24` | `10.10.3.1` |

## What remains before full cluster release

1. Create/upload a fresh GDrive DR bundle and validate `SHA256SUMS`.
2. Record/review Tailscale ACL for the management path; bidirectional ping is already validated across SRV-1/SRV-2/SRV-3 and K3s transport remains WireGuard-dependent for M005.
3. Configure Cloudflare Access if the account supports it for free; otherwise record Basic Auth as the accepted no-cost fallback.
4. Recover Jenkins with Podman socket/CLI so `jenkins.atius.com.br` no longer returns Apache 503.
5. Attach SRV-3 to Ubuntu Pro without logging the token, then enable/validate ESM Apps; SRV-1/SRV-2 already pass.
6. Promote the new runbooks into the canonical vault record along with the DB-backed operational snapshot.

## Release command after blockers close

Run only after the missing values are real and the waivers, if any, are intentional.
This command does not set gates for you; it verifies environment values that
were set after each gate was actually validated:

```bash
cd /home/ubuntu/GitHub/omni-srv-admin-m005-execute
required_gate() {
  name="$1"
  expected="$2"
  actual="${!name:-}"
  test "$actual" = "$expected" || {
    printf 'gate failed: %s expected=%s actual=%s\n' "$name" "$expected" "${actual:-unset}" >&2
    return 1
  }
}
required_gate M004_FLEET_GATE healthy &&
required_gate M005_GDRIVE_DR_GATE uploaded-and-checksum-verified &&
required_gate M005_TAILSCALE_GATE validated-management-fallback &&
required_gate M005_STORAGE_GATE accepted-local-path-gdrive-restore &&
required_gate M005_EDGE_AUTH_GATE cloudflare-access-or-basic-auth-accepted &&
required_gate M005_JENKINS_GATE podman-ui-healthy &&
required_gate M005_UBUNTU_PRO_GATE esm-apps-enabled-on-3srv &&
echo 'M005_FULL_CLUSTER_RELEASE=PASS'
```

GSD route to continue after the shell gate passes:

```text
/gsd-execute-phase 13 --live --confirm-gates --text
```
