---
phase: 23-omni-fleet-governance-cockpit-sem-landscape
plan: 03
type: execute
wave: 2
depends_on:
  - 23-02
files_modified:
  - modules/fleet-control-plane/migrations/0004_governance_profiles.sql
  - cli/omni/fleet.py
  - cli/omni/fleet_governance.py
  - modules/fleet-control-plane/tools/import_managed_apps_seed.py
  - modules/fleet-control-plane/tests/test_m023_governance.py
  - modules/fleet-control-plane/tools/validate_m023.py
autonomous: true
requirements:
  - GOV-04
  - GOV-05
  - GOV-06
  - GOV-07
  - GOV-08
  - GOV-09
must_haves:
  truths:
    - "Desired-state profiles modelam required, forbidden, pinned, held e manual."
    - "Repository/source profiles representam APT .sources, PPAs e Ubuntu Pro ESM sem gravar secrets, per D-06."
    - "Security reporting correlaciona pacote, origem, host, CVE/USN e status central."
    - "Update execution permanece em `TbUpdatePlans` aprovados, local agent e command allowlist, per D-05."
  artifacts:
    - path: "modules/fleet-control-plane/migrations/0004_governance_profiles.sql"
      provides: "Governance schema for profiles, observations, drift, security and update profiles."
    - path: "cli/omni/fleet_governance.py"
      provides: "Governance CLI helpers and report rendering."
      exports: ["governance"]
    - path: "modules/fleet-control-plane/tools/validate_m023.py"
      provides: "Offline validation for GOV schema and execution safety."
    - path: "modules/fleet-control-plane/tools/import_managed_apps_seed.py"
      provides: "Dry-run/import adapter that maps managed-apps programs, repositories, policies and customizations into governance profiles."
    - path: "modules/fleet-control-plane/tests/test_m023_governance.py"
      provides: "Pytest coverage for migration, CLI, secret redaction and no SSH apply."
  key_links:
    - from: "cli/omni/fleet_governance.py"
      to: "modules/fleet-control-plane/migrations/0004_governance_profiles.sql"
      via: "table names and report fields"
      pattern: "TbDesiredStateProfiles|TbSecurityFindings|TbUpdateProfiles"
    - from: "cli/omni/fleet.py"
      to: "TbUpdatePlans"
      via: "existing queue-update and agent execution"
      pattern: "queue-update|agent once|TbUpdatePlans"
---

<objective>
Adicionar o schema e a superficie CLI de governanca que tornam Omni Fleet a
camada central de desired state, repository profiles, update profiles, drift e
security reporting.

Purpose: cobrir GOV-04..GOV-09 sem criar execucao remota generica.
Output: migration `0004_governance_profiles.sql`, grupo `omni fleet governance`,
security report, drift/profile commands, validator M023 e testes.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-CONTEXT.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-RESEARCH.md
@.planning/workstreams/runtime-trust-codex-delivery-convergence/REQUIREMENTS.md
@docs/fleet/control-plane.md
@modules/fleet-control-plane/README.md
@cli/omni/fleet.py
@cli/omni/managed_apps.py
@docs/operations/managed-apps.md
@modules/managed-apps/configs/programs.json
@cli/omni/fleet_collectors.py
@modules/fleet-control-plane/migrations/0001_fleet_control_plane.sql
@modules/fleet-control-plane/migrations/0003_agent_executor_monitoring.sql
@modules/fleet-control-plane/tests/test_m004_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add governance migration and schema validation</name>
  <files>modules/fleet-control-plane/migrations/0004_governance_profiles.sql, modules/fleet-control-plane/tests/test_m023_governance.py, modules/fleet-control-plane/tools/validate_m023.py</files>
  <behavior>
    - Test 1: migration contains `TbDesiredStateProfiles`, `TbDesiredStateRules`, `TbRepositoryProfiles`, `TbRepositorySources`, `TbProgramObservations`, `TbDriftFindings`, `TbSecurityFindings` and `TbUpdateProfiles`.
    - Test 2: rules constrain policy to `required`, `forbidden`, `pinned`, `held`, `manual`.
    - Test 3: repository/source tables contain `secret_ref` and do not contain raw token/password columns.
    - Test 4: `TbUpdateProfiles` models window, serial/staggered strategy, security-only/all-upgrades scope, approval and rollback/audit fields.
    - Test 5: `modules/managed-apps/configs/programs.json` is loaded by the managed-apps seed adapter and Chromium, Firefox and Bitwarden become DB-backed governance profile payloads or explicit compatibility links.
  </behavior>
  <action>Create migration `0004_governance_profiles.sql` as an idempotent PostgreSQL migration. Add new governance tables without dropping or renaming existing M004/M003 tables. Extend `TbUpdatePlans` only with additive nullable columns needed to link `update_profile_id`, `upgrade_scope`, `scheduled_window`, `stagger_group` and governance metadata; keep existing `rollback_ref`, approval constraints, leases and idempotency. Create indexes for host/profile/status/report queries. Reuse the semantics already proven in `managed-apps` for `repositories`, `policies` and `customizations`, including Bitwarden force-install and Chromium Google homepage/search defaults, but move them into central DB-backed structures. Implement `import_managed_apps_seed.py` as a dry-run-first compatibility adapter that reads `modules/managed-apps/configs/programs.json`, emits/loads governance profile payloads for Chromium/Firefox/Bitwarden, preserves source provenance and fails on unmapped policies or customizations. Implement `validate_m023.py` as an offline scanner that reads migrations and fails if required tables/constraints are missing, if forbidden secret columns are introduced, or if the managed-apps seed cannot be mapped without divergence.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest modules/fleet-control-plane/tests/test_m023_governance.py -q && python3 modules/fleet-control-plane/tools/validate_m023.py</automated>
  </verify>
  <done>Migration and validator cover governance schema, additive update-plan profile links, and secret_ref-only source/profile handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add governance CLI and security reporting</name>
  <files>cli/omni/fleet.py, cli/omni/fleet_governance.py, modules/fleet-control-plane/tests/test_m023_governance.py</files>
  <behavior>
    - Test 1: `omni fleet governance profiles list --json` renders profile metadata without secrets.
    - Test 2: `omni fleet governance drift --host atius-srv-1 --json` reports compliant, drifted and missing states from mocked observations/rules.
    - Test 3: `omni fleet governance security report --host atius-srv-1 --json` correlates package, host, origin, CVE/USN and status.
    - Test 4: `agent collect-security --json` is read-only and does not call `pro fix` except when explicitly rendered as dry-run text.
  </behavior>
  <action>Create `cli/omni/fleet_governance.py` and wire it under the existing `fleet` Click group as `governance`. Implement read-only commands: `profiles list`, `profiles show`, `drift --host/--all`, `security report --host/--all`, and `landscape-parity --json` returning machine-readable status. Add `agent collect-security` in `fleet.py` or the governance module using local read-only inputs from Ubuntu Pro/APT where present; missing `pro` must produce a warning and fallback to APT/security-origin evidence. Reuse output/rendering and drift vocabulary from `managed-apps` where it reduces operator surprise, but keep the new surface DB-backed and fleet-generic. Use existing `_db_env()` for `--db` reads/writes and redact secrets in all output per D-06. User-facing command help/errors remain PT-BR.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest modules/fleet-control-plane/tests/test_m023_governance.py modules/fleet-control-plane/tests/test_m023_collectors.py -q</automated>
  </verify>
  <done>Governance CLI reports profiles, drift and security findings from DB or mocked data, and security collection remains read-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve approved update execution boundary</name>
  <files>cli/omni/fleet.py, cli/omni/fleet_governance.py, modules/fleet-control-plane/tests/test_m023_governance.py, modules/fleet-control-plane/tools/validate_m023.py</files>
  <behavior>
    - Test 1: governance remediation output creates pending `queue-update`/`TbUpdatePlans` payloads rather than running shell commands.
    - Test 2: no governance command contains a generic `ssh` apply path.
    - Test 3: approved execution still requires existing `approval_state=approved`, `approved_by`, `approved_at`, target host match and command allowlist.
    - Test 4: validator fails if `ssh` mutation, direct PostgreSQL endpoint bypass or package-manager update commands appear in governance code.
  </behavior>
  <action>Add optional remediation rendering only as dry-run/pending plan output. If a command needs to queue work, it must call or mirror the existing `queue-update` contract and write `TbUpdatePlans` rows with `approval_state=pending` unless the existing explicit `--approve` path is used. Do not add `ssh`, `scp`, `fabric`, Ansible-style apply, or direct package manager execution from the requester. Update `validate_m023.py` to scan `cli/omni/fleet.py`, `cli/omni/fleet_governance.py` and collector modules for generic SSH apply and mutating package commands, while allowing the existing local agent execution functions.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest modules/fleet-control-plane/tests/test_m023_governance.py modules/fleet-control-plane/tests/test_m004_contract.py -q && python3 modules/fleet-control-plane/tools/validate_m023.py</automated>
  </verify>
  <done>GOV-09 is enforced by tests and validator: central governance proposes/queues plans only; execution remains target-local via approved agent allowlist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI-to-DbOmniFleet | Operator requests profile, drift and security data through PgBouncer. |
| collector/security-output-to-DB | Local command output becomes central findings. |
| governance-to-update-queue | Governance recommends remediation but must not execute it directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-23-03-01 | Tampering | desired-state rules | mitigate | SQL constraints for allowed policies and enabled flags; tests cover policy set. |
| T-23-03-02 | Information Disclosure | repository profiles | mitigate | `secret_ref` only, redaction tests, validator forbids token/password columns. |
| T-23-03-03 | Elevation | remediation/update execution | mitigate | No SSH apply; only pending/approved `TbUpdatePlans` and local agent allowlist per D-05. |
| T-23-03-04 | Repudiation | update/security actions | mitigate | Preserve `TbAuditEvents`, `TbUpdatePlans` approval/lease/output metadata and add finding timestamps. |
| T-23-03-SC | Tampering | package installs | accept | No dependency installs in this plan. |
</threat_model>

<verification>
Run:

- `PYTHONPATH=cli pytest modules/fleet-control-plane/tests/test_m023_governance.py modules/fleet-control-plane/tests/test_m023_collectors.py modules/fleet-control-plane/tests/test_m004_contract.py -q`
- `python3 modules/fleet-control-plane/tools/validate_m023.py`
- `python3 modules/fleet-control-plane/tools/import_managed_apps_seed.py --source modules/managed-apps/configs/programs.json --dry-run --json`
- `PYTHONPATH=cli python3 -m omni fleet governance profiles list --json`
- `PYTHONPATH=cli python3 -m omni fleet governance drift --host atius-srv-1 --json`
- `PYTHONPATH=cli python3 -m omni fleet governance security report --host atius-srv-1 --json`
</verification>

<success_criteria>
GOV-04..GOV-09 are complete when the DB schema represents desired/current/policy/drift/security/update profile state, CLI reports that state centrally, managed-apps seeds map into DB-backed governance profiles or explicit compatibility links without divergence, repository profiles avoid secrets, and every remediation path preserves approved `TbUpdatePlans` plus local `omni fleet agent` execution.
</success_criteria>

<output>
Create `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-03-SUMMARY.md` when done.
</output>
