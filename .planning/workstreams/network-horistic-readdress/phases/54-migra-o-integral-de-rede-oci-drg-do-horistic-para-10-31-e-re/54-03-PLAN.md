---
phase: 54-migra-o-integral-de-rede-oci-drg-do-horistic-para-10-31-e-re
plan: 03
type: execute
wave: 3
depends_on: ["54-02"]
files_modified:
  - /etc/netplan/*
  - /etc/rancher/k3s/config.yaml
  - /etc/hosts
  - .planning/workstreams/network-horistic-readdress/phases/54-migra-o-integral-de-rede-oci-drg-do-horistic-para-10-31-e-re/54-03-EVIDENCE.md
autonomous: false
requirements: [NET-04]
must_haves:
  truths:
    - "Horistic anuncia 10.31.1.31 e mantém .21/fallback durante a transição"
    - "K3s InternalIP e rotas de retorno são atualizados sem perder quorum"
    - "163.176.232.119 é reassociado ao private IP novo, nunca liberado"
  artifacts:
    - path: "54-03-EVIDENCE.md"
      provides: "Before/after addresses, routes, public-IP association and K3s readback"
  key_links:
    - from: "new private IP"
      to: "reserved public IP"
      via: "OCI public-ip update/reassign"
    - from: "K3s node config"
      to: "10.31.1.31"
      via: "InternalIP and advertised address"
---

<objective>Configurar o host e mover a identidade de serviço para `.31` com dual-path.</objective>

<tasks>
<task type="auto">
  <name>Task 0: Assertar gates PASS da Wave 1</name>
  <files>54-02-GATE.json; vnic-attachment-receipt.json</files>
  <action>Verificar `status=PASS`, plan/hash e token `APROVADO: vnic-attached` antes de configurar o host.</action>
  <verify><automated>jq -e '.status == "PASS" and .plan == "54-02" and (.evidence_sha256 | length == 64)' .../54-02-GATE.json</automated></verify>
  <done>Sem PASS, o worker e o public IP permanecem intocados.</done>
</task>
<task type="checkpoint:human-verify" gate="before-destructive">
  <name>Task 1: Aprovar cutover do host</name>
  <files>.planning/workstreams/network-horistic-readdress/phases/54-migra-o-integral-de-rede-oci-drg-do-horistic-para-10-31-e-re/54-03-EVIDENCE.md</files>
  <action>Validar acesso SSH por `10.100.100.4`, console serial/OCI, VNIC ACTIVE e janela de manutenção.</action>
  <verify>Registrar fallback alcançável e a autorização.</verify>
  <done>Existe `APROVADO: phase54-wave3`.</done>
  <resume-signal>Prosseguir com `APROVADO: phase54-wave3`.</resume-signal>
</task>
<task type="auto">
  <name>Task 2: Configurar dual-bind e identidade K3s</name>
  <files>/etc/netplan/*; /etc/hosts; /etc/systemd/system/k3s-agent.service; /etc/default/k3s-agent; /etc/rancher/k3s/config.yaml; 54-03-EVIDENCE.md</files>
  <action>Configurar o VNIC/private IP novo no SO sem remover `.21`; testar `ip -br -4`, rotas, ARP, source-address selection e retorno DRG/WG. Como Horistic é `k3s-agent` worker, localizar e registrar a unidade/env real (`systemctl cat k3s-agent`, `/etc/default/k3s-agent` ou equivalente), atualizar `node-ip`/endpoint, drenar e reingressar o worker somente se necessário, mantendo o caminho antigo.</action>
  <verify>Registrar em `54-03-EVIDENCE.md` a unidade/env efetiva, `node-ip` before/after, drain/rejoin trigger, CNI routes, return-route probes e receipt de rollback. `kubectl get nodes -o wide`, Ready/roles, agent service, endpoints e workloads retornam sem regressão; nenhum requisito de quorum etcd é atribuído ao worker.</verify>
  <done>Host anuncia `10.31.1.31`, o worker está Ready e o serviço continua acessível pelo fallback `.21`/WG.</done>
</task>
<task type="auto">
  <name>Task 3: Reassociar public IP e testar edge</name>
  <files>OCI reserved public IP association; 54-03-EVIDENCE.md</files>
  <action>Após TCP/HTTPS/SSH pelo novo caminho, capturar `public_ip_ocid`, `source_private_ip_ocid` (atual `10.0.0.65`) e `target_private_ip_ocid` (novo `10.31.1.31`); validar que o target não possui public IP. Executar `oci network public-ip update --profile horistic --public-ip-id &lt;public_ip_ocid&gt; --private-ip-id &lt;target_private_ip_ocid&gt; --opc-request-id phase54-public-&lt;timestamp&gt;` (ou `oci_plan` equivalente), com aprovação e readback. Preparar e dry-run/testar a reversa `oci network public-ip update --profile horistic --public-ip-id &lt;public_ip_ocid&gt; --private-ip-id &lt;source_private_ip_ocid&gt;` sem liberar a reserva.</action>
  <verify>Readback mostra `RESERVED/ASSIGNED`, OCIDs source/target e public IP no alvo sem outro public IP; executar origin direto e Cloudflare/public-origin smoke, DNS público, TLS, SSH e rota de retorno. Reversa deve ter receipt de dry-run/readback antes do gate.</verify>
  <done>Edge público e rollback de reassociação estão comprovados.</done>
</task>
<task type="auto">
  <name>Task 4: Executar validação automática e liberar a Wave 3</name>
  <files>54-03-EVIDENCE.json; 54-03-GATE.json; public-ip-reassignment-receipt.json</files>
  <action>Executar o runner read-only para endereços/rotas/ARP/source selection, K3s agent worker, CNI/return path, public-IP source/target OCIDs, `RESERVED/ASSIGNED`, ausência de efêmero, origin direto/Cloudflare, DNS/TLS/SSH e reversa dry-run.</action>
  <verify><automated>python3 modules/fleet-control-plane/scripts/phase54_network_gate.py final --plan 54-03 --evidence .../54-03-EVIDENCE.json --gate .../54-03-GATE.json --redact</automated><manual>Confirmar `status=PASS` antes de migrar serviços e DNS.</manual></verify>
  <done>Gate PASS contém receipt de public-IP e worker; qualquer falha restaura o host/K3s pelo fallback e bloqueia 54-04.</done>
</task>
</tasks>
