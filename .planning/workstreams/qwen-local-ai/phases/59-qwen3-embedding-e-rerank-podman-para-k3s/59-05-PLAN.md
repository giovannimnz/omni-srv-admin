---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 05
type: execute
wave: 4
depends_on: [59-04]
execution_root: /home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59
external_preconditions:
  - "The versioned Phase 59 bootstrap contract and its external receipt passed before Wave 0 and remain valid for this exact plan bundle/worktree."
files_modified:
  - scripts/embeddings-bench/router-owner-run.py
  - scripts/embeddings-bench/tests/test_router_owner_run.py
  - scripts/embeddings-bench/qdrant-alias-authority.py
  - scripts/embeddings-bench/tests/test_qdrant_alias_authority.py
  - modules/srv1-ops/configs/qdrant-alias-writer-policy.json
  - modules/srv1-ops/configs/qdrant-alias-arbiter.env.tpl
  - modules/srv1-ops/systemd/qdrant-alias-arbiter.service
  - modules/srv1-ops/scripts/install-qdrant-alias-arbiter.sh
  - "${isolated_worktree_path}/service/embeddinggovernor/governor.go"
  - "${isolated_worktree_path}/service/embeddinggovernor/governor_test.go"
  - "${isolated_worktree_path}/service/modelcatalog/catalog.go"
  - "${isolated_worktree_path}/service/modelcatalog/catalog_test.go"
  - "${isolated_worktree_path}/service/qdrantalias/arbiter.go"
  - "${isolated_worktree_path}/service/qdrantalias/arbiter_test.go"
  - "${isolated_worktree_path}/cmd/qdrant-alias-arbiter/main.go"
  - "${isolated_worktree_path}/cmd/qdrant-alias-arbiter/main_test.go"
  - "${isolated_worktree_path}/relay/embedding_handler.go"
  - "${isolated_worktree_path}/relay/rerank_handler.go"
  - "${isolated_worktree_path}/relay/pipeline_governor_test.go"
  - "${isolated_worktree_path}/router/api-router.go"
  - "${isolated_worktree_path}/dto/embedding.go"
autonomous: true
requirements:
  - QAI-01
  - QAI-03
  - QAI-05
  - D-05
  - D-07
  - D-08
  - D-13
  - D-18
  - D-21
  - D-24
  - D-25
  - D-26
  - D-29
must_haves:
  truths:
    - "The authoritative owner repo is inventoried read-only at `/home/ubuntu/GitHub/containers/router-ai-atius`; its dirty tree is reference-only, while every mutation, test, build, commit, push and activation occurs only in the frozen isolated branch/worktree."
    - "The selected router base contains current `origin/main` without regression and contains the active runtime SHA as an ancestor; owner HEAD, origin/main and active SHA are reconciled explicitly before edits."
    - "Per D-07/D-08, Redis persists the complete pipeline state machine with exactly two global full-cycle slots using one Redis Cluster hash-tag, monotonic fencing epoch/owner token, continuation priority and idempotent success/error/cancel/TTL/restart/failover terminals."
    - "Standalone embedding admission is separate from full-cycle leases; Qwen model aliases and `/v1/rerank` are active without changing titular production routes."
    - "The operationally router-owned but process/UID/network-isolated Qdrant alias arbiter is provisioned as the sole durable writer; the Router, watchdog and reindex processes have no Qdrant write credential or direct egress, old paths are revoked, and every mutation uses only the arbiter's authenticated local interface."
    - "Router DB mutation is backup/CAS/readback protected, raw rerank logging is removed, and all heavy tests/builds use the canonical 20% wrapper."
  artifacts:
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json"
      provides: "Actual files, symbols, tests, DB schema, Redis primitives and immutable isolated worktree identity"
      contains: "isolated_worktree_path"
    - path: "scripts/embeddings-bench/router-owner-run.py"
      provides: "Allowlisted drift-resistant execution exclusively inside the frozen owner worktree"
      exports: ["verify", "run"]
    - path: "${isolated_worktree_path}/service/embeddinggovernor/governor.go"
      provides: "Persistent complete-pipeline Redis lifecycle"
      contains: "QUEUED"
    - path: "${isolated_worktree_path}/relay/rerank_handler.go"
      provides: "Authenticated public /v1/rerank adapter with redacted logging"
      contains: "/v1/rerank"
    - path: "${isolated_worktree_path}/service/qdrantalias/arbiter.go"
      provides: "Sole durable Qdrant alias writer library with Redis-AOF journaled INFLIGHT/reconciliation states"
      contains: "INFLIGHT"
    - path: "modules/srv1-ops/systemd/qdrant-alias-arbiter.service"
      provides: "Dedicated single-active service principal and private Unix-socket boundary"
      contains: "User=qdrant-arbiter"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-LIFECYCLE.json"
      provides: "Tests, build, DB CAS, route activation and rollback evidence"
      contains: "titular_cutover"
  key_links:
    - from: "${isolated_worktree_path}/relay/embedding_handler.go"
      to: "${isolated_worktree_path}/service/embeddinggovernor/governor.go"
      via: "discovered full-cycle or standalone admission interface"
      pattern: "pipeline"
    - from: "${isolated_worktree_path}/relay/rerank_handler.go"
      to: "k8s/qwen-production/qwen3-reranker.yaml"
      via: "private native /rerank service selected by exact model alias"
      pattern: "reranker-qwen3-0.6b-int8-v1"
---

<objective>
Extend the authoritative router/governor with persistent Redis full-cycle control and Qwen routes while preserving GTE as titular.

Purpose: Implement QAI-01/QAI-03/QAI-05 and D-05/D-07/D-08/D-13/D-18/D-21/D-24 without guessing owner-repo symbols.
Output: Owner inventory, isolated source changes, Redis state machine, aliases/API, Router DB CAS evidence and Wave 4 gate.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-VALIDATION.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-04-SUMMARY.md
@modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
</context>

<preconditions>
Require local srv1 execution from `/home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59`, validate the receipt, and run `phase59-autopilot-bootstrap.py verify-runtime` with the frozen executor owner before every task. Run owner-repo work locally; owner checkout is read-only and writes use only the isolated helper. Fallback is `$gsd-execute-phase 59 --ws qwen-local-ai`.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the owner repo and establish non-overlapping write ownership</name>
  <files>scripts/embeddings-bench/router-owner-run.py (NEW), scripts/embeddings-bench/tests/test_router_owner_run.py (NEW), .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json</files>
  <read_first>59-WAVE-3-GATE.json, 59-AUTHORITY-INVENTORY.json, 59-QWEN-ROLLOUT.json, modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md, live router identity, `/home/ubuntu/GitHub/containers/router-ai-atius` branch/HEAD/status/diffs</read_first>
  <router_worktree_lifecycle>The helper implements explicit `BASE_CLEAN`, `OWNED_DIRTY`, and `COMMITTED_DESCENDANT` states. `BASE_CLEAN` requires the frozen base and empty status. `OWNED_DIRTY` permits only allowlisted paths whose preimage and current diff/hunk hash chain are recorded by this phase; untracked files require a predeclared `new_files` entry. `COMMITTED_DESCENDANT` requires the new HEAD to descend from the immutable selected base and records the tested commit. External/unowned dirt, branch movement, force-update, unexpected path/hunk or owner-checkout drift always blocks. The immutable base is never rewritten; receipts advance monotonically through owned states.</router_worktree_lifecycle>
  <action>Create fixture-tested `router-owner-run.py` with `verify`, `begin-owned-change`, `record-owned-diff`, `record-commit`, and `run -- COMMAND`: read `isolated_worktree_path`, selected base, owner HEAD, `origin/main`, active runtime SHA, allowed/new files, lifecycle state and worktree identity from `59-ROUTER-OWNER-INVENTORY.json`; reject a non-absolute path, any path outside `/home/ubuntu/GitHub/worktrees/router-ai-atius-phase59-*`, symlinks/realpath mismatch, owner-checkout path, changed common-dir/worktree registration, unowned dirt/untracked file, unallowlisted file/hunk or inventory/hash-chain drift. `run` sets cwd to the verified isolated worktree and never accepts caller cwd; it accepts legitimate phase-owned edits only in `OWNED_DIRTY` and legitimate tested commits only in `COMMITTED_DESCENDANT`. From the Phase 59 omni worktree, inspect `/home/ubuntu/GitHub/containers/router-ai-atius` read-only, fetch the router remote without changing the owner tree, and record owner HEAD/status/diff hashes, exact `origin/main`, active runtime SHA/image and protected seams. Treat owner dirty content only as reference evidence: never copy, stage, stash, clean or commit it. Select a committed base on a new Phase 59 branch such that `git merge-base --is-ancestor origin/main SELECTED_BASE` and `git merge-base --is-ancestor ACTIVE_SHA SELECTED_BASE` both pass; if the committed owner HEAD has required protected changes not contained by that base, integrate only its committed delta inside the isolated branch and rerun both ancestry gates. A base behind origin/main or excluding the active SHA blocks. Create the dedicated isolated worktree at that selected base, then freeze path/realpath/base/owner HEAD/origin/main/active SHA/common-dir/branch/remote ref/allowed files/new files and zero-overlap proof. Mark the helper test NEW and cover every lifecycle transition, path, ancestry, regression, dirty-owner-copy and drift rejection.</action>
  <acceptance_criteria>The inventory reconciles exact owner HEAD, origin/main and active runtime SHA; selected base contains origin/main and active SHA, one clean non-symlinked branch/worktree is frozen, and the dirty owner checkout remains byte-for-byte untouched/reference-only.</acceptance_criteria>
  <verify>
    <automated>test "$(pwd -P)" = /home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59 &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_router_owner_run.py -v &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py verify --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json --require-origin-main-contained --require-active-sha-ancestor --require-owner-reference-only &amp;&amp; python3 scripts/embeddings-bench/qwen-cutover-inventory.py validate-freeze --router-owner .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json --require-owner-path /home/ubuntu/GitHub/containers/router-ai-atius --require-isolated-worktree --require-no-unknown --require-no-overlap</automated>
  </verify>
  <done>The executor has exact owner-repo interfaces and exclusive file/hunk ownership without invented symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the persistent Redis pipeline state machine and exact two-slot lifecycle</name>
  <files>${isolated_worktree_path}/service/embeddinggovernor/governor.go, ${isolated_worktree_path}/service/embeddinggovernor/governor_test.go, ${isolated_worktree_path}/relay/pipeline_governor_test.go (NEW)</files>
  <read_first>59-ROUTER-OWNER-INVENTORY.json, actual discovered governor/Redis source and tests, 59-CONTEXT.md D-07/D-08/D-13, current Redis topology/persistence/atomic primitives from Wave 0</read_first>
  <redis_cluster_contract>Every key touched by one Lua/Function/MULTI operation must share the literal hash-tag `{embedding-pipeline}`. Admission, state, active set, queue, deadline and terminal receipt carry a monotonic fencing epoch plus owner token; stale owners cannot mutate or release. After each atomic safety write, issue `WAITAOF 1 1 TIMEOUT` on the same connection and verify both returned acknowledgement counts before acknowledging admission/state/terminal or invoking the next backend; never issue WAITAOF inside Lua/MULTI. Timeout/short acknowledgement fails closed, does not expose the side effect, and reconciles from the last durably acknowledged epoch. Run primary process and host/failure-domain loss tests with two router replicas contending for slots: exactly two durably acknowledged pipelines survive, the third waits/rejects, no CROSSSLOT occurs, an unacknowledged lease is never acted upon, and recovery produces one durably acknowledged terminal/release.</redis_cluster_contract>
  <redis_failover_safety_contract>Use Wave0 tenant/dependency inventory to choose one branch. Prefer an isolated Redis topology restored from production-equivalent config/data for destructive failover. Live failover is allowed only when the Redis authority is dedicated to this pipeline or an explicit maintenance window, owner, dependent-service drain, health checks and rollback receipt are frozen. Never fail over a shared unknown-tenant Redis as a test. Both branches must test the same two-router contention and persistence semantics.</redis_failover_safety_contract>
  <action>Use `router-owner-run.py` for every read/write/test command so cwd is always the lifecycle-verified `isolated_worktree_path`; direct `cd` to the owner checkout or isolated path is forbidden. Transition through `begin-owned-change` before edits and continuously hash the allowlisted diff. Using only inventoried interfaces, implement persistent Redis states `QUEUED -> EMBEDDING -> VECTOR_SEARCH -> RERANK -> COMPLETED` and terminals `FAILED`, `CANCELLED`, `EXPIRED`. Persist opaque pipeline ID, version, owner, deadline, aliases, corpus, journal offset and terminal receipt. Pair every atomic transition with same-connection `WAITAOF 1 1`; only a fully acknowledged epoch can acquire/reuse capacity or trigger embedding/vector/rerank. Enforce at most two non-terminal complete pipelines globally; retain the lease through vector search and rerank; prioritize admitted continuations. Freeze a separate standalone-embedding concurrency/queue/fairness budget that cannot consume the continuation capacity reserved for two already-admitted full pipelines; flood tests must prove both full pipelines complete while standalone load is bounded/rejected. Use the discovered atomic primitive, bounded queue, deadline sweeper, exact-once durable release and restart recovery. Execute process/primary/independent-host loss only through the selected isolated-or-maintenance branch and record dependent-service pre/post health, fsynced offsets, promoted replica identity and rollback. Add race/table tests for all lifecycle cases. `relay/pipeline_governor_test.go` is explicitly NEW; any additional new test must be recorded under inventory `new_files` and helper allowlist before creation. Run heavy race tests only after absolute `/home/ubuntu/.local/bin/omni srv1-ops resources doctor builds` and through the isolated worktree's `./scripts/podman-admin.sh profile-run builds --`; absent containment blocks.</action>
  <acceptance_criteria>Focused race/failover tests prove one hash slot/no CROSSSLOT, same-connection local+replica AOF acknowledgement before every external effect, rejected short acknowledgements, primary/host-loss recovery from the acknowledged offset, fenced stale-owner rejection, global capacity two under two replicas plus failover, continuation priority, standalone flood isolation and exactly one durably acknowledged terminal/release for success, every error, cancel and expiry.</acceptance_criteria>
  <verify>
    <automated>test "$(pwd -P)" = /home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59 &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json run -- /home/ubuntu/.local/bin/omni srv1-ops resources doctor builds &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json run -- ./scripts/podman-admin.sh profile-run builds -- go test -race ./service/embeddinggovernor ./relay -run "Pipeline|Lease|Governor|Cancel|Expire|Restart|ClusterHashTag|Fencing|WaitAOF|PrimaryLoss|HostLoss|Failover|ThirdSlot" -count=3</automated>
  </verify>
  <done>The full pipeline lifecycle is Redis-persistent, two-slot and exact-once across replicas/restarts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Qwen aliases/API, protect Router DB and activate non-titular routes</name>
  <files>scripts/embeddings-bench/qdrant-alias-authority.py, scripts/embeddings-bench/tests/test_qdrant_alias_authority.py, modules/srv1-ops/configs/qdrant-alias-writer-policy.json, modules/srv1-ops/configs/qdrant-alias-arbiter.env.tpl, modules/srv1-ops/systemd/qdrant-alias-arbiter.service, modules/srv1-ops/scripts/install-qdrant-alias-arbiter.sh, ${isolated_worktree_path}/service/qdrantalias/arbiter.go, ${isolated_worktree_path}/service/qdrantalias/arbiter_test.go, ${isolated_worktree_path}/cmd/qdrant-alias-arbiter/main.go, ${isolated_worktree_path}/cmd/qdrant-alias-arbiter/main_test.go, ${isolated_worktree_path}/service/modelcatalog/catalog.go, ${isolated_worktree_path}/service/modelcatalog/catalog_test.go, ${isolated_worktree_path}/relay/embedding_handler.go, ${isolated_worktree_path}/relay/rerank_handler.go, ${isolated_worktree_path}/router/api-router.go, ${isolated_worktree_path}/dto/embedding.go, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-DB-WAVE4-PRESTATE.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-LIFECYCLE.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-4-GATE.json</files>
  <read_first>59-ROUTER-OWNER-INVENTORY.json, discovered catalog/DTO/route/handler/logging symbols, 59-POOLING-ORACLE.json, 59-QWEN-ROLLOUT.json, 59-GTE-ROLLBACK-ANCHOR.json, current Router DB generation and titular routes</read_first>
  <backup_contract>Create and use only `59-ROUTER-DB-WAVE4-PRESTATE.json` for Wave 4 schema/catalog rollback. Verify the immutable Wave 2 `59-ROUTER-DB-GTE-ANCHOR.json` hash before and after this task and never overwrite or repurpose it.</backup_contract>
  <coordinator_contract>Inventory the existing authenticated RAG/search coordinator route and its exact owner-repo source/test symbols before editing; if none exists, add one private authenticated coordinator route in an allowlisted file rather than inventing a public bypass. One request must acquire one full-cycle lease, call the selected embedding alias, query Qdrant through the selected stable corpus alias, call `/v1/rerank`, transition through every persisted state and release exactly once. Standalone embedding never enters this coordinator budget. Tests inject all three backends and prove vector candidates/IDs flow into rerank without raw-text logging or lease handoff.</coordinator_contract>
  <qdrant_arbiter_contract>Implement one persistent operationally router-owned but standalone arbiter with durable states PREPARED→INFLIGHT→APPLIED/RECONCILED/BLOCK in a Redis AOF journal under the distinct `{qdrant-alias-arbiter}` hash-tag. Every state write is followed on the same connection by successful `WAITAOF 1 1 TIMEOUT`; INFLIGHT must be acknowledged by active+independent replica before any Qdrant bytes leave, and terminal/reconciliation state must be acknowledged before a successor. It runs as the dedicated static Linux UID `qdrant-arbiter`, exposes only an authenticated private Unix socket, and alone holds the Vault-hydrated Qdrant alias/control-plane credential plus alias-write egress. Router, watchdog, reindex and cutover clients hold only operation-specific client credentials and cannot reach alias mutation endpoints directly; Wave5 uses a separate L7 data broker. Once bytes are sent, no successor write is admitted until the same service records the response or an explicit reconciliation proves the exact map after bounded transport drain and invalidates the previous connection/credential generation. Ambiguous INFLIGHT survives primary/process/host loss and never auto-fails over. Exactly one active service is allowed; a cold standby has no usable alias credential. Handoff order is mandatory: reconcile replicated journal+exact map with zero ambiguous INFLIGHT, revoke the old Vault credential generation, deny old-host egress/socket/token, prove negative operations from the old identity including partition-heal/rejoin, then issue a new generation and enable standby. Any missed/failed step stays drained/BLOCK. Arbiter unavailability blocks mutations while reads/inference remain available. The watchdog requests a pre-signed compensation operation and never receives a Qdrant credential.</qdrant_arbiter_contract>
  <action>Use `router-owner-run.py` for every owner-repo mutation/test/build/commit/push/activation. Implement the standalone executable/library, dedicated systemd unit, env template and fixture-tested installer/policy. Before route activation, create the dedicated Linux/Vault identity without exposing values, bind the Unix socket to the exact client group, apply/read back UID/cgroup/filesystem/firewall/network controls, rotate to the frozen credential generation, revoke every inventoried legacy alias writer and prove direct alias mutations fail from router, watchdog, reindex and external identities while an arbiter no-op succeeds. Require Wave0 Redis durability and replay only acknowledged epochs before opening the socket. Fault-test active restart plus Redis primary/host loss around PREPARED/INFLIGHT/bytes sent; short WAITAOF and ambiguity stay BLOCK. Fault-test clean cold handoff with the exact revoke-old→block-old-egress/socket/token→negative-old-host/partition-heal probe→issue-new→start-standby sequence, and reject every permutation/failed step. Prove a disabled standby cannot write/assume ownership and inference/alias reads survive arbiter outage. Then add Qwen aliases, preserve GTE aliases, wire `/v1/rerank` and coordinator, remove raw logging, protect Router DB, run tests/build/push/activate exact SHA. Independently read back runtime, arbiter/journal/AOF/policy credential generations, old-host denial, routes and aliases. On failure restore DB/runtime/policy and prove GTE.</action>
  <acceptance_criteria>Qwen APIs/coordinator are callable under one lease; the standalone single-active arbiter is the only proven alias writer; its Redis-AOF journal survives process restart; legacy/direct router/watchdog/reindex/external and disabled-standby writes are denied; reads/inference survive arbiter outage; crash/timeout successor quarantine and reconciliation pass; GTE remains titular; exact tested/pushed runtime and DB/arbiter rollback pass.</acceptance_criteria>
  <verify>
    <automated>test "$(pwd -P)" = /home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59 &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_qdrant_alias_authority.py -v &amp;&amp; systemd-analyze verify modules/srv1-ops/systemd/qdrant-alias-arbiter.service &amp;&amp; bash modules/srv1-ops/scripts/install-qdrant-alias-arbiter.sh --check &amp;&amp; python3 scripts/embeddings-bench/qdrant-alias-authority.py fault-test --require-waitaof-local-replica --require-short-ack-block --require-primary-host-loss --require-redis-aof-journal --require-active-restart-inflight-replay --require-bytes-sent-crash-timeout-successor --require-successor-quarantine --require-connection-credential-invalidation --require-ambiguous-block --require-disabled-standby --require-cold-handoff-order revoke-old,block-old-egress-socket-token,negative-old-host,issue-new,start-standby --require-partition-heal-old-host-denied --require-read-path-survival &amp;&amp; python3 scripts/embeddings-bench/qdrant-alias-authority.py apply-and-verify --policy modules/srv1-ops/configs/qdrant-alias-writer-policy.json --require-dedicated-uid qdrant-arbiter --require-private-socket --require-vault-readback --require-firewall-readback --require-revoked-legacy-writers --require-negative-bypass-probes --require-arbiter-noop &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json run -- /home/ubuntu/.local/bin/omni srv1-ops resources doctor builds &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json run -- ./scripts/podman-admin.sh profile-run builds -- go test -race ./... -run "FullCycle|Coordinator|VectorSearch|Rerank|Lease|AliasArbiter|Inflight|Quarantine|WaitAOF|PrimaryLoss|HostLoss|Restart|Standby|PartitionHeal|Failure" -count=1 &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json run -- ./scripts/podman-admin.sh build &amp;&amp; python3 scripts/embeddings-bench/router-owner-run.py verify --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-OWNER-INVENTORY.json --require-origin-main-contained --require-active-sha-ancestor --require-durable-push --require-active-exact &amp;&amp; python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 4 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-4-GATE.json</automated>
  </verify>
  <done>Router/governor supports production-grade Qwen routes and persistent two-slot pipelines without titular cutover.</done>
</task>

</tasks>

## Artifacts this phase produces

- Owner repo inventory and isolated worktree ownership record.
- Redis full-cycle governor, Qwen aliases and `/v1/rerank` changes using discovered interfaces.
- Router DB backup/CAS/readback, lifecycle evidence and canonical Wave 4 gate.

## Scope Exception Rationale

The owner inventory/helper, Redis lifecycle and route/DB activation are one atomic router seam bound to one tested commit and one Router DB generation. Tasks serialize risk as read-only inventory plus isolated-worktree freeze → state-machine implementation → source commit/build/DB CAS/activation. The owner checkout is never a writer target, and activation failure restores DB/runtime to GTE. Execution-generated evidence is omitted from `files_modified` source-edit scope and remains explicit in task outputs.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public client to router | Untrusted payloads, deadlines and cancellation enter admission. |
| Router replicas to Redis/Router DB | Concurrent state and routing mutations require CAS and persistence. |
| Router/watchdog/reindex to standalone alias arbiter | Clients can request allowlisted operations but hold neither Qdrant write credentials nor direct write egress. |
| Standalone alias arbiter to Qdrant/Redis | Dedicated UID owns the sole Qdrant write path and persists every transition in Redis AOF before network effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-59-05-01 | Tampering | Redis leases | mitigate | Versioned CAS, owner token, same-connection `WAITAOF 1 1`, exact-once terminals, process/primary/host-loss recovery from acknowledged offsets and invariant capacity two. |
| T-59-05-02 | Denial of Service | Queue/TTL/restart | mitigate | Bounded queue, continuation priority, sweeper and restart recovery. |
| T-59-05-03 | Information Disclosure | Rerank logging | mitigate | Remove raw data and scan active logs/evidence. |
| T-59-05-04 | Spoofing/Tampering | Qdrant alias writer | mitigate | Dedicated UID/private socket, operation auth, sole egress, same-connection AOF-confirmed INFLIGHT journal, exact-map reconciliation and no automatic successor. |
| T-59-05-SC | Tampering | Router build | mitigate | Owner wrappers, 20% doctor and existing module graph; package changes require prior audit. |
</threat_model>

<verification>
Before Summary execute exactly `python3 scripts/embeddings-bench/phase59-wave-closeout.py --wave 4 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s`; only PASS permits Summary/next-wave consideration, with native post-Summary hooks as a second defense.
</verification>

<success_criteria>
- Owner source was inventoried before edits and user changes were preserved.
- Persistent Redis state machine and exact two-slot lifecycle pass.
- Qwen routes are active, GTE remains titular and Router DB/runtime rollback is proven.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-05-SUMMARY.md` when done.
</output>
