---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 03
type: execute
wave: 2
depends_on: [59-02]
execution_root: /home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59
external_preconditions:
  - "The versioned Phase 59 bootstrap contract and its external receipt passed before Wave 0 and remain valid for this exact plan bundle/worktree."
files_modified:
  - scripts/embeddings-bench/qwen-rollback-anchor.py
  - scripts/embeddings-bench/tests/test_qwen_rollback_anchor.py
  - scripts/embeddings-bench/tests/test_gte_coexistence_manifests.py
  - k8s/ebeddings-local/tei-gte-reranker.yaml
  - k8s/ebeddings-local/gte-recovery-pinned.yaml
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-LIVE-EXPORT.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-DB-GTE-ANCHOR.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-ROLLBACK-ANCHOR.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-2-GATE.json
autonomous: true
requirements:
  - QAI-01
  - QAI-05
  - QAI-08
  - D-01
  - D-05
  - D-12
  - D-15
  - D-21
  - D-23
  - D-24
must_haves:
  truths:
    - "Per D-01/D-23, GTE has immutable live specs, digest/revision-pinned recovery manifest, retained image/model artifacts, aliases, collections and executable restore targets before Qwen rollout."
    - "Router DB backup and Qdrant snapshots plus separate alias export are independently readable and restore-tested in isolated targets."
    - "The incumbent GTE reranker HPA is frozen both live and in versioned desired state at exactly two replicas for coexistence; GTE embedding/rerank smokes pass before any Qwen promotion."
    - "Wave 2 performs no Qwen route or titular alias promotion."
  artifacts:
    - path: "scripts/embeddings-bench/qwen-rollback-anchor.py"
      provides: "Export, snapshot, isolated restore verification and GTE smoke orchestration"
      exports: ["export", "fault-test", "verify-restore", "verify-anchor"]
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json"
      provides: "Snapshot receipts and separate alias map"
      contains: "restore_readback"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-ROLLBACK-ANCHOR.json"
      provides: "Complete rollback target and smoke proof"
      contains: "rollback_target"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-2-GATE.json"
      provides: "Canonical Wave 2 gate"
      contains: "next_wave_allowed"
  key_links:
    - from: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-PRESTATE.json"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-LIVE-EXPORT.json"
      via: "identity/hash comparison before backup"
      pattern: "hpa"
    - from: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-ROLLBACK-ANCHOR.json"
      via: "isolated snapshot and alias restore readback"
      pattern: "snapshot_receipts"
---

<objective>
Create the immutable GTE rollback anchor before any Qwen production rollout.

Purpose: Satisfy QAI-01/QAI-05/QAI-08 and D-01/D-05/D-12/D-15/D-21/D-23/D-24 with tested backups, exports, smokes and restore readbacks.
Output: Router DB backup, Qdrant snapshot/alias backup, frozen GTE live export, verified rollback target and Wave 2 gate.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-VALIDATION.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-SUMMARY.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-SUMMARY.md
@k8s/ebeddings-local/tei-gte.yaml
@k8s/ebeddings-local/tei-gte-reranker.yaml
</context>

<preconditions>
Require local srv1 execution from `/home/ubuntu/GitHub/worktrees/omni-srv-admin-phase59`, validate the receipt, and run `phase59-autopilot-bootstrap.py verify-runtime` with the frozen executor owner before every task. Run repo/API/kubectl locally. The same-root fallback is `$gsd-execute-phase 59 --ws qwen-local-ai`.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Implement and test rollback-anchor export and restore contracts</name>
  <files>scripts/embeddings-bench/qwen-rollback-anchor.py, scripts/embeddings-bench/tests/test_qwen_rollback_anchor.py</files>
  <read_first>59-WAVE-1-GATE.json, 59-AUTHORITY-INVENTORY.json, 59-GTE-PRESTATE.json, Qdrant snapshot/alias behavior in 59-RESEARCH.md, current GTE manifests and router DB backup tooling discovered in Wave 0</read_first>
  <recovery_contract>Implement support for generating `gte-recovery-pinned.yaml` only after Task2 has applied/read back the exact 2..2 HPA state. Task1 must not emit a live recovery artifact. Fixtures reject generation from pre-freeze input, mutable pins, unavailable retained artifacts, warm-cache-only restore and any recovery HPA other than 2..2.</recovery_contract>
  <action>Implement standard-library `export`, `fault-test`, `verify-restore`, and `verify-anchor` commands over injected Router DB, Qdrant and Kubernetes backends. Require immutable prestate hashes, Qdrant collection snapshots plus separate alias export, Router DB generation/version and backup checksum, live GTE object UIDs/spec hashes/image/model IDs, exact 2..2 HPA spec/status, smoke receipts, isolated restore target, independent readback and rollback command. Tests must invoke `fault-test`, cover every HPA transition boundary consumed by Task 2, and reject unimplemented boundaries, pre-freeze recovery generation, snapshots without aliases, stale DB generation, mutable/missing identities, recovery HPA not 2..2, non-isolated restore, target collision, missing smoke, dirty overlap, secrets/raw payloads and unavailable metrics.</action>
  <acceptance_criteria>Fixture tests prove a backup cannot become an anchor without separate alias data, exact image digest/model revision and retained artifact checksums, empty-cache isolated restore/readback, GTE smoke, HPA freeze and exact identities.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/embeddings-bench/tests/test_qwen_rollback_anchor.py -v</automated>
  </verify>
  <done>The rollback-anchor contract is deterministic and fail-closed before touching live backup state.</done>
</task>

<task type="auto">
  <name>Task 2: Export immutable GTE, Router DB and Qdrant backups</name>
  <files>k8s/ebeddings-local/tei-gte-reranker.yaml, k8s/ebeddings-local/gte-recovery-pinned.yaml, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-LIVE-EXPORT.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-DB-GTE-ANCHOR.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json</files>
  <read_first>59-GTE-PRESTATE.json, 59-AUTHORITY-INVENTORY.json, current live GTE Deployments/Services/HPA/PDB, Router DB generation/schema, Qdrant GTE collections/aliases/storage, Wave 0 rollback target</read_first>
  <recovery_contract>Create the immutable `59-ROUTER-DB-GTE-ANCHOR.json`; no later wave may overwrite it. Prove registry retention of the image digest and checksum-addressed retention/readability of the pinned model artifacts.</recovery_contract>
  <hpa_transition_contract>Before mutation, persist exact versioned/live HPA 2..4 preimages and UIDs/resourceVersions. Fixture-test failures after source patch, after server-side dry-run, after live apply, after independent readback and before/after recovery generation. On any failure, conditionally restore source and live 2..4 only if their current hashes/resourceVersions still equal this transaction's poststate, independently read back the prestate, emit a ROLLED_BACK receipt and block Wave 2; concurrent drift remains contained/BLOCK. Recovery generation is forbidden until source and live both prove 2..2.</hpa_transition_contract>
  <action>Re-read all authorities and block on drift. Recompute the Qdrant storage budget. Run the injected HPA transition fault suite, then capture exact source/live 2..4 preimages. Patch the versioned GTE reranker HPA to `minReplicas=2,maxReplicas=2`, run server-side dry-run, apply it and independently read back desired/live 2..2; only after that readback generate `gte-recovery-pinned.yaml` from the frozen live objects, exact image/model pins and retained artifacts. Validate the recovery manifest itself restores 2..2 and aggregate GTE+Qwen coexistence 3500m. A live-only patch, pre-freeze generation, recovery max above two or aggregate CPU above 3500m triggers the conditional compensation contract and cannot pass. Then export live GTE specs/status, create the consistent Router DB backup and Qdrant snapshots plus separate alias map. Store backups at the frozen target, verify hashes from a separate identity and record only redacted metadata.</action>
  <acceptance_criteria>Fault tests and live receipts cover source patch, dry-run, apply, readback and recovery generation with conditional 2..4 restoration on failure; the successful path exports source/live/recovery exactly 2..2. Exports match Wave 0 identities; source/backup/restore free bytes satisfy the frozen formula and margin; coexistence totals 3500m; recovery manifest has no mutable tag/revision; image/model artifacts are retained and checksum-readable; every GTE collection has a snapshot receipt and separate alias map; immutable Router DB GTE anchor is generation-bound and readable.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/embeddings-bench/tests/test_gte_coexistence_manifests.py -v &amp;&amp; python3 scripts/embeddings-bench/qwen-rollback-anchor.py fault-test --require-hpa-boundaries source-patch,server-dry-run,live-apply,independent-readback,recovery-generation --require-conditional-prestate-restore --require-rolled-back-block &amp;&amp; python3 scripts/embeddings-bench/qwen-rollback-anchor.py export --authority .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-AUTHORITY-INVENTORY.json --gte-prestate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-PRESTATE.json --recovery-manifest k8s/ebeddings-local/gte-recovery-pinned.yaml --require-frozen-before-generation --require-immutable-pins --require-retained-artifacts --require-versioned-live-recovery-hpa 2 --require-coexistence-cpu-millicores 3500 --require-storage-authorities source,backup,restore --verify-existing .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-LIVE-EXPORT.json --router-backup .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-DB-GTE-ANCHOR.json --qdrant-backup .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json</automated>
  </verify>
  <done>The incumbent routing, vector and workload state is immutable, restorable and HPA-frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Exercise cold pinned isolated restore, smoke GTE and seal Wave 2</name>
  <files>.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-ROLLBACK-ANCHOR.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-2-GATE.json</files>
  <read_first>59-GTE-LIVE-EXPORT.json, 59-ROUTER-DB-GTE-ANCHOR.json, 59-QDRANT-BACKUP.json, gte-recovery-pinned.yaml, retained image/model receipts, live GTE endpoints/aliases, isolated empty-cache restore target, Wave 0 frozen smoke inputs</read_first>
  <recovery_contract>On the approved non-production target, start the pinned GTE recovery workload with an empty model cache and prove exact image/model retrieval before readiness; then run embedding/rerank smokes. A warm-cache-only test is BLOCK.</recovery_contract>
  <action>Reserve/read back the frozen restore budget, then restore the Router DB backup and each Qdrant snapshot plus alias map into isolated targets, never over production. Independently read back consumed/remaining bytes, DB schema/generation/rows, collection IDs/configs/counts/checksums and alias targets. Run bounded GTE embedding and rerank smokes through the production router and isolated restore checks without changing Qwen routes. Prove live GTE specs and HPA equal the versioned 2..2 export. Write the anchor with restore commands, RTO, receipts and target identities. Generate Wave 2 only with metrics fallback, secret/redaction scan, dirty-overlap check, independent readback and an actual backup/restore rollback receipt.</action>
  <acceptance_criteria>Cold empty-cache workload restore uses exact retained image/model pins and passes smokes; isolated DB/Qdrant restore matches backup hashes, HPA/specs/aliases remain frozen, and no Qwen production route or titular alias changed.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-rollback-anchor.py verify-anchor --gte-export .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-LIVE-EXPORT.json --recovery-manifest k8s/ebeddings-local/gte-recovery-pinned.yaml --require-recovery-hpa 2 --require-cold-empty-cache --require-retained-artifacts --router-backup .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ROUTER-DB-GTE-ANCHOR.json --qdrant-backup .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QDRANT-BACKUP.json --anchor .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-ROLLBACK-ANCHOR.json &amp;&amp; python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 2 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-2-GATE.json</automated>
  </verify>
  <done>GTE is a proven rollback anchor and Wave 3 may roll out Qwen without promoting it.</done>
</task>

</tasks>

## Artifacts this phase produces

- `qwen-rollback-anchor.py`: `export`, `verify-restore`, `verify-anchor`.
- Immutable GTE live export, Router DB backup, Qdrant snapshots/alias export and rollback anchor.
- Canonical Wave 2 gate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Production authorities to backup storage | Router DB and Qdrant state cross into restore artifacts. |
| Backup storage to isolated restore | Corrupt or incomplete backups could create false rollback confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-59-03-01 | Tampering | Backups | mitigate | Hash, version/generation, snapshot receipts and independent restore readback. |
| T-59-03-02 | Denial of Service | GTE HPA/workloads | mitigate | Record observed 2–4 prestate, then patch/apply/read back source/live/recovery 2–2 before anchor; preserve model/Service/alias identity and verify smokes. |
| T-59-03-03 | Information Disclosure | Backup evidence | mitigate | Persist metadata/hashes only; secret and raw-data scans gate PASS. |
| T-59-03-SC | Tampering | Dependencies | accept | No package installation occurs. |
</threat_model>

<verification>
Before Summary execute exactly `python3 scripts/embeddings-bench/phase59-wave-closeout.py --wave 2 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s`; only PASS permits Summary/next-wave consideration, with native post-Summary hooks as a second defense.
</verification>

<success_criteria>
- Router DB, Qdrant aliases/snapshots and GTE live specs are immutable and restore-tested.
- GTE HPA is frozen, GTE smokes pass and no Qwen promotion occurred.
- The Wave 2 gate permits rollout.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-SUMMARY.md` when done.
</output>
