---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 03
type: execute
wave: 2
depends_on:
  - 59-01
  - 59-02
files_modified:
  - k8s/qwen-canary/namespace-resources.yaml
  - k8s/qwen-canary/tei-qwen3-embedding.yaml
  - k8s/qwen-canary/qwen3-reranker.yaml
  - k8s/qwen-canary/network-policy.yaml
  - k8s/qwen-canary/reranker-hpa-staged.yaml
  - k8s/qwen-canary/kustomization.yaml
  - scripts/embeddings-bench/tests/test_qwen_canary_manifests.py
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-K3S-ROLLOUT.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-OVERLAP.json
autonomous: false
requirements: [D-02, D-03, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "The qwen-canary namespace runs isolated Qwen embedding and reranker workloads without changing ebeddings-local."
    - "Every runtime pod is capped at exactly 500m CPU and uses normal pod networking behind a private NodePort."
    - "Embedding has two fixed replicas; reranker is measured at one warmup pod before being set to two fixed replicas, with HPA 2-4 staged but inactive."
    - "NetworkPolicy is applied only when enforcement passed Wave 0; otherwise firewall/nodePortAddresses plus positive and negative probes are the required isolation branch."
    - "Each embedding replica downloads the exact janni-t artifact into its own emptyDir through a 500m init container using .partial, SHA-256 validation, and atomic rename; the reranker artifact remains embedded in the Plan 02 image."
    - "Kubernetes effective pod CPU is proven as max(max init-container request, sum app-container requests)=500m, so four runtime pods plus exactly one 500m tool Job fit the 5-pod/2500m namespace ceiling."
  artifacts:
    - path: "k8s/qwen-canary/namespace-resources.yaml"
      provides: "Namespace, LimitRange, and ResourceQuota isolation"
      contains: "qwen-canary"
    - path: "k8s/qwen-canary/tei-qwen3-embedding.yaml"
      provides: "Two-pod TEI OrtBackend embedding deployment and private service"
      contains: "janni-t/qwen3-embedding-0.6b-int8-tei-onnx"
    - path: "k8s/qwen-canary/qwen3-reranker.yaml"
      provides: "Dedicated reranker deployment using the Plan 02 artifact-embedded image, service, probes, security, and PDB"
      contains: "500m"
    - path: "k8s/qwen-canary/network-policy.yaml"
      provides: "Conditional default deny and explicit router/probe access for the enforced-CNI branch"
      contains: "NetworkPolicy"
    - path: "k8s/qwen-canary/reranker-hpa-staged.yaml"
      provides: "Inactive future HPA 2-4 definition"
      contains: "maxReplicas: 4"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-OVERLAP.json"
      provides: "Per-file ownership and baseline read-only overlap classification"
      contains: "classification"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json"
      provides: "Read-only immutable lineage plus current topology/pin/alias proof for Plan 03"
      contains: "original_gte_freeze_hash"
  key_links:
    - from: "k8s/qwen-canary/kustomization.yaml"
      to: "k8s/qwen-canary/tei-qwen3-embedding.yaml"
      via: "kustomize resource inclusion"
      pattern: "tei-qwen3-embedding.yaml"
    - from: "k8s/qwen-canary/network-policy.yaml"
      to: "59-W0-INVENTORY.json"
      via: "conditional kustomization inclusion only when private_network_branch=network_policy_enforced"
      pattern: "network_policy_enforced"
---

<objective>
Create and safely apply the isolated `qwen-canary` k3s resource set with exact models, 500m pods, private networking, storage, probes, security, disruption controls, and staged scaling.

Purpose: Implement D-02, D-03, D-09, D-10, D-11, D-12, and D-13 while leaving the incumbent `ebeddings-local` namespace and GTE workloads untouched.
Output: Validated manifests, an explicit human mutation checkpoint, and rollout/warmup evidence.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-VALIDATION.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-SUMMARY.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-SUMMARY.md
@k8s/ebeddings-local/tei-gte.yaml
@k8s/ebeddings-local/tei-gte-reranker.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and statically prove the isolated canary manifests</name>
  <files>k8s/qwen-canary/namespace-resources.yaml, k8s/qwen-canary/tei-qwen3-embedding.yaml, k8s/qwen-canary/qwen3-reranker.yaml, k8s/qwen-canary/network-policy.yaml, k8s/qwen-canary/reranker-hpa-staged.yaml, k8s/qwen-canary/kustomization.yaml, scripts/embeddings-bench/tests/test_qwen_canary_manifests.py</files>
  <read_first>59-WAVE0-GATE.json, 59-GTE-BASELINE-FREEZE.json, 59-W0-INVENTORY.json, 59-RERANKER-WARMUP.json, k8s/embeddings-bench/base.yaml, k8s/embeddings-bench/tei-qwen-onnx-int8.yaml, k8s/embeddings-bench/kustomization.yaml, k8s/ebeddings-local/tei-gte.yaml, k8s/ebeddings-local/tei-gte-reranker.yaml, AGENTS.md K3s Resource Unit, 59-CONTEXT.md D-02/D-03/D-09 through D-13</read_first>
  <action>Generate `59-03-GATE-READBACK.json` with `refresh-lineage` and require `assert-lineage` PASS before manifest work; do not rewrite `59-BASELINE-CONTRACT.json`, `59-GTE-BASELINE-FREEZE.json`, or `59-WAVE0-GATE.json`. Record hashes, tracked state, current diffs, intended Phase 59 ownership, and classification for every new `k8s/qwen-canary/` target plus the dirty/untracked read-only baselines `k8s/ebeddings-local/tei-gte.yaml` and `k8s/ebeddings-local/tei-gte-reranker.yaml` in `59-03-OVERLAP.json`; block on unclassified overlap, never edit the baselines, and stage only Phase-owned paths. Use `k8s/embeddings-bench/base.yaml`, `k8s/embeddings-bench/tei-qwen-onnx-int8.yaml`, and `k8s/embeddings-bench/kustomization.yaml` as experimental analogs, and the canonical `k8s/ebeddings-local/tei-gte.yaml` plus `k8s/ebeddings-local/tei-gte-reranker.yaml` as incumbent analogs. Encode these literal divergences in manifests/tests: locked `janni-t/qwen3-embedding-0.6b-int8-tei-onnx`, `pooling=mean`, dimensions `1024`, no `hostNetwork`, no fixed pod hostname/IP or probe host, and conditional `NetworkPolicy`. Create `qwen-canary` Namespace, LimitRange, and ResourceQuota with `pods=5`, `requests.cpu=2500m`, and `limits.cpu=2500m`: four runtime pods at 500m plus exactly one active 500m tool Job. For each embedding pod, mount a per-pod `emptyDir` and run one non-root init container with request/limit `500m` that downloads revision `8fe0c238c7c48016d28e750413ca492024be3ddf` file `model.onnx` to `model.onnx.partial`, validates SHA-256 `bd775071a80a1dde99a18d1a7083bf388a5ad4ce9db6d81806f25c4d6102ff08`, and atomically renames it. Configure the TEI app container at request/limit `500m` to read only that local path with remote model fallback disabled. In the enforced network branch, allow only the controlled init download destination plus required DNS/router/Qdrant flows and deny general TEI runtime egress; in the fallback branch, use the Wave 0 firewall/`nodePortAddresses` controls and prove the same startup-only download boundary and positive/negative reachability. Document and test Kubernetes accounting: effective pod CPU request/limit equals `max(max(initContainers), sum(app containers))`, therefore `max(500m,500m)=500m` for each embedding pod rather than 1000m. Set `maxSurge=0,maxUnavailable=1`. Configure two TEI/OrtBackend replicas with exact alias/settings, immutable ARM64 TEI digest, probes, restricted security, topology spread/anti-affinity, and PDB. Configure the reranker from the Plan 02 digest with its q8 artifact embedded in that image, no model PVC/init-download, initial replicas=1, measured memory, private `/rerank`, probes, security, anti-affinity, and PDB. Use normal pod networking and collision-free private NodePorts. Include `network-policy.yaml` only when `private_network_branch=network_policy_enforced`; otherwise omit it and require fallback controls/probes. Keep the 2-4 HPA staged but out of kustomization. Add tests for both network branches, two embedding replicas with distinct per-pod `emptyDir`, exact revision/file/hash and `.partial` atomic rename, offline TEI runtime, embedded reranker artifact, effective 500m init/app accounting, exact 5-pod/2500m quota, no PVC, and no-surge rollout.</action>
  <acceptance_criteria>Both allowed network branches render deterministically and all other/unknown states fail; server-side dry-run succeeds for the selected branch; tests prove the named analog divergences, two per-pod emptyDir caches, exact janni-t init-download pins and atomic checksum flow, reranker artifact embedded in its image, effective 500m accounting, exact 2500m/5-pod quota, and two-replica rollout; no PVC or incumbent GTE object appears in the rendered set.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 59-03 --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json --require PASS &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_qwen_canary_manifests.py -v &amp;&amp; kubectl kustomize k8s/qwen-canary &gt;/tmp/qwen-canary-rendered.yaml &amp;&amp; kubectl apply --dry-run=server -f /tmp/qwen-canary-rendered.yaml</automated>
  </verify>
  <done>The complete canary resource set is statically validated and ready for an explicitly approved live apply.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authorize the first live qwen-canary apply</name>
  <files>k8s/qwen-canary/, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json</files>
  <read_first>59-WAVE0-GATE.json, Rendered manifest from Task 1, 59-W0-INVENTORY.json, current `kubectl get all,pdb,resourcequota,limitrange,networkpolicy -A`, current host firewall, nodePortAddresses and private reachability evidence</read_first>
  <action>Refresh `59-03-GATE-READBACK.json`, require lineage PASS, and revalidate `59-03-OVERLAP.json` hashes. Present the exact server-side diff, selected private NodePorts, 2500m/5-pod namespace quota, four effective-500m runtime pods plus one 500m tool Job, per-pod embedding emptyDir/init-download revision/file/SHA flow, Kubernetes effective init accounting, embedded reranker artifact/image digest, per-pod memory, selected network branch, startup-only model egress controls, positive/negative proof commands, and a read-only before-snapshot of `ebeddings-local`. Require the owner to confirm target cluster, private CIDRs, ports, storage strategy, branch, and resource envelope. No apply occurs until approval.</action>
  <acceptance_criteria>The checkpoint shows no public/direct NodePort path, no resource-name overlap with `ebeddings-local`, exact init/artifact pins, effective 500m accounting, and sufficient capacity for two embedding pods plus one reranker warmup pod.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 59-03 --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json --require PASS &amp;&amp; { kubectl diff -k k8s/qwen-canary &gt; /tmp/qwen-canary.diff; rc=$?; test "$rc" -eq 0 -o "$rc" -eq 1; }</automated>
    <human-check>Approve only the displayed cluster identity, private NodePorts/CIDRs, selected network branch, per-pod janni-t emptyDir/init-download pins, embedded reranker image, effective 500m accounting, exact five-pod quota, and unchanged incumbent resources.</human-check>
  </verify>
  <done>Resume only with the explicit signal `approved qwen-canary apply`; otherwise stop without cluster mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Warm one reranker pod, freeze sizing, and complete the fixed rollout</name>
  <files>k8s/qwen-canary/qwen3-reranker.yaml, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-K3S-ROLLOUT.json</files>
  <read_first>59-WAVE0-GATE.json, Approved diff, 59-RERANKER-WARMUP.json, current namespace capacity, rendered manifests, live pre-apply snapshot</read_first>
  <action>After approval, refresh `59-03-GATE-READBACK.json`, require lineage PASS, revalidate overlap hashes, and apply only the selected kustomization branch with reranker replicas=1 and embedding replicas=2. Wait for all three pods; prove each embedding pod's init container independently populated its own emptyDir through the exact `.partial`/SHA/rename flow, the TEI process uses local offline files, each pod is effectively 500m under Kubernetes init accounting, and the reranker loads the artifact embedded in the Plan 02 image. Run bounded warmup calls and record startup, peak RSS, CPU throttling, restart/OOM events, probes, per-pod cache hashes, scheduling, egress state, and endpoint reachability. For the enforced-CNI branch require NetworkPolicy positive/negative probes and runtime egress denial; for the fallback branch require firewall/`nodePortAddresses` readback plus equivalent probes and controlled-startup egress proof. If the envelope, cache, or network branch fails, roll back only new namespace resources. Otherwise freeze reranker memory, set fixed replicas=2, apply the no-surge diff, wait for four ready effective-500m pods, and perform a controlled two-replica embedding rolling restart proving new per-pod emptyDir initialization without PVC/mount sharing. Keep HPA unapplied. Export branch, quota/effective accounting, cache, rollout, egress, probe, and before/after incumbent hashes to `59-K3S-ROLLOUT.json`.</action>
  <acceptance_criteria>Two embedding and two reranker pods are ready at effective 500m each; each embedding pod has an independently verified per-pod cache, the reranker uses its embedded artifact, the fifth 500m Job slot remains available, no HPA/PVC exists live, the network branch passes positive/negative/startup-egress probes, and `ebeddings-local` hashes/specs are unchanged.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 59-03 --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py validate --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-K3S-ROLLOUT.json --gate-set k3s-rollout</automated>
  </verify>
  <done>The isolated fixed-size canary workloads are healthy and evidenced without changing incumbent GTE resources.</done>
</task>

</tasks>

## Artifacts this phase produces

- `k8s/qwen-canary/namespace-resources.yaml`: `qwen-canary`, LimitRange, exact 5-pod/2500m ResourceQuota.
- `k8s/qwen-canary/tei-qwen3-embedding.yaml`: two-pod `embedding-qwen3-0.6b-int8-1024-v1` backend, mean pooling, 1024d, per-pod emptyDir/init-download cache, effective 500m accounting, probes, PDB, private NodePort.
- `k8s/qwen-canary/qwen3-reranker.yaml`: warmup-one then fixed-two `reranker-qwen3-0.6b-int8-v1` backend using the artifact embedded in the Plan 02 image, probes, PDB, private `/rerank` NodePort.
- `k8s/qwen-canary/network-policy.yaml`: conditional default-deny policy, included only for proven enforcement.
- `k8s/qwen-canary/reranker-hpa-staged.yaml`: inactive HPA with 2-4 replicas.
- `k8s/qwen-canary/kustomization.yaml`: live fixed-size canary object set.
- `scripts/embeddings-bench/tests/test_qwen_canary_manifests.py`: manifest contract tests.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-K3S-ROLLOUT.json`: before/after hashes, readiness, resources, warmup, and isolation evidence.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-GATE-READBACK.json`: immutable hash lineage and current topology/pin/alias readback.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-OVERLAP.json`: per-file hashes, ownership and baseline no-edit proof.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Private network to NodePorts | Host-level ports can bypass intended service isolation if firewall/CNI controls are wrong. |
| Pods to model registries/Qdrant | Egress crosses namespace and data-service boundaries. |
| Canary namespace to incumbent workloads | Shared node capacity can affect titular GTE services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-59-03-01 | Elevation of Privilege | Pod security | high | mitigate | Non-root, no privilege escalation, dropped capabilities, seccomp, constrained writable volumes. | Manifest tests and live pod securityContext. | Any pod requires privileged/root operation. |
| T-59-03-02 | Information Disclosure | Private NodePorts | critical | mitigate | Freeze private ports/CIDRs, enforce host firewall and NetworkPolicy, and test public/untrusted denial. | Wave 0 proof, diff checkpoint, Plan 06 reachability smoke. | Port is reachable outside approved private sources. |
| T-59-03-03 | Denial of Service | Shared k3s node | high | mitigate | Exact 500m request/limit per pod, namespace quota, memory limits, staged rollout, PDB, and capacity preflight. | Manifest tests and rollout metrics. | OOM/restart or incumbent resource degradation. |
| T-59-03-04 | Tampering | Image/model configuration | high | mitigate | Immutable digests/revisions/checksums; per-pod janni-t init verification; reranker artifact embedded in its image; no mutable tags. | Rendered manifest and live per-pod cache assertions. | Pin differs from Wave 0/Plan 02 evidence. |
| T-59-03-05 | Tampering | Existing `ebeddings-local` | critical | mitigate | Separate namespace/names and before/after object hashes; apply only kustomization paths under qwen-canary. | Rollout evidence hash comparison. | Any incumbent object changes. |
| T-59-03-SC | Tampering | Container supply chain | high | mitigate | Consume only immutable images already validated in Plans 01-02. | Digest and platform checks. | Unverified image is required. |
</threat_model>

<verification>
Run manifest tests, kustomize render, server-side dry-run, and `kubectl diff` before the checkpoint. After approval, require four ready 500m pods, private-only services, no live HPA, no restarts/OOM, and unchanged incumbent hashes.
</verification>

<success_criteria>
- `qwen-canary` owns all new objects and quota.
- Embedding is exact TEI OrtBackend/mean/1024d at two 500m pods.
- Reranker is warmed at one pod and fixed at two 500m pods.
- Normal pod networking and private NodePorts are enforced.
- `ebeddings-local` remains untouched.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-SUMMARY.md` when done.
</output>
