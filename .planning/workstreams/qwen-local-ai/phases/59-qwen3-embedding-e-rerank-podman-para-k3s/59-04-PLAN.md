---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 04
type: execute
wave: 3
depends_on: [59-03]
files_modified:
  - k8s/qwen-production/namespace-resources.yaml
  - k8s/qwen-production/tei-qwen3-embedding.yaml
  - k8s/qwen-production/qwen3-reranker.yaml
  - k8s/qwen-production/services.yaml
  - k8s/qwen-production/network-policy.yaml
  - k8s/qwen-production/kustomization.yaml
  - scripts/embeddings-bench/tests/test_qwen_production_manifests.py
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QWEN-ROLLOUT.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-3-GATE.json
autonomous: true
requirements:
  - QAI-02
  - QAI-05
  - D-02
  - D-04
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
  - D-18
  - D-24
must_haves:
  truths:
    - "Per D-09/D-10, the final live state is exactly two embedding pods plus two reranker pods, with reranker rollout progressing 1 then 2."
    - "Every normal runtime pod has requests.cpu=500m and limits.cpu=500m; there is no Qwen HPA, hostNetwork or tool Job on Horistic."
    - "The `qwen-production` namespace is private, restricted and default-deny with proven router-positive and unauthorized/public-negative reachability."
    - "Memory limits are frozen from one-pod warmup using Prometheus/cgroup/container metrics; absent metrics fail."
  artifacts:
    - path: "k8s/qwen-production/kustomization.yaml"
      provides: "Complete fixed-size production Qwen resource set"
      contains: "qwen3-reranker.yaml"
    - path: "k8s/qwen-production/tei-qwen3-embedding.yaml"
      provides: "Two-replica TEI/ONNX 1024d embedding"
      contains: "500m"
    - path: "k8s/qwen-production/qwen3-reranker.yaml"
      provides: "Progressive one-to-two dedicated reranker rollout"
      contains: "500m"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QWEN-ROLLOUT.json"
      provides: "Server dry-run, rollout, resource, memory, metrics and network readback"
      contains: "final_replicas"
  key_links:
    - from: "k8s/qwen-production/tei-qwen3-embedding.yaml"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-POOLING-ORACLE.json"
      via: "consumes the oracle-selected pooling value and exact artifact lock"
      pattern: "pooling"
    - from: "k8s/qwen-production/network-policy.yaml"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-AUTHORITY-INVENTORY.json"
      via: "uses the frozen private network branch and router identity"
      pattern: "NetworkPolicy"
---

<objective>
Roll out the fixed-size private Qwen production runtime on ARM64 k3s without promoting traffic.

Purpose: Implement QAI-02/QAI-05 and D-02/D-04/D-09 through D-13/D-18 with exact 2+2 resources, private networking and metric-backed memory sizing.
Output: `k8s/qwen-production`, four ready 500m pods, rollout evidence and Wave 3 gate.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-SUMMARY.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-03-SUMMARY.md
@k8s/ebeddings-local/tei-gte.yaml
@k8s/ebeddings-local/tei-gte-reranker.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and server-dry-run the private production manifests</name>
  <files>k8s/qwen-production/namespace-resources.yaml, k8s/qwen-production/services.yaml, k8s/qwen-production/network-policy.yaml, k8s/qwen-production/kustomization.yaml</files>
  <read_first>59-WAVE-2-GATE.json, 59-AUTHORITY-INVENTORY.json, 59-ARTIFACT-LOCK.json, 59-POOLING-ORACLE.json, 59-RERANKER-HARDENING.json, incumbent GTE manifests, k8s/embeddings-bench analogs</read_first>
  <action>Create namespace `qwen-production` with Pod Security restricted labels, ResourceQuota/LimitRange for exactly four 500m runtime pods plus measured memory, no tool Job allowance on Horistic, and no HPA resource. Define private Services from the Wave 0 port/CIDR inventory, no public LoadBalancer/Ingress and no `hostNetwork`. Add default-deny ingress/egress with explicit DNS, router and required model/Qdrant paths; bind policy selectors to frozen identities and prove CNI enforcement. Render deterministically and run `kubectl apply --dry-run=server`; client-only dry-run is insufficient. Tests must reject any missing 500m pair, HPA, hostNetwork, public endpoint, mutable image/model identity, fifth runtime pod or Horistic tool Job.</action>
  <acceptance_criteria>Rendered resources target only `qwen-production`, pass server-side admission, declare private/restricted controls and contain no Qwen HPA, hostNetwork or tool Job.</acceptance_criteria>
  <verify>
    <automated>kubectl kustomize k8s/qwen-production &gt;/tmp/phase59-qwen-production.yaml &amp;&amp; kubectl apply --dry-run=server -f /tmp/phase59-qwen-production.yaml</automated>
  </verify>
  <done>The namespace/control plane contract is accepted by the live API before rollout.</done>
</task>

<task type="auto">
  <name>Task 2: Encode exact embedding and progressive reranker workloads</name>
  <files>k8s/qwen-production/tei-qwen3-embedding.yaml, k8s/qwen-production/qwen3-reranker.yaml, k8s/qwen-production/kustomization.yaml, scripts/embeddings-bench/tests/test_qwen_production_manifests.py</files>
  <read_first>59-ARTIFACT-LOCK.json, 59-POOLING-ORACLE.json, 59-RERANKER-HARDENING.json, 59-GTE-ROLLBACK-ANCHOR.json, current node allocatable/taints/labels, incumbent anti-affinity/probe patterns</read_first>
  <action>Define two TEI embedding replicas using the exact ARM64 TEI/model locks, oracle-selected pooling, 1024d normalized output, offline model files, probes, restricted security and requests/limits CPU both 500m. Define the reranker at one initial replica with the digest-pinned image, context 512/batch 1/max 20/bounded queue settings, probes and both CPU values 500m; retain a deterministic patch or rollout command to set exactly two replicas only after warmup. Add both workload resources to `kustomization.yaml`. Use memory requests/limits supplied by metric-backed sizing, anti-affinity/topology spread and no HPA. Keep the frozen GTE namespace/specs outside this kustomization. Add static tests for exact 2+2 terminal state, 1→2 reranker progression, image/model/pooling hashes, CPU accounting, no tool Jobs and private service wiring.</action>
  <acceptance_criteria>Static tests prove exact locks, 1024d/pooling, 500m request=limit per pod, fixed final replicas 2+2, no HPA and no incumbent-object inclusion.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/embeddings-bench/tests/test_qwen_production_manifests.py -v</automated>
  </verify>
  <done>Workload manifests are deterministic and consume only Wave 1 locks and Wave 2 rollback prerequisites.</done>
</task>

<task type="auto">
  <name>Task 3: Roll out 2 embedding plus reranker 1-to-2 and seal Wave 3</name>
  <files>k8s/qwen-production/qwen3-reranker.yaml, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-QWEN-ROLLOUT.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-3-GATE.json</files>
  <read_first>server-dry-run output, 59-GTE-ROLLBACK-ANCHOR.json, 59-AUTHORITY-INVENTORY.json external runner/metrics source, rendered manifests, current allocatable and GTE HPA/spec readback</read_first>
  <action>Render the completed kustomization and rerun `kubectl apply --dry-run=server`; any server-side schema or admission failure blocks rollout. Apply the namespace and two embedding replicas plus one reranker replica. Wait for readiness and run bounded warmup; obtain startup/steady/peak RSS, CPU throttling, OOM/restarts and probe latency from Prometheus or cgroup/container fallback. Missing metrics fails. Freeze memory requests/limits, update reranker to exactly two and apply; require final live readback exactly two ready embedding plus two ready reranker pods, each requests.cpu=limits.cpu=500m, no HPA/hostNetwork, private router-positive and unauthorized/public-negative probes, and no tool Job scheduled on Horistic. Re-read GTE specs/HPA/aliases unchanged. On failure, remove only Qwen resources and record rollback. Generate Wave 3 with independent Kubernetes/router/network readback, redaction and overlap scans, metrics and rollback receipt.</action>
  <acceptance_criteria>Final state is exactly 2+2 ready Qwen pods at 500m each, memory is metric-backed, network is private, GTE is unchanged and every failed rollout is compensated.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 3 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-3-GATE.json</automated>
  </verify>
  <done>The private Qwen runtime is ready at fixed 2+2 without any production traffic cutover.</done>
</task>

</tasks>

## Artifacts this phase produces

- `k8s/qwen-production/**`: namespace, fixed workloads, services, policy and kustomization.
- Manifest contract tests and `59-QWEN-ROLLOUT.json`.
- Canonical Wave 3 gate.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Router/private network to Qwen Services | Direct access could bypass admission/governor. |
| Qwen pods to shared Horistic node | Resource pressure could degrade incumbent GTE. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-59-04-01 | Elevation of Privilege | Private Services | mitigate | Default deny, explicit router identity and negative public/unauthorized probes. |
| T-59-04-02 | Denial of Service | Node resources | mitigate | Exact 500m request/limit, fixed 2+2, metric-backed memory, no Horistic tool Job. |
| T-59-04-03 | Tampering | Workload identities | mitigate | Digest/revision/pooling readback against Wave 1 locks. |
| T-59-04-SC | Tampering | Images | mitigate | Consume only Wave 1 digest-pinned ARM64 images. |
</threat_model>

<verification>
Primary gate: `python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 3 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-3-GATE.json`. After it passes, run secondary regression: `bash scripts/gsd-wave-regression.sh`.
</verification>

<success_criteria>
- Server dry-run and live rollout pass.
- Final Qwen state is exactly 2 embedding + 2 reranker pods at 500m each.
- Private network, metrics and GTE immutability readbacks pass.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-04-SUMMARY.md` when done.
</output>
