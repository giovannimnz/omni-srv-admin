---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 01
type: execute
wave: 0
depends_on:
  - 50-01
files_modified:
  - scripts/embeddings-bench/qwen-canary-inventory.py
  - scripts/embeddings-bench/tests/test_qwen_canary_inventory.py
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-OVERLAP.json
autonomous: true
requirements: [D-01, D-06, D-07, D-08, D-11, D-12, D-14, D-16, D-22]
must_haves:
  truths:
    - "Phase 50 is complete before Phase 59 Wave 0 can pass."
    - "The executor has current, read-only evidence for router topology, Qdrant safety, private networking, ARM64 pins, and the incumbent GTE baseline before any canary mutation."
    - "A valid GTE-only monitoring window ending before Wave 0 supplies immutable numeric impact bands before any Phase 59 Qwen live result; if no such history exists, Wave 0 blocks until a new baseline-only window completes."
    - "Pipeline lease state is explicitly classified as in-process only for exactly one router replica, otherwise as a named existing shared atomic store."
    - "Any unresolved high-severity preflight condition stops downstream execution."
    - "The baseline contract, GTE freeze, and Wave 0 gate are immutable after Plan 01; every downstream plan creates a read-only lineage artifact that rechecks their original hashes plus current topology, pins, and aliases."
  artifacts:
    - path: "scripts/embeddings-bench/qwen-canary-inventory.py"
      provides: "Repeatable read-only Wave 0 inventory and redacted evidence capture"
      contains: "lease_state_mode"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json"
      provides: "Timestamped live inventory and blocking gate outcomes"
      contains: "network_policy_enforcement"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md"
      provides: "Single-replica versus shared-store lease decision"
      contains: "STOP_CONDITION"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json"
      provides: "Immutable pre-canary GTE measurement schema and tolerance equations"
      contains: "immutable_after_plan_51_01"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json"
      provides: "Immutable GTE-only metrics, window, provenance, numeric bands and content hash"
      contains: "frozen_before_any_qwen_live_result"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json"
      provides: "Machine-readable aggregate PASS/BLOCK/UNKNOWN assertion for every downstream task"
      contains: "status"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-OVERLAP.json"
      provides: "Per-file hash/diff/classification for the currently untracked embeddings-bench scope"
      contains: "classification"
  key_links:
    - from: "scripts/embeddings-bench/qwen-canary-inventory.py"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json"
      via: "atomic redacted JSON export"
      pattern: "W0-INVENTORY"
    - from: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md"
      via: "router replica count and shared-store evidence"
      pattern: "router_replica"
    - from: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json"
      to: "Plans 59-02 through 59-09"
      via: "plan-scoped refresh-lineage and assert-lineage commands that never rewrite Wave 0 artifacts"
      pattern: "immutable_hashes"
---

<objective>
Create the Wave 0 harness, freeze the GTE-only numeric baseline, and seal the immutable artifacts that gate every later Qwen canary action.

Purpose: Enforce the Phase 50 dependency and resolve live facts that research could not safely infer, especially router replica consistency, Qdrant rollback readiness, private NodePort isolation, ARM64 pins, and numeric GTE impact bands, while preserving D-01, D-06, D-07, D-08, D-11, D-12, D-14, D-16, and D-22.
Output: An inventory/lineage tool with tests, redacted inventory evidence, a binding lease-state decision, an immutable baseline contract, an immutable numeric GTE freeze, and an immutable Wave 0 gate.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-VALIDATION.md
@inventory/hosts/horistic-srv.yaml
@k8s/ebeddings-local/tei-gte.yaml
@k8s/ebeddings-local/tei-gte-reranker.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the fail-closed Wave 0 inventory harness</name>
  <files>scripts/embeddings-bench/qwen-canary-inventory.py, scripts/embeddings-bench/tests/test_qwen_canary_inventory.py</files>
  <read_first>AGENTS.md, 59-CONTEXT.md, 59-RESEARCH.md, 59-PATTERNS.md, 59-VALIDATION.md, inventory/hosts/horistic-srv.yaml, existing scripts/embeddings-bench Python conventions</read_first>
  <behavior>
    - A fixture with exactly one router replica yields lease_state_mode=in_process.
    - A fixture with two or more router replicas yields lease_state_mode=shared_atomic_store only when a reachable, named atomic backend and source integration are proven; otherwise it yields a blocking result.
    - Missing Phase 50 completion, Qdrant snapshot/alias/auth/capacity, private-port, NetworkPolicy enforcement, ARM64 pin, or incumbent-baseline evidence produces a blocking gate rather than a permissive default.
    - `refresh-lineage` verifies the immutable contract/freeze/gate hashes and probes current router topology, model/image pins, private endpoints, and GTE/Qwen aliases into a plan-scoped artifact without modifying any Wave 0 file.
    - `assert-lineage` exits non-zero for BLOCK, UNKNOWN, malformed data, original-hash mismatch, topology/pin/alias drift, or missing current readback; elapsed age alone never rewrites or invalidates immutable history.
    - Captured output contains secret variable names and boolean auth results but never token, API-key, document text, query text, or payload values.
  </behavior>
  <action>Before writing, require `.planning/phases/50-atius-wide-sso-closeout/50-01-SUMMARY.md` and its completed verification evidence; emit BLOCK if Phase 50 is not complete. Enumerate every target under the currently untracked `scripts/embeddings-bench/` scope and record path, tracked/untracked state, SHA-256 or absent marker, diff/patch owner, intended Phase 59 hunks, and classification (`phase-owned`, `user-owned-nonoverlap`, `shared-approved`, or `blocked`) in `59-01-OVERLAP.json`; exit non-zero on any unclassified overlap and use selective staging only. Implement a standard-library Python CLI with `collect`, `validate`, fixture-driven `self-test`, `assert-gate`, `refresh-lineage`, and `assert-lineage`. `validate` must accept `--gate-set wave0`, `--gate-set k3s-rollout`, and `--gate-set router-live`. `refresh-lineage` must accept `--plan-id`, the immutable Wave 0 gate, baseline contract, GTE freeze, and inventory paths plus `--output`; it must verify original hashes and execute the allowlisted read-only probes stored by Wave 0 for current router replicas/restart domain, shared lease backend, model/image/artifact pins, private endpoints, and incumbent/canary alias targets. `assert-lineage` must require PASS and exit non-zero for BLOCK/UNKNOWN, malformed or absent readback, original-hash mismatch, or current drift without rewriting the immutable inputs. It must inventory the owner-host router checkout and exact symbols, live deployment/replica topology, any already-integrated shared atomic store, Qdrant version/auth mode/free storage/collections/aliases/snapshots, canonical GBrain/Obsidian/Graphify source-ingestion entrypoints and committed-event/high-water-mark contracts, unused private NodePort candidates, host firewall, `nodePortAddresses`, CNI NetworkPolicy enforcement, node architecture, image manifest platforms, immutable image digests, model revisions and artifact SHA-256 pins, and current GTE aliases/endpoints/pod resources from `k8s/ebeddings-local/tei-gte.yaml` and `k8s/ebeddings-local/tei-gte-reranker.yaml`. Use SSH and kubectl/Qdrant commands supplied through explicit CLI options; redact command output before persistence. Encode D-01, D-06, D-07, D-08, D-11, D-12, D-14, D-16, and validation gates W0-L1 through W0-L5 as named results. Refuse `in_process` unless the observed router replica count is exactly one and emit the exact private-network branch consumed by Plan 03. Do not provision Redis, NATS, collections, ports, firewall rules, workloads, aliases, or Qwen traffic.</action>
  <acceptance_criteria>The harness emits deterministic schema-versioned JSON; all Wave 0 gates have pass/block/unknown status and evidence references; fixture tests cover one replica, multiple replicas with and without a proven shared store, secret redaction, NodePort collision, absent snapshot, unsupported ARM64 image, and missing model checksum.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/embeddings-bench/tests/test_qwen_canary_inventory.py -v</automated>
  </verify>
  <done>The tested harness can collect all required read-only facts and fails closed on every unresolved safety prerequisite.</done>
</task>

<task type="auto">
  <name>Task 2: Capture inventory, freeze the GTE-only numeric baseline, and seal Wave 0</name>
  <files>.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json</files>
  <read_first>scripts/embeddings-bench/qwen-canary-inventory.py, Phase 50 completion summary/evidence, 59-VALIDATION.md, current owner-host router source at /home/ubuntu/GitHub/containers/router-ai-atius, current live k3s and Qdrant read-only state, available GTE-only monitoring history ending before Wave 0</read_first>
  <action>Run the harness from this repo while reading the owner-host router checkout and live k3s/Qdrant without mutation or Qwen traffic. Record exact checkout path, HEAD, dirty status, governor/catalog/rerank symbols, deployment names and replica counts, Qdrant rollback readiness, selected collision-free private NodePorts, firewall/`nodePortAddresses`/CNI enforcement, ARM64 image/model pins, and incumbent GTE coordinates. Write `59-LEASE-STATE-DECISION.md` with exactly one selected mode: `in_process` only for one live router replica, or `shared_atomic_store` with the exact existing backend, atomic operation, ownership, failure semantics, and source paths. If multiple replicas exist and no suitable existing store is proven, mark `STOP_CONDITION: BLOCKED`. Write immutable `59-BASELINE-CONTRACT.json` with schema version, metric names, sample sufficiency/window rules, slice definitions, statistical formulas, floors, numeric-band derivation, GTE-impact measures, reranker warmup prerequisites, `freeze_before_canary_observation=true`, `immutable_after_plan_51_01=true`, and its own content hash. Prefer a valid historical monitoring interval containing GTE-only production samples and ending before Wave 0 began. If no historical interval satisfies declared continuity, sufficiency, provenance, and no-Qwen criteria, set Wave 0 BLOCK and require a new baseline-only interval with Qwen workloads absent; Plan 01 cannot complete until that interval is collected and validated. Write immutable `59-GTE-BASELINE-FREEZE.json` with top-level `metrics`, `window`, `provenance`, `numeric_tolerance_bands`, and `sha256` fields covering exact start/end, timezone, sample cadence/count/gaps, metric source identities, GTE alias/pod/router/topology provenance, raw-source hashes/locations, per-metric summaries, concrete warning/failure numeric bands, derivation references, contract hash, freeze artifact hash, and literal JSON boolean `frozen_before_any_qwen_live_result=true`. Aggregate Phase 50 completion, every named gate, and exact hashes of `59-BASELINE-CONTRACT.json`, `59-GTE-BASELINE-FREEZE.json`, inventory, lease decision, and source pins into immutable `59-WAVE0-GATE.json`; include `immutable_after_plan_51_01=true` and downstream readback schema. Set PASS only when all high/critical gates and the numeric freeze are PASS; otherwise write BLOCK/UNKNOWN with exact reasons and exit non-zero. After PASS, no later plan may modify the contract, freeze, or Wave 0 gate.</action>
  <acceptance_criteria>The five artifacts are timestamped, redacted, internally hash-linked, and contain no unknown high-severity gate. The lease decision is mechanically derived from observed topology. Concrete GTE tolerance bands come from a qualifying GTE-only window that ended before Wave 0, and all three sealed artifacts are immutable after Plan 01.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py validate --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --lease-decision .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --require-phase-50-summary .planning/phases/50-atius-wide-sso-closeout/50-01-SUMMARY.md --require-frozen-before-any-qwen-live-result &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-gate --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --require PASS</automated>
  </verify>
  <done>Wave 0 has passed with a concrete lease-state architecture and complete read-only evidence, or execution has stopped explicitly before implementation.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/embeddings-bench/qwen-canary-inventory.py`: `collect`, `validate --gate-set wave0|k3s-rollout`, `self-test`, `lease_state_mode`, redaction and blocking-gate fields.
- `scripts/embeddings-bench/tests/test_qwen_canary_inventory.py`: Wave 0 fixture suite.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json`: inventory schema, selected private NodePorts, immutable pins, aliases, snapshots, capacities, and baseline coordinates.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-LEASE-STATE-DECISION.md`: `in_process` or `shared_atomic_store` decision plus `STOP_CONDITION`.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json`: `freeze_before_canary_observation` and predeclared GTE-impact measurement schema.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json`: immutable GTE-only window, provenance, concrete numeric bands, hashes, and `frozen_before_any_qwen_live_result=true`.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json`: aggregate machine-readable PASS/BLOCK/UNKNOWN gate and evidence hashes.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-OVERLAP.json`: per-file overlap classification and ownership evidence.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Executor to owner-host router | Read-only SSH crosses from the planning checkout to the authoritative router checkout. |
| Executor to k3s/Qdrant | Cluster and vector-database metadata may reveal topology or auth state. |
| Inventory artifacts to git worktree | Operational evidence must be useful without persisting secrets or raw corpus data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-59-01-01 | Spoofing | Router replica inventory | high | mitigate | Resolve deployment UID, pod owners, ready replicas, checkout HEAD, and host identity before selecting lease mode. | Fixture tests plus live evidence fields. | Identity or replica count is ambiguous. |
| T-59-01-02 | Tampering | Model/image pins | high | mitigate | Require immutable revisions, image digests, ARM64 manifests, and artifact SHA-256 values per D-06. | `validate` rejects tags or missing checksums. | Any runtime artifact remains mutable or lacks ARM64 support. |
| T-59-01-03 | Information Disclosure | Captured commands and API responses | high | mitigate | Allowlist persisted fields and redact credentials, raw text, query bodies, and payloads. | Secret-redaction unit fixtures and content scan. | A secret or raw corpus sample would be written. |
| T-59-01-04 | Denial of Service | Inventory probes | medium | mitigate | Use bounded samples, short timeouts, and read-only APIs only. | Command log shows bounded limits/timeouts. | A probe requires broad scanning or mutation. |
| T-59-01-05 | Elevation of Privilege | NodePort/firewall assumptions | high | mitigate | Prove private CIDRs, host firewall, and actual CNI NetworkPolicy enforcement before reserving ports. | Inventory network gates all pass. | Direct/public reachability or unenforced policy is observed. |
| T-59-01-06 | Tampering | Lease state decision | critical | mitigate | Permit in-process state only with exactly one router replica; otherwise require an existing shared atomic store. | Decision validator cross-checks replica evidence. | Multiple replicas without a proven shared atomic store. |
| T-59-01-07 | Tampering | GTE tolerance freeze | critical | mitigate | Require a qualifying GTE-only window ending before Wave 0, concrete bands, provenance hashes, and immutable contract/freeze/gate hashes before any Qwen plan. | Freeze-order, provenance, numeric-band and immutability validation. | Baseline is post-Qwen, insufficient, mutable, or lacks numeric bands. |
| T-59-01-SC | Tampering | Package supply chain | low | mitigate | No package installation occurs; retain the research legitimacy audit for later exact pins. | Diff contains no dependency change. | An un-audited package install is introduced. |
</threat_model>

<verification>
Run the focused unittest and then the inventory `validate` command. Inspect `git diff -- scripts/embeddings-bench .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s` and confirm no unrelated dirty changes were altered. Do not proceed if any high-or-critical gate is block/unknown.
</verification>

<success_criteria>
- All validation-contract Wave 0 gaps are resolved before implementation.
- Lease scope is a binding, topology-derived decision.
- Private exposure, Qdrant rollback, ARM64 pins, and incumbent GTE facts are evidenced.
- Concrete GTE-only tolerance bands and provenance are frozen before any Phase 59 Qwen live result.
- The baseline contract, GTE freeze, and Wave 0 gate are immutable; >72h execution uses fresh plan-scoped lineage readbacks instead of rewriting or age-expiring them.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-SUMMARY.md` when done.
</output>
