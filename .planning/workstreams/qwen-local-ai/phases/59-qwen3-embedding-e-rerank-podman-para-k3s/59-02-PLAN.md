---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 02
type: tdd
wave: 1
depends_on:
  - 59-01
files_modified:
  - services/qwen-reranker-onnx/server.mjs
  - services/qwen-reranker-onnx/server.test.mjs
  - services/qwen-reranker-onnx/package.json
  - services/qwen-reranker-onnx/package-lock.json
  - services/qwen-reranker-onnx/Containerfile
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-WARMUP.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-OVERLAP.json
autonomous: true
requirements: [QAI-02, D-04, D-05, D-06, D-10, D-18, D-21]
must_haves:
  truths:
    - "Plan 02 never rewrites the immutable Wave 0 contract, GTE freeze, or gate; its plan-scoped readback proves original hashes and current topology/pins/aliases before each Qwen action."
    - "The dedicated private reranker accepts bounded native requests and returns stable scores from the exact pinned Qwen3 INT8/q8 model."
    - "Overload, malformed payloads, cancellation, deadline expiry, and shutdown have deterministic status, metrics, and resource release behavior."
    - "The ARM64 image builds under the host builds profile and one-pod warmup sizing is evidenced before k3s rollout."
    - "No currently untracked reranker file is overwritten or staged before per-file ownership classification."
  artifacts:
    - path: "services/qwen-reranker-onnx/server.mjs"
      provides: "Bounded private /rerank service"
      contains: "RERANK_MAX_QUEUE"
    - path: "services/qwen-reranker-onnx/server.test.mjs"
      provides: "Contract, overload, cancellation, TTL, and shutdown tests"
      contains: "/rerank"
    - path: "services/qwen-reranker-onnx/Containerfile"
      provides: "Pinned ARM64 runtime image"
      contains: "onnx-community/Qwen3-Reranker-0.6B-ONNX"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-WARMUP.json"
      provides: "Startup, RSS, CPU, and first-request sizing evidence"
      contains: "model_revision"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-OVERLAP.json"
      provides: "Hash/diff/classification for the currently untracked reranker service"
      contains: "classification"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json"
      provides: "Read-only Wave 0 lineage and current topology/pin/alias proof for Plan 02"
      contains: "original_wave0_hash"
  key_links:
    - from: "services/qwen-reranker-onnx/server.mjs"
      to: "onnx-community/Qwen3-Reranker-0.6B-ONNX"
      via: "local-files-only model load at immutable revision"
      pattern: "9995c50e2310679108a55f5ccd16ba8be9f17c20"
    - from: "services/qwen-reranker-onnx/Containerfile"
      to: "services/qwen-reranker-onnx/package-lock.json"
      via: "deterministic npm dependency install"
      pattern: "npm ci"
---

<objective>
Harden the standalone Qwen3 reranker through test-first contracts, exact pins, an ARM64 Containerfile, and measured one-pod warmup.

Purpose: Deliver D-04, D-05, D-06, D-10, D-18, and the rerank portion of D-21 without moving reranking into TEI or exposing a public service.
Output: Tested reranker service, locked dependencies and model assets, reproducible ARM64 image, and warmup sizing evidence.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-VALIDATION.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-SUMMARY.md
@services/qwen-reranker-onnx/server.mjs
@services/qwen-reranker-onnx/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement the bounded reranker lifecycle</name>
  <files>services/qwen-reranker-onnx/server.test.mjs, services/qwen-reranker-onnx/server.mjs, services/qwen-reranker-onnx/package.json, services/qwen-reranker-onnx/package-lock.json</files>
  <read_first>59-WAVE0-GATE.json, services/qwen-reranker-onnx/server.mjs, services/qwen-reranker-onnx/package.json, 59-CONTEXT.md decisions D-04 through D-06 and D-18, 59-RESEARCH.md reranker contract and Package Legitimacy Audit</read_first>
  <behavior>
    - POST /rerank accepts one query and a bounded documents array, returns ordered scores and stable request metadata, and never logs text.
    - Payload bytes, query length, per-document length, document count, queue depth, concurrency, and total execution TTL are bounded by named environment settings.
    - Queue overflow returns 429 with Retry-After; malformed input returns 400; body overflow returns 413; timeout returns 504; cancellation closes work and decrements all counters exactly once.
    - SIGTERM stops admission, drains within the configured grace period, cancels remaining work, closes the listener, and exposes shutdown metrics.
    - Only private native /rerank exists in this service; public /v1/rerank conversion belongs to the owner-host router.
  </behavior>
  <action>Generate `59-02-GATE-READBACK.json` with `refresh-lineage` and require `assert-lineage` PASS before Qwen work; the command must verify the original immutable Wave 0/baseline hashes and current topology/pins/aliases without rewriting those artifacts. Then inventory every target in the currently untracked `services/qwen-reranker-onnx/` scope, recording SHA-256/absent state, full diff/patch ownership, intended Phase 59 hunks, and classification in `59-02-OVERLAP.json`; block on unclassified overlap and stage only listed files. Write failing Node tests first using the built-in test runner and an injected fake scorer/clock. Refactor `server.mjs` into testable admission, validation, queue, deadline, cancellation, scoring, metrics, health/readiness, and shutdown units. Load `onnx-community/Qwen3-Reranker-0.6B-ONNX` only from pre-fetched local files at revision `9995c50e2310679108a55f5ccd16ba8be9f17c20`, require q8/INT8 artifact `onnx/model_quantized.onnx` SHA-256 `c9428382bb48bb31e01a6034647c86d6270761781735cafbf6d5cb4a396d0450`, and pin `@huggingface/transformers` exactly to `4.2.0` per the research legitimacy audit and D-06. Export Prometheus counters/gauges/histograms for requests, results, rejects by reason, queue depth/wait, in-flight work, cancellations, deadlines, model-load seconds, and shutdown. Use request IDs and hashed cardinality-safe labels only. Preserve the dedicated service boundary per D-04; remove any public OpenAI-compatible route from this process.</action>
  <acceptance_criteria>RED is observed before implementation; GREEN covers success, malformed/oversized payloads, queue overflow, cancellation before and during scoring, TTL, stable error schema, metric accounting, and graceful shutdown. The lockfile resolves only exact audited dependencies.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 59-02 --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json --require PASS &amp;&amp; npm --prefix services/qwen-reranker-onnx test</automated>
  </verify>
  <done>The private reranker contract is deterministic, bounded, cancellation-aware, observable, and fully covered by focused tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build the pinned ARM64 image and measure one-pod warmup</name>
  <files>services/qwen-reranker-onnx/Containerfile, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-WARMUP.json</files>
  <read_first>59-WAVE0-GATE.json, 59-02-OVERLAP.json, 59-W0-INVENTORY.json, services/qwen-reranker-onnx/package-lock.json, services/qwen-reranker-onnx/server.mjs, modules/fork-sync/projects/atius-router-docs/Dockerfile.template, AGENTS.md CPU Guardrail and K3s Resource Unit</read_first>
  <action>Refresh `59-02-GATE-READBACK.json`, require lineage PASS, and verify the overlap register still matches current hashes; stop on BLOCK/UNKNOWN or drift without changing Wave 0 artifacts. Read `modules/fork-sync/projects/atius-router-docs/Dockerfile.template` only for its multi-stage and non-root image patterns; do not copy its documentation-build packages, commands, ports, or runtime semantics. Create a non-root ARM64 Containerfile with an immutable base-image digest selected by Wave 0, deterministic `npm ci`, prefetch and checksum verification of the exact reranker revision/artifact, read-only runtime filesystem compatibility, explicit health port, and no network model download at startup. Build only on the owner host through `omni srv1-ops resources run builds -- podman build ...` or the verified equivalent `builds` profile, proving the profile is capped at 20% host CPU before starting. Run one rootless warmup container with CPU limited to `500m` equivalent, bounded memory, private loopback publication, and the same probes/settings planned for k3s. Capture image digest, architecture, model/artifact hashes, startup-to-ready, first and warm request latency, peak RSS, CPU-seconds, exit state, and ranking sanity in `59-RERANKER-WARMUP.json`; remove the disposable container after capture. Stop if OOM, restart, checksum drift, wrong platform, or failed ranking occurs.</action>
  <acceptance_criteria>The built image is ARM64, content-addressed, contains the exact q8 artifact, starts without egress model fetches, runs as non-root at 500m CPU, and has measured warmup/RSS evidence suitable for Plan 03 memory sizing.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 59-02 --inventory .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-W0-INVENTORY.json --baseline-contract .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-GTE-BASELINE-FREEZE.json --gate .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE0-GATE.json --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json --require PASS &amp;&amp; python3 -c "import json; p='.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-WARMUP.json'; d=json.load(open(p)); assert d['architecture'] in ('arm64','aarch64'); assert d['model_revision']=='9995c50e2310679108a55f5ccd16ba8be9f17c20'; assert d['artifact_sha256']=='c9428382bb48bb31e01a6034647c86d6270761781735cafbf6d5cb4a396d0450'; assert d['cpu_limit_millicores']==500; assert d['oom_count']==0 and d['ranking_sanity_passed']"</automated>
  </verify>
  <done>The exact reranker image and one-pod operating envelope are proven on ARM64 under the required build and runtime CPU controls.</done>
</task>

</tasks>

## Artifacts this phase produces

- `services/qwen-reranker-onnx/server.mjs`: private `POST /rerank`, health/readiness/metrics endpoints, `RERANK_MAX_*`, `RERANK_REQUEST_TTL_MS`, `RERANK_SHUTDOWN_GRACE_MS`, queue/cancel/error lifecycle.
- `services/qwen-reranker-onnx/server.test.mjs`: deterministic lifecycle and abuse-case tests.
- `services/qwen-reranker-onnx/package.json` and `package-lock.json`: exact `@huggingface/transformers@4.2.0` dependency and test command.
- `services/qwen-reranker-onnx/Containerfile`: non-root ARM64 image with immutable base/model inputs.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-WARMUP.json`: image digest, hashes, startup, RSS, CPU-seconds, and ranking evidence.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-GATE-READBACK.json`: immutable hash lineage plus current topology/pin/alias readback.
- `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-OVERLAP.json`: per-file overlap hashes, classifications and staging ownership.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Router to private reranker | Untrusted request sizes, text, deadlines, and cancellations cross the service boundary. |
| Build host to model registry | Image and model artifacts cross a supply-chain boundary. |
| Reranker process to logs/metrics | Sensitive queries/documents must not escape into observability data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-59-02-01 | Denial of Service | `/rerank` admission | high | mitigate | Bound bytes, lengths, document count, queue, concurrency, and TTL; return deterministic 413/429/504. | Focused overload tests. | Any unbounded request or queue path remains. |
| T-59-02-02 | Information Disclosure | Logs and metrics | high | mitigate | Log request IDs, sizes, timings, and reason codes only; never text or secrets. | Tests inspect captured logs/labels. | Raw query/document/token appears. |
| T-59-02-03 | Tampering | Model/image/dependency inputs | high | mitigate | Exact revision, artifact SHA-256, base digest, and package lock; startup uses local files only. | Build manifest and warmup hashes. | Any tag drift, network fallback, or hash mismatch. |
| T-59-02-04 | Repudiation | Cancellation/error accounting | medium | mitigate | Stable request ID and reason-code counters with exact-once terminal state. | Cancellation/TTL/shutdown tests. | Counters or work release double-count. |
| T-59-02-05 | Elevation of Privilege | Container runtime | high | mitigate | Non-root UID, dropped capabilities, read-only-compatible filesystem, no privilege escalation. | Podman inspect and runtime smoke. | Container requires root/privileged mode. |
| T-59-02-SC | Tampering | npm/model supply chain | high | mitigate | Use only the RESEARCH legitimacy-audited package/version and immutable model hashes. | Lockfile and artifact checksum checks. | Dependency differs from audit or provenance is unresolved. |
</threat_model>

<verification>
Run the Node test suite. Perform the image build only through the verified owner-host `builds` profile at no more than 20% host CPU. Validate the warmup JSON and inspect the image as non-root ARM64 with exact immutable inputs. Run `python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-wave-gate --wave 1 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json`; its non-zero exit is a hard stop.
</verification>

<success_criteria>
- Native private reranking is bounded and deterministic.
- Cancellation, TTL, overload, errors, metrics, and graceful shutdown pass focused tests.
- Exact dependency/model/image pins are proven on ARM64.
- One 500m warmup container completes without OOM and yields sizing evidence.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-SUMMARY.md` when done.
</output>
