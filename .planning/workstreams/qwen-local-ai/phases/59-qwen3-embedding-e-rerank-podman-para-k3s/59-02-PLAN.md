---
phase: 59-qwen3-embedding-e-rerank-podman-para-k3s
plan: 02
type: execute
wave: 1
depends_on: [59-01]
files_modified:
  - scripts/embeddings-bench/qwen-pooling-oracle.py
  - scripts/embeddings-bench/tests/test_qwen_pooling_oracle.py
  - services/qwen-reranker-onnx/server.mjs
  - services/qwen-reranker-onnx/server.test.mjs
  - services/qwen-reranker-onnx/package.json
  - services/qwen-reranker-onnx/package-lock.json
  - services/qwen-reranker-onnx/Containerfile
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ARTIFACT-LOCK.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-POOLING-ORACLE.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-HARDENING.json
  - .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json
autonomous: true
requirements:
  - QAI-02
  - QAI-05
  - QAI-06
  - D-02
  - D-03
  - D-04
  - D-06
  - D-18
  - D-19
  - D-24
must_haves:
  truths:
    - "Per D-02/D-06, TEI, model, revision, ONNX bytes, base image and produced ARM64 image are immutable and digest-verified."
    - "Per D-03/D-19, last-token and mean are compared against the official FP16 oracle outside Horistic; last-token wins unless the frozen evidence proves it fails."
    - "Per D-04/D-18, the dedicated reranker preserves left padding and suffix under truncation and bounds batch, context, documents, queue, TTL, cancellation and shutdown."
    - "All heavy builds and suites run only through the `builds` profile capped at 20% total host CPU; runtime 500m allocation is not conflated with build containment."
  artifacts:
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ARTIFACT-LOCK.json"
      provides: "ARM64 TEI/model/base/image locks"
      contains: "linux/arm64"
    - path: "scripts/embeddings-bench/qwen-pooling-oracle.py"
      provides: "Official FP16 oracle and blinded last-token/mean A/B"
      exports: ["freeze-contract", "run", "verify"]
    - path: "services/qwen-reranker-onnx/server.mjs"
      provides: "Dedicated bounded ONNX reranker"
      contains: "/rerank"
    - path: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json"
      provides: "Canonical Wave 1 gate"
      contains: "next_wave_allowed"
  key_links:
    - from: "scripts/embeddings-bench/qwen-pooling-oracle.py"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-EVAL-FREEZE.json"
      via: "uses only pre-frozen inputs and thresholds"
      pattern: "acceptance_results_observed"
    - from: "services/qwen-reranker-onnx/Containerfile"
      to: ".planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ARTIFACT-LOCK.json"
      via: "base, dependency, model and image digest readback"
      pattern: "9995c50e2310679108a55f5ccd16ba8be9f17c20"
---

<objective>
Produce reproducible ARM64 inference artifacts, select the embedding pooling contract against the official FP16 oracle, and harden the dedicated reranker.

Purpose: Deliver QAI-02/QAI-05/QAI-06 and D-02/D-03/D-04/D-06/D-18/D-19 with supply-chain and behavioral evidence.
Output: Artifact locks, oracle winner, hardened reranker, digest-pinned image and canonical Wave 1 gate.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-CONTEXT.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RESEARCH.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-PATTERNS.md
@.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-01-SUMMARY.md
@services/qwen-reranker-onnx/server.mjs
@services/qwen-reranker-onnx/package.json
@modules/srv1-ops/configs/resource-governor.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock ARM64 artifacts and select pooling against the FP16 oracle</name>
  <files>scripts/embeddings-bench/qwen-pooling-oracle.py, scripts/embeddings-bench/tests/test_qwen_pooling_oracle.py, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ARTIFACT-LOCK.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-POOLING-ORACLE.json</files>
  <read_first>59-WAVE-0-GATE.json, 59-AUTHORITY-INVENTORY.json, 59-EVAL-FREEZE.json, qwen3-corpus.jsonl, qwen3-qrels.json, official Qwen embedding contract summarized in 59-RESEARCH.md, existing compare-embeddings.py</read_first>
  <action>Lock TEI `ghcr.io/huggingface/text-embeddings-inference@sha256:16c0a827cf79d5dc9b9ec1b0b5df7ffd165726f9bdf1daa9d4f7a355dd842f7e`, embedding `janni-t/qwen3-embedding-0.6b-int8-tei-onnx@8fe0c238c7c48016d28e750413ca492024be3ddf` with `model.onnx` SHA-256 `bd775071a80a1dde99a18d1a7083bf388a5ad4ce9db6d81806f25c4d6102ff08`, FP16 oracle `Qwen/Qwen3-Embedding-0.6B@97b0c614be4d77ee51c0cef4e5f07c00f9eb65b3`, and reranker `onnx-community/Qwen3-Reranker-0.6B-ONNX@9995c50e2310679108a55f5ccd16ba8be9f17c20` with quantized ONNX SHA-256 `c9428382bb48bb31e01a6034647c86d6270761781735cafbf6d5cb4a396d0450`. Verify registry manifests and final image bases support linux/arm64; reject tags and `main`. Implement fixture-tested `freeze-contract`, `run`, and `verify` commands. Execute the FP16 oracle and blinded last-token-versus-mean A/B on the Wave 0 corpus outside Horistic using the frozen instruction/query/document/ranking thresholds. Select last-token by normative preference when both pass; select mean only if last-token fails and mean independently passes every frozen functional, instruction-aware and ranking gate; otherwise block.</action>
  <acceptance_criteria>Artifact identities and hashes are immutable; oracle execution is outside Horistic; the winner is mechanically derived from frozen thresholds and records 1024d/L2/query-instruction/document-no-instruction behavior.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest scripts/embeddings-bench/tests/test_qwen_pooling_oracle.py -v &amp;&amp; python3 scripts/embeddings-bench/qwen-pooling-oracle.py verify --lock .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-ARTIFACT-LOCK.json --freeze .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-EVAL-FREEZE.json --evidence .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-POOLING-ORACLE.json</automated>
  </verify>
  <done>The production pooling value and all inference identities are locked before manifests consume them.</done>
</task>

<task type="auto">
  <name>Task 2: Harden and test the dedicated Qwen3 reranker lifecycle</name>
  <files>services/qwen-reranker-onnx/server.mjs, services/qwen-reranker-onnx/server.test.mjs, services/qwen-reranker-onnx/package.json, services/qwen-reranker-onnx/package-lock.json</files>
  <read_first>59-ARTIFACT-LOCK.json, services/qwen-reranker-onnx/server.mjs, services/qwen-reranker-onnx/package.json, 59-RESEARCH.md Pattern 3 and Package Legitimacy Audit, official reranker recipe summarized there</read_first>
  <action>Refactor behind testable tokenizer, truncation, scorer, admission, queue, deadline, cancellation, redaction and shutdown units. Enforce `padding_side=left`; tokenize prefix/body/suffix separately; reserve suffix and prefix tokens before truncating only the body; read yes/no logits at the final non-padding suffix position; use stable probability conversion and deterministic ordering. Fix internal batch size at 1, total context at 512 and documents at no more than 20 sequentially. Use concurrency 1, a bounded queue, TTL/deadline, disconnect cancellation, exactly-once completion/release, `429` overload, bounded request bytes, readiness-off plus bounded drain on shutdown, and local-files-only model loading. Keep only private `/rerank`; log opaque IDs, counts, timings and outcomes, never query/document/prompt/token/raw logits. Pin the audited `@huggingface/transformers@4.2.0` exactly and regenerate the lockfile. Execute the Node suite on srv1 only after `omni srv1-ops resources doctor builds` passes, via `omni srv1-ops resources run builds -- npm --prefix services/qwen-reranker-onnx test`; doctor failure blocks.</action>
  <acceptance_criteria>Tests cover left padding, suffix survival, truncation budget, single/batch score equivalence, ordering, max-20, queue overflow, TTL, cancellation, redaction, local-only loading and graceful shutdown.</acceptance_criteria>
  <verify>
    <automated>ssh -n atius-srv-1 'cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; omni srv1-ops resources doctor builds &amp;&amp; omni srv1-ops resources run builds -- npm --prefix services/qwen-reranker-onnx test'</automated>
  </verify>
  <done>The dedicated reranker is bounded, correct, private and lifecycle-safe under the 20% heavy-suite guard.</done>
</task>

<task type="auto">
  <name>Task 3: Build the pinned reranker image and seal Wave 1</name>
  <files>services/qwen-reranker-onnx/Containerfile, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-RERANKER-HARDENING.json, .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json</files>
  <read_first>59-ARTIFACT-LOCK.json, 59-POOLING-ORACLE.json, package-lock.json, server.mjs, 59-WAVE-0-GATE.json, Wave 0 external-runner and metric-source identities</read_first>
  <action>Create a non-root, read-only-compatible Containerfile with immutable ARM64 base digest, exact `npm ci`, embedded checksum-verified reranker model and no startup download. On srv1 require the builds doctor, then run the image build only as `omni srv1-ops resources run builds -- podman build --platform linux/arm64 -f services/qwen-reranker-onnx/Containerfile services/qwen-reranker-onnx`. Record doctor/profile values, observed CPU containment, image/config/model/package hashes, SBOM, platform and rootless smoke in `59-RERANKER-HARDENING.json`; unavailable containment or metrics fails. Generate the Wave 1 gate with independent image/process readback, redaction scan, dirty-overlap check and no-mutation rollback receipt.</action>
  <acceptance_criteria>The image is content-addressed linux/arm64, non-root, starts offline with exact model/package hashes, and all build/test commands prove the canonical 20% profile before execution.</acceptance_criteria>
  <verify>
    <automated>ssh -n atius-srv-1 'cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; omni srv1-ops resources doctor builds &amp;&amp; omni srv1-ops resources run builds -- podman build --platform linux/arm64 -f services/qwen-reranker-onnx/Containerfile services/qwen-reranker-onnx' &amp;&amp; python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 1 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json</automated>
  </verify>
  <done>Reproducible embedding/reranker artifacts and the selected pooling contract are ready for rollback anchoring and rollout.</done>
</task>

</tasks>

## Artifacts this phase produces

- `qwen-pooling-oracle.py`: `freeze-contract`, `run`, `verify`.
- Hardened reranker service/test/lockfile/Containerfile.
- Artifact, oracle, hardening and canonical Wave 1 evidence.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registries to build host | Model, package and image inputs cross a supply-chain boundary. |
| Router to reranker | Untrusted text, size, TTL and cancellation cross the private API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-59-02-01 | Tampering | Artifacts | mitigate | Exact revisions, SHA-256, digest-only images, lockfile and ARM64 readback. |
| T-59-02-02 | Denial of Service | Reranker | mitigate | Batch 1, context 512, max 20, bounded queue, TTL/cancel/shutdown. |
| T-59-02-03 | Information Disclosure | Logs/evidence | mitigate | Redacted IDs/counts/metrics only and automated scans. |
| T-59-02-SC | Tampering | npm install | mitigate | Use only the RESEARCH legitimacy-audited exact package and lockfile. |
</threat_model>

<verification>
Primary gate: `python3 scripts/embeddings-bench/qwen-cutover-inventory.py assert-wave-gate --wave 1 --phase-dir .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s --output .planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-WAVE-1-GATE.json`. After it passes, run secondary regression: `bash scripts/gsd-wave-regression.sh`.
</verification>

<success_criteria>
- All model/runtime/image/package identities are immutable and ARM64-proven.
- The FP16 oracle selects a production pooling contract from frozen thresholds.
- Reranker hardening and image evidence pass with 20% build containment.
</success_criteria>

<output>
Create `.planning/workstreams/qwen-local-ai/phases/59-qwen3-embedding-e-rerank-podman-para-k3s/59-02-SUMMARY.md` when done.
</output>
