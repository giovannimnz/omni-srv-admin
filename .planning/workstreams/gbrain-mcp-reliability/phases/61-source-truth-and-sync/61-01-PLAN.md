---
phase: 61-source-truth-and-sync
plan: 01
type: execute
wave: 1
depends_on:
  - 60-04
files_modified:
  - modules/srv1-ops/scripts/gbrain-sync-guard.py
  - modules/srv1-ops/tests/test_gbrain_sync_guard.py
  - docs/operations/gbrain-sync-contract.md
autonomous: true
requirements: [SYNC-02, SYNC-04, SYNC-06]
must_haves:
  truths:
    - "Every owned requirement has machine-readable or independently reproducible evidence."
    - "No mutation proceeds without verified prestate and rollback."
    - "Secrets and raw corpus payloads never enter Git, planning, logs, Obsidian or GBrain evidence."
  artifacts:
    - path: "modules/srv1-ops/scripts/gbrain-sync-guard.py"
      provides: "Plan 61-01 deliverable"
    - path: "modules/srv1-ops/tests/test_gbrain_sync_guard.py"
      provides: "Plan 61-01 deliverable"
    - path: "docs/operations/gbrain-sync-contract.md"
      provides: "Plan 61-01 deliverable"
  key_links:
    - from: "modules/srv1-ops/scripts/gbrain-sync-guard.py"
      to: "61-01-SUMMARY.md"
      via: "verified execution evidence"
---

# Phase 61 Plan 01 — Sync contract, freshness truth and host migrations

<objective>
Sync contract, freshness truth and host migrations.

Purpose: advance SYNC-02, SYNC-04, SYNC-06 while preserving backup-first, fail-closed execution.
Output: modules/srv1-ops/scripts/gbrain-sync-guard.py, modules/srv1-ops/tests/test_gbrain_sync_guard.py, docs/operations/gbrain-sync-contract.md.
</objective>

<execution_context>
/home/ubuntu/.hermes/gsd-core/workflows/execute-plan.md
/home/ubuntu/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/gbrain-mcp-reliability/STATE.md
@.planning/workstreams/gbrain-mcp-reliability/ROADMAP.md
@.planning/workstreams/gbrain-mcp-reliability/REQUIREMENTS.md
@.planning/workstreams/gbrain-mcp-reliability/phases/61-source-truth-and-sync/61-CONTEXT.md
@.planning/workstreams/gbrain-mcp-reliability/phases/61-source-truth-and-sync/61-RESEARCH.md
@.planning/workstreams/gbrain-mcp-reliability/phases/61-source-truth-and-sync/61-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construir sync guard</name>
  <files>modules/srv1-ops/scripts/gbrain-sync-guard.py, modules/srv1-ops/tests/test_gbrain_sync_guard.py</files>
  <read_first>61-CONTEXT.md, 61-RESEARCH.md, 61-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Gerar preflight com repo path, HEAD, dirty count, source bookmark, `newest_content_at`, failed-file ledger, scheduler path, lock ownership, layout canônico/legacy e queue state. Bloquear malformed JSON, source errado, regressão de commit, `ideaverse/Ideaverse` não classificado, lock inacessível, job active com lease/timeout expirado, embed-backfill waiting não autorizado ou divergência não explicada. O preflight é read-only e nunca chama autopilot, worker, sync ou reclaim.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Construir sync guard concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir freshness na origem</name>
  <files>modules/srv1-ops/scripts/gbrain-sync-guard.py, modules/srv1-ops/tests/test_gbrain_sync_guard.py</files>
  <read_first>61-CONTEXT.md, 61-RESEARCH.md, 61-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Adicionar fixtures reproduzindo a implementação real: `newest_content_at` nulo cai em wall-clock; conteúdo conhecido anterior ao último sync produz lag zero; HEAD/bookmark divergente não pode ser classificado fresh mesmo quando o lag de conteúdo é zero. Preparar patch versionado e auto-reversível no guard, sem aplicá-lo ao source instalado nem ao scheduler live; calcular freshness por last successful sync + commit equality + geração do conteúdo, sem shell no path MCP. O candidate só pode aceitar GBrain 0.42.36.0 e hashes conhecidos de `sync.ts`/`source-health.ts`; a aplicação pertence ao PLAN 61-02 depois do gate.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Corrigir freshness na origem concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Encerrar migrations pendentes</name>
  <files>docs/operations/gbrain-sync-contract.md</files>
  <read_first>61-CONTEXT.md, 61-RESEARCH.md, 61-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Classificar cada linha de pending-host-work.jsonl como apply, superseded ou blocked; nesta etapa produzir somente decisões/receipts não-live. Qualquer wiring, scheduler, source patch ou file change que toque runtime é candidate para o PLAN 61-02 pós-gate. Nenhuma pendência some sem decisão e evidência.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Encerrar migrations pendentes concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

</tasks>

<verification>
Run focused tests, inspect the scoped git diff for modules/srv1-ops/scripts/gbrain-sync-guard.py modules/srv1-ops/tests/test_gbrain_sync_guard.py docs/operations/gbrain-sync-contract.md, validate references, and create a redacted requirement receipt for SYNC-02, SYNC-04, SYNC-06. This plan prepares candidates only and must not mutate installed GBrain source, scheduler, queue or vault layout.
</verification>

<success_criteria>
- SYNC-02 has evidence and no unresolved high-risk condition.
- SYNC-04 has evidence and no unresolved high-risk condition.
- SYNC-06 has evidence and no unresolved high-risk condition.
- No unrelated dirty file is staged or modified.
- Graphify freshness is rechecked after code/docs/planning changes.
</success_criteria>

<output>
Create .planning/workstreams/gbrain-mcp-reliability/phases/61-source-truth-and-sync/61-01-SUMMARY.md when done.
</output>
