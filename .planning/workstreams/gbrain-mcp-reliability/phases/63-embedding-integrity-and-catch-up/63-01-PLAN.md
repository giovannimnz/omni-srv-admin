---
phase: 63-embedding-integrity-and-catch-up
plan: 01
type: execute
wave: 1
depends_on:
  - 62-03
files_modified:
  - modules/srv1-ops/scripts/gbrain-embedding-audit.py
  - modules/srv1-ops/tests/test_gbrain_embedding_audit.py
  - docs/operations/gbrain-embedding-migration.md
  - .planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-EMBEDDING-CONTRACT.json
autonomous: true
requirements: [EMB-01, EMB-02, EMB-04]
must_haves:
  truths:
    - "Every owned requirement has machine-readable or independently reproducible evidence."
    - "No mutation proceeds without verified prestate and rollback."
    - "Secrets and raw corpus payloads never enter Git, planning, logs, Obsidian or GBrain evidence."
  artifacts:
    - path: "modules/srv1-ops/scripts/gbrain-embedding-audit.py"
      provides: "Plan 63-01 deliverable"
    - path: "modules/srv1-ops/tests/test_gbrain_embedding_audit.py"
      provides: "Plan 63-01 deliverable"
    - path: "docs/operations/gbrain-embedding-migration.md"
      provides: "Plan 63-01 deliverable"
    - path: ".planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-EMBEDDING-CONTRACT.json"
      provides: "Plan 63-01 deliverable"
  key_links:
    - from: "modules/srv1-ops/scripts/gbrain-embedding-audit.py"
      to: "63-01-SUMMARY.md"
      via: "verified execution evidence"
---

# Phase 63 Plan 01 — Embedding space and provenance contract

<objective>
Embedding space and provenance contract.

Purpose: advance EMB-01, EMB-02, EMB-04 while preserving backup-first, fail-closed execution.
Output: modules/srv1-ops/scripts/gbrain-embedding-audit.py, modules/srv1-ops/tests/test_gbrain_embedding_audit.py, docs/operations/gbrain-embedding-migration.md, .planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-EMBEDDING-CONTRACT.json.
</objective>

<execution_context>
/home/ubuntu/.hermes/gsd-core/workflows/execute-plan.md
/home/ubuntu/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/gbrain-mcp-reliability/STATE.md
@.planning/workstreams/gbrain-mcp-reliability/ROADMAP.md
@.planning/workstreams/gbrain-mcp-reliability/REQUIREMENTS.md
@.planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-CONTEXT.md
@.planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-RESEARCH.md
@.planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Congelar contrato 768d</name>
  <files>modules/srv1-ops/scripts/gbrain-embedding-audit.py, modules/srv1-ops/tests/test_gbrain_embedding_audit.py</files>
  <read_first>63-CONTEXT.md, 63-RESEARCH.md, 63-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Resolver endpoint `https://router.atius.com.br/v1`, config `openai:embedding-gte-v1`, outbound model id `embedding-gte-v1`, upstream identity conhecida, dimensões, normalization e signature `openai:embedding-gte-v1:768`. Provar em source/teste que `resolveRecipe()` separa provider/model e que `resolveEmbeddingProvider()` entrega apenas `parsed.modelId` ao SDK; executar `gbrain embed --all --dry-run` e smoke HTTP 200/768 sem salvar key/vector. Bloquear se a chamada outbound contiver o prefixo `openai:` ou se dimensão/signature divergir.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Congelar contrato 768d concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classificar provenance existente</name>
  <files>modules/srv1-ops/tests/test_gbrain_embedding_audit.py, docs/operations/gbrain-embedding-migration.md</files>
  <read_first>63-CONTEXT.md, 63-RESEARCH.md, 63-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Agrupar chunks/pages por signature, model metadata, dimension, embedded_at e geração. Não inferir equivalência só por dimensão/nome; unknown entra em reembed_required.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Classificar provenance existente concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar SQL/CLI de reconciliação dry-run</name>
  <files>docs/operations/gbrain-embedding-migration.md, .planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-EMBEDDING-CONTRACT.json</files>
  <read_first>63-CONTEXT.md, 63-RESEARCH.md, 63-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Gerar manifest de rows `safe-metadata-repair`, `reembed-required`, `deleted-excluded` e `inconsistent`. Recalcular contagens live — a revisão assíncrona trouxe 3.942 enquanto a auditoria canônica trouxe 3.941, portanto nenhum valor stale vira gate. Testes cobrem 1536/768, alias rename equivalente, NULL e metadata mentirosa; metadata aceita deve convergir para model `openai:embedding-gte-v1`, dimensions `768` e signature `openai:embedding-gte-v1:768`.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Criar SQL/CLI de reconciliação dry-run concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

</tasks>

<verification>
Run focused tests, inspect the scoped git diff for modules/srv1-ops/scripts/gbrain-embedding-audit.py modules/srv1-ops/tests/test_gbrain_embedding_audit.py docs/operations/gbrain-embedding-migration.md .planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-EMBEDDING-CONTRACT.json, validate references, and create a redacted requirement receipt for EMB-01, EMB-02, EMB-04. For live plans, stop at the explicit gate until owner authorization.
</verification>

<success_criteria>
- EMB-01 has evidence and no unresolved high-risk condition.
- EMB-02 has evidence and no unresolved high-risk condition.
- EMB-04 has evidence and no unresolved high-risk condition.
- No unrelated dirty file is staged or modified.
- Graphify freshness is rechecked after code/docs/planning changes.
</success_criteria>

<output>
Create .planning/workstreams/gbrain-mcp-reliability/phases/63-embedding-integrity-and-catch-up/63-01-SUMMARY.md when done.
</output>
