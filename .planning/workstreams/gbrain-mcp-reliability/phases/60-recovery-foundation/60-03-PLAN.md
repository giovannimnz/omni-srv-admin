---
phase: 60-recovery-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 60-01
files_modified:
  - modules/srv1-ops/systemd/gbrain-http-mcp.service
  - modules/srv1-ops/config/gbrain-http-mcp.logrotate
  - modules/srv1-ops/scripts/gbrain-secret-audit.py
  - modules/srv1-ops/tests/test_gbrain_secret_audit.py
  - docs/security/gbrain-mcp-token-rotation.md
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03]
must_haves:
  truths:
    - "Every owned requirement has machine-readable or independently reproducible evidence."
    - "No mutation proceeds without verified prestate and rollback."
    - "Secrets and raw corpus payloads never enter Git, planning, logs, Obsidian or GBrain evidence."
  artifacts:
    - path: "modules/srv1-ops/systemd/gbrain-http-mcp.service"
      provides: "Plan 60-03 deliverable"
    - path: "modules/srv1-ops/config/gbrain-http-mcp.logrotate"
      provides: "Plan 60-03 deliverable"
    - path: "modules/srv1-ops/scripts/gbrain-secret-audit.py"
      provides: "Plan 60-03 deliverable"
    - path: "modules/srv1-ops/tests/test_gbrain_secret_audit.py"
      provides: "Plan 60-03 deliverable"
  key_links:
    - from: "modules/srv1-ops/systemd/gbrain-http-mcp.service"
      to: "60-03-SUMMARY.md"
      via: "verified execution evidence"
---

# Phase 60 Plan 03 — MCP secret and log hygiene

<objective>
MCP secret and log hygiene.

Purpose: advance SEC-01, SEC-02, SEC-03 while preserving backup-first, fail-closed execution.
Output: modules/srv1-ops/systemd/gbrain-http-mcp.service, modules/srv1-ops/config/gbrain-http-mcp.logrotate, modules/srv1-ops/scripts/gbrain-secret-audit.py, modules/srv1-ops/tests/test_gbrain_secret_audit.py, docs/security/gbrain-mcp-token-rotation.md.
</objective>

<execution_context>
/home/ubuntu/.hermes/gsd-core/workflows/execute-plan.md
/home/ubuntu/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/gbrain-mcp-reliability/STATE.md
@.planning/workstreams/gbrain-mcp-reliability/ROADMAP.md
@.planning/workstreams/gbrain-mcp-reliability/REQUIREMENTS.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-CONTEXT.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-RESEARCH.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar scanner/redactor fail-closed</name>
  <files>modules/srv1-ops/systemd/gbrain-http-mcp.service, modules/srv1-ops/config/gbrain-http-mcp.logrotate, modules/srv1-ops/scripts/gbrain-secret-audit.py, modules/srv1-ops/tests/test_gbrain_secret_audit.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Detectar Authorization, bearer e token-shaped values sem persistir correspondências; parser deve reconhecer o banner multiline real do GBrain (`Admin Token` + duas linhas do valor) e não depender de regex single-line. Emitir apenas contagens, classes, arquivo e hash salgado não-reversível. Testes provam que stdout, JSON e arquivos de evidência não reproduzem secrets e que três banners multiline viram três findings, não zero nem seis.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Criar scanner/redactor fail-closed concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hardening de unit e log</name>
  <files>modules/srv1-ops/systemd/gbrain-http-mcp.service, modules/srv1-ops/config/gbrain-http-mcp.logrotate, modules/srv1-ops/scripts/gbrain-secret-audit.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Fazer serviço usar journald ou arquivo 0600 rotacionado, `UMask=0077`, diretórios explícitos e redaction antes da escrita. Passar `--suppress-bootstrap-token` e provar que nenhum novo startup imprime token; se bootstrap vier de env, registrar apenas a origem, nunca o valor. Preservar health e auth existentes. Validar `systemd-analyze verify`, fixture de restart e scan pré/pós.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Hardening de unit e log concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate live: autorizar rotação/revogação de tokens e restart do MCP</name>
  <files>.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md</files>
  <what-built>Preflight, dry-run/canary plan, backup evidence, stop conditions and rollback for autorizar rotação/revogação de tokens e restart do MCP.</what-built>
  <how-to-verify>Review the exact redacted prestate and operation receipt. Confirm backup/restore status PASS, target generation/hash, blast radius, timeout, stop conditions and tested rollback. No CLI work is delegated to the user.</how-to-verify>
  <resume-signal>Respond with explicit approval for this live mutation, or reject and state the concern.</resume-signal>
  <action>Pause after all automatable preflight and tests. Present the redacted evidence and request only the approval decision; after approval, the agent performs the live CLI/API operation.</action>
  <verify><human-check>Owner reviewed exact target, blast radius, backup PASS, stop conditions and tested rollback.</human-check></verify>
  <done>Explicit approval is recorded without secret material, or the plan remains blocked with the rejection reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Planejar rotação Vault→clientes</name>
  <files>modules/srv1-ops/scripts/gbrain-secret-audit.py, modules/srv1-ops/tests/test_gbrain_secret_audit.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Inventariar consumidores por nome/fingerprint, criar prestate e rollback. Gate humano obrigatório antes da rotação/revogação live; após autorização, provar token novo nos clientes e rejeição do antigo sem registrar valores.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Planejar rotação Vault→clientes concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

</tasks>

<verification>
Run focused tests, inspect the scoped git diff for modules/srv1-ops/systemd/gbrain-http-mcp.service modules/srv1-ops/config/gbrain-http-mcp.logrotate modules/srv1-ops/scripts/gbrain-secret-audit.py modules/srv1-ops/tests/test_gbrain_secret_audit.py docs/security/gbrain-mcp-token-rotation.md, validate references, and create a redacted requirement receipt for SEC-01, SEC-02, SEC-03. For live plans, stop at the explicit gate until owner authorization.
</verification>

<success_criteria>
- SEC-01 has evidence and no unresolved high-risk condition.
- SEC-02 has evidence and no unresolved high-risk condition.
- SEC-03 has evidence and no unresolved high-risk condition.
- No unrelated dirty file is staged or modified.
- Graphify freshness is rechecked after code/docs/planning changes.
</success_criteria>

<output>
Create .planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-03-SUMMARY.md when done.
</output>
