---
phase: 60-recovery-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/srv1-ops/scripts/gbrain-backup.py
  - modules/srv1-ops/tests/test_gbrain_backup.py
  - docs/operations/gbrain-backup-restore.md
autonomous: false
requirements: [BKP-01, BKP-02, BKP-05]
must_haves:
  truths:
    - "Every owned requirement has machine-readable or independently reproducible evidence."
    - "No mutation proceeds without verified prestate and rollback."
    - "Secrets and raw corpus payloads never enter Git, planning, logs, Obsidian or GBrain evidence."
  artifacts:
    - path: "modules/srv1-ops/scripts/gbrain-backup.py"
      provides: "Plan 60-01 deliverable"
    - path: "modules/srv1-ops/tests/test_gbrain_backup.py"
      provides: "Plan 60-01 deliverable"
    - path: "docs/operations/gbrain-backup-restore.md"
      provides: "Plan 60-01 deliverable"
  key_links:
    - from: "modules/srv1-ops/scripts/gbrain-backup.py"
      to: "60-01-SUMMARY.md"
      via: "verified execution evidence"
---

# Phase 60 Plan 01 — Immutable baseline and restore harness

<objective>
Immutable baseline and restore harness.

Purpose: advance BKP-01, BKP-02, BKP-05 while preserving backup-first, fail-closed execution.
Output: modules/srv1-ops/scripts/gbrain-backup.py, modules/srv1-ops/tests/test_gbrain_backup.py, docs/operations/gbrain-backup-restore.md.
</objective>

<execution_context>
/home/ubuntu/.hermes/gsd-core/workflows/execute-plan.md
/home/ubuntu/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/gbrain-mcp-reliability/STATE.md
@.planning/workstreams/gbrain-mcp-reliability/ROADMAP.md
@.planning/workstreams/gbrain-mcp-reliability/REQUIREMENTS.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-CONTEXT.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-RESEARCH.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar harness de backup redigido</name>
  <files>modules/srv1-ops/scripts/gbrain-backup.py, modules/srv1-ops/tests/test_gbrain_backup.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Criar CLI Python stdlib com subcommands inventory, dump, manifest, restore-smoke e verify. Ler credenciais apenas do runtime aprovado; preferir conexão PostgreSQL direta na porta live descoberta (baseline atual 8745) após provar bind, database e server identity, sem afirmar que PgBouncer necessariamente falha com pg_dump. Usar pg_dump/pg_restore da mesma major do servidor e custom-format; manifesto registra schema/version/counts/checksum sem URL, password ou token. Recusar destino inseguro, source/target ambíguo, dump sem checksum e qualquer target que não tenha marker descartável gerado pelo harness.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Implementar harness de backup redigido concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testar restore descartável</name>
  <files>modules/srv1-ops/tests/test_gbrain_backup.py, docs/operations/gbrain-backup-restore.md</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Criar fixtures e testes para dump inválido, checksum drift, redaction, version mismatch, restore target não-descartável e comparação de pages/content_chunks/links/config. O comando restore-smoke deve gerar nome exclusivo, criar database a partir de `template0`, gravar marker non-production, restaurar, comparar, encerrar somente conexões do alvo marcado e remover somente esse alvo. Interrupção deve deixar cleanup idempotente e nunca selecionar database por glob.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Testar restore descartável concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate live: autorizar primeiro dump PostgreSQL e restore smoke descartável</name>
  <files>.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md</files>
  <what-built>Harness testado, exact source/target identity, pg_dump/pg_restore major, destino local 0700, cleanup idempotente, redaction, checksum e rollback para o primeiro dump/restore.</what-built>
  <how-to-verify>Revisar prestate redigido: database/port/server identity, tamanho e contagens, target marker, comandos exatos, timeout, espaço livre, ausência de conexões externas no alvo, cleanup e stop conditions. Nenhuma credencial ou corpus entra no receipt.</how-to-verify>
  <resume-signal>Responder com aprovação explícita para gerar o dump e criar/restaurar/remover o database descartável, ou rejeitar com o risco.</resume-signal>
  <action>Pausar depois dos testes offline/read-only. Após aprovação, o agente executa todo o dump/restore/cleanup; nenhuma CLI é delegada ao operador.</action>
  <verify><human-check>Owner aprovou source, target descartável, blast radius, timeout, cleanup e rollback testado.</human-check></verify>
  <done>Aprovação explícita registrada sem secrets, ou plano bloqueado com motivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executar baseline, restore smoke e escrever runbook</name>
  <files>docs/operations/gbrain-backup-restore.md</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Após aprovação, executar dump custom-format local em diretório 0700, SHA-256, manifesto redigido e restore smoke no target marcado; comparar contagens e invariantes, remover o target e provar ausência de conexões/artefatos temporários órfãos. Documentar preflight, enqueue posterior na fila, restore smoke, retenção e rollback. Nenhuma operação de Phase 61+ é elegível sem artefato BACKUP-GATE status PASS.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Escrever runbook e gate concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

</tasks>

<verification>
Run focused tests, inspect the scoped git diff for modules/srv1-ops/scripts/gbrain-backup.py modules/srv1-ops/tests/test_gbrain_backup.py docs/operations/gbrain-backup-restore.md, validate references, and create a redacted requirement receipt for BKP-01, BKP-02, BKP-05. For live plans, stop at the explicit gate until owner authorization.
</verification>

<success_criteria>
- BKP-01 has evidence and no unresolved high-risk condition.
- BKP-02 has evidence and no unresolved high-risk condition.
- BKP-05 has evidence and no unresolved high-risk condition.
- No unrelated dirty file is staged or modified.
- Graphify freshness is rechecked after code/docs/planning changes.
</success_criteria>

<output>
Create .planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-01-SUMMARY.md when done.
</output>
