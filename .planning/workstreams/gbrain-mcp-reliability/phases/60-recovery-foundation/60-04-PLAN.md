---
phase: 60-recovery-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 60-01
  - 60-03
files_modified:
  - modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh
  - modules/srv1-ops/scripts/gbrain-runtime-doctor.py
  - modules/srv1-ops/tests/test_gbrain_runtime.py
  - docs/operations/gbrain-pgbouncer-runtime.md
autonomous: false
requirements: [SYNC-01, CTL-05]
must_haves:
  truths:
    - "Every owned requirement has machine-readable or independently reproducible evidence."
    - "No mutation proceeds without verified prestate and rollback."
    - "Secrets and raw corpus payloads never enter Git, planning, logs, Obsidian or GBrain evidence."
  artifacts:
    - path: "modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh"
      provides: "Plan 60-04 deliverable"
    - path: "modules/srv1-ops/scripts/gbrain-runtime-doctor.py"
      provides: "Plan 60-04 deliverable"
    - path: "modules/srv1-ops/tests/test_gbrain_runtime.py"
      provides: "Plan 60-04 deliverable"
    - path: "docs/operations/gbrain-pgbouncer-runtime.md"
      provides: "Plan 60-04 deliverable"
  key_links:
    - from: "modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh"
      to: "60-04-SUMMARY.md"
      via: "verified execution evidence"
---

# Phase 60 Plan 04 — PgBouncer-compatible GBrain runtime

<objective>
PgBouncer-compatible GBrain runtime.

Purpose: advance SYNC-01, CTL-05 while preserving backup-first, fail-closed execution.
Output: modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh, modules/srv1-ops/scripts/gbrain-runtime-doctor.py, modules/srv1-ops/tests/test_gbrain_runtime.py, docs/operations/gbrain-pgbouncer-runtime.md.
</objective>

<execution_context>
/home/ubuntu/.hermes/gsd-core/workflows/execute-plan.md
/home/ubuntu/.hermes/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/gbrain-mcp-reliability/STATE.md
@.planning/workstreams/gbrain-mcp-reliability/ROADMAP.md
@.planning/workstreams/gbrain-mcp-reliability/REQUIREMENTS.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-CONTEXT.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-RESEARCH.md
@.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modelar configuração efetiva</name>
  <files>modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh, modules/srv1-ops/scripts/gbrain-runtime-doctor.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Capturar file plane, DB plane, env allowlist, porta, prepare mode, startup GUCs e cadeia de resolução do executável com valores sensíveis redigidos. Distinguir o wrapper user-owned do symlink Bun para o CLI TypeScript instalado; resolver a porta real em vez de assumir a convenção 6543. Registrar version/hash do wrapper e dos sources críticos, testar que todos os entrypoints gerenciados usam o wrapper e tratar execução direta pelo symlink Bun como bypass bloqueante.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Modelar configuração efetiva concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir startup GUC sem reduzir proteção</name>
  <files>modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh, modules/srv1-ops/scripts/gbrain-runtime-doctor.py, modules/srv1-ops/tests/test_gbrain_runtime.py</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>No path PgBouncer transaction-mode, omitir `statement_timeout` e `idle_in_transaction_session_timeout` rejeitados do startup packet por meio dos knobs suportados e manter `prepare=false`. Não substituir por `SET` de sessão, que não é durável em transaction pooling. Preservar limites com timeout externo por comando e, se necessário, settings server-side/role/database somente após prova de escopo e compatibilidade. Wrapper deve falhar se pool mode, prepare mode, source hash ou entrypoint divergir; teste negativo prova que bypass do wrapper não é aceito.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Corrigir startup GUC sem reduzir proteção concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate live: autorizar troca do wrapper GBrain/PgBouncer live</name>
  <files>.planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-VALIDATION.md</files>
  <what-built>Preflight, dry-run/canary plan, backup evidence, stop conditions and rollback for autorizar troca do wrapper GBrain/PgBouncer live.</what-built>
  <how-to-verify>Review the exact redacted prestate and operation receipt. Confirm backup/restore status PASS, target generation/hash, blast radius, timeout, stop conditions and tested rollback. No CLI work is delegated to the user.</how-to-verify>
  <resume-signal>Respond with explicit approval for this live mutation, or reject and state the concern.</resume-signal>
  <action>Pause after all automatable preflight and tests. Present the redacted evidence and request only the approval decision; after approval, the agent performs the live CLI/API operation.</action>
  <verify><human-check>Owner reviewed exact target, blast radius, backup PASS, stop conditions and tested rollback.</human-check></verify>
  <done>Explicit approval is recorded without secret material, or the plan remains blocked with the rejection reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Provar conexão e dry-run</name>
  <files>modules/srv1-ops/tests/test_gbrain_runtime.py, docs/operations/gbrain-pgbouncer-runtime.md</files>
  <read_first>60-CONTEXT.md, 60-RESEARCH.md, 60-VALIDATION.md, current files and live read-only prestate</read_first>
  <action>Após backup PASS e aprovação, preservar bytes/hash/ownership do wrapper live, aplicar atomicamente o candidate versionado e atualizar somente entrypoints gerenciados aprovados. Então executar smoke read-only, `gbrain stats` e `gbrain sync --dry-run --no-pull` pelo wrapper canônico. Congelar HEAD/index/working-tree e snapshot SQL antes/depois para provar que o dry-run não fez pull, commit, stage nem escrita de corpus/source/job; qualquer mutação torna o teste FAIL e exige investigação. Rollback restaura bytes/hash/ownership da versão anterior e reinicia somente o serviço MCP quando o entrypoint do serviço também tiver mudado.</action>
  <acceptance_criteria>Deterministic tests pass; artifact is redacted and schema-versioned; stop conditions fail closed; rollback/readback is explicit.</acceptance_criteria>
  <verify>
    <automated>python3 -m unittest discover -s modules/srv1-ops/tests -p "test_gbrain*.py" -v</automated>
  </verify>
  <done>Provar conexão e dry-run concluído com evidência, sem alterar escopo não relacionado.</done>
</task>

</tasks>

<verification>
Run focused tests, inspect the scoped git diff for modules/srv1-ops/scripts/gbrain-runtime-wrapper.sh modules/srv1-ops/scripts/gbrain-runtime-doctor.py modules/srv1-ops/tests/test_gbrain_runtime.py docs/operations/gbrain-pgbouncer-runtime.md, validate references, and create a redacted requirement receipt for SYNC-01, CTL-05. For live plans, stop at the explicit gate until owner authorization.
</verification>

<success_criteria>
- SYNC-01 has evidence and no unresolved high-risk condition.
- CTL-05 has evidence and no unresolved high-risk condition.
- No unrelated dirty file is staged or modified.
- Graphify freshness is rechecked after code/docs/planning changes.
</success_criteria>

<output>
Create .planning/workstreams/gbrain-mcp-reliability/phases/60-recovery-foundation/60-04-SUMMARY.md when done.
</output>
