---
phase: 51-contract-threat-model-and-workstream-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - modules/rustdesk-fleet/contracts/scope.json
  - modules/rustdesk-fleet/evidence/phase48-baseline.json
  - modules/rustdesk-fleet/evidence/ledger.json
  - modules/rustdesk-fleet/tools/validate_phase51.py
  - modules/rustdesk-fleet/tests/test_phase51_contracts.py
  - modules/rustdesk-fleet/tests/fixtures/valid/minimal-contracts/bundle.json
  - modules/rustdesk-fleet/tests/fixtures/invalid/unscoped-gsd-command.md
  - modules/rustdesk-fleet/tests/fixtures/invalid/phase48-drift.json
  - modules/rustdesk-fleet/tests/fixtures/invalid/missing-legacy-tool.json
  - modules/rustdesk-fleet/tests/fixtures/invalid/duplicate-secret-ref.json
autonomous: true
requirements:
  - SCP-01
  - SCP-02
  - SCP-03
  - SCP-05
must_haves:
  truths:
    - "D-08: every executable RustDesk GSD lifecycle command is parsed independently and contains the exact token pair --ws rustdesk-fleet; active-marker-only or wrong-workstream routing fails."
    - "D-08: shared PROJECT.md, MILESTONES.md and Graphify mutation use a single serialized writer, with no overlapping writer ownership."
    - "D-09: the Phase 48 baseline contains exactly nine explicit legacy-Git-path to migrated-workstream-path rows, and verifies both legacy blob identity and current filesystem SHA-256 without auto-accepting drift."
    - "D-10: the ledger contains exactly the 36 canonical v1.9 requirement IDs with unique evidence IDs/currentness semantics; narrative or summary-only evidence cannot close a PASS row."
    - "Every task runs its focused tests and the full Phase 51 test file without a build, package installation or runtime mutation."
  artifacts:
    - path: modules/rustdesk-fleet/contracts/scope.json
      provides: "Workstream lifecycle-command and shared-writer policy added to the Wave 1 scope contract."
    - path: modules/rustdesk-fleet/evidence/phase48-baseline.json
      provides: "Nine-file old blob to migrated SHA-256 integrity bridge."
    - path: modules/rustdesk-fleet/evidence/ledger.json
      provides: "Exact 36-row requirement-to-evidence skeleton."
    - path: modules/rustdesk-fleet/tools/validate_phase51.py
      provides: "P51-WS-001, P51-P48-001 and P51-LEDGER-001 checks."
    - path: modules/rustdesk-fleet/tests/fixtures/valid/minimal-contracts/bundle.json
      provides: "Complete secret-free positive contract family with 36 ledger rows and nine baseline mappings."
    - path: modules/rustdesk-fleet/tests/fixtures/invalid/unscoped-gsd-command.md
      provides: "Command-by-command workstream-scope negative case."
    - path: modules/rustdesk-fleet/tests/fixtures/invalid/phase48-drift.json
      provides: "Harmless temp-copy integrity drift negative case."
    - path: modules/rustdesk-fleet/tests/fixtures/invalid/missing-legacy-tool.json
      provides: "Reusable incomplete preserved-tool exact-set case already covered by an in-memory Plan 01 mutation."
    - path: modules/rustdesk-fleet/tests/fixtures/invalid/duplicate-secret-ref.json
      provides: "Reusable duplicated Vault-reference case without values, pending Vault-owner approval in Plan 03."
  key_links:
    - from: modules/rustdesk-fleet/contracts/scope.json
      to: modules/rustdesk-fleet/tools/validate_phase51.py
      via: "command-by-command lifecycle scope and shared-writer enforcement"
      pattern: "P51-WS-001"
    - from: modules/rustdesk-fleet/evidence/phase48-baseline.json
      to: .planning/workstreams/runtime-trust-codex-delivery-convergence/phases/48-codex-oauth-wayland-acp-convergence
      via: "explicit legacy Git blob plus migrated filesystem SHA-256 validation"
      pattern: "file_count.*9"
    - from: modules/rustdesk-fleet/evidence/ledger.json
      to: .planning/workstreams/rustdesk-fleet/REQUIREMENTS.md
      via: "exact canonical requirement-ID equality and owner-phase mapping"
      pattern: "P51-LEDGER-001"
    - from: modules/rustdesk-fleet/tests/fixtures/valid/minimal-contracts/bundle.json
      to: modules/rustdesk-fleet/tests/test_phase51_contracts.py
      via: "materialized tmp_path positive family and negative mutations"
      pattern: "36.*9"
  prohibitions:
    - "Do not install packages or mutate services, hosts, DNS, firewall/OCI rules, Vault values, client configuration, display managers, or legacy access tools."
    - "Do not use git ls-files at the migrated Phase 48 path as evidence of completeness; that path may be untracked."
    - "Do not accept a zero-file, missing-file, extra-file, hash-mismatch or unexplained Phase 48 result; do not auto-rebaseline."
    - "Do not modify any Phase 48 file while testing drift; use a disposable temp copy."
    - "Do not accept summary-only PASS, stale hashes, duplicate/orphan/missing requirement rows, or evidence outside the RustDesk workstream/module."
    - "Do not run concurrent writers on the shared validator/test files or shared planning/Graphify surfaces."
---

<objective>
Bind Phase 51 to its explicit workstream, bridge all nine preserved Phase 48 Git blobs to migrated file hashes, and reserve machine-readable evidence addresses for all 36 milestone requirements.

Purpose: implement D-08 through D-10 and satisfy SCP-05 without relying on the active marker, tracking-state shortcuts, or summary prose.
Output: workstream policy fields, a fail-closed nine-file integrity manifest, an exact 36-row ledger, complete fixtures, and three additional validator checks.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/rustdesk-fleet/STATE.md
@.planning/workstreams/rustdesk-fleet/ROADMAP.md
@.planning/workstreams/rustdesk-fleet/REQUIREMENTS.md
@.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-CONTEXT.md
@.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-RESEARCH.md
@.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-PATTERNS.md
@.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-VALIDATION.md
@.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-01-SUMMARY.md
@scripts/g18-pro-esm-inventory.py
@cli/omni/managed_apps.py
</context>

## Artifacts this phase produces

- `modules/rustdesk-fleet/contracts/scope.json` — adds fields `gsd_lifecycle.required_workstream`, `gsd_lifecycle.required_flag`, `gsd_lifecycle.mutating_verbs`, `gsd_lifecycle.command_sources`, `shared_writers.paths`, `shared_writers.mode`, `transition_gates.precheck_ids`, and `transition_gates.postcheck_ids`.
- `modules/rustdesk-fleet/evidence/phase48-baseline.json` — fields `schema_version`, `source_head`, `source_phase`, `migrated_phase`, `file_count`, `allowed_exclusions`, `rebaseline_policy`, and `files[]` with `legacy_git_path`, `legacy_blob_id`, `workstream_path`, and `workstream_sha256`.
- `modules/rustdesk-fleet/evidence/ledger.json` — fields `schema_version`, `milestone`, `requirement_count`, and `requirements[]` with `requirement_id`, `owner_phase`, `acceptance_kind`, `status`, `evidence_ids`, and `last_verified_at`.
- `modules/rustdesk-fleet/tools/validate_phase51.py` — Python symbols `extract_executable_gsd_commands`, `validate_workstream_commands`, `resolve_legacy_blob`, `validate_phase48_baseline`, `parse_canonical_requirements`, `validate_ledger`, and `materialize_fixture_bundle`; no new CLI flags beyond Plan 01.
- `modules/rustdesk-fleet/tests/test_phase51_contracts.py` — pytest nodes `test_workstream_scope_contract`, `test_phase48_integrity_contract`, `test_requirement_ledger_contract`, exact-36/old-to-new parametrization, and generated temp-copy drift tests.
- `modules/rustdesk-fleet/tests/fixtures/valid/minimal-contracts/bundle.json` — one complete fixture bundle containing named `scope`, `product_decision`, `threat_model`, `permission_profiles`, `secret_roles`, `ledger`, `phase48_baseline`, and `operational_review` documents for materialization into `tmp_path`.
- `modules/rustdesk-fleet/tests/fixtures/invalid/unscoped-gsd-command.md` — one valid scoped example plus a separate executable mutating command that lacks the exact workstream token pair.
- `modules/rustdesk-fleet/tests/fixtures/invalid/phase48-drift.json` — a baseline mutation descriptor applied only to a temporary copy.
- `modules/rustdesk-fleet/tests/fixtures/invalid/missing-legacy-tool.json` — static incomplete preserved-tool fixture matching Plan 01's already-green generated mutation.
- `modules/rustdesk-fleet/tests/fixtures/invalid/duplicate-secret-ref.json` — static duplicated Vault-reference fixture matching Plan 01's already-green generated mutation; it contains no value.

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-02-01: Enforce explicit RustDesk workstream scope and serialized shared writers</name>
  <read_first>
    @AGENTS.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-CONTEXT.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-RESEARCH.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-PATTERNS.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-01-SUMMARY.md
    @scripts/g18-pro-esm-inventory.py
  </read_first>
  <files>modules/rustdesk-fleet/contracts/scope.json, modules/rustdesk-fleet/tools/validate_phase51.py, modules/rustdesk-fleet/tests/test_phase51_contracts.py, modules/rustdesk-fleet/tests/fixtures/invalid/unscoped-gsd-command.md</files>
  <behavior>
    - "Each executable mutating RustDesk GSD command independently contains exact tokens --ws rustdesk-fleet."
    - "A scoped command elsewhere in a file cannot mask an unscoped or wrong-workstream command."
    - "Shared PROJECT.md, MILESTONES.md and Graphify writer surfaces declare one-at-a-time ownership and pre/post integrity checks."
    - "Read-only inspection of the preserved workstream is distinguishable from a RustDesk lifecycle mutation."
  </behavior>
  <action>Per D-08, extend the authoritative scope contract with lifecycle verb classification, the exact `--ws rustdesk-fleet` token requirement, command source regions, and serialized ownership of shared `PROJECT.md`, `MILESTONES.md` and Graphify outputs. Implement command extraction over fenced shell blocks and scripts one command at a time; prose does not satisfy or fail the gate, and one valid command does not bless siblings. Add P51-WS-001 and require P51-WS-001 before every RustDesk lifecycle transition plus P51-P48-001 after it. Keep validator/test edits serialized inside this plan and encode no mutation command against the preserved Phase 48 lane.</action>
  <verify>
    <automated>python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q -k workstream &amp;&amp; python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q &amp;&amp; node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" query state.json --ws rustdesk-fleet --pick current_phase</automated>
  </verify>
  <acceptance_criteria>P51-WS-001 accepts every explicitly scoped RustDesk lifecycle command, rejects the mixed unscoped fixture and wrong scope, proves one serialized writer for every shared path, and the scoped state query returns the RustDesk current phase.</acceptance_criteria>
  <done>D-08 explicit workstream authority and writer serialization are executable contracts rather than active-marker assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-02-02: Bridge all nine Phase 48 legacy blobs to migrated SHA-256 hashes</name>
  <read_first>
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-CONTEXT.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-RESEARCH.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-PATTERNS.md
    @cli/omni/managed_apps.py
    @.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/48-codex-oauth-wayland-acp-convergence/48-CONTEXT.md
    @.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/48-codex-oauth-wayland-acp-convergence/48-VALIDATION.md
  </read_first>
  <files>modules/rustdesk-fleet/evidence/phase48-baseline.json, modules/rustdesk-fleet/tools/validate_phase51.py, modules/rustdesk-fleet/tests/test_phase51_contracts.py, modules/rustdesk-fleet/tests/fixtures/invalid/phase48-drift.json</files>
  <behavior>
    - "Exactly nine rows map old tracked HEAD blobs to the nine migrated workstream files."
    - "Each row verifies legacy Git blob ID and current raw-byte SHA-256 as distinct provenance fields."
    - "Missing, extra, empty, modified or unexplained rows derive BLOCKED; validation never writes/rebaselines."
    - "Drift tests mutate only tmp_path copies and leave Phase 48 untouched."
  </behavior>
  <action>Per D-09, create P51-P48-001 and a baseline with `file_count: 9`, current `source_head`, exact legacy blob IDs and migrated SHA-256 values for these old-to-new pairs: `48-01-PLAN.md`, `48-01-ROUTER-EVIDENCE.md`, `48-02-PLAN.md`, `48-CONTEXT.md`, `48-EXECUTION-CHECKPOINT-2026-07-12.md`, `48-PATTERNS.md`, `48-RESEARCH.md`, `48-VALIDATION.md`, and `tools/verify-router-evidence.py`, each from `HEAD:.planning/phases/48-codex-oauth-wayland-acp-convergence/...` to `.planning/workstreams/runtime-trust-codex-delivery-convergence/phases/48-codex-oauth-wayland-acp-convergence/...`. Record only transient exclusions `__pycache__`, `*.pyc`, editor swap files and editor backup files. Resolve blobs with read-only Git object commands and hash migrated raw bytes with `_sha256_file`; never use an empty migrated `git ls-files` result as success. Validation mode has no rebaseline path. A legitimate Phase 48 change returns BLOCKED until its serialized owner reviews provenance; any later explicit rebaseline implementation requires separate authorization, reason and reviewer.</action>
  <verify>
    <automated>python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q -k phase48 &amp;&amp; python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q &amp;&amp; node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" query state.json --ws runtime-trust-codex-delivery-convergence --pick current_phase</automated>
  </verify>
  <acceptance_criteria>The manifest has nine and only nine non-empty mappings, every old blob/current SHA-256 matches, the temp-copy drift/missing/extra/zero-row cases return BLOCKED/exit 2, no rebaseline option exists, and `git diff` reports no Phase 48 content mutation.</acceptance_criteria>
  <done>D-09 integrity is proved across the tracked legacy and migrated filesystem boundary without false-empty success or automatic drift acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-02-03: Reserve exact 36 requirement evidence addresses and complete fixtures</name>
  <read_first>
    @.planning/workstreams/rustdesk-fleet/REQUIREMENTS.md
    @.planning/workstreams/rustdesk-fleet/ROADMAP.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-CONTEXT.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-RESEARCH.md
    @.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-PATTERNS.md
    @modules/fleet-control-plane/tools/validate_m004.py
    @modules/fleet-control-plane/tests/test_m004_contract.py
  </read_first>
  <files>modules/rustdesk-fleet/evidence/ledger.json, modules/rustdesk-fleet/tools/validate_phase51.py, modules/rustdesk-fleet/tests/test_phase51_contracts.py, modules/rustdesk-fleet/tests/fixtures/valid/minimal-contracts/bundle.json, modules/rustdesk-fleet/tests/fixtures/invalid/missing-legacy-tool.json, modules/rustdesk-fleet/tests/fixtures/invalid/duplicate-secret-ref.json</files>
  <behavior>
    - "Ledger requirement IDs equal the 36 canonical REQUIREMENTS.md IDs exactly, with no duplicate, orphan or missing row."
    - "Every row has owner phase 51-58 matching current traceability, typed acceptance_kind, unique evidence IDs, status and last_verified_at."
    - "Pending skeleton rows may omit timestamps; PASS requires current machine-readable evidence paths/digests and cannot cite only SUMMARY.md."
    - "The complete positive bundle materializes all contracts, 36 rows and nine Phase 48 mappings into tmp_path."
  </behavior>
  <action>Per D-10, parse canonical requirements and phase ownership from workstream sources, then create `ledger.json` with exactly 36 stable rows for SCP-01..OPS-04 as enumerated in REQUIREMENTS.md. Initialize Phase 51 rows as pending until current evidence exists and keep later-phase rows pending with stable evidence reservations; enforce unique evidence IDs, repo-relative RustDesk workstream/module paths, acceptance-kind semantics and currentness. Implement P51-LEDGER-001 so a PASS requires current machine evidence and input digest, and any narrative-only/summary-only closure is BLOCKED; keep that case generated in `tmp_path` here because Plan 03 owns the reusable static summary-only fixture. Create the full positive bundle as named embedded documents that tests materialize into `tmp_path`; include all five contract shapes, the exact 36-row ledger, nine-row Phase 48 baseline and BLOCKED operational-review shape. Add the reusable `missing-legacy-tool.json` and `duplicate-secret-ref.json` structural fixtures, then prove they match the in-memory Plan 01 failures. Add missing/orphan/duplicate/path escape/stale/currentness cases through parametrization rather than extra secret-like fixtures.</action>
  <verify>
    <automated>python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q -k ledger &amp;&amp; python3 -m pytest modules/rustdesk-fleet/tests/test_phase51_contracts.py -q</automated>
  </verify>
  <acceptance_criteria>P51-LEDGER-001 proves exact 36/36 equality and owner-phase mapping, rejects duplicate/orphan/missing/stale/summary-only cases, materializes a complete positive family, and the full focused test file passes after the task.</acceptance_criteria>
  <done>D-10 evidence addressing is deterministic across the entire milestone and cannot be closed by prose.</done>
</task>

</tasks>

<threat_model>
**Security enforcement:** OWASP ASVS 5.0.0 Level 1 baseline plus risk-based Level 2 controls `v5.0.0-16.1.1`, `16.2.5`, `16.3.3`, `16.4.2`, `16.5.1`, and `16.5.3`. Blocking threshold is `high`; unresolved high threats or unexplained integrity drift make the plan and Phase 52 advancement `BLOCKED`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executable planning text -> GSD workstream | Parsed lifecycle commands can mutate the wrong lane if scope is absent or masked. |
| shared planning/Graphify surface -> serialized writer | Workstreams share a few global files whose concurrent mutation is not safe. |
| legacy tracked Phase 48 -> migrated untracked/current files | Git blob identity and filesystem SHA-256 are separate provenance domains. |
| requirements source -> evidence ledger | Canonical IDs/owners cross into acceptance state and must not be invented or closed by prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T51-WORKSTREAM | Tampering/Elevation | lifecycle commands | high | mitigate | D-08 command-by-command parsing requires exact `--ws rustdesk-fleet` and rejects marker-only/wrong-scope routing under P51-WS-001. |
| T51-WRITER | Tampering/Repudiation | PROJECT/MILESTONES/Graphify | high | mitigate | D-08 single-owner policy plus pre/post checks serializes all shared writers. |
| T51-INTEGRITY | Tampering/Repudiation | Phase 48 baseline | high | mitigate | D-09 exact nine-row legacy blob/current SHA-256 bridge blocks missing, extra or unexplained drift under P51-P48-001. |
| T51-EVIDENCE | Repudiation | 36-row ledger | high | mitigate | D-10 exact ID equality, unique evidence IDs, current hashes and machine-path requirements reject summary-only closure. |
| T51-PATH | Tampering/Information Disclosure | validator paths | high | mitigate | Resolve paths beneath repo root, reject traversal/symlink escape and report metadata only per ASVS L1 validation and V16 safe errors. |
| T51-LOG | Information Disclosure | integrity/ledger findings | medium | mitigate | V16 L2 inventory/protection requires structured category/path/field findings without blob contents, hashes of secrets or raw commands. |
| T51-SC | Tampering | package-manager installs | low | accept | No npm, pip, cargo or other package installation is authorized or required by this plan. |
</threat_model>

<verification>
- After each task, run its focused pytest selection followed by the complete Phase 51 test file; no heavy build, broad suite, live mutation or package install is allowed.
- Require P51-WS-001, P51-P48-001 and P51-LEDGER-001 alongside all seven Wave 1 check IDs.
- Run both scoped read-only state queries exactly as shown in the task gates; every RustDesk lifecycle command uses `--ws rustdesk-fleet`.
- Run `git diff --check -- modules/rustdesk-fleet .planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation` and confirm no Phase 48 file appears as modified by this plan.
- Planning observed Graphify fresh at `e36e47b`; this is routing evidence, not execution proof. Recheck live freshness read-only with `node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status`; a stale graph blocks advancement for the serialized Graphify owner and is never auto-rebuilt by this plan.
</verification>

<success_criteria>
- SCP-05 is enforced through explicit workstream scope, serialized shared writers and nine-file Phase 48 integrity.
- SCP-01/SCP-02/SCP-03 evidence slots are present in an exact 36/36 ledger without changing their runtime acceptance gates.
- Drift, unscoped commands and summary-only acceptance fail closed with current, redacted evidence.
- All changes remain governance/validator/test artifacts inside the authorized file set.
</success_criteria>

<output>
Create `.planning/workstreams/rustdesk-fleet/phases/51-contract-threat-model-and-workstream-isolation/51-02-SUMMARY.md` when done.
</output>
