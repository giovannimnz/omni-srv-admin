---
phase: 53-primary-relay-and-public-edge
plan: 05
type: execute
wave: 4
depends_on: [53-04]
files_modified:
  - modules/rustdesk-fleet/tools/run-phase53-live-gate.py
  - modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  - modules/rustdesk-fleet/evidence/phase53/deploy-transaction.json
  - modules/rustdesk-fleet/evidence/phase53/edge-probes.json
  - modules/rustdesk-fleet/evidence/phase53/ops-api-probes.json
autonomous: true
requirements: [SRV-02, SRV-03, SRV-04, OPS-01]
must_haves:
  truths:
    - "D-08: live execution is preflight/backup -> closed runtime/API -> host+OCI -> IP proof -> DNS-last -> hostname proof; failure contains and restores before returning."
    - "D-05/D-07: two genuinely external origins prove TCP positives/forbidden negatives and correlated UDP by IP then hostname."
    - "D-09/D-10/D-11/D-12: the backend service, Vault-hydrated auth, owned vhost and certificate are published by an exact-pre-state Apache transaction that configtests, reloads and proves every existing vhost before public ops HTTPS probes."
  artifacts:
    - path: modules/rustdesk-fleet/evidence/phase53/deploy-transaction.json
      provides: "Value-free ordered live mutation and rollback receipts."
    - path: modules/rustdesk-fleet/evidence/phase53/edge-probes.json
      provides: "Two-origin IP/hostname TCP+UDP proof."
  key_links:
    - from: modules/rustdesk-fleet/tools/run-phase53-live-gate.py
      to: modules/rustdesk-fleet/tools/apply-phase53-edge.py
      via: "explicit-live ordered transaction with DNS stage unreachable before IP proof"
      pattern: "edge-probes"
    - from: modules/rustdesk-fleet/tools/run-phase53-live-gate.py
      to: modules/rustdesk-fleet/apache/rustdesk-ops.atius.com.br.conf
      via: "ownership-scoped backend/service/vhost/certificate transaction with Vault auth hydration, apachectl configtest, reload, existing-vhost regression and exact rollback before external API probes"
      pattern: "ops-api"
  prohibitions:
    - "Do not run without ATIUS_RUN_RUSTDESK_PHASE53_LIVE=1, current Phase 52 11/11 PASS, backups, capacity, source/contracts and rollback preflight."
    - "Do not repeat Gate B, create/change secrets, build images, install Windows/Horistic clients or claim direct/relay sessions."
    - "Do not commit a DNS/edge transaction after any negative probe, API regression or legacy-access failure."
---

<objective>Executar o deploy fechado, publicar DNS por último e provar edge/API externamente sob uma única transação reversível.

Purpose: produzir a prova live de SRV-02/03/04 e OPS-01 antes do lifecycle gate.
Output: runtime/API ativos, edge mínimo, DNS-only A e evidências externas redacted.</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-CONTEXT.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-04-SUMMARY.md
@.planning/workstreams/rustdesk-fleet/phases/52-supply-chain-capacity-and-recoverable-placement/52-07-VERIFICATION.md
@modules/rustdesk-fleet/evidence/phase52/full-gate-summary.json
@modules/rustdesk-fleet/tools/run-phase53-live-gate.py
</context>

## Artifacts this phase produces

- Completed `Phase53LiveGate` handlers `deploy_closed`, `edge_probes`, `publish_dns`, `hostname_probes`, `ops_api` and `contain_on_failure`.
- `deploy-transaction.json`: current hashes, pre-state, mutation classes, stage receipts, rollback terminality and `secret_material_present=false`.
- `edge-probes.json`: origin IDs, IP/hostname TCP results, UDP metadata/counter/owner correlation; no payload.
- `ops-api-probes.json`: public HTTPS authorized/unauthorized/redaction/readiness and existing-vhost regression results.

<tasks>
<task type="auto">
  <name>Task 53-05-01: Executar deploy fechado, edge probes e DNS-last</name>
  <read_first>
    @.planning/workstreams/rustdesk-fleet/phases/52-supply-chain-capacity-and-recoverable-placement/52-07-VERIFICATION.md
    @modules/rustdesk-fleet/evidence/phase52/full-gate-summary.json
    @modules/rustdesk-fleet/tools/install-phase53-server.py
    @modules/rustdesk-fleet/tools/apply-phase53-edge.py
    @modules/rustdesk-fleet/tools/run-phase53-live-gate.py
  </read_first>
  <files>modules/rustdesk-fleet/tools/run-phase53-live-gate.py, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py, modules/rustdesk-fleet/evidence/phase53/deploy-transaction.json, modules/rustdesk-fleet/evidence/phase53/edge-probes.json</files>
  <action>Require explicit live flag and rederive Phase 52 11/11 PASS, Horistic selection, digest/fingerprint/capacity/backups, address consensus, current contract/source hashes and legacy smokes. Hydrate identity using the approved existing provider—never Gate B—and deploy the server plus private ops backend/service with public native ingress and the ops vhost still closed. Verify backend bind/service ownership, local socket owners/digest/cgroups/log bounds, then atomically apply host+effective OCI policy. Use Windows private-first SSH plus a second origin for IP TCP 21115-21117 positives, 21114/21118/21119 negatives and UDP 21116 correlation. Only after both pass create exactly one proxied=false A and no AAAA/CNAME; repeat by hostname/resolvers. At any failure run D-08/D-14 containment-first rollback and keep Phase 54 blocked.</action>
  <verify><automated>ATIUS_RUN_RUSTDESK_PHASE53_LIVE=1 omni srv1-ops resources run builds -- python3 modules/rustdesk-fleet/tools/run-phase53-live-gate.py --stage edge-probes</automated></verify>
  <acceptance_criteria>One current transaction proves exact local/runtime limits and minimal external edge from two origins by IP then hostname; DNS is last; any failed stage restores edge/DNS/runtime scope and retains Phase 52 backups/fallbacks.</acceptance_criteria>
  <done>SRV-02/03/04 have current live external evidence without client installation.</done>
</task>

<task type="auto">
  <name>Task 53-05-02: Provar HTTPS, auth, redaction e readiness da API</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-ops-api.json
    @modules/rustdesk-fleet/tools/rustdesk-ops-api.py
    @modules/rustdesk-fleet/apache/rustdesk-ops.atius.com.br.conf
    @modules/rustdesk-fleet/evidence/phase53/deploy-transaction.json
  </read_first>
  <files>modules/rustdesk-fleet/tools/run-phase53-live-gate.py, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py, modules/rustdesk-fleet/evidence/phase53/ops-api-probes.json</files>
  <action>Before any external HTTPS probe, snapshot exact backend unit/socket state, enabled-vhost links and bytes, certificate/key references and ownership/mode metadata, Apache modules/listeners/config, and the complete existing-vhost probe baseline. Hydrate the backend auth credential at runtime from the approved Vault authority without persisting or logging its value; start the owned private backend and prove its bind, service identity and uniform auth denial. Stage only the ownership-marked Phase 53 vhost, prove the referenced certificate exists and matches the hostname without copying private key material, run `apachectl configtest`, atomically enable and reload Apache, then repeat every pre-existing vhost probe. On configtest/reload/regression failure, restore the exact vhost/service/certificate-reference pre-state, configtest/reload the restored Apache state, re-probe existing vhosts and keep the ops hostname unpublished/BLOCKED. Only after that transaction passes, probe all four HTTPS endpoints from external origins with authorized, missing, malformed and wrong credentials. Verify strict redacted bodies/logs, backend enforcement, current derived readiness and bounded service/Apache deltas. Assert 21114 remains closed and no client `API Server` setting or Pro/native management claim exists. Store no auth header/token and contain/rollback the exact API/Apache ownership scope on any failure.</action>
  <verify><automated>ATIUS_RUN_RUSTDESK_PHASE53_LIVE=1 omni srv1-ops resources run builds -- python3 modules/rustdesk-fleet/tools/run-phase53-live-gate.py --stage ops-api</automated></verify>
  <acceptance_criteria>Exact backend/service/vhost/certificate pre-state and rollback receipts exist; Vault auth hydration, private bind, certificate/hostname proof, `apachectl configtest`, reload and all existing-vhost regressions pass before external requests; authorized requests return all strict endpoints, auth negatives are uniform, secret scan passes, readiness fails under injected current-state drift and 21114 stays not-open.</acceptance_criteria>
  <done>OPS-01 is live, authenticated, redacted and operationally distinct from RustDesk Pro semantics.</done>
</task>
</tasks>

<threat_model>
**Security enforcement:** OWASP ASVS L1; any unresolved high/critical issue triggers rollback and blocks Plan 06.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T53-LIVE | Tampering/DoS | ordered deployment | critical | mitigate | Explicit flag, hash-bound preflight, immutable receipts and containment-first terminal rollback. |
| T53-PUBLIC | Spoofing/Repudiation | DNS/external probes | high | mitigate | Address consensus, two origins, correlated UDP and DNS-last exact rollback. |
| T53-OPS-PUBLIC | Spoofing/Information Disclosure | HTTPS API | critical | mitigate | Backend auth, external negatives, strict redaction/log scan and private backend bind. |
</threat_model>

<verification>Live commands are allowed only as written with the explicit flag and builds governor; run full RustDesk suite after both stages.</verification>
<success_criteria>Runtime, edge, DNS and ops API all have current external proof or the transaction is terminally rolled back with Phase 54 blocked.</success_criteria>
<output>Create `.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-05-SUMMARY.md` when done.</output>
