---
phase: 53-primary-relay-and-public-edge
plan: 03
type: execute
wave: 2
depends_on: [53-02]
files_modified:
  - modules/rustdesk-fleet/tools/rustdesk-ops-api.py
  - modules/rustdesk-fleet/systemd/atius-rustdesk-ops-api.service
  - modules/rustdesk-fleet/apache/rustdesk-ops.atius.com.br.conf
  - modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
autonomous: true
requirements: [OPS-01, SRV-02]
must_haves:
  truths:
    - "D-09/D-10: rustdesk-ops.atius.com.br is separate authenticated HTTPS, read-only/versioned/redacted, with backend enforcement and no RustDesk API Server/21114 semantics."
    - "D-11: readiness is derived from current digest, sockets, fingerprint, edge proof, cgroups, disk/log growth and bounded restart counters; active alone is insufficient."
    - "D-12: direct/relay bytes and failures are observational summaries only and never assert session transport."
  artifacts:
    - path: modules/rustdesk-fleet/tools/rustdesk-ops-api.py
      provides: "Small stdlib read-only authenticated backend and derived readiness evaluator."
    - path: modules/rustdesk-fleet/apache/rustdesk-ops.atius.com.br.conf
      provides: "Separate HTTPS reverse-proxy vhost with sanitized logging."
  key_links:
    - from: modules/rustdesk-fleet/tools/rustdesk-ops-api.py
      to: modules/rustdesk-fleet/contracts/phase53-ops-api.json
      via: "strict endpoint/response/auth/readiness contract"
      pattern: "/v1/readiness"
    - from: modules/rustdesk-fleet/systemd/atius-rustdesk-ops-api.service
      to: modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice
      via: "10% CPU/192 MiB child allocation within aggregate parent"
      pattern: "Slice=atius-rustdesk-phase53.slice"
  prohibitions:
    - "Do not expose backend beyond loopback/Unix socket, open 21114, populate any client API Server field or claim Pro/native RustDesk management."
    - "Do not log Authorization headers, bearer values, keys, passwords, client IDs, payloads or reusable material."
    - "Do not reload Apache live in this plan; produce and fault-test a configtest-first reversible transaction for Plan 05."
---

<objective>
Implementar a API operacional ATIUS autenticada/redacted e sua integração Apache reversível, sem confundi-la com capacidades Pro do RustDesk.

Purpose: tornar health/readiness/status/métricas observáveis sob OPS-01 dentro do budget agregado.
Output: backend, user unit, vhost separado e testes de auth/redaction/readiness/regressão.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-CONTEXT.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-02-SUMMARY.md
@modules/rustdesk-fleet/contracts/phase53-runtime.json
@modules/rustdesk-fleet/contracts/phase53-ops-api.json
@modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice
</context>

## Artifacts this phase produces

- `rustdesk-ops-api.py`: `GET /v1/health`, `/v1/readiness`, `/v1/status`, `/v1/metrics/summary`; `authenticate_request()`, `redact_response()`, `derive_readiness()`, `collect_metric_summary()`.
- `atius-rustdesk-ops-api.service`: loopback/Unix-only backend, 10% CPU, 192 MiB, parent-slice membership and private runtime credential file.
- `rustdesk-ops.atius.com.br.conf`: HTTPS-only reverse proxy, uniform auth-denial path, sanitized access/error logs and reversible ownership marker.

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-03-01: Implementar API operacional autenticada e transação Apache</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-ops-api.json
    @modules/rustdesk-fleet/contracts/phase53-runtime.json
    @.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH.md
    @modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  </read_first>
  <files>modules/rustdesk-fleet/tools/rustdesk-ops-api.py, modules/rustdesk-fleet/systemd/atius-rustdesk-ops-api.service, modules/rustdesk-fleet/apache/rustdesk-ops.atius.com.br.conf, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py</files>
  <behavior>
    - "All four GET endpoints are versioned/read-only; any other method/path is denied without leaking routing/auth detail."
    - "Missing/malformed/wrong auth has one denial shape and neither responses nor logs contain secret-capable fields."
    - "Readiness fails on digest/socket/fingerprint/edge/cgroup/disk/log/restart drift even when processes are active."
    - "Apache configtest failure restores exact pre-state and existing-vhost regression fixtures remain green."
  </behavior>
  <action>Write RED tests then implement a dependency-light Python backend bound only to a private Unix socket or loopback chosen at execution. Per D-09/D-10 enforce authentication in the backend even if Apache also gates it; hydrate credential material at runtime from the approved Vault authority without persistent values. Implement exact versioned response schemas and allowlisted metrics for listeners, restarts, CPU, RAM, disk, log growth, direct/relay bytes and failures. Per D-11/D-12 derive readiness from raw current checks and label byte counters observational, never session truth. Attach the service to the Phase 53 parent slice at 10%/192 MiB. Provide an ownership-marked Apache vhost transaction requiring certificate/pre-state, `apachectl configtest`, reload, all-existing-vhost probes and exact rollback; Plan 05 alone executes it live.</action>
  <verify><automated>omni srv1-ops resources run builds -- pytest -q modules/rustdesk-fleet/tests/test_phase53_primary_edge.py -k 'ops_api or apache or readiness'</automated></verify>
  <acceptance_criteria>Authorized fixtures return strict redacted schemas; all auth/redaction/readiness negatives fail closed; generated service effective limits fit the aggregate; Apache rollback/regression fixtures preserve prior vhosts; no 21114 or client API setting appears.</acceptance_criteria>
  <done>OPS-01 has an implementable, resource-bounded and reversibly published backend ready for live deployment.</done>
</task>

</tasks>

<threat_model>
**Security enforcement:** OWASP ASVS L1; any unresolved high/critical issue blocks Plan 04.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T53-API-AUTH | Spoofing/Elevation | ops backend | high | mitigate | Backend auth, uniform denial, private bind and auth-negative tests. |
| T53-API-LEAK | Information Disclosure | responses/Apache logs | critical | mitigate | Allowlisted schemas, redaction at source, sanitized logs and secret fixtures/scans. |
| T53-READINESS | Tampering/Repudiation | readiness/status | high | mitigate | Derived raw-input readiness with current digests and no stored verdict acceptance. |
| T53-APACHE | DoS/Tampering | shared HTTPS service | high | mitigate | Pre-state, configtest, existing-vhost probes and exact ownership-scoped rollback. |
</threat_model>

<verification>
Run API/Apache/readiness tests and full module tests under the builds governor; do not reload Apache or contact live Vault/DNS.
</verification>

<success_criteria>
- OPS-01 endpoint/auth/redaction/readiness semantics pass hermetic tests.
- API resource allocation keeps the Phase 53 aggregate at 80% CPU/1 GiB and does not alter native RustDesk ports.
</success_criteria>

<output>Create `.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-03-SUMMARY.md` when done.</output>
