---
phase: 53-primary-relay-and-public-edge
plan: 02
type: execute
wave: 1
depends_on: [53-01]
files_modified:
  - modules/rustdesk-fleet/quadlets/atius-rustdesk-server-hbbs.container
  - modules/rustdesk-fleet/quadlets/atius-rustdesk-server-hbbr.container
  - modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice
  - modules/rustdesk-fleet/systemd/atius-rustdesk-server-logrotate.service
  - modules/rustdesk-fleet/systemd/atius-rustdesk-server-logrotate.timer
  - modules/rustdesk-fleet/tools/install-phase53-server.py
  - modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
autonomous: true
requirements: [SRV-02, SRV-06]
must_haves:
  truths:
    - "D-01/D-03: hbbs/hbbr are rootless digest-pinned Quadlets with Network=host, least privilege and effective combined 70% CPU/832 MiB inside an 80%/1 GiB parent slice."
    - "D-02/D-04: server state, identity tmpfs, logs, evidence and rollback paths are dedicated and never overlap future Horistic client assets."
    - "D-13/D-14: installation, linger and identity hydration are transactional, dry-run/fault tested and terminally rollbackable without touching legacy access."
  artifacts:
    - path: modules/rustdesk-fleet/tools/install-phase53-server.py
      provides: "Closed-ingress server installer, identity hydration and terminal rollback orchestration."
    - path: modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice
      provides: "Aggregate cgroup ceiling for hbbs, hbbr and ops backend."
  key_links:
    - from: modules/rustdesk-fleet/tools/install-phase53-server.py
      to: modules/rustdesk-fleet/tools/rustdesk-vault-provider
      via: "bounded tmpfs identity hydration using existing approved refs only"
      pattern: "server-identity"
    - from: modules/rustdesk-fleet/quadlets/atius-rustdesk-server-hbbs.container
      to: modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice
      via: "generated user unit is attached to parent slice and child limit"
      pattern: "Slice=atius-rustdesk-phase53.slice"
  prohibitions:
    - "Do not start public ingress, create DNS, mutate OCI/Cloudflare, install a client, build an image or use rootful Podman."
    - "Do not regenerate/write Vault secrets or repeat Phase 52 Gate B; private bytes exist only in bounded runtime tmpfs."
    - "Do not remove/alter RustGuac, XRDP, AnyDesk, NoMachine, noVNC, k3s or future client paths."
---

<objective>
Implementar o domínio rootless hardened do primary e sua transação de instalação/rollback, ainda com o edge público fechado.

Purpose: satisfazer a base executável de SRV-02 e a persistência de SRV-06 antes da API e do edge.
Output: Quadlets, parent slice, rotação bounded e installer transacional testado.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-CONTEXT.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-01-SUMMARY.md
@modules/rustdesk-fleet/contracts/supply-chain.json
@modules/rustdesk-fleet/contracts/secret-roles.json
@modules/rustdesk-fleet/contracts/phase53-runtime.json
@modules/rustdesk-fleet/tools/phase52_recovery.py
@modules/rustdesk-fleet/tools/phase52-horistic-live-drill.py
@modules/rustdesk-fleet/tools/rustdesk-vault-provider
</context>

## Artifacts this phase produces

- Quadlets `atius-rustdesk-server-hbbs.container` and `...-hbbr.container`, generated user units and exact local socket ownership.
- `atius-rustdesk-phase53.slice` with aggregate `CPUQuota=80%`, `MemoryMax=1G`; child ceilings 35%/448 MiB and 35%/384 MiB reserve 10%/192 MiB for Plan 03.
- Bounded authoritative log rotation service/timer tied to the 128 MiB/day, 30-day contract.
- `install-phase53-server.py`: `Phase53ServerTransaction`, `snapshot_prestate()`, `hydrate_identity()`, `render_and_verify_units()`, `install_closed()`, `rollback_server()`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-02-01: Implementar Quadlets hardened e cgroups agregados</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-runtime.json
    @modules/rustdesk-fleet/contracts/supply-chain.json
    @.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH-DEBATE.md
    @modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  </read_first>
  <files>modules/rustdesk-fleet/quadlets/atius-rustdesk-server-hbbs.container, modules/rustdesk-fleet/quadlets/atius-rustdesk-server-hbbr.container, modules/rustdesk-fleet/systemd/atius-rustdesk-phase53.slice, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py</files>
  <behavior>
    - "Quadlets use the exact ARM64 child digest, Pull=never, Network=host, rootless user units, no-new-privileges, all capabilities dropped, bounded PIDs and only approved writable mounts."
    - "Generated units and live-readback fixtures enforce parent and child CPU/memory arithmetic."
    - "hbbs owns TCP 21115/21116/21118 and UDP 21116; hbbr owns TCP 21117/21119; no 21114 or extra delta is accepted."
  </behavior>
  <action>Extend the RED tests first, then author the two Quadlets and parent slice per D-01/D-03. Use the immutable Phase 52 digest and existing state/identity/log mounts; keep rootfs read-only where the pinned binaries permit and never mount the Podman socket or broad host paths. Make generated-unit tests assert hardening directives and effective cgroup files, not only source text. Encode expected upstream 21118/21119 as local-only and require an edge-policy proof dependency before readiness.</action>
  <verify><automated>omni srv1-ops resources run builds -- pytest -q modules/rustdesk-fleet/tests/test_phase53_primary_edge.py -k 'quadlet or runtime or cgroup'</automated></verify>
  <acceptance_criteria>Static/generated-unit fixtures and effective-readback fakes prove exact digest, rootless ownership, mounts/caps/PIDs, socket ownership, 70%/832 MiB server-pair total and 80%/1 GiB parent ceiling.</acceptance_criteria>
  <done>The server runtime definition is hardened, pinned and resource-bounded before deployment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-02-02: Implementar hydration, persistência, logs e rollback do server</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-runtime.json
    @modules/rustdesk-fleet/contracts/secret-roles.json
    @modules/rustdesk-fleet/tools/phase52_recovery.py
    @modules/rustdesk-fleet/tools/phase52-horistic-live-drill.py
    @modules/rustdesk-fleet/tools/rustdesk-vault-provider
    @modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  </read_first>
  <files>modules/rustdesk-fleet/tools/install-phase53-server.py, modules/rustdesk-fleet/systemd/atius-rustdesk-server-logrotate.service, modules/rustdesk-fleet/systemd/atius-rustdesk-server-logrotate.timer, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py</files>
  <behavior>
    - "Identity enters only `/run/user/<uid>/atius-rustdesk/server-identity`; evidence receives public fingerprint/value-free metadata."
    - "Linger changes only if prior state is no and rolls back conditionally; partial failures restore exact pre-state."
    - "SQLite integrity/state digest, log limits and future client/legacy paths are invariant under fault injection."
  </behavior>
  <action>Implement a transaction object with exclusive lock, pre-state manifest, bounded `ssh -n`/timeouts, existing Vault provider hydration, atomic Quadlet install, generated-unit/cgroup/socket validation and idempotent rollback. Per D-02/D-04, use only dedicated server paths and never persist secret bytes. Do not perform Phase 52 create/write flows. Snapshot linger and install it transactionally; rollback disables it only if this transaction enabled it. Select one authoritative bounded log path, prevent duplicate unbounded stderr, and prove actual rotation/size. The installer exposes a closed-ingress `install_closed()` invoked only by Plan 05, not automatically here.</action>
  <verify><automated>omni srv1-ops resources run builds -- pytest -q modules/rustdesk-fleet/tests/test_phase53_primary_edge.py -k 'identity or sqlite or rollback or linger or log_bound'</automated></verify>
  <acceptance_criteria>Faults at every mutation boundary restore server units, state, logs and linger exactly; public fingerprint/SQLite fixtures persist; secrets and client/legacy paths never appear in mutation sets.</acceptance_criteria>
  <done>The closed server deployment is reproducible and terminally reversible without public exposure.</done>
</task>

</tasks>

<threat_model>
**Security enforcement:** OWASP ASVS L1; any unresolved high/critical issue blocks Plan 03.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T53-RUNTIME | Elevation/Tampering | Quadlets/cgroups | high | mitigate | Rootless digest pin, least privilege, exact mounts, parent+child effective limit assertions. |
| T53-IDENTITY | Information Disclosure/Spoofing | Vault hydration/state | critical | mitigate | Existing bounded provider, tmpfs-only private identity, public fingerprint continuity and secret scan. |
| T53-ROLLBACK-SRV | DoS/Tampering | installer/linger/logs | high | mitigate | Exclusive transaction, exact pre-state, fault injection and conditional terminal rollback preserving legacy/client domains. |
</threat_model>

<verification>
Run the narrow tests and full RustDesk module suite through the builds governor; no explicit live flag, DNS, firewall or client operation is permitted.
</verification>

<success_criteria>
- SRV-02 runtime artifacts satisfy immutable rootless/hardening/resource contracts.
- SRV-06 identity/state/log/linger paths have deterministic install and rollback behavior ready for live stages.
</success_criteria>

<output>Create `.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-02-SUMMARY.md` when done.</output>
