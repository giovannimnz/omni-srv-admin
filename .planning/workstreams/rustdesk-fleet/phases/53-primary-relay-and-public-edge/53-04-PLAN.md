---
phase: 53-primary-relay-and-public-edge
plan: 04
type: execute
wave: 3
depends_on: [53-03]
files_modified:
  - modules/rustdesk-fleet/tools/apply-phase53-edge.py
  - modules/rustdesk-fleet/nftables/atius-rustdesk-phase53.nft
  - modules/rustdesk-fleet/systemd/atius-rustdesk-phase53-edge.service
  - modules/rustdesk-fleet/tools/probe-phase53-edge.py
  - modules/rustdesk-fleet/tools/probe-phase53-edge.ps1
  - modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
autonomous: true
requirements: [SRV-03, SRV-04]
must_haves:
  truths:
    - "D-05/D-06: effective host+OCI public policy permits only IPv4 TCP 21115-21117 and UDP 21116; 21114/21118/21119 and all RustDesk IPv6 exposure fail closed."
    - "D-07: TCP and correlated UDP proofs require Windows plus a second origin outside Horistic; localhost/open-filtered never passes."
    - "D-08: fresh address-consensus barriers immediately precede the first host/OCI mutation and DNS publication; DNS-only A is created last and failures use revisioned semantic restore without overwriting concurrent drift."
  artifacts:
    - path: modules/rustdesk-fleet/tools/apply-phase53-edge.py
      provides: "Pure state machine with injected nft/OCI/Cloudflare backends, optimistic CAS and revisioned semantic rollback."
    - path: modules/rustdesk-fleet/systemd/atius-rustdesk-phase53-edge.service
      provides: "Tracked boot-order contract for loading and verifying the owned candidate policy before network-pre.target; installed only by Plan 05."
    - path: modules/rustdesk-fleet/tools/probe-phase53-edge.py
      provides: "Second-origin TCP/UDP correlation and evidence normalization."
  key_links:
    - from: modules/rustdesk-fleet/tools/apply-phase53-edge.py
      to: modules/rustdesk-fleet/contracts/phase53-edge.json
      via: "strict effective-policy and DNS-last state machine"
      pattern: "dns_last"
  prohibitions:
    - "Do not flush global nftables, edit k3s/CNI chains, trust UFW-inactive state, or treat a narrow NSG as overriding any broad Security List/NSG allow."
    - "Do not mutate live edge/DNS in this plan; Plan 05 executes only through the explicit live runner."
    - "Do not perform provider, network, root nft or systemd calls by default; Plan 04 uses only hermetic fixtures and injected fake backends."
    - "Do not persist UDP payload/nonce or any Cloudflare/OCI/Vault credential."
---

<objective>Implementar e fault-testar a transação deny-first do edge e os probes externos, sem publicar o serviço ainda.

Purpose: tornar SRV-03/SRV-04 executáveis com rollback e prova externa real.
Output: policy nft, orchestrator OCI/DNS, probes Python/PowerShell e testes adversariais.</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-CONTEXT.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH.md
@.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-03-SUMMARY.md
@modules/rustdesk-fleet/contracts/phase53-edge.json
</context>

## Artifacts this phase produces

- `apply-phase53-edge.py`: `EdgeTransaction`, `snapshot_edge()`, `audit_effective_oci_ingress()`, `apply_host_policy()`, `publish_dns_last()`, `rollback_edge()`.
- `atius-rustdesk-phase53.nft`: root-owned marked table/chain loaded only after `nft -c`, without global flush/CNI ownership.
- `atius-rustdesk-phase53-edge.service`: root-owned boot-order template that loads the fixed candidate before `network-pre.target`, verifies semantic readback and fails closed; Plan 04 does not install or start it.
- `probe-phase53-edge.py` and `.ps1`: TCP positive/negative and UDP nonce-counter-capture-owner correlation from distinct origins.

<tasks>
<task type="auto" tdd="true">
  <name>Task 53-04-01: Implementar policy nft/OCI deny-first e auditoria efetiva</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-edge.json
    @.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-RESEARCH-DEBATE.md
    @modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  </read_first>
  <files>modules/rustdesk-fleet/tools/apply-phase53-edge.py, modules/rustdesk-fleet/nftables/atius-rustdesk-phase53.nft, modules/rustdesk-fleet/systemd/atius-rustdesk-phase53-edge.service, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py</files>
  <behavior>
    - "Effective union of every attached Security List/NSG blocks broad/forbidden allows."
    - "nft policy is syntax-checked, ownership-scoped, IPv4 allowlisted/IPv6 denied and never changes k3s/CNI chains."
    - "Every fault uses revisioned semantic restore; concurrent drift is never overwritten and terminates contained for manual recovery."
  </behavior>
  <action>Write RED fixtures for broad OCI unions, IPv6 leakage, extra listeners, ownership drift and every mutation/rollback boundary. Implement D-06/D-08 as a pure state machine with injected fake backends: complete paginated enumeration of every Security List/NSG/attachment, bounded root-only snapshots, generation/ETag or canonical-digest CAS before writes, a root-owned atomic nft scope after `nft -c`, semantic readback, and revisioned semantic restore. Concurrent drift must never be overwritten and terminates `CONTAINED_REQUIRES_MANUAL_RECOVERY`. Require current Phase 52 PASS, selected Horistic, address-consensus Barrier A, retained backups, closed DNS and legacy smokes before a mutation can be authorized. Add the tracked boot-order service contract for Plan 05 installation and Plan 06 reboot proof. Expose library handlers only; no provider/network/root/systemd command runs here.</action>
  <verify><automated>omni srv1-ops resources run builds -- pytest -q modules/rustdesk-fleet/tests/test_phase53_primary_edge.py -k 'nft or oci or listener or ipv6 or pagination or cas or boot or snapshot or storage or rollback'</automated></verify>
  <acceptance_criteria>Fixtures prove pagination completeness, only approved public ports survive the full effective union, forbidden/IPv6 exposure blocks, nft hook/priority/interface/marker and boot ordering are exact, k3s rules remain byte-identical, snapshots are bounded/root-only, stale CAS writes are zero-mutation, and rollback is semantically equal or explicitly contained at every injected fault.</acceptance_criteria>
  <done>The host/OCI edge can be applied narrowly and reversed without collateral mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-04-02: Implementar DNS-last e harness de prova externa</name>
  <read_first>
    @modules/rustdesk-fleet/contracts/phase53-edge.json
    @.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-CONTEXT.md
    @modules/rustdesk-fleet/tests/test_phase53_primary_edge.py
  </read_first>
  <files>modules/rustdesk-fleet/tools/apply-phase53-edge.py, modules/rustdesk-fleet/tools/probe-phase53-edge.py, modules/rustdesk-fleet/tools/probe-phase53-edge.ps1, modules/rustdesk-fleet/tests/test_phase53_primary_edge.py</files>
  <behavior>
    - "DNS accepts exactly one proxied=false A to the Barrier-B-revalidated Horistic IPv4, no AAAA/CNAME, only after IP probes pass."
    - "UDP PASS requires per-origin attempt plus nft counter delta, metadata-only capture and pinned hbbs socket ownership."
    - "Windows private-first SSH failure records both private and public-native fallback probes before unavailability."
  </behavior>
  <action>Implement D-05/D-07/D-08 strict probe adapters and DNS transaction with injected fake backends only. Enforce Barrier A immediately before the first host/OCI mutation and a separately fresh Barrier B immediately before DNS, after IP probes; each revalidates one unambiguous IPv4 across OCI VNIC, Horistic egress and `ssh-horistic-srv.atius.com.br`, with no AAAA/CNAME/attachment/source drift. Require attested Windows plus a configurable independent public origin. TCP observations require positive controls and host socket/container/cgroup/image-digest correlation; UDP observations require per-origin tuple/window/counter/capture/owner correlation and reject replay/ambient traffic. Strict bounded schemas reject duplicate/unknown keys and persist neither verdicts, payloads, nonces, argv/stdout/stderr nor credentials. DNS uses optimistic CAS and revisioned semantic restore; concurrent drift closes ingress and blocks destructive rollback.</action>
  <verify><automated>omni srv1-ops resources run builds -- pytest -q modules/rustdesk-fleet/tests/test_phase53_primary_edge.py -k 'dns or cloudflare or address or external_probe or udp'</automated></verify>
  <acceptance_criteria>Either address barrier drifting/stale/ambiguous, proxied/AAAA/CNAME, unattested or same-egress origins, TCP impostors, UDP replay/ambient traffic, unsafe evidence or record rollback drift all fail; valid fixtures preserve Barrier A -> IP proof -> Barrier B -> DNS-last ordering and semantic CAS rollback.</acceptance_criteria>
  <done>The public proof and DNS publication harness is dependency-safe and fail-closed.</done>
</task>
</tasks>

<threat_model>
**Security enforcement:** OWASP ASVS L1; any unresolved high/critical issue blocks Plan 05.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T53-EDGE | Elevation/DoS | nft/OCI effective policy | critical | mitigate | Full union audit, deny-first IPv4/IPv6 contract, ownership-scoped atomic rules and rollback tests. |
| T53-DNS | Spoofing/Tampering | Cloudflare record-set | high | mitigate | Three-source address consensus, A-only DNS-last, exact snapshot/restore. |
| T53-PROBE | Repudiation/Spoofing | external TCP/UDP evidence | high | mitigate | Two origins plus UDP counter/capture/socket-owner correlation and value-free receipts. |
</threat_model>

<verification>Run edge/DNS/probe tests and full suite through the builds governor; no live flag or provider mutation.</verification>
<success_criteria>SRV-03/04 edge logic is tested against broad-union, IPv6, DNS-order, probe-origin and rollback failures before publication.</success_criteria>
<output>Create `.planning/workstreams/rustdesk-fleet/phases/53-primary-relay-and-public-edge/53-04-SUMMARY.md` when done.</output>
