---
quick_id: 260722-1p9
title: Provisionar plumbing Vault e Backup B Phase 52 no Horistic
status: blocked
scope: phase52-horistic-only
authorization: explicit-in-current-task
---

# Quick Plan 260722-1p9

## Must haves

- [x] O readiness remoto usa o home do usuário autenticado e deixa de reportar
      o helper Vault existente como ausente.
- [x] O provider RustDesk gerenciado aceita somente as sete refs aprovadas e
      falha sem expor valores quando os seis paths ainda não têm conteúdo.
- [x] Backup B usa archive state-only, upload real, hash remoto independente,
      retenção Phase 57 PASS + 30 dias e zero cleanup/delete remoto.
- [x] O modo Horistic do instalador não instala nem ativa a fila/timer ampla.
- [x] srv2/srv3 permanecem sem writes; Horistic só recebe o escopo aprovado.
- [x] Nenhum valor RustDesk/rclone é criado, impresso, documentado ou commitado.
- [x] Phase 53 continua bloqueada enquanto o full gate não for PASS atual.
- [ ] O full gate possui callbacks live reais, mas só as executa quando Vault,
      rclone e contenção já estiverem comprovadamente prontos.
- [x] `authorized_live_write_candidate=horistic-srv`; qualquer outro candidato
      bloqueia antes do primeiro callback capaz de escrever, mesmo se capacity
      mudar inesperadamente para PASS.
- [x] O dirty baseline é preservado; stage/commit usa allowlist explícita e não
      absorve `.planning/`, Graphify ou inventário já modificados pelo usuário.

## Task 1 — Corrigir readiness e versionar o canal Vault dedicado

**Files**

- `modules/rustdesk-fleet/tools/validate_phase52.py`
- `modules/rustdesk-fleet/tools/rustdesk-vault-provider`
- `modules/rustdesk-fleet/tools/install-rustdesk-vault-provider.sh`
- `modules/rustdesk-fleet/tests/test_phase52_supply_capacity_restore.py`

**Actions**

1. Tornar o probe remoto home-aware e separar `vault_helper` do
   `rustdesk_vault_provider`.
2. Criar provider local stdin-safe, com protocolo JSON e allowlist exata das
   sete refs; nenhuma expansão arbitrária de profile/path. Valores podem
   trafegar somente pelo pipe privado provider→consumer; stdout/stderr herdado,
   argv, logs, evidências e environment persistente devem ficar limpos.
3. Criar instalador/rollback idempotente com backup de arquivos-alvo e modes
   0700/0600. Não alterar key, backend, wrapper ou forced-command no srv3. Se o
   canal existente não suportar o contrato, instalar apenas o código local e
   persistir um blocker que exige autorização específica para srv3.
4. Fazer o vault stage executar metadata/distinctness/hydration segura, em vez
   de considerar presença de arquivo como PASS.
5. Aplicar e testar o guard `authorized_live_write_candidate=horistic-srv` nos
   stages vault/backup/restore/capacity_finalize/rollback. Em srv2/srv3, um
   capacity PASS inesperado retorna `BLOCKED/unauthorized-live-write-candidate`,
   `mutation.performed=false` e não chama staging/image/backup/runtime.
6. Implementar callbacks de source e restore usando
   `docker.io/rustdesk/rustdesk-server@sha256:17c3422e0a6a65199ef69ac5cbb265ce9314a04524afcf9bb7a374fec0b1c208`
   em runtimes únicos sob `/run/user/$UID/rustdesk-phase52-*`, rootless, sem
   publish/listener público e sob `omni-builds.slice` com CPUQuota=20%.
   Confirmar que `hbbs` cria/abre a DB, consome a identidade hidratada e mantém
   somente fingerprint público como evidência.

**Verify**

- Testes provam home `/home/horistic`, refs extra/ausente rejeitadas, streams
  top-level e artefatos sem sentinel secreto, cleanup tmpfs, ausência de PASS
  por mera presença, digest/runtime pinados, nenhuma porta publicada e zero
  write quando srv2/srv3 apresentam capacity PASS inesperado.

**Done**

- Readiness reporta o helper real; provider indisponível/paths vazios geram
  blocker preciso e nenhum secret deixa o processo efêmero.

## Task 2 — Implementar Backup B state-only/copy-only gerenciado

**Files**

- `modules/fleet-backup/configs/fleet-backup-map.yaml`
- `modules/fleet-backup/scripts/rclone-copy-verified-phase52.sh`
- `modules/fleet-backup/scripts/atius-rclone-vault-hydrate`
- `modules/fleet-backup/scripts/install-fleet-backup.sh`
- `modules/fleet-backup/tests/test_phase52_backup_b.py`
- `modules/fleet-backup/README.md`
- `modules/rustdesk-fleet/tools/validate_phase52.py`

**Actions**

1. Adicionar entrada Horistic limitada a
   `giovanni-drive:ATIUS-SRV/HORISTIC-SRV/Backup/RustDesk/phase52/backup-b/`.
2. Implementar `copyto` + `cat|sha256sum` com allowlist, baixo paralelismo,
   JSON seguro e retenção sem delete.
3. Hidratar config rclone somente em tmpfs via Vault; nunca persistir token.
   Não há profile/path rclone já aprovado: versionar o hydrator sem inventar
   binding e manter upload live BLOCKED até aprovação separada de path/profile
   e valor.
4. Adicionar `--host horistic-srv --phase52-only [--dry-run]` ao instalador,
   sem timer/fila e usando `$HOME`.
5. Exigir no validator prova real do upload/hash remoto; remover o PASS falso
   por rótulo local.
6. Gerar A e B em chamadas independentes a partir da mesma source quiesced,
   com paths e generation IDs distintos e `source_input_digest` igual; depois
   enviar B e relê-lo com `rclone cat`.
7. Implementar `restore`, `capacity_finalize` e rollback live: restore novo
   realmente abre a DB no `hbbs` pinado, prova SQLite/fingerprint/zero listener;
   finalize usa amostras frescas e bytes reais de A/B contra reservas <=80%; o
   rollback remove apenas runtime/spool marcado disposable e preserva A/B.

**Verify**

- `bash -n`; testes de destino fora da allowlist, verbo destrutivo, hash
  divergente, ausência de config, falha de upload, independência A/B,
  capacity-finalize, rollback seletivo, retenção e timer inativo.

**Done**

- Backup B só pode ser PASS após cópia e rehash remoto independentes, e nenhuma
  rotina desta modalidade consegue apagar conteúdo remoto.

## Task 3 — Aplicar o escopo autorizado e repetir o full gate

**Files**

- `modules/rustdesk-fleet/evidence/phase52/candidate-atius-srv-2.json`
- `modules/rustdesk-fleet/evidence/phase52/candidate-atius-srv-3.json`
- `modules/rustdesk-fleet/evidence/phase52/candidate-horistic-srv.json`
- `modules/rustdesk-fleet/evidence/phase52/full-gate-summary.json`
- `modules/rustdesk-fleet/contracts/placement-decision.json`
- `.planning/quick/260722-1p9-provisionar-helper-profile-vault-e-camin/260722-1p9-SUMMARY.md`
- `docs/security/atius-secrets-vaults.md`
- `~/GitHub/obsidian-vault/AiSecondBrain/60-LOGS/2026-07-19-rustdesk-v19-research-milestone.md`

**Actions**

1. Rodar testes focados sob `omni srv1-ops resources run builds --` e revisar
   diff/secret scan. Capturar baseline dos paths tocados e comparar
   `git diff --name-only` antes/depois; nunca usar glob no stage.
2. Revalidar e remover exatamente o artefato acidental de 43 bytes.
3. Capturar estado prévio de package/files/modes/symlinks/timers, executar
   dry-run e instalar no Horistic apenas package/files do modo Phase52; nenhum
   timer e nenhum write em srv2/srv3. Rollback não remove package preexistente.
4. Reexecutar a cadeia serial. Se os paths/valores RustDesk ou a config rclone
   continuarem ausentes, parar fail-closed antes de backup/restore live.
5. Atualizar somente as evidências allowlisted, quick summary, Obsidian e
   GBrain apenas com paths,
   profiles, status e blockers, nunca valores.

**Verify**

- Metadata/modes/package checks; timer ausente; secret scan; full gate atual;
  Graphify fresh após todas as mudanças. Outputs Graphify já dirty ficam fora
  do stage/commit se não puderem ser atribuídos com segurança a este task.

**Done**

- Runtime e repo concordam; o full gate está PASS ou BLOCKED apenas pelas
  autorizações/valores realmente ausentes, sem promover Phase 53.
