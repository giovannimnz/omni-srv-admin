---
type: project
title: RustDesk v1.9 Phase 52 Horistic plumbing checkpoint
date: 2026-07-22
status: blocked-awaiting-secret-authority
---

# Quick 260722-1p9 — Horistic Vault e Backup B

## Resultado

O plumbing autorizado foi instalado no `horistic-srv`, mas o full gate continua
`BLOCKED` e sem primary selecionado. Não houve criação/leitura/documentação de
valor Vault, upload GDrive, backup live, restore live, listener, mudança de
DNS/edge ou instalação Windows.

## Runtime instalado

- `rclone=1.60.1+dfsg-3ubuntu0.24.04.5+esm1` instalado sem upgrades/removals
  adicionais.
- `/home/horistic/.local/bin/rustdesk-vault-provider`, mode `0700`.
- `/home/horistic/.local/bin/rclone-copy-verified-phase52`, mode `0700`.
- `/home/horistic/.local/bin/atius-rclone-vault-hydrate`, mode `0700`.
- `/home/horistic/.config/atius/fleet-backup/fleet-backup-map.yaml`, mode `0600`.
- `rclone-fleet-queue.timer` permaneceu `not-found/inactive`; o modo Phase 52
  não instala a fila ampla.
- `/home/horistic/.local` e `.config` foram estreitados de `0775` para `0755`
  porque o grupo `horistic` inclui `admin`; rollback de mode, se necessário:
  `chmod 0775 /home/horistic/.local /home/horistic/.config`.
- Rollback do fleet-backup:
  `/home/horistic/.local/state/atius-fleet-backup/phase52-5f4831e0079cd26433e30c143a727d17`.

## Segurança e implementação

- Readiness deriva o home remoto real; o falso blocker de `/home/ubuntu` foi
  removido.
- `authorized_live_write_candidate=horistic-srv` é obrigatório. Capacity PASS
  inesperado em srv2/srv3 bloqueia antes de qualquer callback live capaz de
  escrever.
- Provider RustDesk aceita somente as sete refs contratadas e usa pipe privado;
  input/output são limitados e falhas não geram traceback ou secret em log.
- Backup B é state-only/copy-only, usa snapshot privado, hydrator sibling,
  `rclone copyto`, rehash com `rclone cat`, destino allowlisted, paralelismo 1
  e nenhuma operação delete/purge/move/sync/cleanup.
- Install/rollback são transacionais, stale-safe, parent-symlink-safe e
  recuperáveis após interrupção.
- O artefato acidental `/home/horistic/.cache/nonexistent-path` foi revalidado
  por type/size/owner e removido por path literal; nenhum cleanup amplo ocorreu.

## Gate atual

- Generated at: `2026-07-22T05:50:46Z`.
- srv2: capacity `NO-GO`, mutation `false`.
- srv3: capacity `NO-GO`, mutation `false`.
- Horistic: capacity `PASS`, Vault
  `BLOCKED/rustdesk-vault-backend-missing`, mutation `false`.
- Selected candidate: `null`.
- Phase 53 advance: `BLOCKED`.
- Windows install/access: `false`.
- Public listener e secret material em evidência: `false`.

## Validação

- Dois code reviews independentes: `APPROVED`.
- Suíte completa RustDesk + Backup B: `249 passed` sob `omni-builds.slice`,
  `CPUQuota=80%` em quatro vCPU, equivalente a 20% do host.
- `bash -n`, `shellcheck`, `py_compile`, `git diff --check` e secret scan: PASS.
- Dry-runs remotos: zero-write comprovado.

## Autorizações ainda necessárias

1. Autorizar explicitamente a geração/gravação dos sete valores RustDesk nos
   seis paths já aprovados (server private/public identity e cinco passwords
   permanentes distintos), sem revelar os valores.
2. Aprovar um path/profile/valor Vault para a config `rclone` do remote
   `giovanni-drive`; nenhum binding foi inventado nesta execução.
3. Se o provider dedicado exigir backend/profile/forced-command no
   `atius-srv-3`, autorizar especificamente esse write. O srv3 permaneceu
   read-only nesta execução.

Enquanto qualquer item acima faltar, Backup B/restore/capacity-finalize não
executam e Phase 53/54 permanecem bloqueadas.
