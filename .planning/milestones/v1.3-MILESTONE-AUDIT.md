---
milestone: v1.3
audited: 2026-06-26T09:55:00-03:00
status: passed
scores:
  requirements: 8/8
  phases: 1/1
  integration: 1/1
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 41
    items:
      - "Pin `Alibaba-NLP/gte-multilingual-base` to a concrete Hugging Face snapshot SHA in a future maintenance window if stronger immutability is required."
notes:
  - "This audit covers milestone v1.3 only. v1.2 carry-over phases remain intentionally open in ROADMAP/REQUIREMENTS and are out of scope for this closeout."
---

# Milestone v1.3 Audit

## Result

Milestone `v1.3 — Local AI Embeddings and Semantic Retrieval` passed audit.

The active milestone contains a single delivery phase, `Phase 41`, and its canonical verification is already `status: passed`. The shipped result matches the milestone goal: TEI/GTE runs privately on `horistic-srv`, New API exposes the stable alias `embedding-pt-v1`, the public OpenAI-compatible endpoint is validated, and the migration contract for GBrain, Obsidian and Graphify is documented without leaking secrets.

## Scope

- Milestone: `v1.3 — Local AI Embeddings and Semantic Retrieval`
- Audited phase set: `Phase 41`
- Excluded from this audit: all `v1.2` carry-over work (FreeIPA, Samba, Keycloak, Production Guard, legacy G18/governance backlog)

## Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| EMB-01 | satisfied | Public authenticated embeddings route documented and verified in `41-VERIFICATION.md` |
| EMB-02 | satisfied | Router channel points to `http://10.1.1.4:3000`, not `https://router.atius.com.br/v1` |
| EMB-03 | satisfied | TEI runs on `horistic-srv` in namespace `ai-search` with no public ingress |
| EMB-04 | satisfied | Alias/model/dimension contract documented: `embedding-pt-v1`, `gte-multilingual-base`, `768` |
| EMB-05 | satisfied | Public smoke returned `quantidade=2`, `dimensoes=768`, `error=null` |
| EMB-06 | satisfied | `docs/operations/gbrain-embedding-migration.md` contains backup-first migration contract |
| EMB-07 | satisfied | Obsidian/Graphify auxiliary usage documented in embeddings runbooks |
| EMB-08 | satisfied | Secret hygiene recorded in verification and no token material persisted in planning/docs |

## Phase Verification

| Phase | Verification | Result |
|---|---|---|
| 41 | `.planning/phases/41-local-ai-embeddings-gateway-horistic-srv/41-VERIFICATION.md` | passed |

## Integration Check

Cross-phase integration for this milestone is intentionally narrow:

- `horistic-srv` k3s worker hosts the private TEI backend.
- `router-ai-atius` / New API routes the public alias to the internal TEI URL.
- GBrain/Obsidian/Graphify consumption is documented against the same 768-dimension contract.

No broken intra-milestone wiring was found.

## End-to-End Flows

| Flow | Result |
|---|---|
| Authenticated public `POST /v1/embeddings` using `embedding-pt-v1` | passed |
| Protected public unauthenticated model listing | passed |
| Internal TEI smoke (`2` vectors, `768` dimensions) | passed |
| Migration contract for downstream clients | passed |

## Deferred / Non-blocking Notes

- Pinning the Hugging Face model to a concrete snapshot SHA is still recommended for stricter immutability, but it does not block milestone closeout.

## Audit Decision

`v1.3` is ready for milestone closeout from a requirements and verification standpoint.
