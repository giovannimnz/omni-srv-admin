---
phase: 48-codex-oauth-wayland-acp-convergence
plan: 04
type: execute
wave: 4
depends_on: [48-03]
files_modified:
  - modules/fork-sync/projects/wayland/sync.yaml
  - modules/fork-sync/projects/wayland/UPSTREAM-SYNC-GUARDS.md
  - modules/fleet-control-plane/files/wayland-codex-acp-launcher.sh
  - modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh
  - modules/fleet-control-plane/tests/test_install_wayland_owner_acp.py
  - /usr/local/bin/wayland-codex-acp
  - /opt/wayland-codex-acp/<approved-sha>/<arch>/codex-acp
  - /home/ubuntu/GitHub/wayland/src/process/acp/infra/SshAcpProcess.ts
  - /home/ubuntu/GitHub/wayland/src/process/acp/compat/LegacyConnectorFactory.ts
  - /home/ubuntu/GitHub/wayland/src/process/acp/types.ts
  - /home/ubuntu/GitHub/wayland/src/process/acp/compat/typeBridge.ts
  - /home/ubuntu/GitHub/wayland/src/process/acp/session/SessionLifecycle.ts
  - /home/ubuntu/GitHub/wayland/src/process/task/AcpAgentManager.ts
  - /home/ubuntu/GitHub/wayland/tests/unit/ownerLocalAcpTransport.test.ts
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md
autonomous: false
requirements: [WAC-09, WAC-10]
must_haves:
  truths:
    - Each owner has a pinned `/usr/local/bin/wayland-codex-acp` using that owner's native Codex auth and MCP configuration.
    - Direct Codex conversations can spawn ACP stdio through the dedicated allowlisted SSH alias while the existing ACP object chain remains intact.
    - `cwd` remains the srv-3 NFS/UI/SQLite path and `agentCwd` is sent to owner-side `session/new` and `session/load`.
    - Team and cron owner-local requests fail closed; cancellation keeps the SSH child alive and reconnect failures remain classifiable.
  artifacts:
    - path: modules/fleet-control-plane/files/wayland-codex-acp-launcher.sh
      provides: Versioned text launcher source; no compiled ELF/PE artifact is committed.
    - path: modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh
      provides: Pinned per-architecture launcher plan/install/verify/rollback with digest evidence.
    - path: /usr/local/bin/wayland-codex-acp
      provides: Live fixed launcher pointing to `/opt/wayland-codex-acp/<approved-sha>/<arch>/codex-acp`.
    - path: /home/ubuntu/GitHub/wayland/src/process/acp/infra/SshAcpProcess.ts
      provides: Allowlisted `ssh -T` ChildProcess factory with open stdin and separated stdout/stderr.
    - path: /home/ubuntu/GitHub/wayland/src/process/acp/session/SessionLifecycle.ts
      provides: Owner-side `agentCwd` for new/load while local `cwd` remains unchanged.
    - path: .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md
      provides: Launcher digests, lifecycle results, rollback, and focused-test evidence.
  key_links:
    - from: /home/ubuntu/GitHub/wayland/src/process/task/AcpAgentManager.ts
      to: /home/ubuntu/GitHub/wayland/src/process/acp/compat/LegacyConnectorFactory.ts
      via: A default-off direct-Codex transport descriptor crosses `AcpAgentV2`/`AcpSession` unchanged.
      pattern: ownerTransport|agentCwd
    - from: /home/ubuntu/GitHub/wayland/src/process/acp/compat/LegacyConnectorFactory.ts
      to: /home/ubuntu/GitHub/wayland/src/process/acp/infra/SshAcpProcess.ts
      via: The existing `ProcessAcpClient` receives a ChildProcess-producing spawn function.
      pattern: SshAcpProcess|ProcessAcpClient
---

<objective>
Install pinned owner-native ACP launchers and add the smallest SSH ChildProcess seam inside the existing ACP lifecycle.

Purpose: enable strict owner-local direct Codex execution without RemoteAgentCore, WCore, secret forwarding, remote shell interpolation, or replacement of the established ACP stack.
Output: reversible launcher parity, typed owner transport/agentCwd, SSH child factory, lifecycle wiring, focused tests, and evidence.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Protect all planned Wayland seams before building pinned owner launcher installation</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-SUMMARY.md
    @modules/fork-sync/projects/wayland/sync.yaml
    @modules/fork-sync/projects/wayland/UPSTREAM-SYNC-GUARDS.md
    @/home/ubuntu/GitHub/wayland/codex-acp/Cargo.lock
    @/home/ubuntu/GitHub/wayland/scripts/atius-build-codex-acp.sh
  </read_first>
  <files>modules/fork-sync/projects/wayland/sync.yaml, modules/fork-sync/projects/wayland/UPSTREAM-SYNC-GUARDS.md, modules/fleet-control-plane/files/wayland-codex-acp-launcher.sh, modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh, modules/fleet-control-plane/tests/test_install_wayland_owner_acp.py, /usr/local/bin/wayland-codex-acp, /opt/wayland-codex-acp/&lt;approved-sha&gt;/&lt;arch&gt;/codex-acp, .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md</files>
  <behavior>
    - Plan mode inventories architecture, owner, native Codex health, source commit, artifact digest, target mode/owner, and prior launcher without mutation.
    - Install is one alias at a time, uses CPU-capped per-architecture artifacts, and rollback restores or removes only the exact installed files.
    - The fixed text launcher sanitizes environment inheritance and executes the digest-pinned live binary with owner-native HOME/CODEX_HOME/MCP state; no compiled binary enters Git.
  </behavior>
  <action>Before any edit under `/home/ubuntu/GitHub/wayland`, first update `sync.yaml` and the guard in Omni with every source/test path owned by Plans 48-04 and 48-05, run a deterministic protection query, and commit/record that Omni protection SHA; stop if any planned path is absent. Then write failing installer tests. Create versioned text source `wayland-codex-acp-launcher.sh`; never commit an ELF/PE. The installed root-owned 0755 `/usr/local/bin/wayland-codex-acp` sets a deterministic safe PATH/HOME/CODEX_HOME and execs the approved architecture binary at `/opt/wayland-codex-acp/&lt;approved-sha&gt;/&lt;arch&gt;/codex-acp` in stdio mode; it accepts no command override and reads no srv-3 secret. Create `install-wayland-owner-acp.sh` with plan/install/verify/rollback, architecture detection, source commit and SHA-256 allowlist, build through the 20% `builds` profile, atomic sudo install of the non-versioned build output, encrypted prior-file backup, owner-native `codex debug models`, ACP initialize/session/new/shutdown smoke, and sanitized evidence. Target srv-1, srv-2, Horistic through Phase 48-03 aliases and srv-3 locally. Stop on protection, architecture, digest, auth, or MCP parity drift.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; test "$(hostname -f)" = atius-srv-3.atius.internal &amp;&amp; python3 -m pytest modules/fleet-control-plane/tests/test_install_wayland_owner_acp.py -x &amp;&amp; bash modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh --verify-protection --sync-config modules/fork-sync/projects/wayland/sync.yaml --plans 48-04,48-05 &amp;&amp; bash modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh --plan --all --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>Fixture install/rollback is byte-identical, every artifact is commit/digest/architecture pinned, and plan output proves owner-native auth/MCP without exposing credentials.</acceptance_criteria>
  <done>The four owner launcher transactions are ready for explicit privileged approval.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Authorize owner launcher installation</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md
  </read_first>
  <files>.planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md</files>
  <action>Pause for review of the exact four architecture/digest/install/rollback transactions and interactive sudo authorization on each owner. Do not record passwords or authentication material.</action>
  <what-built>A no-mutation launcher plan with exact hashes and rollback manifests.</what-built>
  <how-to-verify>Confirm all targets resolve through the dedicated aliases, srv-3 is local, artifact digests match the pinned source commit, and each prior target state is backed up.</how-to-verify>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; bash modules/fleet-control-plane/scripts/install-wayland-owner-acp.sh --verify-plan --all --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md</automated>
    <human-check>Operator approves the hashed install plan and completes sudo authorization per owner.</human-check>
  </verify>
  <resume-signal>Type `approved` after all four scoped sudo gates are ready.</resume-signal>
  <acceptance_criteria>Approval identifies the exact manifest and no install occurred before it.</acceptance_criteria>
  <done>Privileged installation is authorized without credential disclosure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add ACP stdio-over-SSH and separated agentCwd through the existing stack</name>
  <read_first>
    @/home/ubuntu/GitHub/wayland/src/process/task/AcpAgentManager.ts
    @/home/ubuntu/GitHub/wayland/src/process/acp/compat/AcpAgentV2.ts
    @/home/ubuntu/GitHub/wayland/src/process/acp/session/AcpSession.ts
    @/home/ubuntu/GitHub/wayland/src/process/acp/infra/ProcessAcpClient.ts
    @/home/ubuntu/GitHub/wayland/src/process/acp/compat/LegacyConnectorFactory.ts
    @/home/ubuntu/GitHub/wayland/src/process/acp/session/SessionLifecycle.ts
  </read_first>
  <files>/home/ubuntu/GitHub/wayland/src/process/acp/infra/SshAcpProcess.ts, /home/ubuntu/GitHub/wayland/src/process/acp/compat/LegacyConnectorFactory.ts, /home/ubuntu/GitHub/wayland/src/process/acp/types.ts, /home/ubuntu/GitHub/wayland/src/process/acp/compat/typeBridge.ts, /home/ubuntu/GitHub/wayland/src/process/acp/session/SessionLifecycle.ts, /home/ubuntu/GitHub/wayland/src/process/task/AcpAgentManager.ts, /home/ubuntu/GitHub/wayland/tests/unit/ownerLocalAcpTransport.test.ts, .planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-ACP-SSH-EVIDENCE.md</files>
  <behavior>
    - Disabled or non-Codex conversations follow the byte-equivalent local connector path; direct Codex opt-in selects only an allowlisted route.
    - SSH spawns with `-T` and open stdin, fixed launcher, strict options, no forwarding/env forwarding, stdout NDJSON, and separate stderr.
    - New/load use `agentCwd`; UI/SQLite config snapshots retain local `cwd`; cancel sends ACP cancel without closing SSH.
    - Team/cron selection fails closed and reconnect classification preserves exit, signal, stderr category, host ID, and retryability without secrets.
  </behavior>
  <action>Read the live Wayland base SHA recorded by the Phase 48-03 trust evidence/summary, require the current HEAD to equal it, require Task 1's protection SHA to contain every planned path, and stop on drift or overlapping dirty files. Install and verify launchers from Task 1. Add typed owner transport and optional `agentCwd` in ACP config/type bridge, but persist no raw path or secret. Add `SshAcpProcess.ts` returning a Node `ChildProcess`; its immutable argv is `ssh -T` plus `BatchMode=yes`, `StrictHostKeyChecking=yes`, `IdentitiesOnly=yes`, `ClearAllForwardings=yes`, `ForwardAgent=no`, `PermitLocalCommand=no`, `RequestTTY=no`, cleared SendEnv, one allowlisted Phase 48-03 alias, and fixed `/usr/local/bin/wayland-codex-acp`. Never add `-n`; stdin carries ACP. Make `LegacyConnectorFactory` select this spawn function while retaining `ProcessAcpClient`. Make `SessionLifecycle` use `agentCwd ?? cwd` for new/load and config advertised to the agent. Gate in `AcpAgentManager` default-false, direct Codex only; reject team/cron. Do not modify/use RemoteAgentCore or WCore and do not forward srv-3 env, tokens, MCP config, or shell fragments.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; WAYLAND_BASE_SHA=$(python3 scripts/wayland-owner-trust-preflight.py --print-wayland-sha --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json) &amp;&amp; cd /home/ubuntu/GitHub/wayland &amp;&amp; test "$(git rev-parse HEAD)" = "$WAYLAND_BASE_SHA" &amp;&amp; omni srv1-ops resources run builds -- pnpm exec vitest run tests/unit/ownerLocalAcpTransport.test.ts</automated>
  </verify>
  <acceptance_criteria>Focused tests prove exact argv/stdin/stream boundaries, unchanged local path, allowlist, direct-Codex-only gate, agentCwd new/load, cancel-with-live-child, reconnect classification, and team/cron rejection.</acceptance_criteria>
  <done>The existing ACP chain reaches the owner launcher over strict SSH with no wrong seam, secret forwarding, or local-cwd regression.</done>
</task>

</tasks>

<stop_conditions>
- Stop on recorded base-SHA drift, missing pre-edit protected path, any overlapping dirty file, missing Phase 48-03 alias/trust, launcher digest/auth/MCP mismatch, committed compiled binary, or privileged install outside its manifest.
- Stop if implementation requires RemoteAgentCore, WCore, ProcessAcpClient replacement, dynamic remote commands, `-n`, env/agent forwarding, non-Codex enablement, or team/cron fallback.
</stop_conditions>

<rollback>
Disable owner transport, restore the local connector descriptor, revert only listed Wayland files, run launcher manifest rollback per host, verify native local ACP, and leave Phase 48-03 trust/NFS untouched.
</rollback>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-04-01 | Spoofing | SSH child | mitigate | Dedicated strict alias allowlist and fixed launcher. |
| T-48-04-02 | Tampering | Launcher artifact | mitigate | Commit/arch/SHA-256 pin, root-owned atomic install, manifest rollback. |
| T-48-04-03 | Information Disclosure | Process environment | mitigate | Clear forwarding/SendEnv and use owner-native auth/MCP only. |
| T-48-04-04 | Elevation of Privilege | Team/cron/dynamic commands | mitigate | Direct Codex only, fixed command, fail-closed unsupported modes. |
| T-48-SC | Tampering | Package supply chain | mitigate | Build only lockfile-pinned existing source; no new dependency or lockfile change. |
</threat_model>

## Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| REQ | WAC-09, WAC-10 | Tasks 1-3 |
| RESEARCH | Encrypted owner session lifecycle and native owner execution | Tasks 1-3 |
| CONTEXT | Correct ACP chain, fixed launcher, agentCwd split, direct Codex gate, no secret forwarding | Task 3 |

<verification>
The unified focused transport suite passes under the builds profile; launcher plan/install/verify/rollback evidence names all four owners and both live destinations; protection precedes external edits; diffs exclude RemoteAgentCore/WCore, compiled binaries, and package locks.
</verification>

<success_criteria>
- Pinned owner launchers pass native ACP lifecycle on all owners.
- The correct ACP stack uses strict stdio-over-SSH and owner agentCwd while local cwd persists.
- Unsupported modes fail closed, cancellation preserves the child, and rollback restores native local ACP.
</success_criteria>

<output>
Create `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-04-SUMMARY.md` when done.
</output>
