---
phase: 48-codex-oauth-wayland-acp-convergence
plan: 03
type: execute
wave: 3
depends_on: [48-02]
files_modified:
  - scripts/wayland-owner-trust-preflight.py
  - tests/test_wayland_owner_trust_preflight.py
  - modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh
  - modules/fleet-control-plane/tests/test_configure_wayland_owner_transport.py
  - inventory/remotes/wayland-github-atius-srv-1.yaml
  - inventory/remotes/wayland-github-atius-srv-2.yaml
  - inventory/remotes/wayland-github-atius-srv-3.yaml
  - inventory/remotes/wayland-github-horistic-srv.yaml
  - /etc/hosts
  - /etc/systemd/resolved.conf.d/60-atius-internal.conf
  - /etc/ssh/ssh_config.d/04-ipa.conf
  - /home/ubuntu/.ssh/config
  - /home/ubuntu/.ssh/config.d/60-wayland-owner-transport.conf
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-ROLLBACK.md
autonomous: false
requirements: [WAC-10]
must_haves:
  truths:
    - On srv-3, canonical lowercase FQDNs resolve to their inventory OCI/DRG addresses and FreeIPA/SSSD supplies the exact four host keys before authentication.
    - Wayland owner commands use only dedicated strict aliases for srv-1, srv-2, and Horistic; srv-3 is explicitly local.
    - The four remote inventories gain only an EOF `owner_transport:` block while every pre-existing byte, including NFS data, remains identical.
    - Resolver, FreeIPA keys, SSH include/drop-in, aliases, and control masters have per-host apply evidence and a rehearsed rollback.
  artifacts:
    - path: scripts/wayland-owner-trust-preflight.py
      provides: Ownership, resolver, FQDN, SSSD 2.9.4, FreeIPA key, inventory-prefix, and rollback readiness gates.
    - path: modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh
      provides: Dedicated plan/apply/verify/rollback renderer with one target per transaction.
    - path: /home/ubuntu/.ssh/config.d/60-wayland-owner-transport.conf
      provides: Three encrypted aliases and srv-3 local-mode metadata using `~/.ssh/cm-wayland/%C`.
    - path: .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json
      provides: Sanitized backups, hashes, DNS, host-key, apply, rollback, and final-state evidence.
  key_links:
    - from: inventory/remotes/wayland-github-atius-srv-1.yaml
      to: modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh
      via: Additive `owner_transport` metadata renders a dedicated alias without reading legacy `access.ssh` or NFS fields.
      pattern: owner_transport
    - from: /etc/ssh/ssh_config.d/04-ipa.conf
      to: /var/lib/sss/pubconf/known_hosts
      via: `GlobalKnownHostsFile` plus `sss_ssh_knownhostsproxy` provide fail-closed FreeIPA trust.
      pattern: GlobalKnownHostsFile|sss_ssh_knownhostsproxy
    - from: /home/ubuntu/.ssh/config
      to: /home/ubuntu/.ssh/config.d/60-wayland-owner-transport.conf
      via: An idempotent `Include ~/.ssh/config.d/*.conf` exposes only the dedicated aliases.
      pattern: Include.*config\.d
---

<objective>
Repair the live srv-3 resolver/FreeIPA host-key boundary and deploy a dedicated, reversible owner-transport configuration for WAC-10.

Purpose: establish strict encrypted identity and routing without changing the legacy fleet renderer, host inventory SSH access, legacy aliases, or any NFS field/unit/export.
Output: deterministic preflight, additive remote inventory metadata, repaired live trust, dedicated aliases, apply evidence, and rehearsed rollback.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a dedicated trust preflight and byte-preserving owner-transport renderer</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-RESEARCH.md
    @scripts/wayland-owner-trust-preflight.py
    @inventory/remotes/wayland-github-atius-srv-1.yaml
    @inventory/remotes/wayland-github-atius-srv-2.yaml
    @inventory/remotes/wayland-github-atius-srv-3.yaml
    @inventory/remotes/wayland-github-horistic-srv.yaml
  </read_first>
  <files>scripts/wayland-owner-trust-preflight.py, tests/test_wayland_owner_trust_preflight.py, modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh, modules/fleet-control-plane/tests/test_configure_wayland_owner_transport.py, inventory/remotes/wayland-github-atius-srv-1.yaml, inventory/remotes/wayland-github-atius-srv-2.yaml, inventory/remotes/wayland-github-atius-srv-3.yaml, inventory/remotes/wayland-github-horistic-srv.yaml</files>
  <behavior>
    - `--plan --evidence <path>` atomically creates the trust evidence with Omni HEAD, live Wayland HEAD, pre-mutation remote-inventory bytes/hashes, owners, modes, and scoped dirty-state before any EOF verification consumes it.
    - The only inventory mutation is one normalized EOF `owner_transport:` block; the complete byte prefix preceding it matches the captured preflight bytes exactly.
    - Render fixtures contain three exact aliases, srv-3 local mode, strict SSSD trust, one managed identity, and a 0700 `%C` socket directory.
  </behavior>
  <action>Write failing tests first. Create the phase-specific `wayland-owner-trust-preflight.py` with `--plan`, `--live-preflight`, `--verify`, `--verify-inventory-eof`, `--print-wayland-sha`, and JSON evidence modes; do not modify the shared FreeIPA preflight. `--plan` must require `--evidence`, capture both Omni and live `/home/ubuntu/GitHub/wayland` SHAs plus each complete pre-mutation remote YAML, and atomically create `48-03-TRUST-EVIDENCE.json`; `--verify-inventory-eof` must fail if that generated baseline is absent or stale. Resolve `atius-srv-{1,2,3}.atius.internal` and `horistic-srv.atius.internal` against each remote inventory OCI/DRG address, assert users `ubuntu/ubuntu/ubuntu/horistic`, inspect srv-3 FQDN ordering/resolved state, and validate the exact SSSD 2.9.4 contract: `/etc/ssh/ssh_config.d/04-ipa.conf`, `GlobalKnownHostsFile /var/lib/sss/pubconf/known_hosts`, `ProxyCommand /usr/bin/sss_ssh_knownhostsproxy -p %p %h`, and key lookup through `sss_ssh_knownhostsproxy -k FQDN`. Create only `configure-wayland-owner-transport.sh`; never invoke or modify the fleet-wide peer renderer. Append exactly one `owner_transport:` block at EOF of each captured remote YAML: dedicated aliases for srv-1/srv-2/Horistic and `mode: local` for srv-3. The EOF verifier must byte-compare the preceding prefix with the generated evidence and reject `access.ssh`, NFS keys, units, exports, or non-allowlisted keys inside the appended block. Render `~/.ssh/config.d/60-wayland-owner-transport.conf`, ensure the user config Include, use `~/.ssh/cm-wayland/%C` mode 0700, `StrictHostKeyChecking yes`, `IdentitiesOnly yes`, bounded persistence, and no separate authoritative known-hosts file. Provide `--apply --host`, `--verify --host`, and `--rollback --manifest` with encrypted backups, hashes, atomic replacement, and one-host scope.</action>
  <verify>
    <automated>python3 -m pytest tests/test_wayland_owner_trust_preflight.py modules/fleet-control-plane/tests/test_configure_wayland_owner_transport.py -x &amp;&amp; python3 scripts/wayland-owner-trust-preflight.py --plan --remotes inventory/remotes --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json &amp;&amp; python3 scripts/wayland-owner-trust-preflight.py --verify-inventory-eof --remotes inventory/remotes --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json &amp;&amp; bash modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh --plan --controller atius-srv-3.atius.internal</automated>
  </verify>
  <acceptance_criteria>Fixture tests prove exact SSSD directives, exact aliases/users/modes, 0700 `%C` sockets, atomic one-host rollback, no TOFU or unencrypted option, and byte-identical NFS/inventory prefixes.</acceptance_criteria>
  <done>The dedicated WAC-10 preflight/renderer is deterministic and the only repo mutations are the new tooling/tests plus additive EOF metadata.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Authorize the scoped FreeIPA and srv-3 trust repair</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json
  </read_first>
  <files>.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json</files>
  <action>Pause after read-only preflight. The operator must review the exact four host records, file hashes, backup destinations, resolver diff, SSH drop-in diff, and per-host apply/rollback commands, then acquire a time-bounded FreeIPA admin ticket and sudo authorization on srv-3. Do not collect credentials in repo artifacts or chat.</action>
  <what-built>A mutation-free plan showing every live FreeIPA, resolver, SSSD, SSH, and inventory change with rollback identifiers.</what-built>
  <how-to-verify>
    1. On `atius-srv-3.atius.internal`, run the preflight from `/home/ubuntu/GitHub/omni-srv-admin` and inspect the sanitized evidence.
    2. Confirm only four `ipaSshPubKey` attributes, `/etc/hosts`, the resolved drop-in, the IPA SSH drop-in, user Include, and dedicated owner drop-in are targeted.
    3. Acquire the admin ticket interactively; never paste its password, cache, or ticket into evidence.
  </how-to-verify>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; test "$(hostname -f)" = atius-srv-3.atius.internal &amp;&amp; python3 scripts/wayland-owner-trust-preflight.py --live-preflight --remotes inventory/remotes --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json</automated>
    <human-check>Operator confirms backups, four-host scope, exact diffs, rollback commands, FreeIPA ticket, and sudo readiness.</human-check>
  </verify>
  <resume-signal>Type `approved` only after the scoped preflight passes and the admin ticket exists on srv-3.</resume-signal>
  <acceptance_criteria>Approval is tied to the hashed preflight manifest and no live mutation or credential disclosure occurred before it.</acceptance_criteria>
  <done>The exact live mutation set is approved and authenticated for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Repair live trust, reconcile four host keys, apply each alias, and rehearse rollback</name>
  <read_first>
    @scripts/wayland-owner-trust-preflight.py
    @modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json
  </read_first>
  <files>/etc/hosts, /etc/systemd/resolved.conf.d/60-atius-internal.conf, /etc/ssh/ssh_config.d/04-ipa.conf, /home/ubuntu/.ssh/config, /home/ubuntu/.ssh/config.d/60-wayland-owner-transport.conf, .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json, .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-ROLLBACK.md</files>
  <action>On srv-3, create encrypted backups plus owner/mode/hash manifests before every write and run `ipa-backup --data --online` before directory mutations. Fix only srv-3's canonical FQDN ordering in `/etc/hosts`; write `60-atius-internal.conf` for the canonical internal resolver; validate both configs, restart `systemd-resolved`, prove DNS, then restart `sssd`. Reconcile exactly four `ipaSshPubKey` values from authenticated host-local public-key evidence, record fingerprints only, and prove `sss_ssh_knownhostsproxy -k FQDN` returns the expected keys. Repair `04-ipa.conf` only to the exact SSSD contract from Task 1. Apply the dedicated renderer one host at a time: srv-1, srv-2, Horistic, then srv-3 local metadata. For each target, apply, verify strict config and an authenticated command, roll back using its manifest, prove hashes/state restored, reapply, and reverify. Generate the JSON and Markdown artifacts from command results; do not hand-author pass claims. Never touch host inventory `access.ssh`, legacy aliases/renderer, fleet peer JSON, NFS configuration, or any unencrypted/HPN transport.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin &amp;&amp; test "$(hostname -f)" = atius-srv-3.atius.internal &amp;&amp; python3 scripts/wayland-owner-trust-preflight.py --verify --remotes inventory/remotes --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json &amp;&amp; python3 scripts/wayland-owner-trust-preflight.py --verify-inventory-eof --remotes inventory/remotes --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json &amp;&amp; for host in atius-srv-1 atius-srv-2 horistic-srv; do bash modules/fleet-control-plane/scripts/configure-wayland-owner-transport.sh --verify --host "$host" --evidence .planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-TRUST-EVIDENCE.json &amp;&amp; ssh -T "wayland-owner-$host" /usr/bin/true; done &amp;&amp; python3 -m pytest tests/test_wayland_owner_trust_preflight.py modules/fleet-control-plane/tests/test_configure_wayland_owner_transport.py -x</automated>
  </verify>
  <acceptance_criteria>DNS, resolver, SSSD config, four FreeIPA keys, exact aliases, strict authenticated commands, per-host rollback/reapply, final hashes, and byte-identical NFS prefixes are machine-verified and sanitized.</acceptance_criteria>
  <done>WAC-10 trust and dedicated aliases are live on srv-3 with independent rollback evidence and no legacy/NFS blast radius.</done>
</task>

</tasks>

<stop_conditions>
- Stop on any scoped dirty path not created by this plan, HEAD/SHA mismatch after preflight, missing backup/hash, failed `ipa-backup`, extra FreeIPA host change, resolver regression, SSSD key mismatch, or unexpected inventory prefix diff.
- Stop if a command would touch `configure-fleet-direct-peers.sh`, `fleet-peers.json`, host inventory `access.ssh`, a legacy alias, an NFS field/unit/export, or a transport confidentiality setting.
- Stop target rollout immediately when its apply, strict connection, rollback, restored hash, or reapply fails; do not continue to the next host.
</stop_conditions>

<rollback>
1. Disable the affected dedicated alias, close only `~/.ssh/cm-wayland/%C` masters for it, and preserve sanitized diagnostics.
2. Run the target manifest rollback and verify user SSH config/drop-in plus remote inventory hashes and modes.
3. Restore `/etc/hosts`, resolved, and IPA SSH drop-ins from their hashed backups in reverse order; restart resolved then SSSD and prove prior service state.
4. Restore only the four captured `ipaSshPubKey` values from the online IPA backup/export, verify fingerprints, and leave unrelated FreeIPA objects unchanged.
5. Confirm NFS inventory prefixes and runtime units/exports remain byte-identical throughout.
</rollback>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-48-03-01 | Spoofing | DNS/host keys | mitigate | OCI/DRG equality plus SSSD-backed exact key lookup before authentication. |
| T-48-03-02 | Tampering | FreeIPA/resolver/SSH state | mitigate | Online IPA backup, encrypted file backups, atomic writes, hashes, one-target transactions, rollback/reapply. |
| T-48-03-03 | Information Disclosure | Identity/evidence | mitigate | Fingerprints and variable names only; no private keys, credentials, tickets, or secret environment. |
| T-48-03-04 | Denial of Service | Resolver/SSSD | mitigate | Validate before ordered restarts and stop on the first failed health check. |
| T-48-SC | Tampering | Package supply chain | accept | No package install or lockfile mutation is permitted in this plan. |
</threat_model>

## Source Coverage Audit

| Source | Items | Coverage |
|---|---|---|
| GOAL | Strict encrypted owner transport prerequisite | Tasks 1-3 |
| REQ | WAC-10 | Tasks 1-3 |
| RESEARCH | FreeIPA/FQDN, fail-closed key trust, multiplex lifecycle, per-host rollback, plaintext negative gate | Tasks 1-3 |
| CONTEXT | Checker-locked dedicated renderer, exact SSSD contract, additive inventory, live repair boundary | Tasks 1-3 |

<verification>
- Both focused pytest files and the byte-exact EOF inventory verifier pass before and after live apply.
- Evidence is command-generated, names only the three remote aliases plus srv-3 local mode, and includes apply/rollback/reapply for each target.
- `git diff --check` passes, and the only permitted inventory diff is exactly one EOF `owner_transport:` block in each of the four `inventory/remotes` files; every preceding byte is unchanged, including `access.ssh` and all NFS fields/config, with no legacy alias/renderer or fleet peer JSON change.
</verification>

<success_criteria>
- Dedicated aliases `wayland-owner-atius-srv-1`, `wayland-owner-atius-srv-2`, and `wayland-owner-horistic-srv` work from srv-3; srv-3 is local.
- SSSD 2.9.4 trust is explicit, four keys are reconciled, and unknown/changed/missing trust fails before auth without creating separate trust state.
- Every live and inventory mutation has verified per-target rollback while NFS data remains byte-identical.
</success_criteria>

<output>
Create `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-03-SUMMARY.md` when done.
</output>
