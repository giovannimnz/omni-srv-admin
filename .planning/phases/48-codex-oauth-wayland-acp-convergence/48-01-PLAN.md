---
phase: 48-codex-oauth-wayland-acp-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-ROUTER-EVIDENCE.md
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-NATIVE-OAUTH-PROOF.md
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-LOCAL-ACP-LIFECYCLE.md
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/run-local-acp-lifecycle.mjs
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-router-evidence.py
  - .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-wave1-asvs.py
autonomous: false
requirements:
  - WAC-01
  - WAC-02
  - WAC-03
  - WAC-04
  - WAC-05
  - WAC-08
must_haves:
  truths:
    - Native Codex OAuth succeeds twice as user ubuntu with CODEX_HOME=/home/ubuntu/.codex across a fresh process boundary.
    - Router evidence distinguishes Router-owned auth from upstream native Codex 401, 403, token_invalidated, refresh_token_invalidated, and invalid_api_key.
    - Local codex-acp completes initialize, session/new, prompt, tool, approval, cancel, resume, and shutdown without Headroom.
  artifacts:
    - path: .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-ROUTER-EVIDENCE.md
      provides: Sanitized Router metadata, refresh, regenerate, probe, and auth classification evidence.
    - path: .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-NATIVE-OAUTH-PROOF.md
      provides: Renewable native Codex prompt proof after human login and after a fresh process boundary.
    - path: .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-LOCAL-ACP-LIFECYCLE.md
      provides: Sanitized local codex-acp lifecycle transcript and rollback notes.
  key_links:
    - from: /home/ubuntu/GitHub/containers/router-ai-atius/scripts/podman-admin.sh
      to: modules/srv1-ops/configs/resource-governor.env
      via: CPU-capped focused Go verification under the builds profile.
      pattern: profile-run
    - from: /home/ubuntu/.codex/auth.json
      to: /home/ubuntu/.local/bin/codex-acp-atius
      via: Native Codex login gates every local ACP turn.
      pattern: CODEX_HOME=/home/ubuntu/.codex
---

<objective>
Reconcile the remaining Router evidence, stop on any unresolved upstream auth drift, and prove native plus local ACP behavior before remote WSS or Wayland are allowed to proceed.

Purpose: clear WAC-01/WAC-02/WAC-03/WAC-04/WAC-05/WAC-08 using only the remaining work, with an explicit human OAuth gate and no Headroom fallback.
Output: sanitized Router evidence, renewable native OAuth proof, local ACP lifecycle evidence, and rollback notes for the real ubuntu runtime.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-CONTEXT.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-RESEARCH.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-PATTERNS.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-VALIDATION.md
@.planning/phases/48-codex-oauth-wayland-acp-convergence/48-EXECUTION-CHECKPOINT-2026-07-12.md
@docs/operations/resource-governor.md
@modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
</context>

## Artifacts this phase produces

- `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-ROUTER-EVIDENCE.md` - sanitized metadata, refresh, regenerate, probe, and upstream-auth classification evidence for Router Phase 32 reconciliation.
- `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-NATIVE-OAUTH-PROOF.md` - renewable native Codex prompt proof as `ubuntu` with `/home/ubuntu/.codex`.
- `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-LOCAL-ACP-LIFECYCLE.md` - local `codex-acp` initialize/session/prompt/tool/approval/cancel/resume/shutdown transcript and rollback notes.
- `.planning/phases/48-codex-oauth-wayland-acp-convergence/tools/run-local-acp-lifecycle.mjs` - disposable JSON-RPC stdio harness that asserts initialize, session/new, prompt, tool request, approval, cancel, resume/reconnect, and clean shutdown.
- `.planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-router-evidence.py` - deterministic verifier for the sanitized Router metadata/refresh/regenerate/probe/auth matrix.
- `.planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-wave1-asvs.py` - deterministic ASVS L1 V2/V3/V4/V5/V6 gate across all three Wave 1 evidence artifacts.
- No production code symbols or live runtime files are modified by this plan. A proven regression stops execution and requires a separately approved repair task naming the exact source file.

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Router evidence and repair the CPU-capped verification lane</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-RESEARCH.md
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-PATTERNS.md
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-EXECUTION-CHECKPOINT-2026-07-12.md
    @modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
    @docs/operations/resource-governor.md
  </read_first>
  <files>.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-ROUTER-EVIDENCE.md, .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-router-evidence.py</files>
  <action>Use the existing Router launcher read-only. First require `./scripts/podman-admin.sh verify-profile` to report `cpu.max=80000 100000` and `profile limits OK`; then run the focused Go suite directly through `./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ...` without nesting another resource wrapper. Exercise the live Phase 32 metadata, refresh, regenerate and probe paths and record a sanitized matrix with rows `internal_invalid`, `upstream_401_token_invalidated`, `upstream_401_refresh_token_invalidated`, `upstream_401_invalid_api_key`, `upstream_403`, `refresh_success`, `regenerate_success`, and `probe_success`. Create `verify-router-evidence.py` to require every row, an HTTP/status classification, and `secret_material_present=false`. Stop and open a separate repair task if any existing Router source or launcher must change.</action>
  <verify>
    <automated>ssh -n atius-srv-1 'cd /home/ubuntu/GitHub/containers/router-ai-atius &amp;&amp; ./scripts/podman-admin.sh verify-profile &amp;&amp; ./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./common ./controller ./service/modelcatalog ./relay/common ./relay/channel/codex ./service ./relay -count=1' &amp;&amp; python .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-router-evidence.py .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-ROUTER-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>Focused Router verification passes under the CPU cap, the evidence file records the repaired command and result, and the final auth matrix distinguishes Router internal auth from upstream native Codex failures without leaking tokens.</acceptance_criteria>
  <done>Focused Router verification passes under the CPU cap, the evidence file records the repaired command and result, and the final auth matrix distinguishes Router internal auth from upstream native Codex failures without leaking tokens.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Renew the real ubuntu ChatGPT login in /home/ubuntu/.codex</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-CONTEXT.md
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-VALIDATION.md
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-EXECUTION-CHECKPOINT-2026-07-12.md
  </read_first>
  <files>.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-NATIVE-OAUTH-PROOF.md</files>
  <action>Block all downstream validation until the operator completes a fresh ChatGPT login as user `ubuntu` with `CODEX_HOME=/home/ubuntu/.codex`. This must renew the native upstream refresh state; do not substitute a Router temporary access token, a different user account, or an alternate `CODEX_HOME`.</action>
  <what-built>Wave 1 preflight, native OAuth stop conditions, and a blocked gate ready for the operator to renew the real ubuntu login.</what-built>
  <how-to-verify>
    1. SSH to `atius-srv-3` as `ubuntu`.
    2. Run `env CODEX_HOME=/home/ubuntu/.codex codex login` and complete the ChatGPT flow for the real ubuntu runtime.
    3. From a fresh shell, run `env CODEX_HOME=/home/ubuntu/.codex codex debug models` to confirm the account-aware catalog, then run `env CODEX_HOME=/home/ubuntu/.codex codex exec "Respond exactly PHASE48_NATIVE_LOGIN_OK"` using the account default and confirm a successful response.
    4. Do not paste tokens, device codes, cookies, or browser URLs into repo artifacts.
  </how-to-verify>
  <verify>
    <automated>ssh -n atius-srv-3 'sudo -iu ubuntu env CODEX_HOME=/home/ubuntu/.codex codex debug models &gt;/tmp/phase48-models.txt &amp;&amp; sudo -iu ubuntu env CODEX_HOME=/home/ubuntu/.codex codex exec "Respond exactly PHASE48_NATIVE_LOGIN_OK"'</automated>
    <human-check>Operator completes the native ChatGPT login as ubuntu and confirms one successful native prompt before execution resumes.</human-check>
  </verify>
  <resume-signal>Type "approved" only after the native ubuntu prompt succeeds and no secret material was written to disk artifacts.</resume-signal>
  <acceptance_criteria>The real ubuntu runtime is logged in, the first native prompt succeeds without 401 or token_expired, and no secret material appears in notes, logs, or repo files.</acceptance_criteria>
  <done>The real ubuntu runtime is logged in, the first native prompt succeeds without 401 or token_expired, and no secret material appears in notes, logs, or repo files.</done>
</task>

<task type="auto">
  <name>Task 3: Prove renewable native OAuth and the full local codex-acp lifecycle</name>
  <read_first>
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-CONTEXT.md
    @.planning/phases/48-codex-oauth-wayland-acp-convergence/48-VALIDATION.md
    @modules/fork-sync/projects/codex-acp/UPSTREAM-SYNC-GUARDS.md
  </read_first>
  <files>.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-NATIVE-OAUTH-PROOF.md, .planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-LOCAL-ACP-LIFECYCLE.md, .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/run-local-acp-lifecycle.mjs, .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-wave1-asvs.py</files>
  <action>After the human login gate, record only auth-file hash/owner/mode metadata, never contents. Run two independent SSH commands so each `codex exec` starts a fresh process and uses the account default model; require exact markers `PHASE48_NATIVE_LOGIN_OK` and `PHASE48_NATIVE_RESTART_OK`. Capture sanitized `codex debug models` output and verify the ACP selected model occurs in that account-aware catalog. Create `run-local-acp-lifecycle.mjs` using Node built-ins to launch `/home/ubuntu/.local/bin/codex-acp-atius` over stdio JSON-RPC and assert normalized evidence fields `initialize=true`, `session_new=true`, `prompt_completed=true`, `tool_requested=true`, `approval_requested=true`, `approval_completed=true`, `cancelled=true`, `resumed=true`, `reconnected=true`, and `shutdown_clean=true`; the tool operation must use a disposable `/tmp/phase48-acp-*` path. Create `verify-wave1-asvs.py` to require an `asvs_l1` section with V2, V3, V4, V5, and V6 set to `pass` in each of `48-01-ROUTER-EVIDENCE.md`, `48-01-NATIVE-OAUTH-PROOF.md`, and `48-01-LOCAL-ACP-LIFECYCLE.md`. Copy the ACP harness to SRV-3 `/tmp`, execute it as `ubuntu`, and store only the sanitized transcript. Fail if any lifecycle event or ASVS row is absent, a prompt works only once, Headroom appears, Router-token fallback is used, or approval is weakened.</action>
  <verify>
    <automated>ssh -n atius-srv-3 'sudo -iu ubuntu env CODEX_HOME=/home/ubuntu/.codex codex exec "Respond exactly PHASE48_NATIVE_LOGIN_OK"' &amp;&amp; ssh -n atius-srv-3 'sudo -iu ubuntu env CODEX_HOME=/home/ubuntu/.codex codex exec "Respond exactly PHASE48_NATIVE_RESTART_OK"' &amp;&amp; scp .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/run-local-acp-lifecycle.mjs atius-srv-3:/tmp/phase48-local-acp.mjs &amp;&amp; ssh -n atius-srv-3 'sudo -iu ubuntu env CODEX_HOME=/home/ubuntu/.codex node /tmp/phase48-local-acp.mjs --agent /home/ubuntu/.local/bin/codex-acp-atius --output /tmp/phase48-local-acp.json' &amp;&amp; ssh -n atius-srv-3 'python3 -c "import json; d=json.load(open(&apos;/tmp/phase48-local-acp.json&apos;)); required=(&apos;initialize&apos;,&apos;session_new&apos;,&apos;prompt_completed&apos;,&apos;tool_requested&apos;,&apos;approval_requested&apos;,&apos;approval_completed&apos;,&apos;cancelled&apos;,&apos;resumed&apos;,&apos;reconnected&apos;,&apos;shutdown_clean&apos;); assert all(d.get(k) is True for k in required)"' &amp;&amp; python .planning/phases/48-codex-oauth-wayland-acp-convergence/tools/verify-wave1-asvs.py .planning/phases/48-codex-oauth-wayland-acp-convergence</automated>
  </verify>
  <acceptance_criteria>The native prompt succeeds twice from fresh processes, the account-aware model and effort catalog are recorded without stale GPT-5.6 assumptions, and the local `codex-acp` lifecycle transcript is complete and sanitized.</acceptance_criteria>
  <done>The native prompt succeeds twice from fresh processes, the account-aware model and effort catalog are recorded without stale GPT-5.6 assumptions, and the local `codex-acp` lifecycle transcript is complete and sanitized.</done>
</task>

</tasks>

<threat_model>
**ASVS baseline:** Level 1. `48-01-ROUTER-EVIDENCE.md`, `48-01-NATIVE-OAUTH-PROOF.md`, and `48-01-LOCAL-ACP-LIFECYCLE.md` must contain a sanitized `asvs_l1` mapping for V2 Authentication, V3 Session Management, V4 Access Control, V5 Validation/Sanitization, and V6 Stored Secrets; execution fails unless every applicable row is `pass`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> native Codex login | Human OAuth renewal changes the live ubuntu auth state and must not leak tokens. |
| Router shell -> upstream Codex auth | Router evidence must classify upstream auth failures without masking them as Router success. |
| local codex-acp -> native Codex CLI | The local ACP lifecycle inherits the native auth and `danger-full-access` identity boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Spoofing | Native OAuth recovery | mitigate | Require the real `ubuntu` login with `CODEX_HOME=/home/ubuntu/.codex`; forbid Router-token fallback or alternate homes. |
| T-48-02 | Information Disclosure | `/home/ubuntu/.codex/auth.json`, evidence files | mitigate | Record only metadata and sanitized results; never paste tokens, cookies, device codes, or raw auth payloads. |
| T-48-03 | Repudiation | Native prompt proof | mitigate | Demand two successful prompt completions from fresh processes so cached-session false positives fail closed. |
| T-48-04 | Elevation of Privilege | local `codex-acp` running with `danger-full-access` | mitigate | Run only as `ubuntu`, preserve the canonical `CODEX_HOME`, and treat any identity drift as a blocker. |
| T-48-05 | Denial of Service | CPU-capped Router verification lane | mitigate | Require the existing `verify-profile` proof, then run the suite directly through `podman-admin.sh profile-run`; stop if `cpu.max=80000 100000` is not observed. |
| T-48-SC | Tampering | package-manager installs | accept | No npm, pip, cargo, or other package-manager install is in scope for this plan. |
</threat_model>

<verification>
- Router focused Go verification passes under the builds profile and its repaired invocation is written to `48-01-ROUTER-EVIDENCE.md`.
- Native `codex exec` succeeds twice as `ubuntu` from fresh processes with `CODEX_HOME=/home/ubuntu/.codex`.
- Local `codex-acp` evidence proves initialize, session/new, prompt, tool, approval, cancel, resume, and shutdown with no Headroom involvement.
- The three Wave 1 evidence artifacts map the applicable ASVS L1 V2/V3/V4/V5/V6 controls and every mapped result is `pass`.
</verification>

<success_criteria>
- WAC-01 and WAC-02 are covered by a sanitized Router evidence bundle that distinguishes Router and upstream auth domains.
- WAC-03 and WAC-04 are covered by renewable native prompt proof plus current account-aware model and effort evidence.
- WAC-05 is covered locally with a complete `codex-acp` lifecycle transcript.
- WAC-08 remains explicit because rollback notes, backups, and the human OAuth gate are documented before Wave 2 can start.
</success_criteria>

<output>
Create `.planning/phases/48-codex-oauth-wayland-acp-convergence/48-01-SUMMARY.md` when done
</output>
