---
phase: 24-production-recovery-guard-ats-horistic
plan: 04
type: execute
wave: 4
depends_on:
  - 24-01
  - 24-02
  - 24-03
files_modified:
  - modules/srv1-ops/scripts/production_guard.py
  - modules/srv1-ops/configs/production-guard.yaml
  - cli/omni/tests/test_srv1_production_guard.py
  - docs/operations/production-guard.md
  - .planning/phases/24-production-recovery-guard-ats-horistic/24-VALIDATION.md
autonomous: true
requirements:
  - PRG-08
  - PRG-09
  - PRG-10
must_haves:
  truths:
    - "Horistic Apache e remoto em horistic-srv."
    - "Remote checks sao read-only; live Apache mutation exige gate separado."
    - "Rename drift e detectado/classificado, nao auto-aplicado."
---

<objective>
Adicionar verificacao do Apache remoto do Horistic e detector de drift de
renomeio de pasta/host/repo/vhost.

Purpose: evitar repeticao do incidente onde PM2 estava correto no SRV-1 mas os
sites Horistic falhavam por Apache remoto, e detectar refs antigas antes de
quebrarem boot/proxy.
Output: checks remotos read-only, rename drift report e validacao final.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-production-recovery-guard-ats-horistic/24-CONTEXT.md
@.planning/phases/24-production-recovery-guard-ats-horistic/24-RESEARCH.md
@inventory/hosts/horistic-srv.yaml
@inventory/hosts/atius-srv-1.yaml
@docs/operations/pm2-canonical.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remote Horistic Apache checks</name>
  <files>modules/srv1-ops/scripts/production_guard.py, modules/srv1-ops/configs/production-guard.yaml, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: remote Apache active/enabled and listening 80/443 returns PASS.
    - Test 2: custom `/etc/systemd/system/apache2.service` or drop-in returns WARN/BLOCK.
    - Test 3: failed SSH returns UNKNOWN unless `--strict-remote` is set.
    - Test 4: endpoint checks use GET/HEAD only and never POST webhook routes.
    - Test 5: `apache2ctl -S`/`sites-enabled` fixtures contain expected Horistic vhosts and proxy targets.
    - Test 6: stale active vhost refs such as `horistic-srv-1` are classified separately from benign inventory history.
  </behavior>
  <action>Extend guard config and script with remote Horistic checks using inventory `access.ssh`. Check `systemctl is-enabled/is-active apache2`, `systemctl show` fragment/drop-ins, `ss -ltn` for 80/443, `apache2ctl -S`, enabled site names, relevant vhost file paths, proxy targets for `dashboard/trade/backtest/painel/api/webhook.horistic.com`, stale active references to `horistic-srv-1`, and public endpoint HTTP status. Treat remote mutation as out of scope; report exact commands for operator review only.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q</automated>
  </verify>
  <done>Production Guard can distinguish SRV-1 app health from remote Horistic proxy health.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rename drift detector</name>
  <files>modules/srv1-ops/scripts/production_guard.py, modules/srv1-ops/configs/production-guard.yaml, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: missing PM2 cwd/script path returns BLOCK.
    - Test 2: inventory `rename_from` references classify as INFO unless used by active runtime path.
    - Test 3: stale vhost/GDrive/path references are classified by severity and do not auto-rename.
    - Test 4: suggested repair is a proposal with backup/symlink/patch options, not a live move.
  </behavior>
  <action>Add rename drift scanning for PM2 cwd/script, configured repo roots, inventory `rename_from`, known vhost names, GDrive/backup paths and workspace folders. The scanner must produce findings with severity, evidence path, current target and suggested safe fix. It must not rename folders or create symlinks automatically.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q</automated>
  </verify>
  <done>Guard reports rename drift before it breaks boot/proxy, while live rename remains gated.</done>
</task>

<task type="auto">
  <name>Task 3: Final validation architecture</name>
  <files>.planning/phases/24-production-recovery-guard-ats-horistic/24-VALIDATION.md, docs/operations/production-guard.md</files>
  <action>Create or update validation architecture mapping PRG-01..PRG-10 to tests, CLI commands and live gates. Include Graphify final freshness as a non-negotiable verification. Add docs examples for healthy, warn and block outputs.</action>
  <verify>
    <automated>rg -n "PRG-01|PRG-10|Graphify|production-guard|rename|horistic-srv" .planning/phases/24-production-recovery-guard-ats-horistic/24-VALIDATION.md docs/operations/production-guard.md</automated>
  </verify>
  <done>Phase 24 has a final validation matrix and docs examples for execution.</done>
</task>

</tasks>

<verification>
Run:

- `PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q`
- `PYTHONPATH=cli python3 -m omni srv1-ops production-guard doctor --json`
- `rg -n "requests\\.post|curl .* -X POST|urllib.*POST" modules/srv1-ops/scripts/production_guard.py && exit 1 || true`
- `node "$HOME/.Codex/get-shit-done/bin/gsd-tools.cjs" graphify status`
</verification>

<success_criteria>
PRG-08..PRG-10 are complete when the guard validates remote Horistic Apache,
detects rename drift safely and documents a final validation path without
mutating production.
</success_criteria>

<output>
Create `.planning/phases/24-production-recovery-guard-ats-horistic/24-04-SUMMARY.md` when done.
</output>
