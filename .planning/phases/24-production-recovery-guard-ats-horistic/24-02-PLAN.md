---
phase: 24-production-recovery-guard-ats-horistic
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - modules/srv1-ops/scripts/production_guard.py
  - cli/omni/srv1_ops.py
  - cli/omni/tests/test_srv1_production_guard.py
autonomous: false
requirements:
  - PRG-06
must_haves:
  truths:
    - "Repair e dry-run por default."
    - "Repair apply exige scope explicito e snapshot pre-live."
    - "Nao usar pm2 kill, restart de RDP/XRDP ou restart de pm2-ubuntu.service como auto-repair."
    - "pm2 save so pode acontecer depois de estado healthy e aprovacao."
---

<objective>
Adicionar motor de repair gateado ao Production Guard.

Purpose: permitir reparo seguro de apps/servicos/containers ausentes sem repetir
intervencoes manuais arriscadas.
Output: `production-guard repair --dry-run` e `--apply --scope ...` com
snapshots, logs e bloqueios de acoes destrutivas.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-production-recovery-guard-ats-horistic/24-CONTEXT.md
@.planning/phases/24-production-recovery-guard-ats-horistic/24-RESEARCH.md
@.planning/phases/24-production-recovery-guard-ats-horistic/24-01-PLAN.md
@docs/operations/pm2-canonical.md
@modules/srv1-ops/scripts/inviolable-watchdog.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repair planner</name>
  <files>modules/srv1-ops/scripts/production_guard.py, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: missing PM2 app creates a dry-run action using canonical ecosystem and `--only`.
    - Test 2: wrong namespace creates BLOCK and refuses automatic `pm2 save`.
    - Test 3: missing known container creates exact-name `podman start` dry-run action.
    - Test 4: generated actions never contain `pm2 kill`, `systemctl restart pm2-ubuntu.service`, `systemctl restart xrdp` or raw secrets.
  </behavior>
  <action>Extend `production_guard.py` with a repair planner that consumes doctor findings and emits actions. Actions must include scope, impact, command_preview, requires_approval, pre_snapshot and rollback_hint. Default command is dry-run only. Allowed scopes: `pm2-app`, `pm2-stack`, `container`, `systemd-user`, `systemd-system-safe`. Anything else blocks.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q</automated>
  </verify>
  <done>Repair planner produces safe, auditable dry-run actions and refuses destructive commands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Gate live repair apply</name>
  <files>modules/srv1-ops/scripts/production_guard.py, cli/omni/srv1_ops.py</files>
  <what-built>Read-only doctor and dry-run repair planner for ATS/Horistic.</what-built>
  <action>Before any `repair --apply`, require explicit operator approval naming the scope and target. The executor must snapshot `pm2 jlist`, `dump.pm2`, relevant logs and systemd state first. The executor must not apply repair if a critical app is partially online and the action could duplicate it.</action>
  <human_validation_required>
    1. Confirm exact scope: `pm2-app`, `pm2-stack`, `container`, `systemd-user`, or `systemd-system-safe`.
    2. Confirm target name.
    3. Confirm snapshot path.
    4. Confirm no RDP/XRDP restart and no `pm2 kill`.
  </human_validation_required>
  <verify>
    <manual>Operator approves the exact repair scope/target after reading dry-run output.</manual>
  </verify>
  <resume-signal>Type `approved repair <scope> <target>` only after reviewing dry-run output.</resume-signal>
  <done>Live repair apply is either approved and audited, or blocked without mutation.</done>
</task>

</tasks>

<verification>
Run:

- `PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q`
- `PYTHONPATH=cli python3 -m omni srv1-ops production-guard repair --dry-run --json`
- `rg -n "pm2 kill|restart xrdp|restart pm2-ubuntu" modules/srv1-ops/scripts/production_guard.py && exit 1 || true`
</verification>

<success_criteria>
PRG-06 is complete when every repair path is dry-run-first, snapshot-first,
scope-limited, audited and unable to kill PM2 or RDP/XRDP without a human gate.
</success_criteria>

<output>
Create `.planning/phases/24-production-recovery-guard-ats-horistic/24-02-SUMMARY.md` when done.
</output>
