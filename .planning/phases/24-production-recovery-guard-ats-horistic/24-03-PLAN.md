---
phase: 24-production-recovery-guard-ats-horistic
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - modules/srv1-ops/systemd/production-guard.service
  - modules/srv1-ops/systemd/production-guard.timer
  - modules/srv1-ops/systemd/production-guard-login.service
  - docs/operations/production-guard.md
  - docs/operations/srv1-ops.md
  - modules/srv1-ops/README.md
autonomous: false
requirements:
  - PRG-07
  - PRG-10
must_haves:
  truths:
    - "Boot/login protocol pode verificar automaticamente, mas repair apply continua gateado."
    - "Units versionadas nao sao instaladas live sem checkpoint."
    - "Docs precisam declarar impacto em RDP/PM2/Apache."
---

<objective>
Versionar o protocolo de verificacao no reboot e no login/session.

Purpose: garantir que uma sessao nova ou reboot detecte cedo PM2/dump/namespace,
Apache remoto, containers e services quebrados.
Output: systemd units/timer, runbook e docs de operacao.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-production-recovery-guard-ats-horistic/24-CONTEXT.md
@.planning/phases/24-production-recovery-guard-ats-horistic/24-RESEARCH.md
@docs/operations/srv1-ops.md
@modules/srv1-ops/README.md
@modules/srv1-ops/systemd/inviolable-watchdog.timer
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add boot/login systemd units</name>
  <files>modules/srv1-ops/systemd/production-guard.service, modules/srv1-ops/systemd/production-guard.timer, modules/srv1-ops/systemd/production-guard-login.service, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: service runs status/doctor, not repair apply.
    - Test 2: timer uses boot/session friendly cadence and does not conflict with `pm2-ubuntu.service`.
    - Test 3: login service is optional and documented as read-only.
  </behavior>
  <action>Create versioned user systemd units for read-only guard execution. `production-guard.service` should run doctor/status and write JSON/log output. `production-guard.timer` should run after boot and periodically. `production-guard-login.service` should be optional for session start and must not mutate state. Do not install live in this task.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q && systemd-analyze verify --user modules/srv1-ops/systemd/production-guard.service modules/srv1-ops/systemd/production-guard.timer modules/srv1-ops/systemd/production-guard-login.service</automated>
  </verify>
  <done>Units are valid, read-only and ready for gated install.</done>
</task>

<task type="auto">
  <name>Task 2: Document operator protocol</name>
  <files>docs/operations/production-guard.md, docs/operations/srv1-ops.md, modules/srv1-ops/README.md</files>
  <action>Document startup/login verification, expected output, repair decision tree, rollback, and impact classes: no RDP impact, possible desktop flicker, PM2 app restart, Apache remote mutation. Include exact commands for status, doctor, dry-run repair and gated apply. State that webhook POST tests are forbidden unless operator explicitly approves because they can affect ATS/Telegram.</action>
  <verify>
    <automated>rg -n "production-guard|reboot|login|dry-run|repair --apply|RDP|PM2|Apache|webhook" docs/operations/production-guard.md docs/operations/srv1-ops.md modules/srv1-ops/README.md</automated>
  </verify>
  <done>Runbook gives a reproducible protocol for reboot/login checks and safe repair.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate live unit install</name>
  <files>modules/srv1-ops/systemd/production-guard.service, modules/srv1-ops/systemd/production-guard.timer, modules/srv1-ops/systemd/production-guard-login.service</files>
  <what-built>Versioned read-only boot/login guard units.</what-built>
  <action>Pause before installing/enabling units in `~/.config/systemd/user`. If approved, install only read-only units and run `systemctl --user daemon-reload`. Do not enable repair apply automation.</action>
  <human_validation_required>
    1. Confirm install of read-only `production-guard.timer`.
    2. Confirm whether optional login service should be installed.
    3. Confirm no repair apply automation.
  </human_validation_required>
  <verify>
    <manual>Operator approves live install of read-only units.</manual>
  </verify>
  <resume-signal>Type `approved install production-guard read-only` to install units; otherwise leave versioned only.</resume-signal>
  <done>Units are either installed read-only with validation, or intentionally left staged.</done>
</task>

</tasks>

<verification>
Run:

- `systemd-analyze verify --user modules/srv1-ops/systemd/production-guard.service modules/srv1-ops/systemd/production-guard.timer modules/srv1-ops/systemd/production-guard-login.service`
- `rg -n "production-guard|repair --apply|RDP|Apache|webhook" docs/operations/production-guard.md docs/operations/srv1-ops.md modules/srv1-ops/README.md`
</verification>

<success_criteria>
PRG-07 and PRG-10 are complete when boot/login verification is versioned,
documented, read-only by default and separated from repair apply automation.
</success_criteria>

<output>
Create `.planning/phases/24-production-recovery-guard-ats-horistic/24-03-SUMMARY.md` when done.
</output>
