---
phase: 23-omni-fleet-governance-cockpit-sem-landscape
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cli/omni/edge.py
  - cli/omni/tests/test_cockpit_edge.py
  - scripts/validate-cockpit-edge.py
  - docs/operations/cockpit-edge.md
autonomous: false
requirements:
  - GOV-01
must_haves:
  truths:
    - "Cockpit e console por host e break-glass, nao control plane central, per D-02."
    - "Porta 9090 publica direta fica bloqueada ou restrita a rede privada/VPN antes de GOV-01 ser declarado fechado."
    - "Cockpit exposto por dominio passa por Cloudflare Access, Apache auth/SSO ou WireGuard, per D-04."
    - "Nenhum secret de Access, Basic Auth, Cloudflare ou host aparece em repo, logs ou .planning, per D-06."
  artifacts:
    - path: "scripts/validate-cockpit-edge.py"
      provides: "Probe offline/live para listener, dominio e auth gate do Cockpit."
      exports: ["main"]
    - path: "docs/operations/cockpit-edge.md"
      provides: "Runbook de hardening, rollback e validacao de Cockpit."
    - path: "cli/omni/tests/test_cockpit_edge.py"
      provides: "Cobertura pytest do contrato Cockpit edge."
  key_links:
    - from: "scripts/validate-cockpit-edge.py"
      to: "docs/operations/cockpit-edge.md"
      via: "mesmos estados esperados: gated, access-live, wireguard-only, blocked"
      pattern: "gated|access-live|wireguard-only|blocked"
---

<objective>
Endurecer o modelo de exposicao do Cockpit sem transforma-lo em control plane.

Purpose: fechar GOV-01 preservando D-02: Cockpit continua apenas console por
host/break-glass mesmo com Landscape entrando como camada complementar de
administracao das maquinas Ubuntu.
Output: probe automatizado, cobertura pytest e runbook com gate bloqueante antes
de qualquer mudanca live de firewall, Apache, Cloudflare ou `cockpit.socket`.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-CONTEXT.md
@.planning/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/16-m005-cloudflare-access/16-SUMMARY.md
@cli/omni/edge.py
@cli/omni/tests/test_edge.py
@scripts/validate-edge-auth.py
@docs/operations/edge-auth.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Cockpit edge contract and tests</name>
  <files>cli/omni/edge.py, cli/omni/tests/test_cockpit_edge.py</files>
  <behavior>
    - Test 1: `edge_url("cockpit")` returns `https://cockpit.atius.com.br`.
    - Test 2: Cockpit edge description marks the service as `break-glass` and not `control-plane`.
    - Test 3: auth/status output redacts Basic Auth, Cloudflare and token-like values.
    - Test 4: unknown Cockpit edge mode fails closed with PT-BR user-facing error text.
  </behavior>
  <action>Extend the existing `cli/omni/edge.py` pattern instead of creating a separate auth stack. Add `cockpit` as an admin edge with explicit metadata: role `break-glass`, service port `9090`, allowed exposure modes `cloudflare-access`, `apache-auth`, `sso`, `wireguard-only`, `blocked`, and a note that it is not the central control plane per D-02. Keep all user-facing CLI strings in PT-BR. Do not add package dependencies. Do not read or print raw Cloudflare, Basic Auth or system credentials per D-06.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_cockpit_edge.py cli/omni/tests/test_edge.py -q</automated>
  </verify>
  <done>`cockpit` is a known admin edge, tests cover metadata/redaction/fail-closed behavior, and existing Phase 16 edge tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Cockpit exposure validator and runbook</name>
  <files>scripts/validate-cockpit-edge.py, docs/operations/cockpit-edge.md</files>
  <behavior>
    - Test 1: mocked `ss -tlnp` output with `0.0.0.0:9090` or `[::]:9090` returns failed direct-listener status unless an explicit firewall/private-network gate is detected.
    - Test 2: mocked HTTPS probe that returns Cloudflare Access redirect or Apache auth challenge returns `gated`.
    - Test 3: output contains no token/password/secret material and uses PT-BR status labels.
  </behavior>
  <action>Create `scripts/validate-cockpit-edge.py` as a read-only validator. It must inspect local listener state, optional Apache vhost evidence, and HTTPS response behavior using GET with a browser User-Agent, matching the Phase 16 WAF lesson. Supported expectations: `--expect gated`, `--expect access-live`, `--expect apache-auth`, `--expect wireguard-only`, `--expect blocked`. The script must fail if direct public 9090 is detected without a gate. Write `docs/operations/cockpit-edge.md` with the approved models: Cloudflare Access when Phase 16 is unblocked, Apache auth/SSO fallback, or WireGuard-only; include rollback and a clear statement that Cockpit is host console only per D-02. Document `LoginTo=false` and direct-port blocking/binding options, but do not execute live mutation from the validator.</action>
  <verify>
    <automated>python3 -m py_compile scripts/validate-cockpit-edge.py && PYTHONPATH=cli pytest cli/omni/tests/test_cockpit_edge.py -q</automated>
  </verify>
  <done>Validator compiles, tests cover listener/domain/auth states, and runbook gives exact validation and rollback paths without secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate live Cockpit hardening</name>
  <files>scripts/validate-cockpit-edge.py, docs/operations/cockpit-edge.md</files>
  <action>Pause before any live firewall, Apache, Cloudflare or `cockpit.socket` mutation. The executor may run the read-only validator and prepare exact commands, but must not apply them until the operator approves the chosen path. This preserves D-04 and D-06.</action>
  <what-built>Read-only validator, CLI metadata and runbook for Cockpit edge hardening.</what-built>
  <how-to-verify>
    1. Run `python3 scripts/validate-cockpit-edge.py --expect gated`.
    2. If it reports direct public `:9090`, choose one live path from `docs/operations/cockpit-edge.md`: Cloudflare Access after Phase 16 dashboard enablement, Apache auth/SSO, WireGuard-only, or blocked.
    3. Before any live firewall, Apache or `cockpit.socket` mutation, create/verify a config backup and confirm the exact command sequence.
    4. Re-run `python3 scripts/validate-cockpit-edge.py --expect gated`; expected outcome is no anonymous public direct Cockpit exposure.
  </how-to-verify>
  <verify>
    <automated>python3 scripts/validate-cockpit-edge.py --expect gated</automated>
  </verify>
  <done>Operator-approved live hardening path is applied or explicitly blocked, and the validator reports no anonymous public direct Cockpit exposure.</done>
  <resume-signal>Type `approved` with the chosen live hardening path, or describe the blocker. Do not mutate live Cockpit exposure without this approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser-to-Cockpit | Untrusted web traffic can reach a host admin console if edge gating is wrong. |
| validator-to-host | Read-only local probes observe listener and Apache state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-23-01-01 | Spoofing/Elevation | Cockpit login surface | mitigate | Require Access/auth/SSO/WireGuard or blocked direct port; fail validation on public `:9090`. |
| T-23-01-02 | Information Disclosure | validator output | mitigate | Redact token/password/secret-like strings and avoid printing auth headers. |
| T-23-01-03 | Tampering | live firewall/Apache/socket changes | mitigate | Blocking human checkpoint plus backup before live mutation. |
| T-23-01-SC | Tampering | package installs | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
Run:

- `PYTHONPATH=cli pytest cli/omni/tests/test_cockpit_edge.py cli/omni/tests/test_edge.py -q`
- `python3 -m py_compile scripts/validate-cockpit-edge.py`
- `python3 scripts/validate-cockpit-edge.py --expect gated`

The last command may fail before the live checkpoint if the current host is still exposed; treat that as the gate finding, not as permission to mutate without approval.
</verification>

<success_criteria>
GOV-01 is complete when Cockpit is documented and validated as a host-level break-glass console only, direct anonymous public `:9090` is blocked/restricted, and any domain exposure is behind Cloudflare Access, Apache auth/SSO or WireGuard.
</success_criteria>

<output>
Create `.planning/phases/23-omni-fleet-governance-cockpit-sem-landscape/23-01-SUMMARY.md` when done.
</output>
