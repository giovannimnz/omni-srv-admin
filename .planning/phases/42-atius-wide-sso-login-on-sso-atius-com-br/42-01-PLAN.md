---
phase: 42-atius-wide-sso-login-on-sso-atius-com-br
plan: 42-01
type: execute
wave: 0
depends_on: []
files_modified:
  - /home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_redirect_allowlist.test.js
  - /home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_oidc_bridge.test.js
  - /home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js
  - /home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_global_logout.spec.ts
  - /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh
  - /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh
autonomous: true
requirements:
  - SSO-01
  - SSO-02
  - SSO-03
  - SSO-04
  - SSO-05
  - SSO-06
must_haves:
  truths:
    - "D-01/SSO-01: Wave 0 proves `sso.atius.com.br` edge behavior can be asserted before live publication."
    - "D-02/SSO-02: Wave 0 has a Keycloak/OIDC bridge test contract before ATS consumes the provider."
    - "D-03/D-04/SSO-03: Wave 0 tests require ATS `auth-token`, refresh/logout, and DB-backed RBAC compatibility."
    - "D-05/SSO-04: redirect targets are tested against exact allowlists and bypass payloads before implementation."
    - "D-06/SSO-05: global logout has browser-level coverage for Keycloak and both legacy cookie variants."
    - "D-07/SSO-01: app-host forwarded-header smoke covers trade, painel, dashboard, backtest, strategy, and admin."
    - "D-08/SSO-06: tests/scripts fail closed without credentials and do not print token, cookie, password, or client-secret values."
  artifacts:
    - "/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_redirect_allowlist.test.js"
    - "/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_oidc_bridge.test.js"
    - "/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js"
    - "/home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_global_logout.spec.ts"
    - "/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh"
    - "/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh"
  key_links:
    - "Graphify status/query -> file routing before Wave 0 edits"
    - "Redirect allowlist test matrix -> ATS SSO redirect utility in Plan 42-02"
    - "OIDC bridge test contract -> ATS backend SSO route in Plan 42-02"
    - "Global logout E2E -> SSO logout route and edge publication in Plans 42-02/42-03"
    - "Edge smoke script -> Apache/Cloudflare/app-host header gates in Plan 42-03"
    - "Secret hygiene scan -> all Phase 42 source/docs/evidence before publication"
---

<objective>
Create the Wave 0 validation and secret-hygiene foundation for Atius-wide SSO.

Purpose: Make Phase 42 executable without relying on ambiguous discovery, live credentials, raw curl checks, or implicit Apache forwarded-header behavior.
Output: Tests and smoke scripts that fail closed, assert expected status/headers, and cover SSO-01 through SSO-06 before ATS implementation or live edge publication.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-CONTEXT.md
@.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-RESEARCH.md
@.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-PATTERNS.md
@.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md
@/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js
@/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_auth_rbac_api.runtime.test.js
@/home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_regression.spec.ts
@/home/ubuntu/GitHub/Atius-Capital/ats/playwright.config.js
@/home/ubuntu/GitHub/Atius-Capital/ats/package.json
</context>

## Artifacts This Phase Produces

- `/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_redirect_allowlist.test.js`: no-open-redirect matrix for allowed Atius app hosts and bypass payloads.
- `/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_oidc_bridge.test.js`: mocked Keycloak auth-code bridge, ATS user mapping, `auth-token` issuance, and RBAC preservation tests.
- `/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js`: env-only live runtime smoke with no default credentials and fail-closed missing-env behavior.
- `/home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_global_logout.spec.ts`: browser global logout coverage for Keycloak handoff, `.atius.com.br` cookie cleanup, no-domain cleanup, and no auto-login loop.
- `/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh`: dry-run/local smoke with script-level assertions for HTTP status, `Location`, `Set-Cookie`, discovery JSON, app-host redirects, and forwarded-header expectations.
- `/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh`: denylist scanner that reports path/category only and never prints raw secret values.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 auth, redirect, and logout tests</name>
  <files>/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_redirect_allowlist.test.js, /home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_oidc_bridge.test.js, /home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js, /home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_global_logout.spec.ts</files>
  <read_first>
    - `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md`
    - `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-PATTERNS.md`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_sso_auth_endpoints.runtime.test.js`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/tests/backend/auth/test_auth_rbac_api.runtime.test.js`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/tests/frontend/e2e/test_sso_regression.spec.ts`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/jest.backend.config.js`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/jest.backend.runtime.config.js`
    - `/home/ubuntu/GitHub/Atius-Capital/ats/playwright.config.js`
  </read_first>
  <behavior>
    - SSO-04/D-05: accepted targets are exactly `https://trade.atius.com.br/`, `https://painel.atius.com.br/`, `https://dashboard.atius.com.br/`, `https://backtest.atius.com.br/`, `https://strategy.atius.com.br/`, and `https://admin.atius.com.br/` plus approved path prefixes.
    - SSO-04/D-05: rejected targets include external hosts, protocol-relative URLs, userinfo URLs, non-HTTPS production URLs, encoded-host confusion, suffix confusion, malformed URLs, unknown Atius subdomains, and unexpected paths.
    - SSO-02/SSO-03/D-02/D-03/D-04: mocked OIDC callback exchanges a code, maps Keycloak identity to exactly one active ATS `"user"` row, issues only the existing `auth-token` cookie shape, and proves `permissions.js` remains authoritative.
    - SSO-05/D-06: logout sends cookie-clearing assertions for `.atius.com.br` and no-domain `auth-token`, redirects through Keycloak end-session, and does not auto-login loop.
    - SSO-06/D-08: live runtime tests read credentials only from environment variables and abort with a non-secret missing-env message.
  </behavior>
  <action>Before choosing files, run Graphify status and a task-specific query from `/home/ubuntu/GitHub/omni-srv-admin`; if the graph is stale, refresh it before relying on route hints. Create the redirect allowlist, OIDC bridge, and global logout tests first so the ATS implementation in Plan 42-02 has executable contracts. Harden the existing runtime SSO auth test by removing live credential fallback values and replacing them with required env var checks. Do not introduce package installs, committed fixtures with credentials, logged cookie values, or test output that prints raw authorization material.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify query "Phase 42 Wave 0 SSO redirect OIDC logout validation"</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && npx jest --config jest.backend.config.js tests/backend/auth/test_sso_redirect_allowlist.test.js --runInBand</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && npx jest --config jest.backend.config.js tests/backend/auth/test_sso_oidc_bridge.test.js --runInBand</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && set +e; env -u SSO_TEST_EMAIL -u ADMIN_TEST_EMAIL -u SSO_TEST_PASSWORD -u ADMIN_TEST_PASSWORD RUN_LIVE_API_TESTS=1 npx jest --config jest.backend.runtime.config.js tests/backend/auth/test_sso_auth_endpoints.runtime.test.js --runInBand >/tmp/phase42-runtime-missing-env.log 2>&1; status=$?; set -e; test "$status" -ne 0 && rg -n "SSO_TEST_EMAIL|SSO_TEST_PASSWORD|ADMIN_TEST_EMAIL|ADMIN_TEST_PASSWORD|missing|required" /tmp/phase42-runtime-missing-env.log</automated>
    <automated>cd /home/ubuntu/GitHub/Atius-Capital/ats && npx playwright test tests/frontend/e2e/test_sso_global_logout.spec.ts --project=chromium --workers=1</automated>
  </verify>
  <acceptance_criteria>
    - `test_sso_auth_endpoints.runtime.test.js` contains no live credential fallback values and the missing-env verify command fails closed without contacting live auth endpoints.
    - Redirect tests include all six current ATS app hosts and the bypass matrix from `42-VALIDATION.md`.
    - OIDC bridge tests require an active ATS user mapping and verify that Keycloak claims do not grant ATS permissions directly.
    - Logout E2E asserts both cookie variants and Keycloak logout handoff without printing cookie values.
    - Test failures before Plan 42-02 are limited to expected missing production symbols, not syntax errors or missing test dependencies.
  </acceptance_criteria>
  <done>Wave 0 auth/redirect/logout tests exist, execute under the existing ATS test tooling, and encode D-02 through D-06 plus SSO-02 through SSO-05 before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add secret hygiene and edge/header smoke scripts</name>
  <files>/home/ubuntu/GitHub/omni-srv-admin/scripts/sso-edge-smoke.sh, /home/ubuntu/GitHub/omni-srv-admin/scripts/sso-secret-hygiene-scan.sh</files>
  <read_first>
    - `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-VALIDATION.md`
    - `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-RESEARCH.md`
    - `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-PATTERNS.md`
    - `.planning/codebase/CONCERNS.md`
    - `/etc/apache2/sites-available/auth.atius.com.br.conf`
    - `/etc/apache2/sites-enabled/painel.atius.com.br.conf`
  </read_first>
  <action>Create `scripts/sso-secret-hygiene-scan.sh` with denylist checks for token, cookie, password, client-secret, JWT-secret, private-key, and bearer material across Phase 42 planning artifacts, ATS SSO files, tests, scripts, docs, and generated evidence. The script must report path/category and line metadata without echoing raw matched values. Create `scripts/sso-edge-smoke.sh` with `--dry-run`, `--local`, and `--assert-app-hosts` modes. All curl calls must use `--fail-with-body` for expected 2xx responses or explicit script-level HTTP status comparisons for 3xx responses. The app-host smoke must cover `trade.atius.com.br`, `painel.atius.com.br`, `dashboard.atius.com.br`, `backtest.atius.com.br`, `strategy.atius.com.br`, and `admin.atius.com.br`, asserting that unauthenticated redirects point to `https://sso.atius.com.br/login` with a normalized `return_to` for the original app host. The script must also assert expected headers and avoid treating raw curl output as success.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/sso-secret-hygiene-scan.sh</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && bash scripts/sso-edge-smoke.sh --dry-run --assert-app-hosts trade.atius.com.br,painel.atius.com.br,dashboard.atius.com.br,backtest.atius.com.br,strategy.atius.com.br,admin.atius.com.br</automated>
    <automated>cd /home/ubuntu/GitHub/omni-srv-admin && node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status</automated>
  </verify>
  <acceptance_criteria>
    - Secret scan emits no raw credential, token, cookie, JWT, password, private key, or client secret value.
    - Edge smoke has explicit status assertions and header/redirect assertions; a raw successful curl invocation alone cannot pass the script.
    - D-07 is covered for all six ATS app hosts by script-level assertions, not by a generic note.
    - Dry-run mode prints planned checks and rollback prerequisites without changing Apache, Cloudflare, DNS, Keycloak, PM2, or ATS runtime state.
  </acceptance_criteria>
  <done>Secret hygiene and edge/header smoke scripts are ready for Plans 42-02 and 42-03, and Wave 0 remains autonomous because it writes only tests/scripts.</done>
</task>

</tasks>

## Source Coverage For This Plan

| Source | ID | Coverage |
|--------|----|----------|
| GOAL | Phase 42 | Covers the validation precondition for the canonical SSO host and ATS reference migration. |
| REQ | SSO-01 | Edge smoke script asserts `sso.atius.com.br` local/publication prerequisites. |
| REQ | SSO-02 | OIDC bridge tests and Keycloak discovery smoke contract. |
| REQ | SSO-03 | ATS `auth-token` and RBAC compatibility tests. |
| REQ | SSO-04 | Redirect allowlist and bypass matrix. |
| REQ | SSO-05 | Global logout E2E contract. |
| REQ | SSO-06 | Secret hygiene scanner and env-only live tests. |
| RESEARCH | Open Questions (RESOLVED) | Consumes resolved decisions for ATS-hosted facade, Keycloak client checkpoint, read-only mapping audit, and manual Cloudflare gate. |
| CONTEXT | D-01..D-08 | Each locked decision has a Wave 0 test or script contract; deferred non-ATS migrations, Horistic cross-domain SSO, OS domain login, and secret creation are not planned here. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/test runner -> SSO routes | Untrusted redirect, callback, logout, cookie, and state data are modeled in tests. |
| Test scripts -> logs/Git/.planning | Evidence can accidentally persist secrets if scans or tests print raw values. |
| Edge smoke -> Apache/Cloudflare assumptions | Header and redirect behavior must be asserted before live publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01-01 | Spoofing / Information Disclosure | `test_sso_redirect_allowlist.test.js` | high | mitigate | Build no-open-redirect matrix for exact scheme, host, path, and bypass payloads per D-05. |
| T-42-01-02 | Elevation of Privilege | `test_sso_oidc_bridge.test.js` | high | mitigate | Require ATS active-user lookup and `permissions.js` RBAC checks; Keycloak claims alone do not grant access per D-04. |
| T-42-01-03 | Tampering / Spoofing | `sso-edge-smoke.sh` | high | mitigate | Assert app-host redirect behavior for trade, painel, dashboard, backtest, strategy, and admin per D-07. |
| T-42-01-04 | Information Disclosure | `sso-secret-hygiene-scan.sh`, runtime tests | high | mitigate | Remove credential fallbacks, fail closed on missing env, and redact findings per D-08. |
| T-42-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package install is planned; existing ATS lockfile tooling is used. If an executor adds an install, stop and run package legitimacy gate first. |
</threat_model>

<verification>
Plan 42-01 is complete when the focused Jest/Playwright tests and both scripts exist, run without syntax errors, and encode all required failures before any implementation or live infra change. Final Graphify status must be checked after script/test edits; if stale or commit-stale, refresh using the repo Graphify sync path before summarizing.
</verification>

<success_criteria>
- Wave 0 validation covers redirect allowlist, OIDC bridge/RBAC, global logout, app-host forwarded-header behavior, edge status/header assertions, and secret hygiene.
- No code path in this plan mutates live DNS, Cloudflare, Apache reload state, Keycloak clients, PM2, ATS production runtime, or secrets.
- All six requirement IDs SSO-01 through SSO-06 are represented by executable validation artifacts.
</success_criteria>

<output>
Create `.planning/phases/42-atius-wide-sso-login-on-sso-atius-com-br/42-01-SUMMARY.md` when done.
</output>
