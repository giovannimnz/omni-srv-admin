---
phase: 25-production-guard-repair-engine
plan: 01
type: execute
wave: 1
depends_on:
  - 24-01
files_modified:
  - modules/srv1-ops/scripts/production_guard.py
  - cli/omni/srv1_ops.py
  - cli/omni/tests/test_srv1_production_guard.py
  - docs/operations/production-guard.md
autonomous: false
context_budget_target: "75k-95k tokens"
execution_model_target: "gpt-5.3-codex-spark"
requirements:
  - PRG-06
  - PRG-10
must_haves:
  truths:
    - "Dry-run e default."
    - "Apply exige scope, target, explicit production-risk flag and snapshot."
    - "Forbidden commands are tested and scanned."
    - "Audit output is redacted."
  artifacts:
    - path: "modules/srv1-ops/scripts/production_guard.py"
      provides: "repair planner, apply gate, snapshot/audit flow."
    - path: "cli/omni/tests/test_srv1_production_guard.py"
      provides: "tests de repair, apply gate, audit redaction e forbidden actions."
    - path: "docs/operations/production-guard.md"
      provides: "runbook de dry-run, apply, abort criteria e rollback."
---

<objective>
Implementar o repair planner e o checkpoint de apply do Production Guard.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-production-guard-repair-engine/25-CONTEXT.md
@.planning/phases/25-production-guard-repair-engine/25-RESEARCH.md
@.planning/phases/25-production-guard-repair-engine/25-VALIDATION.md
@modules/srv1-ops/scripts/production_guard.py
@cli/omni/srv1_ops.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dry-run repair planner</name>
  <files>modules/srv1-ops/scripts/production_guard.py, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: dry-run produces repair candidates without executing commands.
    - Test 2: critical Phase 24 blocks prevent apply candidates.
    - Test 3: namespace mismatch blocks `pm2 save` and app start.
    - Test 4: command previews redact secrets.
  </behavior>
  <action>Add a repair planner that maps known findings to safe candidate actions. Include reason, risk, side_effect, command_preview, rollback_hint and blocked_reason. Never execute commands in dry-run.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "repair and dry"</automated>
  </verify>
  <done>`production-guard repair --dry-run --json` works without mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit apply checkpoint</name>
  <files>modules/srv1-ops/scripts/production_guard.py, cli/omni/srv1_ops.py, docs/operations/production-guard.md, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: apply without `--scope` and `--target` fails.
    - Test 2: apply without explicit production-risk flag fails.
    - Test 3: apply creates/records snapshot before command execution.
    - Test 4: forbidden commands are rejected: `pm2 kill`, RDP/XRDP restart, Apache mutation, webhook POST.
    - Test 5: audit log is machine-readable and redacted.
  </behavior>
  <action>Wire CLI flags for guarded apply. Keep the default path dry-run. Implement an allowlist for exact safe actions only. Document impact, rollback and abort criteria in `docs/operations/production-guard.md`.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "repair or audit or forbidden"</automated>
  </verify>
  <done>Apply cannot run accidentally and every applied action is auditable.</done>
</task>

</tasks>

<verification>
Run in this order:

1. `python3 -m py_compile modules/srv1-ops/scripts/production_guard.py`
2. `PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "repair or audit or forbidden"`
3. `! rg -n "pm2 kill|systemctl (restart|stop) xrdp|xrdp-sesman|curl .*POST|requests\\.post|urllib.*POST" modules/srv1-ops/scripts/production_guard.py cli/omni/srv1_ops.py cli/omni/tests/test_srv1_production_guard.py`
4. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard repair --dry-run --json`
5. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard status --json`
6. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard doctor --json`
7. `node "$HOME/.Codex/get-shit-done/bin/gsd-tools.cjs" graphify status`
8. `$gsd-verify-work 25`
</verification>

<success_criteria>
PRG-06 and PRG-10 are complete when repair is safe-by-default, dry-run-first,
snapshot-first, explicitly gated for apply, and auditable without secrets.
</success_criteria>

<output>
Create `.planning/phases/25-production-guard-repair-engine/25-01-SUMMARY.md`.
</output>
