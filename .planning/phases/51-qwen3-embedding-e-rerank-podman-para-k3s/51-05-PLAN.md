---
phase: 51-qwen3-embedding-e-rerank-podman-para-k3s
plan: 05
type: execute
wave: 4
depends_on:
  - 51-03
  - 51-04
files_modified:
  - scripts/embeddings-bench/qdrant-qwen-canary.py
  - scripts/embeddings-bench/tests/test_qdrant_qwen_canary.py
  - scripts/embeddings-bench/Containerfile.qdrant-qwen-canary
  - k8s/qwen-canary/qdrant-seed-jobs.yaml
  - k8s/qwen-canary/kustomization.yaml
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-ALIAS-EXPORT.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-REINDEX.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-TOOL-IMAGE.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-OVERLAP.json
autonomous: false
requirements: [D-14, D-15, D-16, D-17, D-23, D-24]
must_haves:
  truths:
    - "Qwen vectors live only in three named 1024d/Cosine collections with an immutable embedding signature."
    - "Equivalent source corpus, chunking, and logical IDs make GTE/Qwen paired comparison valid without padding or mixing dimensions."
    - "Seed, dual-index, full reindex, alias export/switch, and rollback are idempotent and atomic."
    - "Incumbent GTE collections and aliases remain available and unchanged."
    - "Controlled new-document ingestion for GBrain, Obsidian and Graphify uses one resumable source-checkpointed dual-index path without replacing incumbent writers."
    - "Every Qdrant Job runs the exact script from a digest-pinned tooling image and at most one 500m Job is active."
    - "The named dual-index CronJob supports checkpointed suspend/resume and exposes source high-water marks so Plan 08 can serialize the soak Job and Plan 09 can replay without lost or duplicate writes."
  artifacts:
    - path: "scripts/embeddings-bench/qdrant-qwen-canary.py"
      provides: "Idempotent Qdrant preflight, seed, reindex, verify, export, switch, and rollback operations"
      contains: "gbrain_qwen3_1024_v1"
    - path: "k8s/qwen-canary/qdrant-seed-jobs.yaml"
      provides: "Bounded one-shot reindex Jobs and one sequential controlled dual-index CronJob"
      contains: "concurrencyPolicy: Forbid"
    - path: "scripts/embeddings-bench/Containerfile.qdrant-qwen-canary"
      provides: "Digest-pinned stdlib tooling image containing the exact Qdrant canary script"
      contains: "qdrant-qwen-canary.py"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-ALIAS-EXPORT.json"
      provides: "Pre-mutation alias targets and atomic rollback payload"
      contains: "rollback_operations"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-REINDEX.json"
      provides: "Per-corpus counts, signatures, ID equivalence, and vector validation"
      contains: "embedding_signature"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-TOOL-IMAGE.json"
      provides: "ARM64 image digest, script hash and rendered Job provenance"
      contains: "image_digest"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-OVERLAP.json"
      provides: "Per-file ownership classification for shared untracked scripts and qwen-canary manifests"
      contains: "classification"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json"
      provides: "Plan 05 immutable lineage and current topology/pin/alias readback"
      contains: "original_wave0_hash"
  key_links:
    - from: "scripts/embeddings-bench/qdrant-qwen-canary.py"
      to: "qwen-canary embedding service"
      via: "pinned alias and signature-checked 1024d embeddings"
      pattern: "embedding-qwen3-0.6b-int8-1024-v1"
    - from: "k8s/qwen-canary/qdrant-seed-jobs.yaml"
      to: "scripts/embeddings-bench/qdrant-qwen-canary.py"
      via: "digest-pinned tooling image with reindex and dual-index subcommands"
      pattern: "dual-index"
---

<objective>
Create isolated Qdrant 1024d/Cosine collections and a reversible, idempotent dual-index/reindex workflow with atomic alias export, switch, and rollback.

Purpose: Implement D-14, D-15, D-16, D-17, D-23, and D-24 without mixing or padding GTE 768d vectors and without promoting titular aliases.
Output: Tested Qdrant tooling and Jobs, an approved live creation/reindex, and complete rollback evidence.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-CONTEXT.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-RESEARCH.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-PATTERNS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-VALIDATION.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-01-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-03-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dimension-safe Qdrant migration and rollback harness</name>
  <files>scripts/embeddings-bench/qdrant-qwen-canary.py, scripts/embeddings-bench/tests/test_qdrant_qwen_canary.py, scripts/embeddings-bench/Containerfile.qdrant-qwen-canary, k8s/qwen-canary/qdrant-seed-jobs.yaml, k8s/qwen-canary/kustomization.yaml, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-TOOL-IMAGE.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-OVERLAP.json</files>
  <read_first>51-WAVE0-GATE.json, 51-W0-INVENTORY.json Qdrant and source-ingestion sections, current corpus source/chunking/index contracts for GBrain/Obsidian/Graphify, 51-K3S-ROLLOUT.json quota/network branch, 51-CONTEXT.md D-14 through D-17 and D-23/D-24, 51-RESEARCH.md Qdrant patterns</read_first>
  <behavior>
    - Creation accepts only gbrain_qwen3_1024_v1, obsidian_qwen3_1024_v1, and graphify_qwen3_1024_v1 with size=1024 and distance=Cosine.
    - Every point carries a versioned embedding signature with model, immutable revision/artifact hash, pooling=mean, dimensions=1024, corpus, chunker version, and logical ID.
    - A 768d, padded, truncated, wrong-signature, duplicate-conflict, or cross-corpus vector is rejected before Qdrant write.
    - Repeated seed/reindex converges to the same point IDs/counts/checksums and resumes safely from bounded checkpoints.
    - `dual-index` consumes source-specific committed event IDs/high-water marks for gbrain, obsidian and graphify, retries idempotently, and advances a checkpoint only after incumbent-write evidence and Qwen signature verification.
    - The single controller processes the three sources sequentially under one lease; a retry never duplicates or skips a logical document.
    - Suspending the dual-index CronJob after its current Job finishes preserves per-source high-water marks; resuming and replaying from those marks yields no lost or duplicate writes.
    - Alias export emits exact current targets and one atomic rollback operation; switch refuses titular aliases and incomplete reindexes.
    - Rendered Jobs reference one immutable ARM64 image digest whose labels/script hash match `51-QDRANT-TOOL-IMAGE.json`; no host-path or implicit local script is assumed.
  </behavior>
  <action>Generate `51-05-GATE-READBACK.json`, require lineage PASS, then record hashes/status/diffs and classify every target in the shared currently untracked `scripts/embeddings-bench/` and `k8s/qwen-canary/` scopes in `51-05-OVERLAP.json`; block on unclassified overlap and stage only listed files. Implement a standard-library HTTP CLI with `preflight`, `export-aliases`, `create`, `seed`, `dual-index`, `reindex`, `verify`, `switch-canary-aliases`, `rollback`, `verify-rollback`, and `dry-run`. `dual-index` must expose exact adapters `--source gbrain|obsidian|graphify`, require a committed source event ID/high-water mark and incumbent GTE write receipt, derive deterministic logical/point IDs, validate immutable embedding signatures, retry with bounded exponential backoff, persist atomic per-source checkpoints, and acknowledge only after Qwen readback; one sequential controller invocation processes all three sources and never replaces or edits incumbent GTE writers. Use deterministic IDs for full reindex, validate every vector/signature before writes, and require source parity before aliases. Build `Containerfile.qdrant-qwen-canary` from an immutable ARM64 Python base digest selected by Wave 0, copy the exact script, use no undeclared package install, label the script SHA-256, and build through the owner-host `builds` profile. Record image digest/platform/script hash in `51-QDRANT-TOOL-IMAGE.json`. Define bounded 500m reindex Jobs with `parallelism=1`, deadlines/backoff and one named dual-index CronJob with `concurrencyPolicy: Forbid`, `suspend: false`, source order GBrain→Obsidian→Graphify, one active pod maximum, Vault-hydrated Secret references, atomic per-source high-water checkpoints, and digest-only image reference. Make suspend/readback/resume/replay commands explicit and idempotent: suspension waits for the current owned Job to terminate before high-water capture; replay starts from the recorded exclusive high-water marks and verifies incumbent/Qwen receipts and duplicate-free logical IDs before advancing. Add rendered tests proving digest/script provenance, exact commands/dependencies, one-Job quota compatibility, all three adapters, retries/checkpoints/signatures, suspend-with-no-active-Job, resume/replay without loss/duplicates, alias atomicity, rollback and incumbent-writer/alias preservation. Keep Jobs/CronJob out of live kustomization until checkpoint approval.</action>
  <acceptance_criteria>Tests prove exact collection schemas, signature/stable-ID enforcement, source-specific controlled dual-index with retries/checkpoints, idempotent full reindex, digest-pinned Job delivery, one active 500m Job, atomic canary alias switch/rollback, and unchanged incumbent GTE writers/aliases.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-05 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json --require PASS &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_qdrant_qwen_canary.py -v &amp;&amp; python3 scripts/embeddings-bench/qdrant-qwen-canary.py dry-run --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --all-corpora --include-dual-index</automated>
  </verify>
  <done>The migration harness can safely create, populate, verify, switch canary aliases, and roll back without dimensional or corpus contamination.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authorize isolated collection creation and full Qwen reindex</name>
  <files>.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-ALIAS-EXPORT.json, k8s/qwen-canary/qdrant-seed-jobs.yaml</files>
  <read_first>51-WAVE0-GATE.json, 51-05-OVERLAP.json, Dry-run output, live Qdrant version/auth/capacity/snapshot evidence, tooling image digest/script hash, exact pre-mutation collection/alias export, source high-water marks, dual-index checkpoint plan, planned point counts/checksums, rollback operations</read_first>
  <action>Refresh `51-05-GATE-READBACK.json`, require lineage PASS and overlap/image hashes, run read-only preflight and alias export, confirm a current restorable Qdrant snapshot, prove capacity for three 1024d copies plus headroom, and display exact collection schemas, expected corpus counts, GBrain/Obsidian/Graphify high-water marks and incumbent receipts, single-controller dual-index commands/retry/checkpoint/suspend/resume rules, digest-pinned Job image, selected canary aliases, secret references, and rollback transaction. Require explicit approval before creating collections, writing points, or enabling controlled dual-index. Titular/GTE aliases and incumbent writers remain out of scope.</action>
  <acceptance_criteria>The export is complete and redacted; snapshot restore readiness and capacity pass; all target collections are new or already signature-compatible; the proposed alias set contains canary aliases only.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-05 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qdrant-qwen-canary.py preflight --read-only --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --tool-image-evidence .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-TOOL-IMAGE.json --export-aliases .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-ALIAS-EXPORT.json</automated>
    <human-check>Approve only the three exact 1024d/Cosine collections, source high-water marks, controlled dual-index wiring, digest-pinned image, capacity/snapshot evidence, canary-only aliases, and atomic rollback payload.</human-check>
  </verify>
  <done>Resume only with `approved qdrant qwen reindex`; otherwise stop with no Qdrant mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Create, fully reindex, verify, and expose canary-only aliases</name>
  <files>k8s/qwen-canary/kustomization.yaml, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-REINDEX.json</files>
  <read_first>51-WAVE0-GATE.json, Approved alias export, validated digest-pinned Jobs, exact embedding signature, current source snapshots/high-water marks and incumbent GTE receipts, current Qdrant aliases immediately before mutation</read_first>
  <action>After approval, refresh `51-05-GATE-READBACK.json`, require lineage PASS, and revalidate overlap/image/alias-export hashes. Create or verify the three physical collections, run one bounded 500m digest-pinned reindex Job at a time for equivalent GBrain, Obsidian and Graphify snapshots, and resume from atomic checkpoints after transient failures. Capture each source high-water mark, complete full reindex, then enable the single sequential dual-index CronJob to replay committed post-snapshot events and continue controlled new-document ingestion with per-source checkpoints. Verify every signature, vector length, logical ID/chunker equivalence, count/checksum parity and absence of 768d/padded vectors. Execute one atomic canary-only alias operation only after full reindex plus high-water replay passes. Export `51-QDRANT-REINDEX.json` with the exact CronJob identity, suspension/readback/resume/replay protocol, snapshots, high-water marks, dual-index checkpoints/retries, image/script digest, signatures, parity, timings/storage, aliases before/after and rollback. Re-read incumbent writers, collections and aliases; suspend dual-index and execute rollback immediately if any incumbent object changed.</action>
  <acceptance_criteria>All three 1024d/Cosine collections have source parity and valid signatures; controlled new documents flow idempotently through source-specific checkpoints; only canary aliases point to Qwen; incumbent GTE writers/collections/aliases are unchanged; replay produces no conflicts.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-05 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qdrant-qwen-canary.py verify --evidence .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-REINDEX.json --require-idempotent-replay --require-dual-index-sources gbrain,obsidian,graphify --require-incumbent-unchanged</automated>
  </verify>
  <done>Isolated, equivalent, reversible Qwen indexes are ready for paired functional and quality evaluation, with no titular promotion.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/embeddings-bench/qdrant-qwen-canary.py`: `preflight`, `export-aliases`, `create`, `seed`, `dual-index`, `reindex`, `verify`, `switch-canary-aliases`, `rollback`, `verify-rollback`, `dry-run`; source checkpoints, embedding-signature and dimension/corpus guards.
- `scripts/embeddings-bench/tests/test_qdrant_qwen_canary.py`: idempotency, partial recovery, dimensional isolation, alias atomicity, and rollback tests.
- `scripts/embeddings-bench/Containerfile.qdrant-qwen-canary`: immutable ARM64 stdlib tooling image.
- `k8s/qwen-canary/qdrant-seed-jobs.yaml`: sequential bounded 500m reindex Jobs and controlled dual-index CronJob.
- `k8s/qwen-canary/kustomization.yaml`: approved inclusion of completed one-shot Jobs only.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-ALIAS-EXPORT.json`: before targets and atomic rollback operation.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-REINDEX.json`: source/signature/count/checksum/storage/alias evidence.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-QDRANT-TOOL-IMAGE.json`: image digest/platform/script hash and rendered provenance.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-GATE-READBACK.json`: immutable lineage plus current topology/pin/alias proof.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-OVERLAP.json`: per-file overlap ownership and staging evidence.
- Qdrant collections: `gbrain_qwen3_1024_v1`, `obsidian_qwen3_1024_v1`, `graphify_qwen3_1024_v1`.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Corpus sources to reindex Jobs | Source chunks and logical IDs cross into the Qwen indexing pipeline. |
| Reindex Jobs to Qdrant | Authenticated bulk writes and alias operations mutate live vector state. |
| GTE and Qwen collections | Dimension/model/corpus boundaries must remain physically and logically separate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-51-05-01 | Tampering | Vector dimensions/signatures | critical | mitigate | Exact 1024 length, model revision/hash, mean pooling, corpus/chunker/logical-ID signature on every write. | Unit tests and full verification scan. | Any missing/mismatched signature or non-1024 vector. |
| T-51-05-02 | Tampering | Corpus equivalence | high | mitigate | Frozen source snapshots, deterministic IDs/chunks, count/checksum parity against GTE source. | Reindex evidence parity gates. | Corpus, chunk, or logical-ID mismatch. |
| T-51-05-03 | Denial of Service | Bulk reindex | high | mitigate | Capacity preflight, snapshot, bounded batches/retries/deadline, 500m Jobs, resumable checkpoints. | Job and Qdrant telemetry. | Capacity headroom fails, OOM/restarts recur, or incumbent latency breaches baseline guard. |
| T-51-05-04 | Information Disclosure | Raw corpus and Qdrant auth | high | mitigate | Secrets by reference, no raw text in logs/evidence, hashes/counts only. | Artifact/log scan. | Secret or raw document data is persisted. |
| T-51-05-05 | Tampering | Alias switch/promotion | critical | mitigate | Atomic canary-only allowlist, full-reindex prerequisite, exported rollback, explicit rejection of titular aliases. | Tests and before/after alias diff. | Incumbent alias is targeted or reindex is incomplete. |
| T-51-05-SC | Tampering | Python dependencies | medium | mitigate | Reuse installed project dependencies or standard library; no unaudited install. | Dependency diff check. | New unaudited package is required. |
</threat_model>

<verification>
Run focused tests and read-only dry-run. After the checkpoint, require complete signature/count/checksum verification, idempotent replay, canary-only atomic aliases, and unchanged incumbent GTE collections/aliases.
</verification>

<success_criteria>
- Three exact 1024d/Cosine physical collections exist and are complete.
- Every Qwen vector has a valid immutable signature and equivalent logical source/chunk.
- No 768d vector is mixed, padded, or truncated.
- Seed/reindex and rollback are idempotent and atomic.
- No titular alias is promoted.
</success_criteria>

<output>
Create `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-SUMMARY.md` when done.
</output>
