---
phase: 51-qwen3-embedding-e-rerank-podman-para-k3s
plan: 06
type: tdd
wave: 5
depends_on:
  - 51-03
  - 51-04
  - 51-05
files_modified:
  - scripts/embeddings-bench/qwen-canary-smoke.py
  - scripts/embeddings-bench/tests/test_qwen_canary_smoke.py
  - scripts/reranker-smoke.py
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-FUNCTIONAL-SMOKE.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-OVERLAP.json
autonomous: true
requirements: [D-07, D-08, D-18, D-20]
must_haves:
  truths:
    - "Embedding health, batches 1 and 4, 1024 dimensions, normalization, and cosine reproducibility all pass."
    - "Native private and public router reranking produce consistent ordering and stable error semantics."
    - "Three concurrent full cycles prove two admitted slots and one waiter, including timeout, cancellation, and TTL release."
    - "Private services reject unapproved reachability and incumbent GTE aliases/indexes remain isolated."
    - "Functional traffic starts only after Plan 04 proves the exact committed router source is active live with matching SHA, replicas, version and aliases."
    - "Every functional task refreshes Plan 06 lineage against immutable Wave 0 hashes and current topology/pins/aliases without modifying sealed artifacts."
  artifacts:
    - path: "scripts/embeddings-bench/qwen-canary-smoke.py"
      provides: "End-to-end functional, concurrency, network, and isolation smoke runner"
      contains: "cosine_threshold"
    - path: "scripts/reranker-smoke.py"
      provides: "Native/public rerank parity and error smoke"
      contains: "/v1/rerank"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-FUNCTIONAL-SMOKE.json"
      provides: "Redacted gate-by-gate functional evidence"
      contains: "three_cycle_admission"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-OVERLAP.json"
      provides: "Per-file ownership for currently untracked smoke scripts"
      contains: "classification"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json"
      provides: "Plan 06 immutable lineage and live router/topology/pin/alias readback"
      contains: "current_aliases"
  key_links:
    - from: "scripts/embeddings-bench/qwen-canary-smoke.py"
      to: "owner-host router pipeline governor"
      via: "three concurrent pipeline_id cycles"
      pattern: "pipeline_id"
    - from: "scripts/reranker-smoke.py"
      to: "private /rerank and public /v1/rerank"
      via: "same frozen request and ordering comparison"
      pattern: "rerank"
    - from: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json"
      to: "scripts/embeddings-bench/qwen-canary-smoke.py"
      via: "live_activation.status=PASS and active_router_sha=source_commit.after_sha"
      pattern: "live_activation"
---

<objective>
Implement and run deterministic functional/integration smokes for embeddings, reranking, two-slot cycles, failure release, private reachability, and GTE isolation.

Purpose: Satisfy D-07, D-08, D-18, and D-20 before expensive quality/capacity evaluation.
Output: Tested smoke tooling and one redacted evidence bundle with every functional gate.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-CONTEXT.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-VALIDATION.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-03-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json
@scripts/embeddings-bench/benchmark.py
@scripts/embeddings-bench/compare-embeddings.py
@scripts/reranker-smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic functional and concurrency smoke contracts</name>
  <files>scripts/embeddings-bench/qwen-canary-smoke.py, scripts/embeddings-bench/tests/test_qwen_canary_smoke.py, scripts/reranker-smoke.py</files>
  <read_first>51-WAVE0-GATE.json, 51-ROUTER-LIFECYCLE.json, Existing benchmark/compare/reranker smoke scripts, router public request contracts from Plan 04, native reranker schema from Plan 02, rollout and reindex evidence, 51-VALIDATION.md L1-L5 gates</read_first>
  <behavior>
    - Health/readiness passes for both private services and the public router alias.
    - Frozen inputs at batch sizes 1 and 4 return exactly 1024 finite values per embedding with norm within declared epsilon.
    - Repeat single and batch embeddings produce cosine similarity at least 0.9999.
    - Native and public rerank return the same document ordering within declared score tolerance.
    - Three synchronized complete cycles show two acquired and one waiting until a terminal release; cancellation, timeout, and TTL each return capacity to two exactly once.
    - Approved private source succeeds while public and an available unapproved private source fail; GTE alias dimensions/targets and Qwen collection signatures remain isolated.
  </behavior>
  <action>Generate `51-06-GATE-READBACK.json`, require lineage PASS, and require `51-ROUTER-LIFECYCLE.json.live_activation.status=PASS` with `active_router_sha=source_commit.after_sha` before work. Record hashes/status/diffs and classification for the currently untracked smoke targets in `51-06-OVERLAP.json`; block on unclassified overlap and stage only listed paths. Build a CLI with fixture mode and live mode, deterministic frozen Portuguese technical/code inputs, monotonic timing, bounded deadlines, concurrent barriers, and a schema-versioned JSON report. Test response validators, norm/cosine calculations, rerank parity, event-order assertions, exact-once capacity snapshots, reachability result classification, secret/raw-text redaction, and GTE isolation diffs. Extend `scripts/reranker-smoke.py` to call both private native `/rerank` and router `/v1/rerank`, validate ranking and error mapping, and accept endpoints/tokens only via arguments or environment variable names. The live runner must capture IDs as hashes and metrics deltas, never request/document text. It must fail closed on skipped checks; only explicitly unavailable extra source-host tests may be marked not-applicable with evidence.</action>
  <acceptance_criteria>Focused tests cover pass/fail boundaries for dimensions, normalization, cosine 0.9999, ranking parity, two-slot event order, release anomalies, reachability, redaction, and incumbent isolation.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-06 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json --require PASS &amp;&amp; python3 -c "import json; d=json.load(open('.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json')); assert d['live_activation']['status']=='PASS'; assert d['active_router_sha']==d['source_commit']['after_sha']" &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_qwen_canary_smoke.py -v</automated>
  </verify>
  <done>The smoke tools deterministically evaluate every required functional and isolation gate.</done>
</task>

<task type="auto">
  <name>Task 2: Run private/native/public and three-cycle live smokes</name>
  <files>.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-FUNCTIONAL-SMOKE.json</files>
  <read_first>51-WAVE0-GATE.json, 51-06-OVERLAP.json, 51-ROUTER-LIFECYCLE.json, 51-K3S-ROLLOUT.json, 51-QDRANT-REINDEX.json, live endpoint/alias inventory, smoke tool help</read_first>
  <action>Refresh `51-06-GATE-READBACK.json`, require lineage PASS, overlap hashes, and the live router activation SHA/version/replicas/aliases immediately before traffic. Use Vault-hydrated environment variable names for router/Qdrant auth without printing values. Run health, embedding batch 1/4, 1024 dimension, norm, repeated single/batch cosine, native rerank, public rerank, and error smokes. Launch three synchronized full Embedding→VectorDB→Rerank cycles with unique pipeline IDs; prove from response/metrics timestamps that exactly two enter, the third waits, admitted rerank continuations complete, and the waiter starts only after one exact terminal release. Repeat controlled caller cancellation, backend timeout, queue TTL, and active TTL cases, confirming capacity returns to two and no leaked leases remain. Test approved private reachability, denial from the public path and an available unapproved network context, normal pod networking, and incumbent GTE alias/index hashes. Write only redacted measurements and gate outcomes to `51-FUNCTIONAL-SMOKE.json`. Stop before Plan 07 on any failure or router drift.</action>
  <acceptance_criteria>Every required smoke gate passes, cosine is at least 0.9999 for single and batch tests, event evidence proves two slots and exact release, private reachability is constrained, and GTE aliases/indexes are unchanged.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-06 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json --require PASS &amp;&amp; python3 -c "import json; d=json.load(open('.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json')); assert d['live_activation']['status']=='PASS'; assert d['active_router_sha']==d['source_commit']['after_sha']" &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-smoke.py verify-report --report .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-FUNCTIONAL-SMOKE.json --require-all</automated>
  </verify>
  <done>The complete functional/integration gate is evidenced and no lease, network, model, or incumbent-isolation defect remains.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/embeddings-bench/qwen-canary-smoke.py`: fixture/live/verify-report commands; health, batch, dimension, norm, cosine, cycle, TTL/cancel, reachability, and isolation checks.
- `scripts/embeddings-bench/tests/test_qwen_canary_smoke.py`: deterministic validator and concurrency-event tests.
- `scripts/reranker-smoke.py`: native `POST /rerank`, public `POST /v1/rerank`, ranking parity, and stable error checks.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-FUNCTIONAL-SMOKE.json`: all functional gate outcomes, metric deltas, timing/order evidence, and redacted topology hashes.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-OVERLAP.json`: per-file overlap classification and selective staging proof.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-GATE-READBACK.json`: immutable lineage plus current live router/topology/pin/alias proof.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Smoke runner to public/private endpoints | Test payloads and credentials cross router, NodePort, and service boundaries. |
| Concurrent clients to pipeline governor | Race timing can expose over-admission or leaked leases. |
| Evidence output to worktree | Raw corpus/request data and secrets must not be persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-51-06-01 | Denial of Service | Live smokes | high | mitigate | Frozen small batches, exactly three cycles, strict deadlines, controlled error injection, no broad load test. | Runner configuration in report. | Unbounded load or incumbent degradation appears. |
| T-51-06-02 | Tampering | Cycle evidence | high | mitigate | Barrier synchronization, monotonic timestamps, governor metric deltas, unique hashed IDs, invariant checks. | Verify-report event-order assertions. | More than two acquisitions or leaked capacity. |
| T-51-06-03 | Information Disclosure | Evidence/logs | high | mitigate | Vault env references, hashed IDs, aggregate values, no raw text/tokens. | Redaction tests and artifact scan. | Secret/raw request appears. |
| T-51-06-04 | Elevation of Privilege | Private reachability | critical | mitigate | Positive approved-source and negative public/unapproved-source probes; verify normal pod networking. | Report requires both positive and negative results. | Direct/public access succeeds. |
| T-51-06-05 | Tampering | GTE/Qwen isolation | critical | mitigate | Re-read dimensions, signatures, alias targets, and incumbent hashes before/after. | Isolation report gate. | GTE alias/index changes or dimensions mix. |
| T-51-06-SC | Tampering | Test dependencies | low | mitigate | Use standard library/existing dependencies only. | Dependency diff inspection. | New unaudited package is introduced. |
</threat_model>

<verification>
Run focused tests, then the bounded live suite. Verify the report with `--require-all`, independently query governor slot metrics for zero active/waiting work after tests, and compare incumbent hashes.
</verification>

<success_criteria>
- Health, batch 1/4, dimension, normalization, and cosine gates pass.
- Native/public rerank parity and error semantics pass.
- Three cycles prove two slots, one waiter, continuation priority, and exact release.
- Cancellation, timeout, and TTL leave no lease.
- Private reachability and GTE isolation pass.
</success_criteria>

<output>
Create `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-06-SUMMARY.md` when done.
</output>
