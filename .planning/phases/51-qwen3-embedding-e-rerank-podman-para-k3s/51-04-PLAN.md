---
phase: 51-qwen3-embedding-e-rerank-podman-para-k3s
plan: 04
type: tdd
wave: 3
depends_on:
  - 51-03
files_modified:
  - /home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor_test.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/service/modelcatalog/catalog.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/service/modelcatalog/catalog_test.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/relay/embedding_handler.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/relay/rerank_handler.go
  - /home/ubuntu/GitHub/containers/router-ai-atius/relay/pipeline_governor_test.go
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-OVERLAP.json
autonomous: false
requirements: [D-03, D-05, D-07, D-08, D-13, D-18]
must_haves:
  truths:
    - "The public router exposes canary aliases and converts `/v1/rerank` to the private native reranker without changing incumbent GTE aliases."
    - "At most two complete Embedding-to-VectorDB-to-Rerank cycles hold admission; a third waits, while admitted rerank continuations cannot deadlock behind new embedding work."
    - "Every pipeline slot releases exactly once on success, failure, cancellation, or TTL, and standalone embedding calls remain governed."
    - "Lease consistency matches the Wave 0 replica decision."
    - "The exact committed owner-host source is narrowly activated live only after a blocking human checkpoint, with rollback and alias/version/replica readback."
    - "Plan 04 runs only after Plan 03 rollout completion and refreshes immutable Wave 0 lineage plus current topology/pins/aliases without rewriting sealed artifacts."
  artifacts:
    - path: "/home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor.go"
      provides: "Cycle-wide pipeline governor and exact-once lease lifecycle"
      contains: "PipelineID"
    - path: "/home/ubuntu/GitHub/containers/router-ai-atius/relay/rerank_handler.go"
      provides: "Public /v1/rerank conversion and rerank continuation"
      contains: "/v1/rerank"
    - path: "/home/ubuntu/GitHub/containers/router-ai-atius/service/modelcatalog/catalog.go"
      provides: "Qwen canary alias catalog entries"
      contains: "embedding-qwen3-0.6b-int8-1024-v1"
    - path: "/home/ubuntu/GitHub/containers/router-ai-atius/relay/pipeline_governor_test.go"
      provides: "Race, concurrency, TTL, cancellation, and replica-mode tests"
      contains: "pipeline_id"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json"
      provides: "Owner-host before/after SHA, patch hashes, commit, live activation, rollback and exact readback evidence"
      contains: "live_activation"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-OVERLAP.json"
      provides: "Fail-closed remote dirty-file ownership classification"
      contains: "relay/rerank_handler.go"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json"
      provides: "Plan 04 immutable lineage and current topology/pin/alias readback"
      contains: "current_router_topology"
  key_links:
    - from: "/home/ubuntu/GitHub/containers/router-ai-atius/relay/embedding_handler.go"
      to: "/home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor.go"
      via: "pipeline acquisition or standalone admission"
      pattern: "Acquire"
    - from: "/home/ubuntu/GitHub/containers/router-ai-atius/relay/rerank_handler.go"
      to: "qwen-canary private /rerank"
      via: "alias resolution and native request conversion"
      pattern: "reranker-qwen3-0.6b-int8-v1"
    - from: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json"
      to: "Plan 51-06"
      via: "live_activation.status=PASS and active SHA/replica/alias readback"
      pattern: "active_router_sha"
---

<objective>
Extend the authoritative owner-host router catalog, rerank conversion, and governor into a two-slot cycle-wide pipeline with exact-once release and topology-correct state.

Purpose: Implement D-03, D-05, D-07, D-08, D-13, and D-18 while preserving incumbent aliases and standalone embedding admission.
Output: Selectively committed owner-host Go changes, a pre-rollout human checkpoint, narrow live activation with rollback, and exact SHA/replica/version/alias proof.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-CONTEXT.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-RESEARCH.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-PATTERNS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-VALIDATION.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-01-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-02-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preflight, implement, test, and selectively commit the owner-host router lifecycle</name>
  <files>/home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor.go, /home/ubuntu/GitHub/containers/router-ai-atius/service/embeddinggovernor/governor_test.go, /home/ubuntu/GitHub/containers/router-ai-atius/service/modelcatalog/catalog.go, /home/ubuntu/GitHub/containers/router-ai-atius/service/modelcatalog/catalog_test.go, /home/ubuntu/GitHub/containers/router-ai-atius/relay/embedding_handler.go, /home/ubuntu/GitHub/containers/router-ai-atius/relay/rerank_handler.go, /home/ubuntu/GitHub/containers/router-ai-atius/relay/pipeline_governor_test.go, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-OVERLAP.json</files>
  <read_first>51-WAVE0-GATE.json, 51-LEASE-STATE-DECISION.md, 51-W0-INVENTORY.json, current owner-host branch/HEAD/remotes/status and exact target-file diffs, current owner-host governor/catalog/relay source and tests, 51-CONTEXT.md D-03 through D-08/D-13/D-18</read_first>
  <behavior>
    - Two unique pipeline IDs acquire immediately; a third waits until one complete cycle finishes.
    - The embedding stage retains its slot through VectorDB work; rerank continuation for the same pipeline ID proceeds without reacquiring behind queued new work.
    - Success, embedding error, vector error, rerank error, caller cancellation, queue TTL, and active TTL produce one terminal reason and one release.
    - Duplicate finish/cancel calls are idempotent and never increase available slots above two.
    - Standalone embedding calls use a separate explicit admission path that cannot steal a reserved continuation and remains observable.
    - The implementation uses the exact `in_process` or `shared_atomic_store` mechanism selected in Plan 01 and rejects configuration inconsistent with live replica topology.
    - Public `/v1/rerank` validates and converts to the private native backend with stable bounded errors, and handler terminal paths release exactly once.
    - The remote commit contains only classified Phase 51 hunks in the allowlisted files and records reproducible before/after patch evidence.
  </behavior>
  <action>Generate `51-04-GATE-READBACK.json` and require lineage PASS before touching the owner host; it must preserve the original sealed hashes and prove current topology/pins/aliases. Require `51-K3S-ROLLOUT.json` PASS from Plan 03. In `/home/ubuntu/GitHub/containers/router-ai-atius`, perform a read-only preflight recording host identity, canonical path, branch, upstreams, before SHA, runtime topology, `git status --short`, per-target SHA-256, complete per-file diffs, and untracked state. Classify each target hunk as `phase-owned`, `user-owned-nonoverlap`, `shared-approved`, or `blocked` in `51-04-OVERLAP.json`; the observed dirty `relay/rerank_handler.go` must be explicitly classified. Exit non-zero on any unclassified or overlapping hunk. Use the existing checkout only when non-overlap is mechanically provable; otherwise require a safe owner-approved worktree/patch ownership protocol rooted at the recorded SHA and block until approved. Write failing table/handler/race tests first, then implement the explicit PipelineID state machine, capacity two, FIFO initial admission, continuation priority, TTL/cancel/exact-once release, standalone class, topology-selected state mechanism, exact Qwen catalog additions, embedding admission, and public `/v1/rerank` conversion with bounded validation/errors/metrics. Preserve GTE aliases byte-for-byte. Run focused tests and any required router build through `omni srv1-ops resources run builds --` after proving the 20% profile. Re-read diffs, reject files/hunks outside the overlap allowlist, stage only the seven listed remote files, verify the staged name list exactly, and create one selective remote commit without staging unrelated changes. Record before/after SHA, commit metadata, patch-id, per-file before/after hashes, diff artifact hash, test/build commands/results, and `live_activation.status=PENDING` in `51-ROUTER-LIFECYCLE.json`.</action>
  <acceptance_criteria>All remote target hunks are classified; focused race/handler/catalog tests pass under the builds profile; one selective commit contains only the allowlisted Phase 51 files/hunks; lifecycle evidence ties before SHA, after SHA, patch and test hashes together while live activation remains pending.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-04 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py validate --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-K3S-ROLLOUT.json --gate-set k3s-rollout &amp;&amp; ssh -n atius-srv-1 'cd /home/ubuntu/GitHub/containers/router-ai-atius &amp;&amp; omni srv1-ops resources run builds -- go test -race ./service/embeddinggovernor ./service/modelcatalog ./relay -run "Pipeline|Governor|Embedding|Catalog|Rerank" -count=3 &amp;&amp; test -z "$(git diff --cached --name-only)"'</automated>
  </verify>
  <done>The owner-host source lifecycle is classified, tested, selectively committed, and evidenced without restart or unrelated staging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Authorize the exact committed router rollout and restart</name>
  <files>.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-OVERLAP.json</files>
  <read_first>51-WAVE0-GATE.json, 51-K3S-ROLLOUT.json, 51-ROUTER-LIFECYCLE.json, 51-04-OVERLAP.json, current owner-host HEAD/status/runtime replicas/version/aliases, exact narrow rollout and rollback commands</read_first>
  <action>Immediately before any build/restart/rollout, refresh `51-04-GATE-READBACK.json`, require lineage PASS, and require `51-K3S-ROLLOUT.json` to prove both canary backends live/private with exact pins. Re-read the owner-host checkout and runtime; present cluster/router identity, committed after SHA, current active SHA/image/version, replica count, complete classified patch hash, exact canary additions, byte-stable GTE aliases, rollout command, restart target, pre-rollout snapshot, rollback artifact/command, and focused smoke commands. Block if source, overlap classification, runtime topology, k3s rollout, or aliases drifted after Task 1.</action>
  <acceptance_criteria>The packet proves the exact committed source will be activated on the observed router only, canary backends are ready, GTE aliases remain unchanged, and rollback restores the current active SHA/image/config.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-04 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py validate --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-K3S-ROLLOUT.json --gate-set k3s-rollout &amp;&amp; python3 -c "import json; p='.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json'; d=json.load(open(p)); assert d['source_commit']['status']=='PASS'; assert d['live_activation']['status']=='PENDING'"</automated>
    <human-check>Approve only the displayed owner-host/router identity, exact committed SHA/patch, active version/replicas/aliases, narrow rollout/restart target, pre-rollout snapshot, rollback command and bounded smokes.</human-check>
  </verify>
  <done>Resume only with `approved router qwen canary activation`; otherwise leave the committed source inactive.</done>
</task>

<task type="auto">
  <name>Task 3: Activate the exact router commit, read back live state, and retain rollback</name>
  <files>.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json</files>
  <read_first>Approved lifecycle packet, exact owner-host commit, current router deployment/unit/image/config, pre-rollout snapshot and rollback command, live qwen-canary endpoints</read_first>
  <action>After approval, refresh `51-04-GATE-READBACK.json`, require lineage PASS, and recheck every source/runtime/overlap hash from the packet. Build or package the exact committed owner-host SHA only through the verified `builds` profile; perform the narrow rollout/restart against the single observed router runtime target, never a broad PM2/system restart. Read back active source/image/version digest, process or pod owner, exact ready replica count, route registration, canary aliases/endpoints/dimensions, and byte-stable incumbent GTE aliases. Run bounded authenticated public `/v1/models`, Qwen embedding, public `/v1/rerank`, two-cycle/third-waiter, and incumbent GTE smokes without logging tokens or text. On any mismatch or smoke failure, immediately execute the recorded rollback, restart only the same target, and prove prior SHA/image/version/replicas/aliases plus GTE smokes. Update `51-ROUTER-LIFECYCLE.json` atomically with before/after/rollback hashes and set `live_activation.status=PASS` only when exact activation/readback and rollback readiness pass; otherwise set BLOCK and exit non-zero.</action>
  <acceptance_criteria>The live router runs the exact committed SHA/image with expected replica count and canary aliases; GTE aliases remain identical; rollback is executable and evidenced; any failed activation has already restored the prior runtime.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-04 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py validate --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json --gate-set router-live</automated>
  </verify>
  <done>The exact source commit is live and proven by SHA/version/replica/alias readback, or the prior runtime has been restored and Plan 06 remains blocked.</done>
</task>

</tasks>

## Artifacts this phase produces

- Owner-host selective commit: exact before/after SHA and allowlisted patch for governor, catalog and relay files.
- Owner-host `service/embeddinggovernor/governor.go`: `PipelineID`, capacity-two admission, continuation priority, standalone class, TTL/cancel/finish terminal reasons, exact-once release, selected shared/in-process state adapter, pipeline metrics.
- Owner-host governor and relay tests: race/concurrency/replica consistency evidence.
- Owner-host `service/modelcatalog/catalog.go`: `embedding-qwen3-0.6b-int8-1024-v1` and `reranker-qwen3-0.6b-int8-v1`.
- Owner-host `relay/embedding_handler.go`: `pipeline_id` start/continuation and standalone admission.
- Owner-host `relay/rerank_handler.go`: public `POST /v1/rerank` to private native `POST /rerank` conversion and stable errors.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-OVERLAP.json`: remote dirty-file hashes, hunk classification and ownership.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-GATE-READBACK.json`: immutable lineage plus current topology/pin/alias proof.
- `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-ROUTER-LIFECYCLE.json`: source commit, patch, build/test, checkpoint, live activation/readback and rollback evidence.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Public client to router | Untrusted payloads, pipeline IDs, cancellation, and deadlines enter the admission system. |
| Router replica to lease state | Concurrent replicas/processes must not independently over-admit or release another owner’s lease. |
| Router to private canary services | Alias resolution crosses from public contract to private endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-51-04-01 | Spoofing | `pipeline_id` | high | mitigate | Opaque random IDs plus ownership token/state checks; reject unknown, foreign, and expired IDs. | Handler/state-machine tests. | IDs are predictable or transferable. |
| T-51-04-02 | Tampering | Lease state | critical | mitigate | Topology-selected atomic state, compare-and-release, exact-once terminal transition, invariant capacity=2. | Race tests and invariant snapshots. | More than two active cycles or over-release occurs. |
| T-51-04-03 | Denial of Service | Queue and leaked leases | critical | mitigate | Bounded queue, queue/active TTL, cancellation, crash expiry, continuation priority, and anomaly metrics. | Three-cycle, cancellation, TTL, crash tests. | Lease can survive beyond bounded TTL or rerank continuation starves. |
| T-51-04-04 | Information Disclosure | Logs/metrics | high | mitigate | Never record request text, documents, secrets, or raw IDs; use reason codes and safe aggregates. | Captured-log assertions. | Raw data or secret is emitted. |
| T-51-04-05 | Tampering | Alias resolution | high | mitigate | Exact allowlisted canary aliases/endpoints; preserve GTE entries and prohibit implicit promotion. | Catalog tests and diff review. | Existing alias target changes. |
| T-51-04-SC | Tampering | Go dependencies | medium | mitigate | No new dependency unless already present and Wave 0 selected it; use standard library/current modules. | `go.mod`/`go.sum` diff inspection. | An un-audited package is added. |
</threat_model>

<verification>
Run focused Go tests with `-race` three times through the owner-host `builds` profile capped at 20% CPU. Confirm the selective commit contains only classified files/hunks. After the checkpoint, validate `51-ROUTER-LIFECYCLE.json` with `router-live`, independently read active SHA/version/replicas/aliases, and retain exact rollback evidence.
</verification>

<success_criteria>
- Exactly two complete cycle leases are globally enforceable for the observed replica topology.
- `pipeline_id` lifecycle releases once across every terminal path.
- Rerank continuations have priority and standalone embedding remains admitted.
- Public rerank conversion targets only the private native service.
- GTE aliases and titular traffic remain unchanged.
- The exact committed owner-host source is activated live only after approval and is proven by SHA/version/replica/alias readback with rollback.
</success_criteria>

<output>
Create `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-04-SUMMARY.md` when done.
</output>
