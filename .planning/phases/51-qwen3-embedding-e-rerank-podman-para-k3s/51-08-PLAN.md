---
phase: 51-qwen3-embedding-e-rerank-podman-para-k3s
plan: 08
type: execute
wave: 7
depends_on:
  - 51-07
files_modified:
  - scripts/embeddings-bench/qwen-canary-soak.py
  - scripts/embeddings-bench/tests/test_qwen_canary_soak.py
  - scripts/embeddings-bench/Containerfile.qwen-canary-soak
  - k8s/qwen-canary/qwen-soak-job.yaml
  - .planning/async-jobs/phase-51-qwen-soak.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-TOOL-IMAGE.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-EVIDENCE.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json
  - .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-OVERLAP.json
autonomous: true
requirements: [D-01, D-06, D-17, D-21, D-22]
must_haves:
  truths:
    - "The soak tooling runs from a dedicated digest-pinned ARM64 image containing the exact qwen-canary-soak.py hash."
    - "Before soak dispatch, the Plan 05 dual-index CronJob is suspended, its active Job count is zero, and per-source high-water marks are recorded."
    - "The namespace contains four 500m runtime pods plus exactly one 500m soak Job, staying at the five-pod/2500m ceiling."
    - "Exactly one persistent Job is dispatched and represented by one resume-safe external_job_waiting manifest."
    - "The manifest verification_command reconciles the original Job UID without redispatch, verifies at least 72 continuous hours against immutable GTE bands, writes 51-SOAK-EVIDENCE.json, refreshes read-only lineage, and only then allows the core to close 51-08-SUMMARY.md."
  artifacts:
    - path: "scripts/embeddings-bench/Containerfile.qwen-canary-soak"
      provides: "Dedicated ARM64 soak tooling image definition"
      contains: "qwen-canary-soak.py"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-TOOL-IMAGE.json"
      provides: "Image digest/platform/script-hash provenance"
      contains: "image_digest"
    - path: ".planning/async-jobs/phase-51-qwen-soak.json"
      provides: "Original Job UID, suspended dual-index state, high-water marks, and core verification command"
      contains: "external_job_waiting"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-EVIDENCE.json"
      provides: "Core-collected continuous 72-hour evidence for the original Job"
      contains: "continuous_hours"
    - path: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json"
      provides: "Current lineage readback over immutable Wave 0 hashes"
      contains: "original_gte_freeze_hash"
  key_links:
    - from: "k8s/qwen-canary/qwen-soak-job.yaml"
      to: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-TOOL-IMAGE.json"
      via: "digest-only ARM64 tooling image reference"
      pattern: "image_digest"
    - from: ".planning/async-jobs/phase-51-qwen-soak.json"
      to: ".planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-EVIDENCE.json"
      via: "verification_command reconciles original UID and collects without redispatch"
      pattern: "verification_command"
---

<objective>
Build the dedicated soak tooling image, serialize namespace tool usage, and dispatch the single 72-hour Job as the terminal async action.

Purpose: Satisfy D-01, D-06, D-17, D-21, and D-22 while preserving the immutable Plan 01 baseline/gate, the five-pod/2500m quota, and the Plan 05 dual-index high-water marks.
Output: Tested digest-pinned ARM64 tooling, one serialized soak Job, and one external_job_waiting manifest whose core verification closes the plan only after the original Job passes.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-CONTEXT.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-RESEARCH.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-VALIDATION.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-01-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-05-SUMMARY.md
@.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and prove the dedicated ARM64 soak tooling image</name>
  <files>scripts/embeddings-bench/qwen-canary-soak.py, scripts/embeddings-bench/tests/test_qwen_canary_soak.py, scripts/embeddings-bench/Containerfile.qwen-canary-soak, k8s/qwen-canary/qwen-soak-job.yaml, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-TOOL-IMAGE.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-OVERLAP.json</files>
  <read_first>51-BASELINE-CONTRACT.json, 51-GTE-BASELINE-FREEZE.json, 51-WAVE0-GATE.json, 51-QDRANT-REINDEX.json dual-index identity/checkpoints, 51-QUALITY-CAPACITY-EVAL.json, scripts/embeddings-bench/benchmark.py, scripts/embeddings-bench/canary-control.sh, Plan 05 tooling-image pattern, AGENTS.md CPU and k3s quota rules, execute-plan external_job_waiting contract</read_first>
  <behavior>
    - Immutable Wave 0 artifacts remain byte-identical; a current Plan 08 readback verifies their hashes and live topology/pins/aliases regardless of elapsed age.
    - Reconcile accepts only the original cluster/namespace/Job UID and never contains a dispatch path.
    - Verification rejects less than 72 continuous hours, excessive gaps, immutable-band breach, alias/pin drift, lease leak, restart/OOM, public reachability, or GTE impact.
    - Tooling-image evidence binds ARM64 platform, immutable base digest, image digest, script hash, config hash, and rendered Job hash.
  </behavior>
  <action>Generate `51-08-GATE-READBACK.json` and require lineage PASS without modifying `51-BASELINE-CONTRACT.json`, `51-GTE-BASELINE-FREEZE.json`, or `51-WAVE0-GATE.json`. Classify every soak-script/image/manifest target in `51-08-OVERLAP.json` and block on overlap. Reuse `scripts/embeddings-bench/benchmark.py` specifically for warmup exclusion, sampler cleanup, CPU/RSS and aggregate resource metrics; reuse `scripts/embeddings-bench/canary-control.sh` specifically for allowlisted workload identity and dry-run-before-mutation behavior. Implement fixture-tested `sample`, `reconcile`, `collect`, `verify-report`, and `verify-dispatch`; `reconcile` must require the manifest's original Job UID and expose no submit/redispatch operation. Verify continuous coverage against the numeric bands and provenance frozen in Plan 01, with age handled by fresh lineage readback rather than artifact rewrite. Create `Containerfile.qwen-canary-soak` from an immutable ARM64-capable base digest, copy the exact script, install no undeclared package, run non-root, label the script/config hashes, and build through `omni srv1-ops resources run builds --` after proving the 20% profile. Record platform/image/base/script/config/render hashes in `51-SOAK-TOOL-IMAGE.json`. Render `qwen-soak-job.yaml` with the digest only, request/limit 500m, `parallelism=1`, `completions=1`, bounded retry/deadline above 72h, restricted security, persistent checkpoint storage owned only by this Job, and no dispatch controller or CronJob.</action>
  <acceptance_criteria>Tests prove original-UID-only reconciliation, no redispatch, 72-hour continuity/gap boundaries, immutable numeric bands, redaction and hash lineage; the ARM64 image digest and exact script hash match the rendered 500m Job.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-08 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json --require PASS &amp;&amp; python3 -m unittest scripts/embeddings-bench/tests/test_qwen_canary_soak.py -v &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-soak.py verify-image --evidence .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-TOOL-IMAGE.json --job k8s/qwen-canary/qwen-soak-job.yaml --require-arm64 --require-digest-pin --require-cpu-millicores 500</automated>
  </verify>
  <done>The dedicated digest-pinned ARM64 soak image and original-UID-only reconciliation tooling are ready for serialized dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Suspend dual-index, dispatch exactly one soak Job, and return external_job_waiting</name>
  <files>.planning/async-jobs/phase-51-qwen-soak.json, .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json</files>
  <read_first>51-08-GATE-READBACK.json, 51-SOAK-TOOL-IMAGE.json, exact Plan 05 CronJob identity and checkpoints, current qwen-canary pods/Jobs/quota, existing async manifest if any, rendered qwen-soak-job.yaml</read_first>
  <action>FINAL TASK. Refresh lineage and block on drift. Resolve the exact Plan 05 dual-index CronJob and atomically set `spec.suspend=true`; record UID, resourceVersion, prior suspend state, image digest and per-source GBrain/Obsidian/Graphify high-water marks. Wait for every Job owned by that CronJob to finish and prove zero active tool Jobs in `qwen-canary`. Require exactly four ready runtime pods at 500m and quota headroom for exactly one additional 500m pod. If a matching async manifest/Job already exists, reconcile its original UID and never submit again; block on conflict or duplicate. Otherwise submit exactly one digest-pinned `qwen-soak-job` and atomically write `.planning/async-jobs/phase-51-qwen-soak.json` with `version`, `plan_id=51-08`, `backend=k3s-job`, `status=running`, `result=external_job_waiting`, cluster/namespace/name/UID, submitted timestamp, four-runtime-plus-one-tool quota proof, suspended CronJob identity/state, zero-pre-dispatch-active-Job proof, high-water marks, immutable Wave 0 hashes, lineage/image/script/config/render hashes, expected artifacts, resume command, and safe-resume gate. Set the manifest's literal core `verification_command` to `python3 scripts/embeddings-bench/qwen-canary-soak.py reconcile --manifest .planning/async-jobs/phase-51-qwen-soak.json --require-original-job-uid --no-redispatch --collect .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-EVIDENCE.json --min-hours 72 --require-continuous &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-soak.py verify-report --report .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-SOAK-EVIDENCE.json --contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --min-hours 72 --require-original-job --require-continuous --require-dual-index-suspended &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-08 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json --require PASS`. The execute-phase core may create `51-08-SUMMARY.md` only after that command succeeds for the original UID. Immediately return the literal `external_job_waiting`; perform no rollback, dual-index restore, docs, knowledge write, promotion decision, or post-wait task in this plan.</action>
  <acceptance_criteria>The dual-index CronJob is suspended with captured high-water marks, no prior tool Job is active, exactly one 500m soak Job runs beside four runtime pods, one manifest records the original UID and non-redispatch verification command, and execution stops at external_job_waiting.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/embeddings-bench/qwen-canary-inventory.py refresh-lineage --plan-id 51-08 --inventory .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-W0-INVENTORY.json --baseline-contract .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-BASELINE-CONTRACT.json --gte-baseline-freeze .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-GTE-BASELINE-FREEZE.json --gate .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-WAVE0-GATE.json --output .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-inventory.py assert-lineage --readback .planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-GATE-READBACK.json --require PASS &amp;&amp; python3 scripts/embeddings-bench/qwen-canary-soak.py verify-dispatch --manifest .planning/async-jobs/phase-51-qwen-soak.json --job k8s/qwen-canary/qwen-soak-job.yaml --require-original-job-uid --require-single-job --require-dual-index-suspended --require-zero-active-before-dispatch --require-runtime-pods 4 --require-tool-pods 1 --require-total-cpu-millicores 2500 --require-status running</automated>
  </verify>
  <done>Return literal `external_job_waiting`; the plan has no task after this dispatch.</done>
</task>

</tasks>

## Artifacts this phase produces

- Dedicated soak script/tests/Containerfile and digest-pinned ARM64 image evidence.
- One 500m k3s soak Job manifest.
- One async manifest recording original UID, suspended dual-index/high-water marks, core verification command, and `external_job_waiting`.
- `51-SOAK-EVIDENCE.json`, written only by core reconciliation after the original Job supplies at least 72 continuous hours.
- `51-08-GATE-READBACK.json` and `51-08-OVERLAP.json`.

<threat_model>
standard: OWASP ASVS
level: L1
applicable_categories: [V2, V3, V4, V5, V8, V10, V12, V13]
block_on: high

## Trust Boundaries

| Boundary | Description |
|---|---|
| Immutable Wave 0 evidence to long-running soak | Historical hashes remain sealed while current lineage must stay fresh beyond 72 hours. |
| Dual-index CronJob to soak Job | Two tool workloads compete for one 500m quota slot and can corrupt checkpoint ordering if concurrent. |
| Async manifest to execute-phase core | Resume must reconcile the original Job UID without redispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Verification | Stop Condition |
|---|---|---|---|---|---|---|---|
| T-51-08-01 | Tampering | Baseline/gate lineage | critical | mitigate | Seal Plan 01 artifacts; verify original hashes and current topology/pins/aliases through plan-scoped readback before dispatch and after >72h. | `refresh-lineage`/`assert-lineage`. | Immutable hash mismatch or current drift. |
| T-51-08-02 | Denial of Service | Namespace quota | critical | mitigate | Suspend dual-index, prove zero active tool Jobs, then run four 500m runtime pods plus one 500m soak Job only. | Dispatch verifier and live owner/CPU readback. | More than one tool Job or more than 2500m. |
| T-51-08-03 | Tampering | Async identity | critical | mitigate | Persist cluster/namespace/name/UID and original hashes; reconciliation has no submit path. | Original-UID and duplicate tests. | UID conflict, duplicate Job, or redispatch attempt. |
| T-51-08-04 | Information Disclosure | Soak samples | high | mitigate | Persist aggregates, hashes and reason codes only; no secrets/raw query/document/vector data. | Redaction tests. | Sensitive content appears. |
| T-51-08-SC | Tampering | Tooling image | high | mitigate | Immutable ARM64 base/image digest and exact script/config hashes; no new packages. | Image evidence and rendered Job hash. | Mutable/wrong-platform image or script mismatch. |
</threat_model>

<verification>
Task 1 proves the exact ARM64 tooling image. Task 2 proves dual-index suspension, zero prior active tool Job, exact quota arithmetic, one original Job UID, and a core verification command that alone can collect/verify the 72-hour result without redispatch.
</verification>

<success_criteria>
- Immutable Wave 0 evidence remains byte-identical and current lineage is PASS.
- The dual-index CronJob is suspended with high-water marks and zero active owned Jobs.
- Exactly one digest-pinned 500m soak Job runs beside four 500m runtime pods.
- The terminal task returns `external_job_waiting`.
- No post-wait work exists in Plan 08; Plan 09 starts only after core closes `51-08-SUMMARY.md`.
</success_criteria>

<output>
The execute-phase core creates `.planning/phases/51-qwen3-embedding-e-rerank-podman-para-k3s/51-08-SUMMARY.md` only after the async manifest `verification_command` succeeds for the original Job UID.
</output>
