---
phase: 49-wayland-codex-headroom
plan: 01
type: execute
wave: 1
depends_on: [48]
requirements: [HDR-01, HDR-02, HDR-03, HDR-04, HDR-05, HDR-06, HDR-07, HDR-08]
files_modified:
  - docs/operations/WAYLAND-CODEX-HEADROOM-PLAN.md
  - modules/fork-sync/projects/wayland/
  - /home/ubuntu/GitHub/wayland/src/process/task/codexConfig.ts
  - /home/ubuntu/GitHub/wayland/src/process/task/AcpAgentManager.ts
  - /home/ubuntu/GitHub/codex-acp/scripts/
must_haves:
  truths:
    - Active native Codex home and session providers remain unchanged.
    - Eligible requests show real compression savings through loopback only.
    - Rollback restores native Wayland Codex with no task loss.
  artifacts:
    - Pinned install and systemd service definition.
    - Direct Codex, ACP and Wayland canary evidence.
  key_links:
    - Isolated CODEX_HOME selects Headroom provider without replacing codex-acp.
    - Phase 48 native baseline is the rollback authority.
autonomous: true
---

<objective>
Integrate pinned Headroom into the Codex CLI used by Wayland without replacing
codex-acp or mutating the active native Codex home.
</objective>

<tasks>
<task type="auto">
  <name>Provision isolated transport-only canary</name>
  <files>Headroom user install, loopback service, isolated CODEX_HOME</files>
  <action>Install the pinned package, create a loopback service and prepare an isolated CODEX_HOME without session SQLite or optional Headroom features.</action>
  <verify>Version/hash, permissions, loopback bind, proxy health and native-home diff checks pass.</verify>
  <done>Transport-only canary is isolated, reproducible and healthy.</done>
</task>
<task type="auto">
  <name>Prove direct Codex and eligible savings</name>
  <files>isolated Codex config, Headroom telemetry and redacted canary evidence</files>
  <action>Run HTTP, WebSocket, tool, cancel/reconnect and model parity smokes; then use an eligible large tool result to prove transformation and non-zero savings.</action>
  <verify>No auth/transport regression and savings evidence is non-zero.</verify>
  <done>Direct Codex parity and real eligible savings are proven.</done>
</task>
<task type="auto">
  <name>Promote through ACP and Wayland</name>
  <files>codex-acp wrapper, Wayland codexConfig.ts, AcpAgentManager.ts, fork-sync guards</files>
  <action>Validate codex-acp against the canary, expose a reversible Wayland-scoped selection, run Chromium headless E2E and rehearse rollback.</action>
  <verify>All checks in 49-VALIDATION.md pass.</verify>
  <done>ACP/Wayland integration and native rollback rehearsal both pass.</done>
</task>
</tasks>
