---
phase: 43-codex-mcp-bootstrap-hardening
plan: 43-02
type: execute
wave: 1
depends_on:
  - 43-01
files_modified:
  - C:/Users/muniz/.codex/browser-mcp.config.toml
  - C:/Users/muniz/.codex/cloud-ops-mcp.config.toml
  - C:/Users/muniz/.codex/knowledge-mcp.config.toml
  - C:/Users/muniz/.codex/oci-mcp.config.toml
  - C:/Users/muniz/.codex/lab-mcp.config.toml
  - scripts/codex-mcp-startup-smoke.ps1
  - docs/operations/codex-mcp-startup-standard.md
autonomous: false
requirements:
  - CDX-01
  - CDX-02
  - CDX-03
  - CDX-04
  - CDX-05
  - CDX-06
must_haves:
  truths:
    - "Every non-baseline profile has explicit preflight rules for its prerequisites."
    - "Relevant profile MCPs use explicit `startup_timeout_sec` values when timeout is a real lever."
    - "No profile expected for frequent use keeps `@latest` on the startup hot path without a documented reason."
    - "The smoke path can distinguish `missing-env`, `unreachable`, `slow-start`, `disabled`, and `ok`."
    - "Validation output redacts tokens, headers, and cookies."
  prohibitions:
    - statement: "Do not raise timeouts for `obsidian_rest` as a substitute for reachability diagnosis."
      status: unresolved
      verification: "profile preflight tests transport reachability separately."
    - statement: "Do not copy Cloudflare or Obsidian auth material into the smoke script."
      status: unresolved
      verification: "script reads env/transport status only and prints classifications, not values."
  artifacts:
    - path: "scripts/codex-mcp-startup-smoke.ps1"
      provides: "Repeatable local smoke and failure-classification entrypoint."
    - path: "docs/operations/codex-mcp-startup-standard.md"
      provides: "Pinned timeout and preflight policy."
---

<objective>
Harden the extracted MCP profiles with explicit startup budgets, prerequisite
checks, and repeatable cold-start validation.

Purpose: make the opt-in profiles usable on demand without collapsing back into
the current "everything times out together" behavior.

Output: explicit timeout policy, prerequisite-aware profiles, and a local smoke
script that classifies failures cleanly.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-CONTEXT.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-01-PLAN.md
@docs/operations/codex-mcp-startup-standard.md
@C:/Users/muniz/.codex/browser-mcp.config.toml
@C:/Users/muniz/.codex/cloud-ops-mcp.config.toml
@C:/Users/muniz/.codex/knowledge-mcp.config.toml
@C:/Users/muniz/.codex/oci-mcp.config.toml
@C:/Users/muniz/.codex/lab-mcp.config.toml
</context>

## Artifacts This Phase Produces

- Hardened profile files with explicit startup budgets and preflight comments
- `scripts/codex-mcp-startup-smoke.ps1`
- Updated `docs/operations/codex-mcp-startup-standard.md`

## Decision Coverage

| Decision | Covered by |
|---|---|
| D-03 | Task 1 |
| D-04 | Tasks 1, 2 |
| D-05 | Task 1 |
| D-06 | Task 1 |
| D-07 | Tasks 2, 3 |
| D-08 | Task 1 |
| D-09 | Task 3 |
| D-10 | Tasks 2, 3 |
| D-11 | Tasks 1, 2, 3 |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add explicit timeout and prerequisite policy to every opt-in profile</name>
  <files>C:/Users/muniz/.codex/browser-mcp.config.toml, C:/Users/muniz/.codex/cloud-ops-mcp.config.toml, C:/Users/muniz/.codex/knowledge-mcp.config.toml, C:/Users/muniz/.codex/oci-mcp.config.toml, C:/Users/muniz/.codex/lab-mcp.config.toml</files>
  <read_first>
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
    - C:/Users/muniz/.codex/config.toml
    - C:/Users/muniz/.codex/browser-mcp.config.toml
    - C:/Users/muniz/.codex/oci-mcp.config.toml
  </read_first>
  <action>For every extracted profile, make the bootstrap contract explicit. Add `startup_timeout_sec` where timeout is a real control. Use concrete budgets by class, for example: local light helpers around 60 seconds, browser and OCI profiles around 120 seconds unless measurement justifies a different number. Remove `@latest` from any browser/tooling profile that is expected to be used frequently, replacing it with a pinned package version or a local stable wrapper only after verifying the path exists. Add comments or adjacent doc references that spell out prerequisite gates: `CF_GLOBAL_API_KEY` for Cloudflare, VPN or reachability for `obsidian_rest`, local Chrome path for browser tools, `uv` plus repo path plus OCI profile availability for OCI tools. Do not treat unreachable HTTP MCPs as timeout-only issues.</action>
  <done>Every opt-in profile has explicit timeout and prerequisite policy, and frequently used browser/tooling profiles avoid unbounded `@latest` resolution on the startup path.</done>
  <verify>
    <automated>rg -n "startup_timeout_sec|@latest|CF_GLOBAL_API_KEY|10\\.1\\.1\\.1:27124|uv|PLAYWRIGHT|chrome" C:/Users/muniz/.codex/*.config.toml</automated>
    <automated>codex -p cloud-ops-mcp mcp get cloudflare-api</automated>
    <automated>codex -p knowledge-mcp mcp get obsidian_rest</automated>
  </verify>
  <acceptance_criteria>
    - Every non-baseline profile that starts an external process has explicit timeout policy or an explicit reason not to need one.
    - Browser profiles no longer rely on `@latest` in their common startup path unless the doc records a deliberate exception.
    - Cloudflare, Obsidian, browser, and OCI prerequisites are named explicitly instead of being left implicit in warning text.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create a repeatable smoke and failure-classification script</name>
  <files>scripts/codex-mcp-startup-smoke.ps1</files>
  <read_first>
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
    - C:/Users/muniz/.codex/config.toml
  </read_first>
  <action>Create `scripts/codex-mcp-startup-smoke.ps1` to run the local Codex bootstrap checks without printing secret values. The script should support at least: base baseline check; `knowledge-mcp`; `browser-mcp`; `oci-mcp`; `cloud-ops-mcp`; `lab-mcp`. It should run the cheapest truthful checks first: `codex doctor --json`, `codex mcp list`, `codex mcp get <name>`, environment-variable presence checks, `Test-NetConnection` or equivalent for `10.1.1.1:27124`, `Test-Path` for local Chrome executable, and `uv --version` / repo-path existence for OCI. Its output should classify each server/profile as `ok`, `disabled`, `missing-env`, `unreachable`, or `slow-start`, never dumping token values, bearer headers, or cookies.</action>
  <done>The smoke script exists, supports baseline plus profile-targeted runs, and reports classified outcomes without printing secrets.</done>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/codex-mcp-startup-smoke.ps1 -Profile baseline</automated>
    <automated>powershell -ExecutionPolicy Bypass -File scripts/codex-mcp-startup-smoke.ps1 -Profile knowledge-mcp</automated>
  </verify>
  <acceptance_criteria>
    - The script exists and accepts at least one baseline run plus one profile-targeted run.
    - The script classifies failures without ambiguous "just timed out" output.
    - The script output contains no secret values.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Validate the lean baseline and update the operator doc with observed outcomes</name>
  <files>docs/operations/codex-mcp-startup-standard.md</files>
  <read_first>
    - docs/operations/codex-mcp-startup-standard.md
    - scripts/codex-mcp-startup-smoke.ps1
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
  </read_first>
  <action>Run the smoke path after the profile hardening pass and write the observed expected outcomes into the operator doc. The baseline should now be the default daily command. Each optional profile should document what "healthy", "blocked", or "intentionally skipped" looks like, and which command the operator should run next. Explicitly capture that `obsidian_rest` can still be profile-blocked when VPN/reachability is down and that this is not a baseline regression. Capture rollback commands and the exact backup file naming pattern created during implementation.</action>
  <done>The operator doc records expected baseline and profile outcomes, including blocked states, next actions, and exact rollback commands.</done>
  <verify>
    <automated>codex doctor --json</automated>
    <automated>rg -n "baseline|knowledge-mcp|browser-mcp|oci-mcp|cloud-ops-mcp|blocked|rollback|backup" docs/operations/codex-mcp-startup-standard.md</automated>
  </verify>
  <acceptance_criteria>
    - The lean baseline doctor result is documented as the expected daily state.
    - The doc tells the operator what to do when a profile is blocked by missing env or reachability.
    - Rollback commands and backup patterns are present in the doc.
  </acceptance_criteria>
</task>

</tasks>
