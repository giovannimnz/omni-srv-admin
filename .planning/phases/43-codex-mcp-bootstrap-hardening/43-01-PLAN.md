---
phase: 43-codex-mcp-bootstrap-hardening
plan: 43-01
type: execute
wave: 0
depends_on: []
files_modified:
  - C:/Users/muniz/.codex/config.toml
  - C:/Users/muniz/.codex/browser-mcp.config.toml
  - C:/Users/muniz/.codex/cloud-ops-mcp.config.toml
  - C:/Users/muniz/.codex/knowledge-mcp.config.toml
  - C:/Users/muniz/.codex/oci-mcp.config.toml
  - C:/Users/muniz/.codex/lab-mcp.config.toml
  - docs/operations/codex-runtime-standard.md
  - docs/operations/codex-mcp-startup-standard.md
autonomous: false
requirements:
  - CDX-01
  - CDX-02
  - CDX-03
  - CDX-04
  - CDX-05
  - CDX-06
must_haves:
  truths:
    - "The default Windows Codex bootstrap is lean and does not auto-start browser, OCI, Cloudflare, or lab MCPs."
    - "Every extracted MCP group has a named profile file loaded with `codex -p <profile>`."
    - "The base config preserves only the always-on MCP set approved by the phase."
    - "Backups of every touched Codex config file exist before mutation."
    - "No secret values are copied from tokens, headers, or remote shells into repo docs or profile files."
  prohibitions:
    - statement: "Do not leave `cloudflare-api` in the default startup path when `CF_GLOBAL_API_KEY` is absent."
      status: unresolved
      verification: "lean baseline contains no `cloudflare-api` block."
    - statement: "Do not keep Playwright, Chrome DevTools, or the OCI MCP fleet in the base `config.toml`."
      status: unresolved
      verification: "base config MCP inventory excludes those groups."
  artifacts:
    - path: "C:/Users/muniz/.codex/browser-mcp.config.toml"
      provides: "Opt-in browser MCP surface."
    - path: "C:/Users/muniz/.codex/knowledge-mcp.config.toml"
      provides: "Opt-in knowledge MCP surface for Obsidian."
    - path: "C:/Users/muniz/.codex/oci-mcp.config.toml"
      provides: "Opt-in OCI MCP surface."
    - path: "C:/Users/muniz/.codex/cloud-ops-mcp.config.toml"
      provides: "Opt-in Cloudflare MCP surface."
    - path: "C:/Users/muniz/.codex/lab-mcp.config.toml"
      provides: "Opt-in lab helper MCP surface."
    - path: "docs/operations/codex-mcp-startup-standard.md"
      provides: "Canonical operator guide for lean baseline and profile usage."
---

<objective>
Create the lean default MCP baseline and split task-specific MCPs into explicit
Codex profiles.

Purpose: stop paying the startup cost of every optional MCP on every Codex
launch, while preserving a documented path to enable those MCPs intentionally.

Output: lean base `config.toml`, named opt-in profile files, backup-first
rollback, and updated runtime docs.
</objective>

<execution_context>
@C:/Users/muniz/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/muniz/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-CONTEXT.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
@.planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
@docs/operations/codex-runtime-standard.md
@docs/operations/ATIUS-FLEET-NETWORK-PORT-MAP.md
@C:/Users/muniz/.codex/config.toml
@C:/Users/muniz/.codex/mcp-patch.toml
</context>

## Artifacts This Phase Produces

- Lean daily baseline in `C:/Users/muniz/.codex/config.toml`
- Profile files:
  - `C:/Users/muniz/.codex/browser-mcp.config.toml`
  - `C:/Users/muniz/.codex/cloud-ops-mcp.config.toml`
  - `C:/Users/muniz/.codex/knowledge-mcp.config.toml`
  - `C:/Users/muniz/.codex/oci-mcp.config.toml`
  - `C:/Users/muniz/.codex/lab-mcp.config.toml`
- Canonical doc: `docs/operations/codex-mcp-startup-standard.md`
- Updated baseline doc: `docs/operations/codex-runtime-standard.md`

## Decision Coverage

| Decision | Covered by |
|---|---|
| D-01 | Task 1 |
| D-02 | Task 1 |
| D-03 | Tasks 1, 2 |
| D-04 | Task 2 |
| D-05 | Task 1 |
| D-06 | Task 2 |
| D-07 | Task 2 |
| D-08 | Task 1 |
| D-09 | Task 1 |
| D-10 | Task 2 |
| D-11 | Tasks 1, 2 |

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backup the current runtime and split the MCP inventory into lean baseline plus profiles</name>
  <files>C:/Users/muniz/.codex/config.toml, C:/Users/muniz/.codex/browser-mcp.config.toml, C:/Users/muniz/.codex/cloud-ops-mcp.config.toml, C:/Users/muniz/.codex/knowledge-mcp.config.toml, C:/Users/muniz/.codex/oci-mcp.config.toml, C:/Users/muniz/.codex/lab-mcp.config.toml</files>
  <read_first>
    - C:/Users/muniz/.codex/config.toml
    - C:/Users/muniz/.codex/mcp-patch.toml
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-CONTEXT.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
  </read_first>
  <action>Before editing, create dated backups of `config.toml` and any new profile target files if they already exist. Rewrite the local MCP layout so the base `config.toml` keeps only the approved always-on MCP set. Extract browser MCPs (`chrome-devtools`, `playwright-desktop`, `playwright-mobile`) into `browser-mcp.config.toml`; extract `cloudflare-api` into `cloud-ops-mcp.config.toml`; extract `obsidian_rest` into `knowledge-mcp.config.toml`; extract all `oci-api-*` and `oci-compute-*` into `oci-mcp.config.toml`; extract `memory`, `filesystem`, and `sequentialthinking` into `lab-mcp.config.toml`. Preserve comments and auth/env structure, but do not duplicate raw secret values into repo docs. Add short header comments in each profile saying when it should be used and what it intentionally excludes.</action>
  <done>Lean baseline keeps only the approved always-on MCP set and every optional MCP group lives in exactly one named profile file with pre-mutation backups recorded.</done>
  <verify>
    <automated>rg -n "^\[mcp_servers\." C:/Users/muniz/.codex/config.toml C:/Users/muniz/.codex/*.config.toml</automated>
    <automated>codex mcp list</automated>
    <automated>codex -p browser-mcp mcp list</automated>
    <automated>codex -p oci-mcp mcp list</automated>
  </verify>
  <acceptance_criteria>
    - The base `config.toml` no longer contains `cloudflare-api`, `obsidian_rest`, `chrome-devtools`, `playwright-*`, `memory`, `filesystem`, `sequentialthinking`, `oci-api-*`, or `oci-compute-*`.
    - Each extracted group exists in exactly one named profile file.
    - Backup files for the touched local Codex config surfaces exist before the final rewrite.
    - The lean baseline still retains the core daily runtime and remains syntactically valid TOML.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document baseline, profile usage, and rollback</name>
  <files>docs/operations/codex-runtime-standard.md, docs/operations/codex-mcp-startup-standard.md</files>
  <read_first>
    - docs/operations/codex-runtime-standard.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-CONTEXT.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-RESEARCH.md
    - .planning/phases/43-codex-mcp-bootstrap-hardening/43-VALIDATION.md
  </read_first>
  <action>Update the existing runtime standard doc so it no longer treats the base config as the whole operator story. Add a dedicated `docs/operations/codex-mcp-startup-standard.md` that defines: the lean default philosophy; profile names and when to use them; exact commands such as `codex -p knowledge-mcp`, `codex -p browser-mcp`, `codex -p oci-mcp`, `codex -p cloud-ops-mcp`, and `codex -p lab-mcp`; backup and rollback steps for `config.toml` and profile files; and the rule that missing Cloudflare env or unreachable Obsidian endpoint should be handled by profile choice or preflight, not by silently copying secrets or only raising timeouts.</action>
  <done>The operator docs distinguish the lean baseline from opt-in profiles, include exact `codex -p <profile>` commands, and document rollback plus secret-handling rules.</done>
  <verify>
    <automated>rg -n "knowledge-mcp|browser-mcp|oci-mcp|cloud-ops-mcp|lab-mcp|rollback|CF_GLOBAL_API_KEY|obsidian_rest" docs/operations/codex-runtime-standard.md docs/operations/codex-mcp-startup-standard.md</automated>
  </verify>
  <acceptance_criteria>
    - The new doc names all five profiles and their intended work types.
    - The docs include exact `codex -p <profile>` commands.
    - The docs explicitly say that `obsidian_rest` reachability is not solved by timeout inflation alone.
    - The docs explicitly forbid copying Cloudflare or Obsidian secrets into Git-tracked artifacts.
  </acceptance_criteria>
</task>

</tasks>
