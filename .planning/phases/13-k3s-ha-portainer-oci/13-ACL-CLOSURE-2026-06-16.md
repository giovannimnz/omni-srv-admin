---
type: gate-closure
date: 2026-06-16
phase: 13
milestone: M005 → M007 follow-up
gate: Tailscale ACL (was PARTIAL in `13-GATE-REVIEW-2026-06-14.md`)
status: closed
---

# Tailscale ACL Closure — 2026-06-16

Closes the M005 gate `Tailscale fallback outside WireGuard` that was PARTIAL in `13-GATE-REVIEW-2026-06-14.md` since 2026-06-14.

## Changes applied

1. **ACL restricted** via Tailnet admin API (`POST /api/v2/tailnet/-/acl`).
   - Source: `group:admins` (giovannimunizds@gmail.com) + 3 SRV node IPs.
   - Dst: SRV-{1,2,3} on ports `22,6432,8505,8506` (admin) and `+6443` (SRV↔SRV).
   - SSH rule: `group:admins` → `autogroup:self` user `ubuntu`.
   - Backups: `.backups/tailscale-pre-acl-2026-06-16/acl-current.json` (default open) + `acl-new.hujson` (closed).
2. **MagicDNS disabled** via `POST /api/v2/tailnet/-/dns/preferences {"magicDNS": false}`.
3. **`tailscale set --accept-dns=false`** on SRV-1, SRV-2, SRV-3.

## ACL source-of-truth

Live at: https://login.tailscale.com/admin/acls/file (tailnet `tail1233af.ts.net`).
Canonical mirror: `~/GitHub/omni-srv-admin/.backups/tailscale-pre-acl-2026-06-16/acl-new.hujson`.
Vault doc: `30-RECURSOS/31-NETWORKING/02-Tailscale/Tailscale-Setup-ATIUS.md` (in obsidian-vault).

## Validation

- `tailscale status` — all 3 SRVs online, peer direct (p2p) when possible, DERP/São Paulo fallback.
- `tailscale ping` between SRV-{1,2,3}: bidirectional, 1-3ms via WireGuard (preferred) or DERP.
- `nc <tailscale-ip> 22` between SRVs: ✅ open.
- `nc <tailscale-ip> 80` (deny test, SRV→SRV): ❌ timeout (ACL blocks).

## Key decision ratificado

**WireGuard = transporte K3s. Tailscale = rede de gerenciamento complementar.** Tailscale nunca substitui WireGuard como transporte do cluster. Se WireGuard cair, Tailscale serve pra acessar os hosts e restaurar `wg0` antes de qualquer K3s restart/rejoin.

## Incident separado (não bloqueante)

SRV-2 tem `/etc/resolv.conf` symlink pro stub-resolv.conf do systemd-resolved, mas o stub está ausente. Tailscale health check reclama. Ver `61-INCIDENTS/2026-06-16-srv2-resolv-stub-missing.md` (vault) — não bloqueia SSH/K3s/Tailscale, pode esperar upgrade Jammy→Noble.
