---
phase: 13
slug: k3s-ha-portainer-oci
date: 2026-06-13
status: preflight-passed-with-live-gates
branch: codex/k3s-portainer-oci-plan
---

# Phase 13 Preflight — 2026-06-13

## Result

K3s was not installed. Portainer was not installed. No OCI firewall or
Cloudflare Tunnel changes were made.

Read-only preflight passed after safe log cleanup on SRV-2 and SRV-3. Live
installation remains gated by OCI snapshots/backups, OCI NSG/Security List
confirmation and an out-of-band Cloudflare Tunnel token.

## Host Results

| Host | OS | Arch | Time sync | Root disk after cleanup | Private routes/ping | Failed units |
|---|---|---|---|---|---|---|
| ATIUS-SRV-1 | Ubuntu 24.04.4 LTS | aarch64 | yes | 46G free / 77% used | ok | 5 reviewed |
| ATIUS-SRV-2 | Ubuntu 22.04.5 LTS | aarch64 | yes | 26G free / 87% used | ok | 9 reviewed |
| ATIUS-SRV-3 | Ubuntu 22.04.5 LTS | aarch64 | yes | 50G free / 75% used | ok | 0 |

Disk gate is now satisfied for all three nodes (`>=25G` free).

## Safe Changes Applied

| Host | Change | Rationale |
|---|---|---|
| SRV-2 | Truncated Docker JSON logs larger than 100M | Free disk without deleting containers, images or volumes |
| SRV-2 | Truncated `/var/log/xrdp.log`, `/var/log/syslog`, `/var/log/syslog.1` after saving tails under `~/.logs/k3s-preflight/` | Free disk from oversized logs |
| SRV-2 | Installed `/etc/logrotate.d/docker-json-containers` | Prevent Docker JSON logs from growing unbounded |
| SRV-3 | Truncated one 45G Docker JSON log from GitLab container after saving a tail under `~/.logs/k3s-preflight/` | Free disk without touching GitLab data |
| SRV-3 | Installed `/etc/logrotate.d/docker-json-containers` | Prevent Docker JSON logs from growing unbounded |

The logrotate config is versioned at
`modules/k3s-ha-portainer-oci/logrotate/docker-json-containers`.

## Reviewed Failed Units

SRV-1:

- `new-api.service`: Docker Compose references missing service `db-newapi`.
- `samba-shared-mount.service`: missing `/home/ubuntu/Shared_Network`.
- `snap.certbot.renew.service`: certbot renewal failures.
- `xrdp.service` and `xrdp-sesman.service`: failed during prior stop/start; not restarted during this work to avoid RDP impact.

SRV-2:

- `certbot.service`: renewal and broken symlink errors.
- `docker-autostart.service` and `samba-docker.service`: old failures from 2026-05-22.
- Podman healthcheck transient units: stale failed healthcheck runs.

These were reviewed as not direct K3s blockers, but they should be fixed in a
separate operational cleanup before declaring the whole fleet healthy.

## Remaining Live Gates

- OCI snapshots or equivalent backups for all three nodes.
- OCI NSG/Security List rules for K3s private ports:
  - TCP 6443
  - TCP 2379-2380
  - UDP 8472
  - TCP 10250
  - TCP/UDP egress 7844 for Cloudflare Tunnel
- Cloudflare remote Tunnel `atius-k3s-portainer` and shell-only token.
- Human approval to write `/etc/rancher/k3s/config.yaml` and install K3s.

## Prepared Artifacts

- `modules/k3s-ha-portainer-oci/k3s/config-srv1.example.yaml`
- `modules/k3s-ha-portainer-oci/k3s/config-srv2.example.yaml`
- `modules/k3s-ha-portainer-oci/k3s/config-srv3.example.yaml`
- `modules/k3s-ha-portainer-oci/k8s/portainer-values.yaml`
- `modules/k3s-ha-portainer-oci/k8s/cloudflared-deployment.yaml`
- `modules/k3s-ha-portainer-oci/logrotate/docker-json-containers`
