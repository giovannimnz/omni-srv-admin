---
phase: 27-production-guard-horistic-remote-rename-drift
plan: 01
type: execute
wave: 1
depends_on:
  - 26-01
files_modified:
  - modules/srv1-ops/configs/production-guard.yaml
  - modules/srv1-ops/scripts/production_guard.py
  - cli/omni/tests/test_srv1_production_guard.py
  - docs/operations/production-guard.md
autonomous: true
context_budget_target: "75k-95k tokens"
execution_model_target: "gpt-5.3-codex-spark"
requirements:
  - PRG-08
  - PRG-09
  - PRG-10
  - PRG-11
must_haves:
  truths:
    - "SSH remote checks are read-only."
    - "apache2ctl -S and sites-enabled are checked where available."
    - "Rename drift is reported, not applied."
    - "Endpoint methods are GET/HEAD only by default."
    - "No real POST to Horistic/ATS/Telegram."
---

<objective>
Implementar remote Apache checks, rename drift detector e webhook-safe validation.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-production-guard-horistic-remote-rename-drift/27-CONTEXT.md
@.planning/phases/27-production-guard-horistic-remote-rename-drift/27-RESEARCH.md
@.planning/phases/27-production-guard-horistic-remote-rename-drift/27-VALIDATION.md
@modules/srv1-ops/scripts/production_guard.py
@docs/operations/production-guard.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add remote Horistic Apache checks</name>
  <files>modules/srv1-ops/configs/production-guard.yaml, modules/srv1-ops/scripts/production_guard.py, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: remote Apache unit package-default state returns PASS.
    - Test 2: custom unit/drop-in returns WARN/BLOCK.
    - Test 3: missing 80/443 listener returns BLOCK.
    - Test 4: `apache2ctl -S` or vhost parse failure returns BLOCK.
    - Test 5: public endpoint GET failures are reported without POST.
  </behavior>
  <action>Add config and checker for `horistic-srv` remote Apache. Use read-only SSH commands: `systemctl show`, `systemctl is-enabled`, `ss -tlnp`, `apache2ctl -S`, `find /etc/apache2/sites-enabled -maxdepth 1 -type l -o -type f`. Redact paths/values if needed. Do not modify remote state.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "apache or remote or horistic"</automated>
  </verify>
  <done>Remote Apache findings are visible in status/doctor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add rename drift detector</name>
  <files>modules/srv1-ops/configs/production-guard.yaml, modules/srv1-ops/scripts/production_guard.py, cli/omni/tests/test_srv1_production_guard.py</files>
  <behavior>
    - Test 1: stale `horistic-srv-1` doc reference is classified benign.
    - Test 2: active PM2 cwd/script reference to missing path is BLOCK.
    - Test 3: active Apache vhost/proxy reference to old host/path is BLOCK.
    - Test 4: GDrive/backup old path is WARN unless actively referenced.
    - Test 5: detector proposes fix but does not rename or symlink.
  </behavior>
  <action>Add drift probes for hostnames, cwd/script paths, Apache vhost/proxy refs, inventory, backup/GDrive paths and symlink expectations. Output severity, evidence path, suggested fix and apply gate text only.</action>
  <verify>
    <automated>PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "rename or drift"</automated>
  </verify>
  <done>Rename/folder drift is reported without mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add webhook-safe validation contract</name>
  <files>modules/srv1-ops/configs/production-guard.yaml, modules/srv1-ops/scripts/production_guard.py, cli/omni/tests/test_srv1_production_guard.py, docs/operations/production-guard.md</files>
  <behavior>
    - Test 1: webhook endpoints are configured as GET/HEAD health checks only.
    - Test 2: scanner fails if checker code contains default POST to Horistic/ATS/Telegram.
    - Test 3: docs state no real POST without operator approval.
    - Test 4: docs reference Horistic scalp split and Circuit Breaker Telegram-only suppression as external contract.
  </behavior>
  <action>Encode endpoint method policy and no-POST guardrails. Document why real POST checks are prohibited and where the Horistic repo contract tests live. Do not modify the Horistic repo.</action>
  <verify>
    <automated>! rg -n "requests\\.post|urllib.*POST|curl .*POST|method=.*POST" modules/srv1-ops/scripts/production_guard.py modules/srv1-ops/configs/production-guard.yaml</automated>
  </verify>
  <done>Production Guard cannot accidentally fire webhook/trading/Telegram POSTs.</done>
</task>

</tasks>

<verification>
Run in this order:

1. `python3 -m py_compile modules/srv1-ops/scripts/production_guard.py`
2. `! rg -n "requests\\.post|urllib.*POST|curl .*POST|method=.*POST" modules/srv1-ops/scripts/production_guard.py modules/srv1-ops/configs/production-guard.yaml docs/operations/production-guard.md`
3. `PYTHONPATH=cli pytest cli/omni/tests/test_srv1_production_guard.py -q -k "apache or remote or rename or drift or webhook"`
4. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard status --json`
5. `PYTHONPATH=cli python3 -m omni srv1-ops production-guard doctor --json`
6. `ssh horistic@10.1.1.4 'hostname; systemctl is-enabled apache2; systemctl is-active apache2; systemctl show apache2 -p FragmentPath -p DropInPaths -p NeedDaemonReload; ss -tlnp | grep -E ":(80|443) "; apache2ctl -S'`
7. `curl -fsSI https://dashboard.horistic.com/login`
8. `curl -fsSI https://api.horistic.com/v1/health`
9. `curl -fsSI https://webhook.horistic.com/`
10. `node "$HOME/.Codex/get-shit-done/bin/gsd-tools.cjs" graphify status`
11. `$gsd-verify-work 27`
</verification>

<success_criteria>
PRG-08..PRG-11 are complete when remote Apache, rename drift and webhook-safe
checks are in `production-guard`, read-only by default, auditable and free of
real POST side effects.
</success_criteria>

<output>
Create `.planning/phases/27-production-guard-horistic-remote-rename-drift/27-01-SUMMARY.md`.
</output>
